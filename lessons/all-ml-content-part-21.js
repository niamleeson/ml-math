/* All ML — authored content for Part 21: Classical / Symbolic AI (21.1–21.13).
   Appends to window.ALLML_CONTENT. Every notebook was executed before writing.
   LaTeX via String.raw; emphasis is bold only (no prose italics). */
window.ALLML_CONTENT = window.ALLML_CONTENT || {};

/* ---------------- 21.1 Propositional logic ---------------- */
window.ALLML_CONTENT["21.1"] = {
  tagline: "Truth tables let us replace argument by inspection: if every remaining world makes the conclusion true, the conclusion follows.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/21.1-propositional-logic.ipynb",
  context: String.raw`
    <p>Propositional logic begins symbolic AI with whole statements that are either true or false.</p><ul><li><b>Boolean algebra</b> supplies AND, OR, and NOT; those connectives are the switches in every truth table.</li><li><b>Set thinking</b> helps because each formula selects the worlds where it is true.</li><li><b>Search</b> appears already: checking a formula means scanning assignments, the small ancestor of SAT solving (21.8).</li></ul><p>Where it leads: first-order logic (21.2) adds objects and variables, knowledge representation (21.3) chooses what facts to store, and SAT solvers (21.8) turn these truth tables into industrial-scale search.</p>`,
  intuition: String.raw`
    <p>The concrete problem is deciding whether facts force a conclusion without relying on a human hunch. The naive approach is to read an English sentence and hope the reasoning is sound; propositional logic strips away the phrasing and keeps only the truth conditions.</p><p>The mental model is a table of possible worlds. A formula is not mysterious: it is the set of rows where the formula is true. Entailment means the rows that satisfy the knowledge base sit inside the rows that satisfy the query.</p><p>The design decision people gloss over is the material implication $P \to Q$. It is false only when $P$ is true and $Q$ is false because a promise is broken only when it is triggered and then fails.</p>`,
  mathematics: String.raw`
    <p>Let $P,Q,R \in \{0,1\}$ be proposition truth values. The core connectives are $\neg P$, $P\land Q$, $P\lor Q$, and implication $P\to Q\equiv 
    eg P\lor Q$. Entailment $KB\models q$ means every assignment satisfying $KB$ also satisfies $q$.</p><p><b>Implication.</b></p><ol class="work"><li>$P=0,Q=0$: $\neg P\lor Q=1\lor0=1$</li><li>$P=0,Q=1$: $1\lor1=1$</li><li>$P=1,Q=0$: $0\lor0=0$</li><li>$P=1,Q=1$: $0\lor1=1$</li></ol><p>Only the made-and-broken promise fails, so implication is a compact way to encode rules.</p><p><b>Entailment.</b> For $KB=\{P, P\to Q\}$ over $(P,Q)$:</p><ol class="work"><li>$(0,0)$: $P=0$, so $KB=0$</li><li>$(0,1)$: $P=0$, so $KB=0$</li><li>$(1,0)$: $P=1$ and $P\to Q=0$, so $KB=0$</li><li>$(1,1)$: $P=1$ and $P\to Q=1$, so $KB=1$ and $Q=1$</li></ol><p>The only model of the knowledge base has $Q=1$, which is exactly what $KB\models Q$ says.</p><p><b>A compound sentence.</b> $(P\land Q)\to R$ fails once among eight assignments: when $P=1,Q=1,R=0$. That one bad row is the complete reason the formula is not a tautology.</p>`,
  pitfalls: String.raw`
    <ul><li><b>Confusing implication with causation.</b> $P\to Q$ is a truth condition, not a time-ordered mechanism; the Mathematics section shows it is just $\neg P\lor Q$.</li><li><b>Checking only the world you believe is real.</b> Entailment quantifies over every model of $KB$, so one satisfying example never proves a conclusion.</li><li><b>Forgetting parentheses.</b> $P\land Q\to R$ is about the conjunction triggering the rule; changing grouping changes the truth column.</li></ul>`
};

/* ---------------- 21.2 First-order logic ---------------- */
window.ALLML_CONTENT["21.2"] = {
  tagline: "Variables and quantifiers let logic talk about objects, relations, and reusable rules instead of one flat proposition at a time.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/21.2-first-order-logic.ipynb",
  context: String.raw`
    <p>First-order logic keeps the truth-table discipline of 21.1 but opens the statements up so they can mention objects.</p><ul><li><b>Propositional logic (21.1)</b> contributes connectives and entailment; first-order logic applies them inside quantified formulas.</li><li><b>Sets and relations</b> give predicates their meaning: $Bird(x)$ is the set of birds, and $Parent(x,y)$ is a set of ordered pairs.</li><li><b>Pattern matching</b> becomes unification, the engine later used by theorem proving (21.10) and rule systems (21.6).</li></ul><p>Where it leads: ontologies (21.4) restrict first-order expressiveness to stay decidable, and theorem provers (21.10) automate proof search over quantified rules.</p>`,
  intuition: String.raw`
    <p>The pain in propositional logic is repetition. If you need one atom for Tweety-is-a-bird and another for Opus-is-a-bird, the rule that birds have wings must be copied for every individual.</p><p>First-order logic replaces copies with variables: one formula $\forall x\, Bird(x)\to Wings(x)$ covers the whole domain. The mental model is a spreadsheet: rows are objects, predicate columns mark properties, and quantifiers scan rows.</p><p>The design decision is to separate syntax from interpretation. The formula says how to reason, while the domain and predicate extensions say what objects make it true. That separation is why the same rule can run over a toy domain or a production knowledge graph.</p>`,
  mathematics: String.raw`
    <p>A first-order structure has a domain $D$, predicates such as $Bird:D\to\{0,1\}$ and $Parent:D\times D\to\{0,1\}$, connectives, and quantifiers $\forall$ and $\exists$.</p><p><b>Universal rule.</b> With objects Tweety, Opus, Fido, let birds be $[1,1,0]$ and wings be $[1,1,0]$.</p><ol class="work"><li>Tweety: $Bird=1,Wings=1$, so $Bird\to Wings=1$</li><li>Opus: $1\to1=1$</li><li>Fido: $0\to0=1$ because the rule is not triggered</li></ol><p>All three rows are true, so $\forall x\,Bird(x)\to Wings(x)$ holds in this structure.</p><p><b>Existential witness.</b> If flies is $[1,0,0]$, then $Bird(x)\land Flies(x)$ is $[1,0,0]$.</p><ol class="work"><li>Tweety: $1\land1=1$</li><li>Opus: $1\land0=0$</li><li>Fido: $0\land0=0$</li></ol><p>The witness is Tweety, so the existential claim is true for a concrete reason.</p><p><b>Relational inference.</b> From $Parent(Ann,Bob)=1$ and $Parent(Bob,Cal)=1$, the grandparent rule gives $Grandparent(Ann,Cal)=1$ by matching the shared middle object Bob.</p>`,
  pitfalls: String.raw`
    <ul><li><b>Letting the domain drift.</b> Quantifiers range over the chosen $D$; changing $D$ changes the truth of $\forall$ and $\exists$ claims.</li><li><b>Mixing variables and constants.</b> $x$ is a placeholder to bind; Ann is an object. Unification works only when that distinction is respected.</li><li><b>Assuming existence from a universal rule.</b> $\forall x\,Bird(x)\to Wings(x)$ can be true even if there are no birds, because every implication is untriggered.</li></ul>`
};

/* ---------------- 21.3 Knowledge representation & reasoning ---------------- */
window.ALLML_CONTENT["21.3"] = {
  tagline: "A representation is useful when the facts you store make the conclusions you need cheap, explainable, and safe.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/21.3-knowledge-representation.ipynb",
  context: String.raw`
    <p>Knowledge representation is the bridge between raw logic and useful AI systems.</p><ul><li><b>First-order logic (21.2)</b> contributes predicates and relations; KR decides which predicates deserve to exist and how they should be connected.</li><li><b>Graphs and inheritance</b> supply the mechanism for reusing facts, so a property stored once at Bird can flow to Penguin and Tweety.</li><li><b>Rule-based systems (21.6)</b> use the representation as their working memory, while ontologies (21.4) make the class hierarchy more formal.</li></ul><p>Where it leads: the same choice of entities, edges, defaults, and explanations determines whether RDF graphs (21.5), expert systems (21.6), and theorem provers (21.10) can reason cleanly instead of drowning in ad hoc facts.</p>`,
  intuition: String.raw`
    <p>The concrete problem is that facts alone do not answer the questions people care about. If a system stores only Tweety is a penguin, it still needs a principled way to conclude Tweety has wings, to notice that flying is blocked, and to explain both steps.</p><p>The naive approach is a giant lookup table of every possible conclusion. That is brittle: adding one new class forces many new rows, and contradictions hide because nothing records which fact should override which.</p><p>The mental model is a labeled map. Nodes are concepts or individuals, edges say how they inherit or relate, and defaults are soft paths that a more specific path can block. The design decision people gloss over is that representation is not neutral: storing Penguin under Bird makes inheritance cheap, while storing it as an unrelated label makes even obvious reasoning expensive.</p>`,
  mathematics: String.raw`
    <p>Let classes form edges $Tweety\to Penguin\to Bird$, and let properties attach to classes. Inheritance is set union along the path.</p><ol class="work"><li>$Props(Bird)=\{wings\}$</li><li>$Props(Penguin)=\{swims\}$</li><li>$Props(Tweety)=\varnothing$</li><li>union along Tweety, Penguin, Bird gives $\{wings,swims\}$</li></ol><p>The conclusion carries an explanation path, not just a label. A default conflict can be scored as $+1$ for bird-flies and $-1$ for penguin-not-flies, so $+1-1=0$ and the specific override blocks the default.</p>`,
  pitfalls: String.raw`
    <ul><li><b>Choosing a representation that hides the constraint.</b> The solver can only exploit structure that appears in the formal variables, clauses, roles, or triples.</li><li><b>Confusing a surviving candidate with a proved answer.</b> The Mathematics section always checks every relevant rule or constraint; skipping that check turns search into guesswork.</li><li><b>Ignoring explanation paths.</b> Symbolic systems are valuable because they can show which rule, clause, or similarity calculation produced the answer.</li></ul>`
};

/* ---------------- 21.4 Description logics & ontologies ---------------- */
window.ALLML_CONTENT["21.4"] = {
  tagline: "Ontologies trade some expressiveness for reliable classification: concepts, roles, subsumption, and consistency checks.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/21.4-description-logics.ipynb",
  context: String.raw`
    <p>Description logics make the class-and-role part of first-order logic precise enough for ontology reasoners.</p><ul><li><b>First-order predicates (21.2)</b> become concepts such as Professor and roles such as hasChild; their extensions are the sets and pairs used in the calculations.</li><li><b>Knowledge representation (21.3)</b> supplies inheritance and exceptions; description logics restrict those ideas so subsumption and consistency remain decidable.</li><li><b>RDF and semantic web work (21.5)</b> often uses ontology axioms to infer new type triples and catch bad data.</li></ul><p>Where it leads: ontology classification feeds semantic search, data validation, and symbolic explanations before heavier proof search in 21.10 is needed.</p>`,
  intuition: String.raw`
    <p>The concrete problem is maintaining a vocabulary that does more than name things. If Professor is a kind of Person, every professor query should also find people; if Cat and Dog are disjoint, the ontology should flag an individual typed as both.</p><p>The naive approach is to encode these checks in application code. That scatters meaning across services and makes two teams disagree about the same class hierarchy.</p><p>The mental model is a careful filing system with mathematical shelves. Each concept is a set, each role is a relation, and reasoning is set containment plus consistency checking. The design decision is the trade: description logics intentionally avoid full first-order freedom so the reasoner can always finish with a dependable classification.</p>`,
  mathematics: String.raw`
    <p>A concept $C$ denotes a set $C^I$ of individuals, and a role $R$ denotes pairs. Subsumption $C\sqsubseteq D$ means $C^I\subseteq D^I$.</p><ol class="work"><li>$Professor=\{Ada,Cy\}$ and $Person=\{Ada,Bo,Cy\}$, so $Professor\subseteq Person$ is true</li><li>$Student=\{Bo,Cy\}$ and $Employee=\{Ada,Cy\}$, so $Student\sqcap Employee=\{Cy\}$</li><li>$hasChild=\{(Ann,Cal),(Bob,Dan)\}$ and $Doctor=\{Cal\}$, so $\exists hasChild.Doctor=\{Ann\}$</li><li>$Cat\cap Dog=\{Milo\}$ violates disjointness because the required size is $0$ but the actual size is $1$</li></ol><p>Each number is small, but the mechanism is the same classifier an ontology reasoner uses at scale.</p>`,
  pitfalls: String.raw`
    <ul><li><b>Choosing a representation that hides the constraint.</b> The solver can only exploit structure that appears in the formal variables, clauses, roles, or triples.</li><li><b>Confusing a surviving candidate with a proved answer.</b> The Mathematics section always checks every relevant rule or constraint; skipping that check turns search into guesswork.</li><li><b>Ignoring explanation paths.</b> Symbolic systems are valuable because they can show which rule, clause, or similarity calculation produced the answer.</li></ul>`
};

/* ---------------- 21.5 Semantic web & RDF ---------------- */
window.ALLML_CONTENT["21.5"] = {
  tagline: "RDF makes knowledge portable by turning every fact into a graph triple that can be matched, joined, and lightly inferred over.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/21.5-semantic-web-rdf.ipynb",
  context: String.raw`
    <p>Semantic web and RDF put symbolic knowledge into a portable graph format.</p><ul><li><b>Knowledge representation (21.3)</b> contributes the entity-relation view; RDF standardizes each relation as a subject-predicate-object triple.</li><li><b>Description logics (21.4)</b> contribute class and subclass meaning that can add inferred type triples.</li><li><b>Database joins</b> provide the query mechanism: SPARQL patterns bind variables across triples.</li></ul><p>Where it leads: RDF is the graph substrate that lets ontologies, enterprise metadata, and linked data share a common reasoning surface before solver-heavy methods such as SAT (21.8) or theorem proving (21.10) are needed.</p>`,
  intuition: String.raw`
    <p>The concrete problem is exchanging knowledge across systems that do not share tables. One service may know Ada is a professor; another may know Ada works at Uni. RDF gives both facts the same shape so they can meet in a graph.</p><p>The naive approach is a custom schema for every pair of systems. That makes each integration a one-off translation project.</p><p>The mental model is a directed labeled graph where every fact is an edge. The design decision people miss is making even types into triples. That uniformity feels verbose, but it lets the same pattern matcher handle membership, attributes, and links.</p>`,
  mathematics: String.raw`
    <p>An RDF triple is $(s,p,o)$. A graph is the set of triples, and a query pattern matches triples by binding variables.</p><ol class="work"><li>Two triples for Ada give $2$ graph edges</li><li>Type triples $Professor(Ada)$ and $Professor(Cy)$ return $2$ professor bindings</li><li>If $Professor\sqsubseteq Person$, those $2$ professor triples infer $2$ person triples</li><li>The pattern $\ ?x\ worksAt\ Uni$ matches Ada and Cy, so the result count is $2$</li><li>Joining type Professor with worksAt Uni keeps Ada and Cy, again $2$ bindings</li></ol><p>The arithmetic is just filtering and joining, which is why RDF can be both logical and database-like.</p>`,
  pitfalls: String.raw`
    <ul><li><b>Choosing a representation that hides the constraint.</b> The solver can only exploit structure that appears in the formal variables, clauses, roles, or triples.</li><li><b>Confusing a surviving candidate with a proved answer.</b> The Mathematics section always checks every relevant rule or constraint; skipping that check turns search into guesswork.</li><li><b>Ignoring explanation paths.</b> Symbolic systems are valuable because they can show which rule, clause, or similarity calculation produced the answer.</li></ul>`
};

/* ---------------- 21.6 Rule-based & expert systems ---------------- */
window.ALLML_CONTENT["21.6"] = {
  tagline: "Expert systems make reasoning inspectable: facts trigger rules, rules add facts, and the fired chain explains the answer.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/21.6-rule-based-expert-systems.ipynb",
  context: String.raw`
    <p>Rule-based expert systems turn symbolic knowledge into an inspectable decision process.</p><ul><li><b>Propositional and first-order logic (21.1, 21.2)</b> supply the if-then form and variable matching used in rule antecedents.</li><li><b>Knowledge representation (21.3)</b> supplies the working facts that rules read and extend.</li><li><b>Theorem proving (21.10)</b> generalizes the same proof idea, while expert systems keep the rule trace closer to human explanations.</li></ul><p>Where it leads: rules are still used for policy, triage, and business logic because a fired chain can be audited; later solver lessons show what to do when rule firing becomes a hard search problem.</p>`,
  intuition: String.raw`
    <p>The concrete problem is capturing expert judgment so a system can make a recommendation and explain it. A doctor or operator may say, if fever and cough are present, consider flu; that knowledge should not be buried in opaque code.</p><p>The naive approach is a pile of nested if statements. It works at first, then becomes impossible to audit because facts, priorities, and explanations are tangled together.</p><p>The mental model is a production line. Facts enter, rules whose prerequisites are satisfied fire, and new facts are added until no rule can add more. The design decision is separating the rule base from the inference engine, which lets the same engine explain, prioritize, and rerun rules as knowledge changes.</p>`,
  mathematics: String.raw`
    <p>A rule has antecedents $A$ and consequent $c$. Forward chaining adds $c$ whenever $A\subseteq Facts$.</p><ol class="work"><li>$\{fever,cough\}\subseteq\{fever,cough\}$, so flu is added</li><li>Then $\{flu\}\subseteq Facts$, so rest is added</li><li>Then $\{rest\}\subseteq Facts$, so hydrate is added</li><li>The fact count grows $2\to3\to4\to5$ and stops at a fixed point</li><li>With certainty factors, $min(0.8,0.7)\times0.9=0.7\times0.9=0.63$</li></ol><p>The final diagnosis is useful because the rule trace shows why it appeared.</p>`,
  pitfalls: String.raw`
    <ul><li><b>Choosing a representation that hides the constraint.</b> The solver can only exploit structure that appears in the formal variables, clauses, roles, or triples.</li><li><b>Confusing a surviving candidate with a proved answer.</b> The Mathematics section always checks every relevant rule or constraint; skipping that check turns search into guesswork.</li><li><b>Ignoring explanation paths.</b> Symbolic systems are valuable because they can show which rule, clause, or similarity calculation produced the answer.</li></ul>`
};

/* ---------------- 21.7 Answer-set programming ---------------- */
window.ALLML_CONTENT["21.7"] = {
  tagline: "Answer-set programs describe constraints on possible worlds, then keep the worlds that are self-consistent and supported.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/21.7-answer-set-programming.ipynb",
  context: String.raw`
    <p>Answer-set programming is logic programming for problems where several possible worlds may satisfy the rules.</p><ul><li><b>Logic (21.1 and 21.2)</b> supplies atoms and rules, but ASP adds negation-as-failure to reason about what is not derivable.</li><li><b>Constraint satisfaction (21.13)</b> supplies the search flavor: generate candidate worlds, then reject worlds that violate constraints.</li><li><b>SAT solving (21.8)</b> is often the engine underneath modern ASP systems, translating stable-model search into Boolean machinery.</li></ul><p>Where it leads: ASP is useful for scheduling, configuration, and planning tasks where the model should describe the desired world rather than a step-by-step algorithm.</p>`,
  intuition: String.raw`
    <p>The concrete problem is choosing among self-consistent possibilities. In a schedule, a task may be assigned AM or PM, but not both, and every chosen assignment must be justified by the rules.</p><p>The naive approach is procedural backtracking code. It hides the problem statement inside control flow, so changing a constraint means changing the search program.</p><p>The mental model is possible worlds with a support test. A world survives only if it satisfies the constraints and every atom in it has a reason to be there. The design decision people gloss over is negation-as-failure: not proving busy is treated as permission to schedule, which is powerful but must be scoped carefully.</p>`,
  mathematics: String.raw`
    <p>An answer set is a supported set of atoms that satisfies every rule and constraint after negation-as-failure is evaluated.</p><ol class="work"><li>The possible subsets of $\{a,b\}$ are $4$ worlds</li><li>The choice rule keeps $\{a\}$ and $\{b\}$, so $2$ stable choices remain</li><li>The constraint not both removes $\{a,b\}$</li><li>A support check rejects $\{see\}$ when $light$ is absent</li><li>Optimization compares costs $cost(\{a\})=2$ and $cost(\{b\})=1$, so $\{b\}$ wins</li></ol><p>The solver is not merely finding any model; it is finding a self-justifying model.</p>`,
  pitfalls: String.raw`
    <ul><li><b>Choosing a representation that hides the constraint.</b> The solver can only exploit structure that appears in the formal variables, clauses, roles, or triples.</li><li><b>Confusing a surviving candidate with a proved answer.</b> The Mathematics section always checks every relevant rule or constraint; skipping that check turns search into guesswork.</li><li><b>Ignoring explanation paths.</b> Symbolic systems are valuable because they can show which rule, clause, or similarity calculation produced the answer.</li></ul>`
};

/* ---------------- 21.8 SAT solvers ---------------- */
window.ALLML_CONTENT["21.8"] = {
  tagline: "SAT solving scales truth tables by searching Boolean assignments with propagation, pruning, and backtracking.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/21.8-sat-solvers.ipynb",
  context: String.raw`
    <p>SAT solvers industrialize the truth-table idea from propositional logic.</p><ul><li><b>Propositional logic (21.1)</b> supplies Boolean variables, clauses, and satisfaction.</li><li><b>Search and pruning</b> supply the operational mechanism: try assignments, propagate forced literals, and backtrack only when necessary.</li><li><b>SMT (21.9) and ASP (21.7)</b> build on SAT by adding theories or stable-model semantics above the Boolean core.</li></ul><p>Where it leads: many planning, verification, scheduling, and synthesis problems become practical only after they are encoded into SAT or a SAT-like solver.</p>`,
  intuition: String.raw`
    <p>The concrete problem is finding one assignment that makes a large pile of Boolean requirements true. A truth table is exact, but it doubles with every variable, so it fails before real problems begin.</p><p>The mental model is a maze where every variable choice closes doors. Unit propagation walks through doors that are forced; pure literals remove clauses that can be satisfied without risk; backtracking returns only when a contradiction appears.</p><p>The design decision is to represent formulas in CNF. CNF may look restrictive, but clauses make local failure and local satisfaction cheap to detect, which is exactly what the search engine needs.</p>`,
  mathematics: String.raw`
    <p>A CNF formula is a conjunction of clauses, and each clause is a disjunction of literals. For $(A\lor B)\land(\neg A\lor C)$ over three variables there are $2^3=8$ assignments.</p><ol class="work"><li>Brute force checks $8$ assignments</li><li>Exactly $4$ make both clauses true</li><li>The unit clause $(A)$ forces $A=1$</li><li>Substituting $A=1$ turns $(\neg A\lor C)$ into $(C)$</li><li>Literal $B$ appearing only positively is pure, so $B=1$ satisfies every clause containing it</li></ol><p>DPLL is powerful because each forced assignment shrinks the remaining search tree.</p>`,
  pitfalls: String.raw`
    <ul><li><b>Choosing a representation that hides the constraint.</b> The solver can only exploit structure that appears in the formal variables, clauses, roles, or triples.</li><li><b>Confusing a surviving candidate with a proved answer.</b> The Mathematics section always checks every relevant rule or constraint; skipping that check turns search into guesswork.</li><li><b>Ignoring explanation paths.</b> Symbolic systems are valuable because they can show which rule, clause, or similarity calculation produced the answer.</li></ul>`
};

/* ---------------- 21.9 SMT solvers ---------------- */
window.ALLML_CONTENT["21.9"] = {
  tagline: "SMT combines Boolean search with theory reasoning, so assignments must satisfy both clauses and arithmetic meaning.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/21.9-smt-solvers.ipynb",
  context: String.raw`
    <p>SMT solvers extend SAT from pure Booleans to formulas whose atoms have arithmetic or other theory meaning.</p><ul><li><b>SAT solving (21.8)</b> provides the Boolean search over which atoms are assumed true.</li><li><b>Linear algebra and inequalities</b> provide the theory checks, such as whether $x\ge2$ and $x\le5$ share a feasible interval.</li><li><b>Automated theorem proving (21.10)</b> shares the goal of machine-checked reasoning, but SMT specializes in decidable theory fragments.</li></ul><p>Where it leads: SMT is central in program verification, symbolic execution, and constraint-heavy configuration where Boolean structure and numeric feasibility must agree.</p>`,
  intuition: String.raw`
    <p>The concrete problem is that many requirements are not merely true or false labels. A program path might require $x\ge2$, $x\le5$, and a branch flag; a Boolean SAT solver can choose the flags but cannot understand the interval.</p><p>The mental model is a partnership. The SAT engine proposes a combination of atoms, and the theory solver says whether those atoms can all be true in the intended mathematics.</p><p>The design decision people miss is abstraction. SMT first treats arithmetic atoms as Boolean names so it can reuse SAT search, then sends only promising combinations to the theory solver. That separation is why SMT can be both general and fast.</p>`,
  mathematics: String.raw`
    <p>SMT abstracts theory atoms into Boolean variables, then asks a theory solver whether the chosen atoms can be true together.</p><ol class="work"><li>$P\land Q$ has $1$ satisfying Boolean row out of $4$</li><li>The theory atoms $x\ge2$ and $x\le5$ define the interval $[2,5]$, which is nonempty</li><li>The atoms $x\ge4$ and $x\le1$ define an empty interval because $4\gt1$</li><li>Every $x$ in $[2,5]$ also satisfies $x\le6$, so that atom is theory-implied</li><li>A Boolean branch is accepted only when the arithmetic region is nonempty</li></ol><p>The Boolean engine proposes; the theory engine disposes.</p>`,
  pitfalls: String.raw`
    <ul><li><b>Choosing a representation that hides the constraint.</b> The solver can only exploit structure that appears in the formal variables, clauses, roles, or triples.</li><li><b>Confusing a surviving candidate with a proved answer.</b> The Mathematics section always checks every relevant rule or constraint; skipping that check turns search into guesswork.</li><li><b>Ignoring explanation paths.</b> Symbolic systems are valuable because they can show which rule, clause, or similarity calculation produced the answer.</li></ul>`
};

/* ---------------- 21.10 Automated theorem proving ---------------- */
window.ALLML_CONTENT["21.10"] = {
  tagline: "Resolution proves a goal by adding its negation and deriving the empty clause, leaving a checkable proof trail.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/21.10-automated-theorem-proving.ipynb",
  context: String.raw`
    <p>Automated theorem proving turns symbolic reasoning into a proof search with checkable steps.</p><ul><li><b>First-order logic (21.2)</b> supplies quantified rules, predicates, and unification.</li><li><b>Propositional logic (21.1)</b> supplies the clause-level truth conditions used after formulas are converted to clause form.</li><li><b>Rule-based systems (21.6)</b> show forward explanations; theorem proving generalizes this to contradiction, resolution, and proof DAGs.</li></ul><p>Where it leads: theorem provers support formal verification, proof assistants, and logic-heavy AI systems where an answer is not enough unless the proof can be checked.</p>`,
  intuition: String.raw`
    <p>The concrete problem is proving that a goal follows from a knowledge base without trusting a human chain of reasoning. The naive approach is forward chaining every possible conclusion, which can explode or wander away from the goal.</p><p>The mental model is to argue by contradiction. Add the negation of what you want to prove; if the knowledge base plus that negation collapses into the empty clause, the negation was impossible.</p><p>The design decision is resolution on clauses. It reduces many proof steps to one repeatable operation: find complementary literals, cancel them, and keep the resolvent. That uniform step is what makes proof search automatable and proof checking simple.</p>`,
  mathematics: String.raw`
    <p>Resolution uses clauses. Resolving $(P\lor Q)$ with $(\neg P\lor R)$ cancels the complementary literals and yields $(Q\lor R)$.</p><ol class="work"><li>$P\to Q$ is rewritten as $\neg P\lor Q$</li><li>With clauses $(P)$ and $(\neg P\lor Q)$, resolution derives $(Q)$</li><li>Adding the negated goal gives $(\neg Q)$</li><li>Resolving $(Q)$ with $(\neg Q)$ yields the empty clause with $0$ literals</li><li>The active clause sizes can be read as $3\to2\to1\to0$ in a tiny proof trace</li></ol><p>Empty clause means the negated goal was impossible with the knowledge base, so the original goal is proved.</p>`,
  pitfalls: String.raw`
    <ul><li><b>Choosing a representation that hides the constraint.</b> The solver can only exploit structure that appears in the formal variables, clauses, roles, or triples.</li><li><b>Confusing a surviving candidate with a proved answer.</b> The Mathematics section always checks every relevant rule or constraint; skipping that check turns search into guesswork.</li><li><b>Ignoring explanation paths.</b> Symbolic systems are valuable because they can show which rule, clause, or similarity calculation produced the answer.</li></ul>`
};

/* ---------------- 21.11 Case-based reasoning ---------------- */
window.ALLML_CONTENT["21.11"] = {
  tagline: "Case-based reasoning solves the new problem by retrieving a similar old one, adapting it, checking it, and retaining the lesson.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/21.11-case-based-reasoning.ipynb",
  context: String.raw`
    <p>Case-based reasoning is symbolic memory with a similarity metric.</p><ul><li><b>Nearest-neighbor geometry</b> supplies retrieval: cases become feature vectors and distance ranks relevance.</li><li><b>Knowledge representation (21.3)</b> supplies the case schema, deciding which features and solution fields are stored.</li><li><b>Fuzzy logic (21.12)</b> connects naturally because similarity and partial match often need graded rather than crisp judgments.</li></ul><p>Where it leads: CBR appears in support, diagnosis, recommendation, and legal or medical analogical reasoning where precedent matters and explanation by similar example is valuable.</p>`,
  intuition: String.raw`
    <p>The concrete problem is solving a new case when rules are hard to write but old solved cases are plentiful. A support agent often asks, what previous incident looks most like this one?</p><p>The naive approach is exact matching. It fails when the new case is close but not identical, which is precisely when memory should help.</p><p>The mental model is retrieve, reuse, revise, retain. Find a similar case, adapt its solution, check the result, and store the corrected experience. The design decision people gloss over is the similarity metric: it quietly defines what the system considers relevant experience.</p>`,
  mathematics: String.raw`
    <p>Let cases be feature vectors $x_i\in\mathbb{R}^2$. Retrieval often uses distance $d_i=\sqrt{\sum_j w_j(x_{ij}-q_j)^2}$.</p><ol class="work"><li>For query $(2.2,2.0)$ and case $(2,2)$, distance is $\sqrt{0.2^2+0^2}=0.2$</li><li>Cases $(1,1)$ and $(5,5)$ are farther, so case 2 is retrieved</li><li>With weights $(3,1)$, the nearest case remains case 2 because the weighted distance is $\sqrt{3\cdot0.2^2+0}=0.346$</li><li>Adaptation from old solution $10$ with slope $2$ and feature change $1$ gives $10+2\cdot1=12$</li><li>If the actual value is $11.5$, revision error is $|12-11.5|=0.5$</li></ol><p>The retained corrected case improves the library exactly where it was weak.</p>`,
  pitfalls: String.raw`
    <ul><li><b>Choosing a representation that hides the constraint.</b> The solver can only exploit structure that appears in the formal variables, clauses, roles, or triples.</li><li><b>Confusing a surviving candidate with a proved answer.</b> The Mathematics section always checks every relevant rule or constraint; skipping that check turns search into guesswork.</li><li><b>Ignoring explanation paths.</b> Symbolic systems are valuable because they can show which rule, clause, or similarity calculation produced the answer.</li></ul>`
};

/* ---------------- 21.12 Fuzzy logic ---------------- */
window.ALLML_CONTENT["21.12"] = {
  tagline: "Fuzzy logic replaces brittle yes-or-no membership with degrees of truth that combine smoothly and defuzzify into actions.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/21.12-fuzzy-logic.ipynb",
  context: String.raw`
    <p>Fuzzy logic handles symbolic categories whose boundaries are gradual rather than crisp.</p><ul><li><b>Classical logic (21.1)</b> supplies AND, OR, and NOT; fuzzy logic replaces their truth values with degrees in $[0,1]$.</li><li><b>Expert systems (21.6)</b> supply rules; fuzzy systems let a rule fire partially instead of all-or-nothing.</li><li><b>Control and decision systems</b> use defuzzification to turn graded conclusions into a single action.</li></ul><p>Where it leads: fuzzy logic is a practical bridge between human terms like warm or high risk and numerical controllers, and it also clarifies why degrees of membership are different from probabilities.</p>`,
  intuition: String.raw`
    <p>The concrete problem is that many useful words do not have sharp boundaries. A room at 24.9 degrees is not cold while 25.1 is suddenly warm; a crisp threshold creates brittle decisions.</p><p>The mental model is a dimmer switch for truth. Inputs belong to concepts by degree, rules fire by the weakest matching antecedent, and overlapping recommendations are blended into one action.</p><p>The design decision is to model compatibility, not randomness. A membership value of 0.7 for warm says the temperature fits the word warm fairly well; it does not say there is a 70% chance of being warm.</p>`,
  mathematics: String.raw`
    <p>A fuzzy set has membership $\mu(x)\in[0,1]$. Common connectives are $\mu_{A\land B}=min(\mu_A,\mu_B)$ and $\mu_{A\lor B}=max(\mu_A,\mu_B)$.</p><ol class="work"><li>For memberships $0.7$ and $0.4$, fuzzy AND is $min(0.7,0.4)=0.4$</li><li>Fuzzy OR is $max(0.7,0.4)=0.7$</li><li>A rule with strength $0.6$ clips a consequent so its maximum is $0.6$</li><li>A symmetric triangular output centered at $6$ has centroid $6.00$</li><li>A smooth controller using $5+2\tanh(e)$ stays between about $3.07$ and $6.93$ for $e\in[-2,2]$</li></ol><p>Degrees of truth are not probabilities; they are graded compatibility with a linguistic concept.</p>`,
  pitfalls: String.raw`
    <ul><li><b>Choosing a representation that hides the constraint.</b> The solver can only exploit structure that appears in the formal variables, clauses, roles, or triples.</li><li><b>Confusing a surviving candidate with a proved answer.</b> The Mathematics section always checks every relevant rule or constraint; skipping that check turns search into guesswork.</li><li><b>Ignoring explanation paths.</b> Symbolic systems are valuable because they can show which rule, clause, or similarity calculation produced the answer.</li></ul>`
};

/* ---------------- 21.13 Constraint satisfaction problems ---------------- */
window.ALLML_CONTENT["21.13"] = {
  tagline: "CSPs make search explicit: choose variable values from domains while constraints prune impossible partial assignments.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/21.13-constraint-satisfaction-problems.ipynb",
  context: String.raw`
    <p>Constraint satisfaction problems make symbolic search explicit and modular.</p><ul><li><b>SAT solving (21.8)</b> is the Boolean special case; CSPs allow variables with richer finite domains such as colors or time slots.</li><li><b>Graph structure</b> supplies the constraint graph, telling the solver which variables can prune each other.</li><li><b>Answer-set programming (21.7)</b> and SMT (21.9) share the generate-and-check spirit but add different languages above the core constraints.</li></ul><p>Where it leads: CSPs are the clean model behind scheduling, timetabling, map coloring, configuration, and many planning systems.</p>`,
  intuition: String.raw`
    <p>The concrete problem is assigning values without violating relationships. A map-coloring problem is easy to state: each region gets a color, and neighboring regions must differ.</p><p>The naive approach is brute force: try every complete assignment and reject bad ones at the end. That wastes time because many partial assignments are already doomed.</p><p>The mental model is disciplined narrowing. Pick a variable, assign a value, immediately remove incompatible values from neighbors, and backtrack as soon as a domain becomes empty. The design decision is to expose domains and constraints separately, so pruning can happen before the full assignment exists.</p>`,
  mathematics: String.raw`
    <p>A CSP is $(X,D,C)$: variables $X$, domains $D_i$, and constraints $C$. A solution assigns every variable a value while satisfying every constraint.</p><ol class="work"><li>A triangle map has $3$ variables and $3$ inequality constraints</li><li>With colors $\{R,G,B\}$ there are $3^3=27$ assignments</li><li>All-different leaves $3\times2\times1=6$ valid colorings</li><li>Backtracking with early rejection visits $16$ nodes instead of checking only full assignments blindly</li><li>After assigning $A=R$, forward checking shrinks $B$ and $C$ from $3$ colors to $2$ each</li></ol><p>The central idea is not clever guessing; it is pruning impossible partial worlds as soon as they become impossible.</p>`,
  pitfalls: String.raw`
    <ul><li><b>Choosing a representation that hides the constraint.</b> The solver can only exploit structure that appears in the formal variables, clauses, roles, or triples.</li><li><b>Confusing a surviving candidate with a proved answer.</b> The Mathematics section always checks every relevant rule or constraint; skipping that check turns search into guesswork.</li><li><b>Ignoring explanation paths.</b> Symbolic systems are valuable because they can show which rule, clause, or similarity calculation produced the answer.</li></ul>`
};
