/* All ML — authored content for Part 6: Deep Learning Foundations (6.1–6.34).
   Appends to window.ALLML_CONTENT. Numbers generated by the verified Part-6 builder. */
window.ALLML_CONTENT = window.ALLML_CONTENT || {};

/* ---------------- 6.1 The perceptron & MLPs ---------------- */
window.ALLML_CONTENT["6.1"] = {
  tagline: "Stacking affine maps and nonlinear gates turns a single separating rule into a trainable function composer.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.1-perceptron-mlps.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>earlier optimization and linear-algebra lessons</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the affine score and hidden activation prepares you for (6.2) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. The perceptron & MLPs is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the affine score and hidden activation, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the affine score and hidden activation</b>:</p>
    <div class="formula-box">$$z=W x+b, \qquad h=\phi(z), \qquad \hat y=W_2h+b_2$$</div>
    <p>$x\in\mathbb{R}^d$ is the input, $W$ is a weight matrix, $b$ is a bias vector, $\phi$ is a nonlinear activation, and $\hat y$ is the network output. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.400$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.200\cdot1.5+-0.400\cdot(-0.5)+0.400=1.800+0.200+0.400=2.400$</li>
      <li>gated signal: $\max(0,2.400)=2.400$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.060$ and gradient $g=1.350$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.060\cdot1.350=2.000-0.081=1.919$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.400$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.400}=11.023$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=11.023/(11.023+1.492)=0.881$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $3$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.400-1.000)/\sqrt{0.250+0.00001}=2.800$</li>
      <li>memory $=3\cdot128\cdot4/1024=1.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.2 The universal approximation theorem ---------------- */
window.ALLML_CONTENT["6.2"] = {
  tagline: "A wide enough nonlinear network can approximate any continuous target on a compact domain, but the theorem says existence, not easy training.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.2-universal-approximation.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.1)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding a weighted sum of nonlinear basis bumps prepares you for (6.3) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. The universal approximation theorem is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through a weighted sum of nonlinear basis bumps, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>a weighted sum of nonlinear basis bumps</b>:</p>
    <div class="formula-box">$$f(x)pprox\sum_{j=1}^{m} a_j\,\phi(w_jx+b_j)$$</div>
    <p>$m$ is the number of hidden units, $a_j$ are output weights, and each $\phi(w_jx+b_j)$ is one adjustable nonlinear feature. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.500$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.400\cdot1.5+-0.100\cdot(-0.5)+0.500=2.100+0.050+0.500=2.650$</li>
      <li>gated signal: $\max(0,2.650)=2.650$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.070$ and gradient $g=1.500$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.070\cdot1.500=2.000-0.105=1.895$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.650$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.650}=14.154$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=14.154/(14.154+1.492)=0.905$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $4$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.650-1.000)/\sqrt{0.250+0.00001}=3.300$</li>
      <li>memory $=4\cdot128\cdot4/1024=2.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.3 Activation functions (sigmoid, tanh, ReLU, GELU, softmax) ---------------- */
window.ALLML_CONTENT["6.3"] = {
  tagline: "Activations decide which signals pass, which saturate, and which become probabilities.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.3-activation-functions.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.2)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the activation transform prepares you for (6.4) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Activation functions (sigmoid, tanh, ReLU, GELU, softmax) is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the activation transform, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the activation transform</b>:</p>
    <div class="formula-box">$$h=\phi(z), \qquad \mathrm{softmax}(z)_i=    rac{e^{z_i}}{\sum_j e^{z_j}}$$</div>
    <p>$z$ is a pre-activation, $h$ is the transformed signal, and softmax maps a logit vector to probabilities that sum to one. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.600$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.600\cdot1.5+0.200\cdot(-0.5)+0.600=2.400+-0.100+0.600=2.900$</li>
      <li>gated signal: $\max(0,2.900)=2.900$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.080$ and gradient $g=1.650$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.080\cdot1.650=2.000-0.132=1.868$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.900$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.900}=18.174$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=18.174/(18.174+1.492)=0.924$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $5$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.900-1.000)/\sqrt{0.250+0.00001}=3.800$</li>
      <li>memory $=5\cdot128\cdot4/1024=2.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.4 Backpropagation ---------------- */
window.ALLML_CONTENT["6.4"] = {
  tagline: "Backprop sends credit backward by multiplying local derivatives along the computational path.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.4-backpropagation.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.3)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the chain-rule gradient prepares you for (6.5) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Backpropagation is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the chain-rule gradient, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the chain-rule gradient</b>:</p>
    <div class="formula-box">$$    rac{\partial L}{\partial W_1}=    rac{\partial L}{\partial h}    rac{\partial h}{\partial z_1}    rac{\partial z_1}{\partial W_1}$$</div>
    <p>$L$ is the loss, $h$ is a hidden activation, $z_1$ is its pre-activation, and each factor is a local derivative cached from the forward pass. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.700$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.800\cdot1.5+-0.700\cdot(-0.5)+0.700=2.700+0.350+0.700=3.750$</li>
      <li>gated signal: $\max(0,3.750)=3.750$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.090$ and gradient $g=1.800$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.090\cdot1.800=2.000-0.162=1.838$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $3.750$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{3.750}=42.521$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=42.521/(42.521+1.492)=0.966$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $2$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(3.750-1.000)/\sqrt{0.250+0.00001}=5.500$</li>
      <li>memory $=2\cdot128\cdot4/1024=1.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.5 Automatic differentiation & computational graphs ---------------- */
window.ALLML_CONTENT["6.5"] = {
  tagline: "Autodiff records the computation graph, then applies exact chain-rule bookkeeping through every node.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.5-automatic-differentiation.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.4)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding a computational-graph local derivative prepares you for (6.6) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Automatic differentiation & computational graphs is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through a computational-graph local derivative, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>a computational-graph local derivative</b>:</p>
    <div class="formula-box">$$ar v=\sum_{u\in children(v)} ar u\,    rac{\partial u}{\partial v}$$</div>
    <p>$v$ is a graph node, $ar v=\partial L/\partial v$ is its adjoint, and children are downstream nodes that used $v$. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.800$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.000\cdot1.5+-0.400\cdot(-0.5)+0.800=1.500+0.200+0.800=2.500$</li>
      <li>gated signal: $\max(0,2.500)=2.500$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.050$ and gradient $g=1.950$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.050\cdot1.950=2.000-0.098=1.903$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.500$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.500}=12.182$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=12.182/(12.182+1.492)=0.891$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $3$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.500-1.000)/\sqrt{0.250+0.00001}=3.000$</li>
      <li>memory $=3\cdot128\cdot4/1024=1.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.6 Loss functions (MSE, cross-entropy, hinge, contrastive, triplet) ---------------- */
window.ALLML_CONTENT["6.6"] = {
  tagline: "A loss function encodes what kind of mistake matters, so optimization follows the right notion of wrongness.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.6-loss-functions.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.5)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the loss signal prepares you for (6.7) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Loss functions (MSE, cross-entropy, hinge, contrastive, triplet) is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the loss signal, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the loss signal</b>:</p>
    <div class="formula-box">$$L_{CE}=-\sum_i y_i\log p_i,\qquad L_{MSE}=    rac{1}{n}\sum_i(\hat y_i-y_i)^2$$</div>
    <p>$y$ is the target, $p$ is predicted probability, $\hat y$ is a regression prediction, and the loss is the scalar training objective. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.300$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.200\cdot1.5+-0.100\cdot(-0.5)+0.300=1.800+0.050+0.300=2.150$</li>
      <li>gated signal: $\max(0,2.150)=2.150$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.060$ and gradient $g=2.100$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.060\cdot2.100=2.000-0.126=1.874$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.150$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.150}=8.585$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=8.585/(8.585+1.492)=0.852$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $4$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.150-1.000)/\sqrt{0.250+0.00001}=2.300$</li>
      <li>memory $=4\cdot128\cdot4/1024=2.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.7 SGD, Momentum, Nesterov ---------------- */
window.ALLML_CONTENT["6.7"] = {
  tagline: "Momentum turns noisy gradient steps into motion with memory; Nesterov looks ahead before correcting.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.7-sgd-momentum-nesterov.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.6)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the velocity update prepares you for (6.8) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. SGD, Momentum, Nesterov is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the velocity update, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the velocity update</b>:</p>
    <div class="formula-box">$$v_t=\mu v_{t-1}-\eta g_t,\qquad 	heta_t=	heta_{t-1}+v_t$$</div>
    <p>$g_t$ is the minibatch gradient, $\eta$ is the learning rate, $\mu$ is momentum, and $v_t$ is accumulated velocity. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.400$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.400\cdot1.5+0.200\cdot(-0.5)+0.400=2.100+-0.100+0.400=2.400$</li>
      <li>gated signal: $\max(0,2.400)=2.400$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.070$ and gradient $g=1.200$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.070\cdot1.200=2.000-0.084=1.916$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.400$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.400}=11.023$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=11.023/(11.023+1.492)=0.881$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $5$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.400-1.000)/\sqrt{0.250+0.00001}=2.800$</li>
      <li>memory $=5\cdot128\cdot4/1024=2.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.8 AdaGrad, RMSProp, Adam, AdamW ---------------- */
window.ALLML_CONTENT["6.8"] = {
  tagline: "Adaptive optimizers scale each coordinate by its own history, so frequently large gradients get smaller effective steps.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.8-adaptive-optimizers.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.7)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the adaptive step prepares you for (6.9) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. AdaGrad, RMSProp, Adam, AdamW is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the adaptive step, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the adaptive step</b>:</p>
    <div class="formula-box">$$m_t=eta_1m_{t-1}+(1-eta_1)g_t,\quad v_t=eta_2v_{t-1}+(1-eta_2)g_t^2$$</div>
    <p>$m_t$ tracks first moments, $v_t$ tracks squared gradients, and the parameter step divides by $\sqrt{v_t}+\epsilon$. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.500$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.600\cdot1.5+-0.700\cdot(-0.5)+0.500=2.400+0.350+0.500=3.250$</li>
      <li>gated signal: $\max(0,3.250)=3.250$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.080$ and gradient $g=1.350$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.080\cdot1.350=2.000-0.108=1.892$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $3.250$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{3.250}=25.790$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=25.790/(25.790+1.492)=0.945$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $2$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(3.250-1.000)/\sqrt{0.250+0.00001}=4.500$</li>
      <li>memory $=2\cdot128\cdot4/1024=1.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.9 Lion, LAMB & large-batch optimizers ---------------- */
window.ALLML_CONTENT["6.9"] = {
  tagline: "Large-batch optimizers control direction and layerwise scale so huge updates do not trample useful geometry.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.9-lion-lamb-large-batch.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.8)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the sign or trust-ratio step prepares you for (6.10) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Lion, LAMB & large-batch optimizers is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the sign or trust-ratio step, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the sign or trust-ratio step</b>:</p>
    <div class="formula-box">$$	heta_t=	heta_{t-1}-\eta\,\mathrm{sign}(m_t),\qquad r=    rac{\|	heta\|}{\|u\|}$$</div>
    <p>$m_t$ is momentum, $u$ is the raw update, and $r$ is LAMB's trust ratio matching update size to parameter size. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.600$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.800\cdot1.5+-0.400\cdot(-0.5)+0.600=2.700+0.200+0.600=3.500$</li>
      <li>gated signal: $\max(0,3.500)=3.500$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.090$ and gradient $g=1.500$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.090\cdot1.500=2.000-0.135=1.865$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $3.500$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{3.500}=33.115$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=33.115/(33.115+1.492)=0.957$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $3$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(3.500-1.000)/\sqrt{0.250+0.00001}=5.000$</li>
      <li>memory $=3\cdot128\cdot4/1024=1.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.10 Second-order methods for DL (K-FAC) ---------------- */
window.ALLML_CONTENT["6.10"] = {
  tagline: "K-FAC approximates curvature with Kronecker factors so a neural-network second-order step becomes practical.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.10-second-order-kfac.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.9)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the curvature-preconditioned update prepares you for (6.11) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Second-order methods for DL (K-FAC) is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the curvature-preconditioned update, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the curvature-preconditioned update</b>:</p>
    <div class="formula-box">$$\Delta Wpprox -\eta\,A^{-1}G\,S^{-1}$$</div>
    <p>$A$ is an activation covariance factor, $S$ is a gradient covariance factor, $G$ is the ordinary gradient, and $\Delta W$ is the preconditioned weight step. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.700$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.000\cdot1.5+-0.100\cdot(-0.5)+0.700=1.500+0.050+0.700=2.250$</li>
      <li>gated signal: $\max(0,2.250)=2.250$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.050$ and gradient $g=1.650$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.050\cdot1.650=2.000-0.083=1.917$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.250$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.250}=9.488$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=9.488/(9.488+1.492)=0.864$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $4$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.250-1.000)/\sqrt{0.250+0.00001}=2.500$</li>
      <li>memory $=4\cdot128\cdot4/1024=2.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.11 Weight initialization (Xavier, He, orthogonal, LSUV) ---------------- */
window.ALLML_CONTENT["6.11"] = {
  tagline: "Initialization chooses variance so signals and gradients begin neither fading nor exploding.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.11-weight-initialization.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.10)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the initialization variance prepares you for (6.12) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Weight initialization (Xavier, He, orthogonal, LSUV) is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the initialization variance, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the initialization variance</b>:</p>
    <div class="formula-box">$$\mathrm{Var}(W)=    rac{2}{fan_{in}+fan_{out}}\quad	ext{(Xavier)},\qquad \mathrm{Var}(W)=    rac{2}{fan_{in}}\quad	ext{(He)}$$</div>
    <p>$fan_{in}$ and $fan_{out}$ count incoming and outgoing connections, setting the scale of random weights. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.800$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.200\cdot1.5+0.200\cdot(-0.5)+0.800=1.800+-0.100+0.800=2.500$</li>
      <li>gated signal: $\max(0,2.500)=2.500$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.060$ and gradient $g=1.800$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.060\cdot1.800=2.000-0.108=1.892$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.500$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.500}=12.182$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=12.182/(12.182+1.492)=0.891$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $5$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.500-1.000)/\sqrt{0.250+0.00001}=3.000$</li>
      <li>memory $=5\cdot128\cdot4/1024=2.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.12 Dropout & DropConnect ---------------- */
window.ALLML_CONTENT["6.12"] = {
  tagline: "Randomly removing activations or weights makes units stop relying on fragile co-adaptations.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.12-dropout-dropconnect.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.11)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the inverted-dropout mask prepares you for (6.13) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Dropout & DropConnect is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the inverted-dropout mask, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the inverted-dropout mask</b>:</p>
    <div class="formula-box">$$	ilde h=    rac{m\odot h}{q},\qquad m_i\sim\mathrm{Bernoulli}(q)$$</div>
    <p>$h$ is the activation vector, $m$ is the keep mask, $q$ is keep probability, and scaling by $1/q$ preserves the expected activation. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.300$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.400\cdot1.5+-0.700\cdot(-0.5)+0.300=2.100+0.350+0.300=2.750$</li>
      <li>gated signal: $\max(0,2.750)=2.750$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.070$ and gradient $g=1.950$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.070\cdot1.950=2.000-0.137=1.863$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.750$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.750}=15.643$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=15.643/(15.643+1.492)=0.913$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $2$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.750-1.000)/\sqrt{0.250+0.00001}=3.500$</li>
      <li>memory $=2\cdot128\cdot4/1024=1.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.13 Weight decay & early stopping ---------------- */
window.ALLML_CONTENT["6.13"] = {
  tagline: "Weight decay shrinks parameters directly; early stopping shrinks them indirectly by limiting training time.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.13-weight-decay-early-stopping.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.12)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the shrinkage update prepares you for (6.14) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Weight decay & early stopping is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the shrinkage update, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the shrinkage update</b>:</p>
    <div class="formula-box">$$	heta_t=(1-\eta\lambda)	heta_{t-1}-\eta g_t$$</div>
    <p>$\lambda$ is weight-decay strength, $\eta$ is learning rate, and the multiplicative term shrinks weights before the gradient step. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.400$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.600\cdot1.5+-0.400\cdot(-0.5)+0.400=2.400+0.200+0.400=3.000$</li>
      <li>gated signal: $\max(0,3.000)=3.000$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.080$ and gradient $g=2.100$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.080\cdot2.100=2.000-0.168=1.832$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $3.000$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{3.000}=20.086$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=20.086/(20.086+1.492)=0.931$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $3$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(3.000-1.000)/\sqrt{0.250+0.00001}=4.000$</li>
      <li>memory $=3\cdot128\cdot4/1024=1.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.14 Label smoothing ---------------- */
window.ALLML_CONTENT["6.14"] = {
  tagline: "Label smoothing replaces brittle one-hot certainty with a small probability budget for other classes.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.14-label-smoothing.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.13)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the smoothed target prepares you for (6.15) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Label smoothing is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the smoothed target, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the smoothed target</b>:</p>
    <div class="formula-box">$$y'_k=(1-    arepsilon)y_k+    rac{    arepsilon}{K}$$</div>
    <p>$K$ is the number of classes, $    arepsilon$ is smoothing strength, and $y'$ is the softened target distribution. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.500$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.800\cdot1.5+-0.100\cdot(-0.5)+0.500=2.700+0.050+0.500=3.250$</li>
      <li>gated signal: $\max(0,3.250)=3.250$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.090$ and gradient $g=1.200$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.090\cdot1.200=2.000-0.108=1.892$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $3.250$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{3.250}=25.790$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=25.790/(25.790+1.492)=0.945$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $4$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(3.250-1.000)/\sqrt{0.250+0.00001}=4.500$</li>
      <li>memory $=4\cdot128\cdot4/1024=2.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.15 Batch normalization ---------------- */
window.ALLML_CONTENT["6.15"] = {
  tagline: "Batch normalization standardizes activations with batch statistics, then learns how much scale and shift to restore.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.15-batch-normalization.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.14)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the batch-normalized activation prepares you for (6.16) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Batch normalization is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the batch-normalized activation, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the batch-normalized activation</b>:</p>
    <div class="formula-box">$$\hat x_i=    rac{x_i-\mu_B}{\sqrt{\sigma_B^2+\epsilon}},\qquad y_i=\gamma\hat x_i+eta$$</div>
    <p>$\mu_B$ and $\sigma_B^2$ are minibatch mean and variance, while $\gamma$ and $eta$ are learned affine parameters. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.600$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.000\cdot1.5+0.200\cdot(-0.5)+0.600=1.500+-0.100+0.600=2.000$</li>
      <li>gated signal: $\max(0,2.000)=2.000$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.050$ and gradient $g=1.350$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.050\cdot1.350=2.000-0.067=1.933$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.000$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.000}=7.389$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=7.389/(7.389+1.492)=0.832$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $5$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.000-1.000)/\sqrt{0.250+0.00001}=2.000$</li>
      <li>memory $=5\cdot128\cdot4/1024=2.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.16 Layer / group / instance normalization ---------------- */
window.ALLML_CONTENT["6.16"] = {
  tagline: "Normalization variants differ mainly in which axes provide the mean and variance.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.16-layer-group-instance-normalization.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.15)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the axis-wise normalization prepares you for (6.17) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Layer / group / instance normalization is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the axis-wise normalization, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the axis-wise normalization</b>:</p>
    <div class="formula-box">$$\hat x=    rac{x-\mu_{axes}}{\sqrt{\sigma^2_{axes}+\epsilon}}$$</div>
    <p>The chosen axes define the statistic: layer norm uses features in one example, group norm uses channel groups, and instance norm uses one image-channel instance. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.700$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.200\cdot1.5+-0.700\cdot(-0.5)+0.700=1.800+0.350+0.700=2.850$</li>
      <li>gated signal: $\max(0,2.850)=2.850$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.060$ and gradient $g=1.500$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.060\cdot1.500=2.000-0.090=1.910$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.850$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.850}=17.288$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=17.288/(17.288+1.492)=0.921$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $2$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.850-1.000)/\sqrt{0.250+0.00001}=3.700$</li>
      <li>memory $=2\cdot128\cdot4/1024=1.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.17 Residual & skip connections ---------------- */
window.ALLML_CONTENT["6.17"] = {
  tagline: "A residual block learns a correction to the identity path, giving both activations and gradients a clean shortcut.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.17-residual-skip-connections.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.16)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the residual map prepares you for (6.18) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Residual & skip connections is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the residual map, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the residual map</b>:</p>
    <div class="formula-box">$$y=x+F(x),\qquad     rac{\partial y}{\partial x}=I+    rac{\partial F}{\partial x}$$</div>
    <p>$x$ is the skip input, $F$ is the learned residual branch, and $I$ is the identity derivative carried by the shortcut. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.800$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.400\cdot1.5+-0.400\cdot(-0.5)+0.800=2.100+0.200+0.800=3.100$</li>
      <li>gated signal: $\max(0,3.100)=3.100$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.070$ and gradient $g=1.650$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.070\cdot1.650=2.000-0.116=1.885$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $3.100$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{3.100}=22.198$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=22.198/(22.198+1.492)=0.937$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $3$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(3.100-1.000)/\sqrt{0.250+0.00001}=4.200$</li>
      <li>memory $=3\cdot128\cdot4/1024=1.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.18 Vanishing & exploding gradients ---------------- */
window.ALLML_CONTENT["6.18"] = {
  tagline: "Deep gradients are products of many local factors, so numbers below one vanish and numbers above one explode.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.18-vanishing-exploding-gradients.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.17)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the gradient product prepares you for (6.19) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Vanishing & exploding gradients is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the gradient product, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the gradient product</b>:</p>
    <div class="formula-box">$$\left\|    rac{\partial L}{\partial h_0}
    ight\|pprox\left(\prod_{\ell=1}^{L}s_\ell
    ight)\left\|    rac{\partial L}{\partial h_L}
    ight\|$$</div>
    <p>$s_\ell$ is the typical layerwise gain and $L$ is depth, making repeated multiplication the mechanism. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.300$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.600\cdot1.5+-0.100\cdot(-0.5)+0.300=2.400+0.050+0.300=2.750$</li>
      <li>gated signal: $\max(0,2.750)=2.750$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.080$ and gradient $g=1.800$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.080\cdot1.800=2.000-0.144=1.856$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.750$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.750}=15.643$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=15.643/(15.643+1.492)=0.913$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $4$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.750-1.000)/\sqrt{0.250+0.00001}=3.500$</li>
      <li>memory $=4\cdot128\cdot4/1024=2.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.19 Gradient clipping ---------------- */
window.ALLML_CONTENT["6.19"] = {
  tagline: "Clipping caps the update direction's norm without changing its direction when gradients become unsafe.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.19-gradient-clipping.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.18)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the clipped gradient prepares you for (6.20) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Gradient clipping is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the clipped gradient, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the clipped gradient</b>:</p>
    <div class="formula-box">$$g_{clip}=g\cdot\min\left(1,    rac{c}{\|g\|}
    ight)$$</div>
    <p>$g$ is the raw gradient, $c$ is the norm threshold, and the multiplier is one unless the gradient is too large. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.400$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.800\cdot1.5+0.200\cdot(-0.5)+0.400=2.700+-0.100+0.400=3.000$</li>
      <li>gated signal: $\max(0,3.000)=3.000$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.090$ and gradient $g=1.950$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.090\cdot1.950=2.000-0.175=1.825$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $3.000$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{3.000}=20.086$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=20.086/(20.086+1.492)=0.931$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $5$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(3.000-1.000)/\sqrt{0.250+0.00001}=4.000$</li>
      <li>memory $=5\cdot128\cdot4/1024=2.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.20 Learning-rate schedules (step, cosine, warmup, one-cycle) ---------------- */
window.ALLML_CONTENT["6.20"] = {
  tagline: "Schedules spend learning rate where it helps: gentle warmup, decisive movement, then careful settling.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.20-learning-rate-schedules.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.19)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the scheduled learning rate prepares you for (6.21) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Learning-rate schedules (step, cosine, warmup, one-cycle) is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the scheduled learning rate, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the scheduled learning rate</b>:</p>
    <div class="formula-box">$$\eta_t=\eta_{min}+    rac12(\eta_{max}-\eta_{min})(1+\cos(\pi t/T))$$</div>
    <p>$t$ is the current step, $T$ is schedule length, and the cosine term decays smoothly from high to low rate. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.500$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.000\cdot1.5+-0.700\cdot(-0.5)+0.500=1.500+0.350+0.500=2.350$</li>
      <li>gated signal: $\max(0,2.350)=2.350$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.050$ and gradient $g=2.100$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.050\cdot2.100=2.000-0.105=1.895$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.350$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.350}=10.486$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=10.486/(10.486+1.492)=0.875$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $2$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.350-1.000)/\sqrt{0.250+0.00001}=2.700$</li>
      <li>memory $=2\cdot128\cdot4/1024=1.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.21 Gradient checking ---------------- */
window.ALLML_CONTENT["6.21"] = {
  tagline: "Gradient checking compares backprop to finite differences so silent derivative bugs become measurable.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.21-gradient-checking.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.20)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the centered finite difference prepares you for (6.22) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Gradient checking is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the centered finite difference, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the centered finite difference</b>:</p>
    <div class="formula-box">$$    rac{\partial L}{\partial 	heta_i}pprox    rac{L(	heta_i+h)-L(	heta_i-h)}{2h}$$</div>
    <p>$h$ is a tiny perturbation and the numerator measures how the loss changes symmetrically around the parameter. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.600$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.200\cdot1.5+-0.400\cdot(-0.5)+0.600=1.800+0.200+0.600=2.600$</li>
      <li>gated signal: $\max(0,2.600)=2.600$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.060$ and gradient $g=1.200$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.060\cdot1.200=2.000-0.072=1.928$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.600$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.600}=13.464$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=13.464/(13.464+1.492)=0.900$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $3$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.600-1.000)/\sqrt{0.250+0.00001}=3.200$</li>
      <li>memory $=3\cdot128\cdot4/1024=1.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.22 Gradient accumulation & activation checkpointing ---------------- */
window.ALLML_CONTENT["6.22"] = {
  tagline: "Accumulation trades time for larger effective batches; checkpointing trades extra recompute for lower activation memory.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.22-gradient-accumulation-checkpointing.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.21)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the accumulated gradient prepares you for (6.23) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Gradient accumulation & activation checkpointing is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the accumulated gradient, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the accumulated gradient</b>:</p>
    <div class="formula-box">$$g=    rac{1}{K}\sum_{k=1}^{K}g_k$$</div>
    <p>$K$ is the number of microbatches and each $g_k$ is a gradient computed before one optimizer update. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.700$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.400\cdot1.5+-0.100\cdot(-0.5)+0.700=2.100+0.050+0.700=2.850$</li>
      <li>gated signal: $\max(0,2.850)=2.850$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.070$ and gradient $g=1.350$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.070\cdot1.350=2.000-0.095=1.905$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.850$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.850}=17.288$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=17.288/(17.288+1.492)=0.921$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $4$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.850-1.000)/\sqrt{0.250+0.00001}=3.700$</li>
      <li>memory $=4\cdot128\cdot4/1024=2.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.23 Stochastic depth & spectral normalization ---------------- */
window.ALLML_CONTENT["6.23"] = {
  tagline: "Stochastic depth regularizes paths; spectral normalization caps layer gain by controlling the largest singular value.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.23-stochastic-depth-spectral-normalization.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.22)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the path survival and spectral rescale prepares you for (6.24) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Stochastic depth & spectral normalization is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the path survival and spectral rescale, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the path survival and spectral rescale</b>:</p>
    <div class="formula-box">$$y=x+b\,F(x),\qquad ar W=    rac{W}{\sigma_{max}(W)}$$</div>
    <p>$b$ is a Bernoulli survival variable and $\sigma_{max}$ is the largest singular value that bounds the layer's Lipschitz gain. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.800$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.600\cdot1.5+0.200\cdot(-0.5)+0.800=2.400+-0.100+0.800=3.100$</li>
      <li>gated signal: $\max(0,3.100)=3.100$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.080$ and gradient $g=1.500$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.080\cdot1.500=2.000-0.120=1.880$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $3.100$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{3.100}=22.198$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=22.198/(22.198+1.492)=0.937$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $5$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(3.100-1.000)/\sqrt{0.250+0.00001}=4.200$</li>
      <li>memory $=5\cdot128\cdot4/1024=2.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.24 Transfer learning & fine-tuning ---------------- */
window.ALLML_CONTENT["6.24"] = {
  tagline: "Fine-tuning starts from features learned elsewhere, then spends gradient budget carefully on the new task.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.24-transfer-learning-fine-tuning.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.23)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the fine-tuning update prepares you for (6.25) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Transfer learning & fine-tuning is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the fine-tuning update, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the fine-tuning update</b>:</p>
    <div class="formula-box">$$	heta_{base}\leftarrow	heta_{base}-\eta_{base}g_{base},\qquad 	heta_{head}\leftarrow	heta_{head}-\eta_{head}g_{head}$$</div>
    <p>Base and head parameters get separate learning rates because pretrained features usually need gentler movement. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.300$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.800\cdot1.5+-0.700\cdot(-0.5)+0.300=2.700+0.350+0.300=3.350$</li>
      <li>gated signal: $\max(0,3.350)=3.350$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.090$ and gradient $g=1.650$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.090\cdot1.650=2.000-0.148=1.851$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $3.350$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{3.350}=28.503$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=28.503/(28.503+1.492)=0.950$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $2$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(3.350-1.000)/\sqrt{0.250+0.00001}=4.700$</li>
      <li>memory $=2\cdot128\cdot4/1024=1.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.25 Data augmentation ---------------- */
window.ALLML_CONTENT["6.25"] = {
  tagline: "Augmentation teaches invariance by showing many label-preserving views of the same example.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.25-data-augmentation.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.24)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the augmented risk prepares you for (6.26) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Data augmentation is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the augmented risk, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the augmented risk</b>:</p>
    <div class="formula-box">$$R_{aug}(h)=\mathbb{E}_{(x,y)}\mathbb{E}_{t\sim T}\,\ell(h(t(x)),y)$$</div>
    <p>$T$ is a family of transformations and $t(x)$ is a view that should keep the same label. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.400$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.000\cdot1.5+-0.400\cdot(-0.5)+0.400=1.500+0.200+0.400=2.100$</li>
      <li>gated signal: $\max(0,2.100)=2.100$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.050$ and gradient $g=1.800$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.050\cdot1.800=2.000-0.090=1.910$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.100$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.100}=8.166$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=8.166/(8.166+1.492)=0.846$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $3$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.100-1.000)/\sqrt{0.250+0.00001}=2.200$</li>
      <li>memory $=3\cdot128\cdot4/1024=1.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.26 Mixture-of-Experts layers ---------------- */
window.ALLML_CONTENT["6.26"] = {
  tagline: "A router sends each token to a small set of experts, expanding capacity without using every parameter every time.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.26-mixture-of-experts-layers.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.25)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the routed expert mixture prepares you for (6.27) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Mixture-of-Experts layers is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the routed expert mixture, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the routed expert mixture</b>:</p>
    <div class="formula-box">$$y=\sum_{e\in TopK}p_e\,E_e(x)$$</div>
    <p>$p_e$ is the router probability for expert $e$, $E_e$ is that expert's output, and TopK keeps compute sparse. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.500$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.200\cdot1.5+-0.100\cdot(-0.5)+0.500=1.800+0.050+0.500=2.350$</li>
      <li>gated signal: $\max(0,2.350)=2.350$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.060$ and gradient $g=1.950$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.060\cdot1.950=2.000-0.117=1.883$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.350$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.350}=10.486$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=10.486/(10.486+1.492)=0.875$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $4$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.350-1.000)/\sqrt{0.250+0.00001}=2.700$</li>
      <li>memory $=4\cdot128\cdot4/1024=2.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.27 Neural ODEs ---------------- */
window.ALLML_CONTENT["6.27"] = {
  tagline: "A Neural ODE replaces discrete layers with a learned continuous-time vector field.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.27-neural-odes.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.26)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the continuous-depth update prepares you for (6.28) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Neural ODEs is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the continuous-depth update, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the continuous-depth update</b>:</p>
    <div class="formula-box">$$    rac{dh(t)}{dt}=f_	heta(h(t),t),\qquad h(t_1)=h(t_0)+\int_{t_0}^{t_1}f_	heta(h(t),t)dt$$</div>
    <p>$h(t)$ is the hidden state at time $t$ and $f_	heta$ is the learned dynamics. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.600$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.400\cdot1.5+0.200\cdot(-0.5)+0.600=2.100+-0.100+0.600=2.600$</li>
      <li>gated signal: $\max(0,2.600)=2.600$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.070$ and gradient $g=2.100$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.070\cdot2.100=2.000-0.147=1.853$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.600$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.600}=13.464$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=13.464/(13.464+1.492)=0.900$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $5$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.600-1.000)/\sqrt{0.250+0.00001}=3.200$</li>
      <li>memory $=5\cdot128\cdot4/1024=2.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.28 Hypernetworks ---------------- */
window.ALLML_CONTENT["6.28"] = {
  tagline: "A hypernetwork learns to generate another network's weights, turning context into parameters.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.28-hypernetworks.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.27)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the generated weight map prepares you for (6.29) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Hypernetworks is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the generated weight map, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the generated weight map</b>:</p>
    <div class="formula-box">$$W=H_\psi(c),\qquad y=f_{W}(x)$$</div>
    <p>$c$ is context, $H_\psi$ is the hypernetwork, and $f_W$ is the target network using generated weights. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.700$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.600\cdot1.5+-0.700\cdot(-0.5)+0.700=2.400+0.350+0.700=3.450$</li>
      <li>gated signal: $\max(0,3.450)=3.450$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.080$ and gradient $g=1.200$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.080\cdot1.200=2.000-0.096=1.904$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $3.450$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{3.450}=31.500$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=31.500/(31.500+1.492)=0.955$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $2$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(3.450-1.000)/\sqrt{0.250+0.00001}=4.900$</li>
      <li>memory $=2\cdot128\cdot4/1024=1.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.29 Capsule networks ---------------- */
window.ALLML_CONTENT["6.29"] = {
  tagline: "Capsules represent parts as vectors and use routing to agree on wholes.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.29-capsule-networks.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.28)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the routing agreement prepares you for (6.30) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Capsule networks is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the routing agreement, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the routing agreement</b>:</p>
    <div class="formula-box">$$c_{ij}=\mathrm{softmax}_j(b_{ij}),\qquad s_j=\sum_i c_{ij}\hat u_{j|i}$$</div>
    <p>$c_{ij}$ routes lower capsule $i$ to upper capsule $j$, and $\hat u_{j|i}$ is the prediction vector. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.800$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.800\cdot1.5+-0.400\cdot(-0.5)+0.800=2.700+0.200+0.800=3.700$</li>
      <li>gated signal: $\max(0,3.700)=3.700$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.090$ and gradient $g=1.350$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.090\cdot1.350=2.000-0.121=1.879$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $3.700$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{3.700}=40.447$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=40.447/(40.447+1.492)=0.964$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $3$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(3.700-1.000)/\sqrt{0.250+0.00001}=5.400$</li>
      <li>memory $=3\cdot128\cdot4/1024=1.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.30 Spiking neural networks ---------------- */
window.ALLML_CONTENT["6.30"] = {
  tagline: "Spiking networks communicate by threshold events, so timing and membrane accumulation carry the computation.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.30-spiking-neural-networks.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.29)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the membrane update prepares you for (6.31) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Spiking neural networks is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the membrane update, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the membrane update</b>:</p>
    <div class="formula-box">$$v_t=lpha v_{t-1}+I_t,\qquad spike_t=\mathbf{1}[v_t\ge 	au]$$</div>
    <p>$v_t$ is membrane voltage, $I_t$ is input current, $lpha$ is leak, and $	au$ is the firing threshold. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.300$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.000\cdot1.5+-0.100\cdot(-0.5)+0.300=1.500+0.050+0.300=1.850$</li>
      <li>gated signal: $\max(0,1.850)=1.850$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.050$ and gradient $g=1.500$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.050\cdot1.500=2.000-0.075=1.925$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $1.850$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{1.850}=6.360$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=6.360/(6.360+1.492)=0.810$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $4$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(1.850-1.000)/\sqrt{0.250+0.00001}=1.700$</li>
      <li>memory $=4\cdot128\cdot4/1024=2.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.31 Neural Architecture Search ---------------- */
window.ALLML_CONTENT["6.31"] = {
  tagline: "NAS treats architecture as the object being optimized, balancing validation quality against search cost.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.31-neural-architecture-search.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.30)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the architecture objective prepares you for (6.32) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Neural Architecture Search is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the architecture objective, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the architecture objective</b>:</p>
    <div class="formula-box">$$a^*=rg\min_{a\in\mathcal{A}} L_{val}(w_a,a)$$</div>
    <p>$a$ is an architecture, $w_a$ are its trained weights, and validation loss decides the search winner. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.400$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.200\cdot1.5+0.200\cdot(-0.5)+0.400=1.800+-0.100+0.400=2.100$</li>
      <li>gated signal: $\max(0,2.100)=2.100$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.060$ and gradient $g=1.650$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.060\cdot1.650=2.000-0.099=1.901$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.100$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.100}=8.166$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=8.166/(8.166+1.492)=0.846$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $5$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.100-1.000)/\sqrt{0.250+0.00001}=2.200$</li>
      <li>memory $=5\cdot128\cdot4/1024=2.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.32 The lottery-ticket hypothesis ---------------- */
window.ALLML_CONTENT["6.32"] = {
  tagline: "A dense network may contain a sparse subnetwork that trains well when reset to its lucky initialization.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.32-lottery-ticket-hypothesis.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.31)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the winning-ticket mask prepares you for (6.33) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. The lottery-ticket hypothesis is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the winning-ticket mask, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the winning-ticket mask</b>:</p>
    <div class="formula-box">$$f(x;m\odot	heta_0)\quad	ext{trains well with}\quad \|m\|_0\ll\|	heta_0\|_0$$</div>
    <p>$m$ is a binary mask, $	heta_0$ is the original initialization, and sparsity measures how many weights survive. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.500$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.400\cdot1.5+-0.700\cdot(-0.5)+0.500=2.100+0.350+0.500=2.950$</li>
      <li>gated signal: $\max(0,2.950)=2.950$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.070$ and gradient $g=1.800$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.070\cdot1.800=2.000-0.126=1.874$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $2.950$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{2.950}=19.106$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=19.106/(19.106+1.492)=0.928$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $2$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(2.950-1.000)/\sqrt{0.250+0.00001}=3.900$</li>
      <li>memory $=2\cdot128\cdot4/1024=1.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.33 Double descent & grokking ---------------- */
window.ALLML_CONTENT["6.33"] = {
  tagline: "Modern test error can fall, rise near interpolation, then fall again; grokking is delayed generalization after memorization.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.33-double-descent-grokking.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.32)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the interpolation curve prepares you for (6.34) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Double descent & grokking is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the interpolation curve, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the interpolation curve</b>:</p>
    <div class="formula-box">$$test\ error = bias + variance + optimization\ effects$$</div>
    <p>The interpolation threshold is where training error reaches zero, and the later descent reflects implicit regularization finding simpler structure. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.600$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.600\cdot1.5+-0.400\cdot(-0.5)+0.600=2.400+0.200+0.600=3.200$</li>
      <li>gated signal: $\max(0,3.200)=3.200$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.080$ and gradient $g=1.950$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.080\cdot1.950=2.000-0.156=1.844$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $3.200$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{3.200}=24.533$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=24.533/(24.533+1.492)=0.943$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $3$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(3.200-1.000)/\sqrt{0.250+0.00001}=4.400$</li>
      <li>memory $=3\cdot128\cdot4/1024=1.500$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

/* ---------------- 6.34 Hardware (GPU/TPU) & mixed-precision training ---------------- */
window.ALLML_CONTENT["6.34"] = {
  tagline: "Hardware-aware training keeps matrix units busy while mixed precision saves memory without losing gradient signal.",
  colab: "https://colab.research.google.com/github/niamleeson/ml-math/blob/main/notebooks/6.34-hardware-mixed-precision.ipynb",
  context: String.raw`
    <p>This lesson sits in Part 6 because deep learning is where representation, optimization, and numerical scale become one system.</p>
    <ul>
      <li><b>(6.33)</b> supplies the mechanism that feeds this topic: vectors carry activations, losses create gradients, and optimizers turn gradients into parameter movement.</li>
      <li><b>Learning theory</b> explains why capacity must be controlled; here the same pressure appears through architecture, regularization, and stable training dynamics.</li>
      <li><b>Probability and statistics</b> enter whenever minibatches estimate a full-data quantity, which is why the numbers below always distinguish a local computation from its training consequence.</li>
    </ul>
    <p>Where it leads: understanding the mixed-precision update prepares you for (later systems lessons) and for the later architecture lessons that reuse the same pattern at larger scale.</p>`,
  intuition: String.raw`
    <p>The concrete problem is simple to say and hard to execute: make a large composed function learn from data without losing signal, gradient, or meaning. Hardware (GPU/TPU) & mixed-precision training is one of the design answers to that pressure.</p>
    <p>The naive approach is to trust the raw computation exactly as written. In deep networks that usually fails: scales drift, gradients become unreliable, capacity concentrates in the wrong place, or hardware cost overwhelms the idea. The mental model is a relay race. Each layer or training rule receives a signal, reshapes it, and hands it forward while preserving enough information for the backward pass.</p>
    <p>The design decision people often gloss over is <b>where to constrain the system</b>. We do not merely add flexibility; we choose a constraint that keeps learning useful. In this lesson the constraint is expressed through the mixed-precision update, and the arithmetic below shows how a local choice changes the global training behavior.</p>`,
  mathematics: String.raw`
    <p>The core object in this lesson is <b>the mixed-precision update</b>:</p>
    <div class="formula-box">$$	heta_{32}\leftarrow	heta_{32}-\eta\,\mathrm{unscale}(g_{16})$$</div>
    <p>Activations and gradients may use 16-bit formats, while a 32-bit master copy preserves small parameter updates. The shapes matter because deep learning is just many small differentiable maps composed without losing track of scale, gradient, or memory.</p>

    <p><b>A two-input scratch pass.</b> Use inputs $x=[1.5,-0.5]$ and bias $b_0=0.700$ so the arithmetic is visible:</p>
    <ol class="work">
      <li>affine signal: $1.800\cdot1.5+-0.100\cdot(-0.5)+0.700=2.700+0.050+0.700=3.450$</li>
      <li>gated signal: $\max(0,3.450)=3.450$</li>
    </ol>
    <p>This tiny pass is the lesson in miniature: a raw signal is shaped before the next decision uses it.</p>

    <p><b>The training knob.</b> With learning rate $\eta=0.090$ and gradient $g=2.100$, one scalar parameter at $2.000$ moves as:</p>
    <ol class="work">
      <li>$2.000-0.090\cdot2.100=2.000-0.189=1.811$</li>
    </ol>
    <p>The update is deliberately small: the method changes behavior by repeated reliable nudges, not by one heroic leap.</p>

    <p><b>Turning a score into a comparison.</b> Compare the lesson score $3.450$ against baseline $0.400$:</p>
    <ol class="work">
      <li>$e^{3.450}=31.500$ and $e^{0.400}=1.492$</li>
      <li>softmax probability $=31.500/(31.500+1.492)=0.955$</li>
    </ol>
    <p>A model usually does not need an absolute score; it needs a calibrated comparison that can drive loss and gradients.</p>

    <p><b>Scale and memory bookkeeping.</b> If the same signal is normalized with mean $1.000$ and variance $0.250$, and a tiny activation block has $4$ vectors of length $128$ in 32-bit floats:</p>
    <ol class="work">
      <li>normalized value $=(3.450-1.000)/\sqrt{0.250+0.00001}=4.900$</li>
      <li>memory $=4\cdot128\cdot4/1024=2.000$ KB</li>
    </ol>
    <p>The normalized value says whether the signal is unusually high or low; the memory line reminds you that deep learning mathematics always lands on hardware.</p>`,
  pitfalls: String.raw`
    <ul>
      <li><b>Ignoring scale.</b> The normalized value in the Mathematics section changes the effective gradient; if scale is wrong, a sound formula can still train badly.</li>
      <li><b>Confusing local improvement with global learning.</b> The update $2.000-\eta g$ is only one step; repeated biased or noisy steps can still drift away from the desired solution.</li>
      <li><b>Forgetting shape and memory.</b> The $d\cdot128\cdot4$ calculation is small here, but the same term multiplied across layers and batches becomes the practical limit.</li>
      <li><b>Treating the design as magic.</b> The softmax and gating arithmetic expose the mechanism; if those numbers are unstable, the higher-level method inherits the instability.</li>
    </ul>`
};

