/* Rigorous Courses · Diffusion Models — Part 12: Steering it: guidance, latents, and Stable Diffusion.
   Owns conditional generation, classifier guidance from Bayes on scores, classifier-free
   guidance and the knob w, latent diffusion's cost argument, and the assembled Stable
   Diffusion walkthrough that closes the course. */
(function () {
  window.LESSONS.push({
    id: "diff-12-guidance-latent",
    title: "Part 12 — Steering it: guidance, latents, and Stable Diffusion",
    tagline: "Add the steering wheel with one Bayes tilt on the score, shrink the canvas with latents, and assemble Stable Diffusion from parts you built yourself",
    module: "Rigorous Courses · Diffusion Models",
    superGroup: "Rigorous Courses",
    template: "course",
    part: 12,
    partTotal: 12,
    prereqs: ["diff-11-score-ddim"],
    goal: `<p>After this part you can:</p>
      <ul>
        <li>State what conditional generation asks for — a sample from $p(x \\mid y)$ — and explain how a label reaches the network: an embedding, the same trick as the timestep.</li>
        <li>Derive classifier guidance in three one-op steps from Bayes' rule on scores, and say exactly why the $q(y)$ term drops.</li>
        <li>Derive the classifier-free guidance update $\\tilde\\epsilon = \\epsilon_\\varnothing + w\\,(\\epsilon_y - \\epsilon_\\varnothing)$ as the score of a sharpened density, compute it by hand on concrete vectors, and read what the knob $w$ trades.</li>
        <li>Make the latent-diffusion cost argument with real numbers — 786,432 pixel values versus 16,384 latent values, a 48-fold shrink — and say what the autoencoder contributes.</li>
        <li>Walk through Stable Diffusion end to end and point, for every component, at the part of this course where you built it.</li>
      </ul>`,
    sections: [
      { h: "Where we are",
        body: `<p>The machine is built and it is fast. Part 10 trained the real thing: a noise-guessing network $\\epsilon_\\theta(x_t, t)$ that turns pure noise into an eight-cluster ring by running Algorithm 2, the loss $L_{\\mathrm{simple}} = \\mathbb{E}\\,\\|\\epsilon - \\epsilon_\\theta(x_t, t)\\|^2$ doing all the teaching. Part 11 then re-read that trained network without changing a single weight: Tweedie's formula showed the noise guess is a scaled estimate of the <b>score</b> $\\nabla_{x_t} \\log q(x_t)$ — the uphill direction of the noised data's log density — and from that reading came the denoised estimate $\\hat{x}_0$, the deterministic DDIM sampler that jumps between distant noise levels legitimately, and a starting noise that behaves like a latent code.</p>
        <p>One capability is still missing, and it is the one every user of these systems actually touches: <b>control</b>. Our model samples from the whole ring — you take whatever cluster the noise happens to become. Nobody uses Stable Diffusion that way. You type "a fox in the snow" and the model steers toward exactly that region of image space. This final part builds the steering wheel: first <b>conditional generation</b> (what it means to ask for a specific kind of sample), then <b>classifier guidance</b> and <b>classifier-free guidance</b> (a dial for how hard to steer, derived from Bayes' rule on scores — part 11's score view is the whole tool), then <b>latent diffusion</b> (the compression trick that makes real images affordable). And then we are done building: the last section assembles every piece into Stable Diffusion itself and points, component by component, at the part of this course where you built it. The notebook ends the course the only honest way — by generating recognizable digit images from pure noise, on your CPU, with a latent diffusion model you train yourself.</p>` },

      { h: "Conditional generation: asking for what you want",
        body: `<p>Everything so far samples from the whole data cloud. The draw $x_{\\text{new}} \\sim p(x)$ from part 1 is a lottery over all of it: train on the ring and you get some cluster; train on photographs and you get some photograph. <b>Conditional generation</b> adds a request. Alongside each data point $x_0$, suppose the dataset carries a <b>label</b> $y$ — for our ring, the cluster index 0 through 7; for the digits later in the notebook, which digit it is; for Stable Diffusion, the text prompt. The ask becomes:</p>
        <div class="formula-box">$$x_{\\text{new}} \\;\\sim\\; p(x \\mid y)$$</div>
        <p><b>Read it out loud:</b> draw a new point, not from the whole cloud, but from the slice of the cloud that matches the label $y$ — recall from part 2 that a conditional distribution is a slice of the joint, renormalized so its probabilities again total 1. "Generate a 3" means: sample from the cloud of images, restricted to the region where the images are 3s, with the odds inside that region kept in proportion.</p>
        <p>How does the network learn this? The answer is pleasantly small: <b>give it the label as a third input</b>, and change nothing else. The network becomes $\\epsilon_\\theta(x_t, t, y)$, and Algorithm 1 trains it on triples where the label rides along: sample a pair $(x_0, y)$ from the dataset, noise $x_0$ to $x_t$ exactly as before, and ask the network for the noise given $x_t$, $t$, <b>and</b> $y$. The loss is the same $L_{\\mathrm{simple}}$. The schedule is the same. Algorithm 2 is the same, with the label passed in at every step. Every theorem from parts 6 through 9 was proved for whatever data distribution you feed in — restrict the data to one label's sub-population and every proof reruns word for word.</p>
        <p>The only real design question is how a label — an integer — enters a network that eats lists of numbers. Feeding the raw integer has exactly the problems raw $t$ had in part 10: bad scale, and one number is a cramped code for several distinct behaviors. The fix is also the same trick as $t$: a <b>label embedding</b>. Keep a learned table with one row of numbers per label (ours: 8 cluster labels plus one special row, 32 numbers each); to encode label $y$, look up row $y$ and hand those numbers to the network alongside the time embedding. Unlike the sinusoidal time embedding, this table is <b>learned</b> — the rows start random and training shapes them — because labels, unlike timesteps, have no built-in order that sines and cosines could exploit.</p>
        <p>One honest scaling note. In Stable Diffusion the "label" is a sentence, and a lookup table cannot hold every sentence. A <b>text encoder</b> — a separate network that turns a sentence into a list of vectors — plays the role of the table. The interface is identical: extra numbers describing the condition, handed to the noise-guesser at every call. Our one-integer label is the honest small version of the same contract.</p>` },

      { h: "Classifier guidance: a Bayes tilt on the score",
        body: `<p>A conditional network already steers. But practitioners found they wanted a <b>dial</b> — a way to push the samples harder onto the condition, trading some variety for more obedience. The cleanest way to see where such a dial can come from is part 11's score view: sampling walks uphill along the score, so to sample from the conditional noised density $q(x_t \\mid y)$ we need its score, $\\nabla_{x_t} \\log q(x_t \\mid y)$. Bayes' rule from part 2 takes it apart in three operations.</p>
        <ol>
          <li><b>Do:</b> write Bayes' rule for the noised data at level $t$: $q(x_t \\mid y) = \\frac{q(y \\mid x_t)\\, q(x_t)}{q(y)}$. <b>Why:</b> part 2's rule, applied to the pair (noisy point, label): the slice given $y$ equals the label-checker's verdict times the overall density, over the label's overall probability.</li>
          <li><b>Do:</b> take the log of both sides: $\\log q(x_t \\mid y) = \\log q(y \\mid x_t) + \\log q(x_t) - \\log q(y)$. <b>Why:</b> the log of a product is the sum of the logs, and the log of a quotient is a difference (part 5).</li>
          <li><b>Do:</b> take the gradient in $x_t$ of every term; the last term gives $\\nabla_{x_t} \\log q(y) = 0$. <b>Why:</b> slopes add (part 11), and once $y$ is fixed, $q(y)$ is a single number — it does not mention $x_t$ at all, and the slope of a constant is zero.</li>
        </ol>
        <div class="formula-box">$$\\nabla_{x_t} \\log q(x_t \\mid y) \\;=\\; \\nabla_{x_t} \\log q(x_t) \\;+\\; \\nabla_{x_t} \\log q(y \\mid x_t)$$</div>
        <p><b>Read it out loud:</b> the uphill direction for "realistic <b>and</b> matching the label" is the uphill direction for "realistic" plus the uphill direction for "a classifier would call this $y$". Steering is an added tilt: keep walking toward the data, and lean, at every step, toward the region where the label becomes more believable.</p>
        <p>This is <b>classifier guidance</b> (Dhariwal and Nichol, 2021), and it comes with a dial for free: multiply the tilt by a <b>guidance scale</b> $w$ before adding it, and you lean gently ($w$ small) or hard ($w$ large). But turning it into a working system exposes three practical pains. First, the tilt term $\\nabla_{x_t} \\log q(y \\mid x_t)$ is the slope of a <b>classifier</b> — and not an ordinary one: it must classify half-destroyed inputs correctly at <b>every</b> noise level $t$, so you must train a separate, special, noise-robust classifier alongside the diffusion model. Second, at sampling time you need that classifier's slope with respect to its input at every one of the 20 to 200 steps — extra machinery and extra compute per step. Third, the steering quality is capped by the classifier: wherever it is confidently wrong on noisy inputs, the tilt points confidently the wrong way. The next section deletes all three pains with one observation.</p>` },

      { h: "Classifier-free guidance: two answers from one network",
        body: `<p>Here is the observation (Ho and Salimans, 2022). Rearrange the boxed identity above by subtracting $\\nabla \\log q(x_t)$ from both sides: $\\nabla_{x_t} \\log q(y \\mid x_t) = \\nabla_{x_t} \\log q(x_t \\mid y) - \\nabla_{x_t} \\log q(x_t)$. The classifier's tilt is a <b>difference of two scores we already know how to estimate</b>: the conditional score and the unconditional one. No classifier needs to exist — a conditional noise-guesser and an unconditional noise-guesser together contain the tilt.</p>
        <p>And one network can be both. During training, replace the true label with a special <b>null token</b> $\\varnothing$ ("no label given") a fraction of the time — 10 to 20 percent is standard; we use 10. The null token has its own row in the embedding table, so the network sees mostly labeled examples and learns the conditional guess $\\epsilon_\\theta(x_t, t, y)$, and sometimes label-less examples and learns the unconditional guess $\\epsilon_\\theta(x_t, t, \\varnothing)$ — one set of weights, both jobs. At sampling time, call it twice per step: once with the label, once with the null token. That is the entire training recipe of <b>classifier-free guidance</b> (CFG).</p>
        <p>Now the dial, derived rather than bolted on. Fix a guidance scale $w$ and aim, at each noise level, at the <b>sharpened</b> density — the one proportional to $q(x_t)\\, q(y \\mid x_t)^w$. In plain English: realistic, and $w$ times as insistent that the label should be believable. One operation per step:</p>
        <ol>
          <li><b>Do:</b> name the target: a density proportional to $q(x_t)\\, q(y \\mid x_t)^w$. <b>Why:</b> raising the label-checker's verdict to a power $w > 1$ exaggerates its differences — regions where the label is believable gain weight, regions where it is not lose weight; dividing by a normalizer makes it a density again, and the next two steps show the normalizer never matters.</li>
          <li><b>Do:</b> take the log: $\\log q(x_t) + w\\, \\log q(y \\mid x_t) + \\text{const}$. <b>Why:</b> the log of a product is the sum of the logs, the log of a power is the power times the log (part 5), and the normalizer's log is a constant in $x_t$.</li>
          <li><b>Do:</b> take the gradient in $x_t$: $\\nabla \\log q(x_t) + w\\, \\nabla \\log q(y \\mid x_t)$. <b>Why:</b> slopes add, scaling a function by $w$ scales its slope by $w$, and the constant's slope is zero — the score never needs the normalizer, exactly as part 11 promised.</li>
          <li><b>Do:</b> substitute the rearranged Bayes identity for the tilt: $\\nabla \\log q(x_t) + w\\,\\big(\\nabla \\log q(x_t \\mid y) - \\nabla \\log q(x_t)\\big)$. <b>Why:</b> replacing a quantity by something proved equal to it changes nothing.</li>
          <li><b>Do:</b> convert every score into a noise guess by multiplying the whole equation by $-\\sqrt{1-\\bar\\alpha_t}$. <b>Why:</b> part 11's bridge, $\\nabla_{x_t} \\log q = -\\epsilon_\\theta^{*}/\\sqrt{1-\\bar\\alpha_t}$, holds for the unconditional density (estimated by the null-token call) and, rerunning part 11's derivation on the label-$y$ sub-population, for the conditional density too (estimated by the labeled call); multiplying both sides of a true equation by the same factor keeps it true.</li>
        </ol>
        <p>Writing $\\epsilon_y = \\epsilon_\\theta(x_t, t, y)$ and $\\epsilon_\\varnothing = \\epsilon_\\theta(x_t, t, \\varnothing)$ for the two calls, the result is the update this part owns — the formula promised in part 1's roadmap:</p>
        <div class="formula-box">$$\\tilde\\epsilon \\;=\\; \\epsilon_\\varnothing \\;+\\; w\\,\\big(\\epsilon_y - \\epsilon_\\varnothing\\big)$$</div>
        <p><b>Read it out loud:</b> the guided noise guess is the unconditional guess, plus $w$ times the difference between the conditional and unconditional guesses. The difference $\\epsilon_y - \\epsilon_\\varnothing$ is the steering direction — what the label changes about the network's opinion — and $w$ says how far along it to go. Then hand $\\tilde\\epsilon$ to any sampler from this course — Algorithm 2 or DDIM — in place of the plain guess, at every step.</p>
        <p><b>Worked example (redo it on paper).</b> At some step the two calls return $\\epsilon_\\varnothing = (0.2,\\ -0.1)$ and $\\epsilon_y = (0.8,\\ 0.3)$, and $w = 3$. The steering direction is $\\epsilon_y - \\epsilon_\\varnothing = (0.8 - 0.2,\\ 0.3 - (-0.1)) = (0.6,\\ 0.4)$. Scale it: $3 \\times (0.6,\\ 0.4) = (1.8,\\ 1.2)$. Add the base: $\\tilde\\epsilon = (0.2 + 1.8,\\ -0.1 + 1.2) = (2.0,\\ 1.1)$. Two special cases you should check in your head: $w = 0$ gives $\\tilde\\epsilon = \\epsilon_\\varnothing$ — the label is ignored entirely; $w = 1$ gives $\\tilde\\epsilon = \\epsilon_y$ — plain conditional generation, no sharpening. Every $w > 1$ <b>extrapolates past</b> the conditional guess, pushing harder than the honest posterior would.</p>
        <p>What does the knob trade? At $w = 1$ you sample the true conditional — full variety within the label. As $w$ grows, samples crowd toward the region where the label is most unambiguous: obedience and typicality rise (<b>fidelity</b>), spread and edge cases fall (<b>diversity</b>). Push far enough and the samples cluster tightly around the label's most stereotypical core — for images, oversaturated, over-typical outputs. The notebook makes the trade visible: sampling our ring with the label "cluster 2" at $w = 0, 1, 3, 7$, the fraction of samples landing on cluster 2 climbs from about one eighth (the lottery) toward one, while the surviving samples pack into an ever-tighter blob. Real systems default to $w$ around 7 (the guidance-scale slider in every Stable Diffusion interface is exactly this $w$).</p>` },

      { h: "Latent diffusion: shrink the canvas first",
        body: `<p>Now the last gap between our toy and the real thing: <b>size</b>. Count what a real image costs. A 512-by-512 color photograph is $512 \\times 512$ pixels times 3 color numbers each: $786{,}432$ numbers per image. Every one of our objects — $x_t$, $\\epsilon$, the network's output — is a list that long, the network must eat and produce lists that long, and Algorithm 2 calls it dozens of times per image. Diffusing directly in pixel space at this size is possible (the original DDPM and its successors did it) but brutally expensive to train and run.</p>
        <p><b>Latent diffusion</b> (Rombach and colleagues, 2022) spends the compute where it matters. The observation: most of those 786,432 numbers are perceptual detail — textures, grain, exact edge positions — not decisions about <b>what is in the picture</b>. So compress first. An <b>autoencoder</b> is a pair of networks trained together: an <b>encoder</b> $E$ squeezes an image into a much shorter list called a <b>latent</b>, and a <b>decoder</b> $D$ inflates a latent back into an image, the pair trained so that $D(E(x))$ matches $x$ closely. (The VAE from <a onclick="App.open('mod-vae')">the VAE lesson</a> is the classic version; Stable Diffusion's autoencoder is a close relative.) Stable Diffusion's encoder maps a $512 \\times 512 \\times 3$ image to a $64 \\times 64 \\times 4$ latent: $16{,}384$ numbers.</p>
        <div class="formula-box">$$\\frac{512 \\times 512 \\times 3}{64 \\times 64 \\times 4} \\;=\\; \\frac{786{,}432}{16{,}384} \\;=\\; 48$$</div>
        <p><b>Read it out loud:</b> the latent is forty-eight times shorter than the image. Every tensor the diffusion model touches shrinks 48-fold, and the network's cost per call falls with it — that, more than any single algorithmic idea, is why image generation runs on consumer hardware.</p>
        <p>The recipe: train the autoencoder once, then freeze it. Encode the whole dataset: each image $x_0$ becomes a latent $z_0 = E(x_0)$. Now run <b>this entire course</b> on the latents as if they were the data: noise $z_0$ to $z_t$ with part 6's schedule, train $\\epsilon_\\theta(z_t, t, y)$ with part 9's loss, sample latents from pure noise with Algorithm 2 or DDIM, steer with CFG. Nothing in parts 6 through 11 cared what the coordinates meant — the theorems are about lists of numbers. The only new step is the last one: decode, $x_{\\text{new}} = D(z_{\\text{new}})$, one call, turning the generated latent back into pixels.</p>
        <p>The notebook builds this in honest miniature, and it is the course finale. The images are the 8-by-8 digits (64 pixels). The autoencoder is <b>PCA</b> — principal component analysis, the linear special case of an autoencoder: it finds, by matrix algebra alone (numpy's SVD), the 16 directions along which the digits vary most, encodes each digit as its 16 coordinates along those directions, and decodes by re-adding the directions; those 16 numbers keep most of the digits' variation. Then a small DDPM trains in the 16-dimensional latent space and samples fresh latents from pure noise; the PCA decoder inflates them to 64 pixels — and you will see a grid of recognizable, never-before-seen digit images, generated on a CPU in seconds. A 4-fold shrink standing in for Stable Diffusion's 48-fold, with every moving part visible.</p>` },

      { h: "Stable Diffusion, assembled",
        body: `<p>You now hold every part. Here is the whole machine, in the order a prompt flows through it.</p>
        <p><b>Step 1 — the prompt becomes numbers.</b> You type "a fox in the snow". The <b>text encoder</b> (in Stable Diffusion, a transformer named CLIP, trained separately on image-caption pairs) turns the sentence into a list of embedding vectors — the condition $y$. This is our label embedding scaled up from a 9-row table to a network that can encode any sentence.</p>
        <p><b>Step 2 — start from pure noise, in latent space.</b> Draw $z_T \\sim \\mathcal{N}(0, \\mathbf{I})$ — a $64 \\times 64 \\times 4$ block of Gaussian noise. This is Algorithm 2's first line, and part 6 is why it is legitimate: the forward process ends at pure noise, so pure noise is where generation begins. Thanks to part 11, this starting block is also a latent code you can keep, to reproduce or interpolate the image later.</p>
        <p><b>Step 3 — the guided denoising loop.</b> For 20 to 50 sampler steps: call the noise-guessing network twice — with the prompt and with the null token (an empty prompt) — and combine the calls with $\\tilde\\epsilon = \\epsilon_\\varnothing + w\\,(\\epsilon_y - \\epsilon_\\varnothing)$; then take one DDIM-style update with $\\tilde\\epsilon$. The network is a <b>U-Net</b> — a convolutional image-to-image architecture, the industrial-strength version of our data-shaped-in, data-shaped-out MLP — with the timestep entering through a sinusoidal embedding, exactly part 10's, and the prompt entering through attention layers (the interface: extra condition numbers at every call). It was trained with $L_{\\mathrm{simple}}$ — Algorithm 1, minibatches, noise prediction — on encoded images, with prompts dropped 10 percent of the time for CFG.</p>
        <p><b>Step 4 — decode.</b> The finished latent $z_0$ goes through the frozen decoder $D$ once, inflating 16,384 latent numbers into 786,432 pixel numbers: your image.</p>
        <p>Every component, mapped to where you built it:</p>
        <table class="symbols">
          <tr><td><b>Component of Stable Diffusion</b></td><td><b>Where you learned it</b></td></tr>
          <tr><td>What "generate an image" even means — sampling a new point from a data cloud's rule</td><td>Part 1</td></tr>
          <tr><td>The probability language: conditionals, Bayes, expectation, variance, Gaussians, KL, ELBO</td><td>Parts 2, 3, 5</td></tr>
          <tr><td>The forward noising schedule ($\\beta_t$, $\\bar\\alpha_t$, the one-line sampler)</td><td>Parts 4, 6</td></tr>
          <tr><td>Why reversing noise is possible, and the exact one-step denoiser</td><td>Part 7</td></tr>
          <tr><td>The training objective: ELBO $\\to$ KL terms $\\to$ $L_{\\mathrm{simple}}$, Algorithms 1 and 2</td><td>Parts 8, 9</td></tr>
          <tr><td>The noise-guessing network (U-Net at scale; our MLP), with the sinusoidal time embedding</td><td>Part 10</td></tr>
          <tr><td>The fast sampler: scores, Tweedie, $\\hat{x}_0$, DDIM in 20-50 steps; the start noise as a latent code</td><td>Part 11</td></tr>
          <tr><td>The condition entering the network (label/text embedding), CFG's two calls and the knob $w$</td><td>Part 12</td></tr>
          <tr><td>The autoencoder shrinking pixels to latents 48-fold (our PCA miniature), decoder at the end</td><td>Part 12</td></tr>
          <tr><td>The text encoder (CLIP) itself — a separately trained transformer</td><td>The one import: its interface is part 12's embedding contract, its insides are a transformers course</td></tr>
        </table>
        <p>Read the right-hand column once more. With the single exception of the text encoder's insides, <b>nothing in the machine is left unexplained</b> — every box is a derivation you did with your own hands, one operation per step, checked with real numbers in a notebook. That was the course's promise in part 1, and this table is the receipt.</p>` },

      { h: "Sanity checks",
        body: `<p>Every numeric claim below is verified in the notebook.</p>
        <ul>
          <li><b>The dropped Bayes term is honestly zero.</b> Once $y$ is fixed, $q(y)$ is one number — a function of $x_t$ that never changes — and the slope of a constant is zero. Nothing was swept under a rug.</li>
          <li><b>CFG's endpoints are old friends.</b> Plug $w = 0$ into $\\tilde\\epsilon = \\epsilon_\\varnothing + w(\\epsilon_y - \\epsilon_\\varnothing)$: the parenthesis vanishes and $\\tilde\\epsilon = \\epsilon_\\varnothing$ — unconditional generation, parts 10-11 exactly. Plug $w = 1$: $\\epsilon_\\varnothing$ cancels and $\\tilde\\epsilon = \\epsilon_y$ — plain conditional generation, no sharpening. The dial contains both machines you already trust.</li>
          <li><b>The worked vectors.</b> $\\epsilon_\\varnothing = (0.2, -0.1)$, $\\epsilon_y = (0.8, 0.3)$, $w = 3$: difference $(0.6, 0.4)$, scaled $(1.8, 1.2)$, total $(2.0, 1.1)$. The notebook asserts these to the last decimal.</li>
          <li><b>The knob behaves on the ring.</b> Targeting cluster 2: at $w = 0$ about one eighth of samples land there (the unconditional lottery over 8 equally likely clusters); at $w = 1$ most do; by $w = 3$ nearly all do. The notebook asserts the fraction rises from $w = 0$ to $w = 3$.</li>
          <li><b>The trade is visible at $w = 7$.</b> The on-target fraction is near its ceiling, and the sample cloud is visibly tighter than the data's true within-cluster spread — fidelity bought with diversity, the over-typicality that heavy guidance causes in real image systems.</li>
          <li><b>The cost ratio is exact.</b> $786{,}432 / 16{,}384 = 48$ — no rounding. And the miniature repeats the pattern: 64 pixels to 16 latent numbers is a 4-fold shrink.</li>
          <li><b>The miniature autoencoder is honest.</b> PCA's 16 directions keep most of the digits' variance (the notebook prints the exact fraction and asserts it above 75 percent), so decoded reconstructions are recognizably the same digits — the compression loses texture, not identity, which is the entire bet of latent diffusion.</li>
          <li><b>The finale is checked, not vibed.</b> The generated digit batch is asserted to have sane pixel values and a per-pixel spread within a factor of 2 of the real digits' — the generated images live in the same statistical neighborhood as the data, and your eyes confirm the rest.</li>
        </ul>` },

      { h: "What you can do now",
        body: `<p>You made it. Twelve parts ago, "generate an image" was not even a well-posed sentence. You then built, in order: the problem statement (a cloud's rule, and sampling from it — part 1); the probability to speak about it (parts 2-5); the forward process that destroys data on schedule (part 6); the exact reverse step (part 7); the ELBO that scores a learned reverser (part 8); the noise-prediction loss and both algorithms (part 9); a trained, working DDPM (part 10); the score view and 20-step DDIM sampling (part 11); and now steering — Bayes on scores, classifier-free guidance, the knob $w$ — plus latent diffusion, and with them the complete assembled picture of Stable Diffusion, every component of which you can point to a derivation for. In this part's notebook you trained a guided conditional model and watched a dial you derived by hand move samples onto a chosen cluster, then trained a latent diffusion model that draws recognizable digits out of pure noise on a CPU. That is not a metaphor for how the big systems work. It is how they work.</p>
        <p>Where next? Four doors, all of which you now hold the keys to. Read the original papers as annotated walkthroughs: <a onclick="App.open('paper-ddpm')">the DDPM paper</a> — you have derived every equation in it, including the ones the paper skips; <a onclick="App.open('paper-cfg')">the classifier-free guidance paper</a> — this part's two-calls-and-a-knob, as its authors told it; and <a onclick="App.open('paper-latent-diffusion')">the latent diffusion paper</a> — the 48-fold shrink at industrial scale, with the autoencoder details we summarized in a paragraph. Then, when you want to build rather than read, <a onclick="App.open('capstone-diffusion')">the diffusion capstone</a> puts the whole pipeline in your hands at real-image scale. Go make something from noise.</p>` }
    ],
    symbols: [
      { sym: "$x_0$", desc: "a clean data point — a 2-D ring point, or an image as a list of numbers" },
      { sym: "$x_t$", desc: "the data after $t$ small doses of noise" },
      { sym: "$T$", desc: "total number of noising steps; 200 in the notebook" },
      { sym: "$\\bar\\alpha_t$", desc: "the fraction of the original signal surviving to step $t$ (part 6)" },
      { sym: "$y$", desc: "the condition: a class label here (cluster 0-7, or which digit); a text prompt in the wild" },
      { sym: "$\\varnothing$", desc: "the null token — the special \"no label given\" value with its own embedding row" },
      { sym: "$p(x \\mid y)$", desc: "the conditional data distribution: the label-$y$ slice of the cloud, renormalized (part 2)" },
      { sym: "$q(x_t \\mid y)$", desc: "the density of noised data at level $t$, restricted to the label-$y$ sub-population" },
      { sym: "$q(y \\mid x_t)$", desc: "the noise-level classifier: how believable label $y$ is, given the noisy point" },
      { sym: "$\\nabla_{x} \\log p(x)$", desc: "the score — the uphill direction of the log density (part 11)" },
      { sym: "$\\epsilon_\\theta(x_t, t, y)$", desc: "the conditional noise-guessing network: noisy point, timestep, and condition in; noise guess out" },
      { sym: "$\\epsilon_y,\\ \\epsilon_\\varnothing$", desc: "shorthands for the two calls: $\\epsilon_\\theta(x_t, t, y)$ with the label, $\\epsilon_\\theta(x_t, t, \\varnothing)$ with the null token" },
      { sym: "$w$", desc: "the guidance scale: how hard we push toward the condition; 0 = ignore it, 1 = plain conditional, 7 = a common default" },
      { sym: "$\\tilde\\epsilon$", desc: "the guided noise guess $\\epsilon_\\varnothing + w(\\epsilon_y - \\epsilon_\\varnothing)$, handed to the sampler in place of the plain guess" },
      { sym: "$E,\\ D$", desc: "the autoencoder's encoder (image to latent) and decoder (latent to image); PCA in our miniature" },
      { sym: "$z_0,\\ z_t$", desc: "a latent (the encoded data point) and its noised version — the diffusion now runs on these; not to be confused with the sampler's fresh kick $z$ from Algorithm 2" }
    ],
    recall: [
      "State conditional generation in one line of math and one plain sentence. What plays the role of $y$ for our ring, and what plays it in Stable Diffusion?",
      "How does a label reach the network, and why is a learned embedding table used instead of the raw integer?",
      "Derive the classifier-guidance identity from Bayes' rule in three steps. Which term drops, and why exactly?",
      "Name the three practical pains of classifier guidance that classifier-free guidance removes.",
      "How does one network learn both the conditional and the unconditional noise guess? What is the null token?",
      "Write the CFG formula from memory. What do $w = 0$ and $w = 1$ reduce to, and what does raising $w$ past 1 trade?",
      "Make the latent-diffusion cost argument with the real numbers: what are the two dimension counts, and what is their ratio?",
      "Walk Stable Diffusion end to end — prompt to pixels — naming each component and the course part where you built it."
    ],
    practice: [
      { q: "CFG arithmetic. At some sampling step the two network calls return $\\epsilon_\\varnothing = (0.4,\\ 0.0)$ and $\\epsilon_y = (0.1,\\ 0.6)$. Compute the guided guess $\\tilde\\epsilon$ for $w = 0$, $w = 1$, $w = 2$, and $w = 5$, and describe the pattern the four answers make.",
        steps: [
          { do: "Compute the steering direction once: $\\epsilon_y - \\epsilon_\\varnothing = (0.1 - 0.4,\\ 0.6 - 0.0) = (-0.3,\\ 0.6)$.", why: "Every $w$ reuses this same difference — it is what the label changes about the network's opinion, computed coordinate by coordinate." },
          { do: "$w = 0$: $\\tilde\\epsilon = (0.4,\\ 0.0) + 0 \\times (-0.3,\\ 0.6) = (0.4,\\ 0.0) = \\epsilon_\\varnothing$.", why: "Multiplying the direction by zero erases it — the label is ignored, unconditional generation." },
          { do: "$w = 1$: $\\tilde\\epsilon = (0.4 - 0.3,\\ 0.0 + 0.6) = (0.1,\\ 0.6) = \\epsilon_y$.", why: "One full step along the difference lands exactly on the conditional guess — plain conditional generation." },
          { do: "$w = 2$: scale the direction, $2 \\times (-0.3,\\ 0.6) = (-0.6,\\ 1.2)$; add the base, $\\tilde\\epsilon = (0.4 - 0.6,\\ 0.0 + 1.2) = (-0.2,\\ 1.2)$.", why: "One operation at a time: scale, then add coordinate-wise — the same two moves as the lesson's worked example." },
          { do: "$w = 5$: $5 \\times (-0.3,\\ 0.6) = (-1.5,\\ 3.0)$, so $\\tilde\\epsilon = (0.4 - 1.5,\\ 0.0 + 3.0) = (-1.1,\\ 3.0)$.", why: "Same two moves with the larger scale." },
          { do: "Read the pattern: all four answers sit on the straight line through $\\epsilon_\\varnothing$ and $\\epsilon_y$; $w$ is the position along it — at the base for $w=0$, at the conditional guess for $w=1$, and extrapolated past it, ever farther, for $w = 2$ and $5$.", why: "The formula $\\epsilon_\\varnothing + w\\,(\\text{direction})$ is the equation of a line with parameter $w$ — guidance never bends, it only slides along the label's direction." }
        ],
        answer: "$\\tilde\\epsilon$ is $(0.4, 0.0)$ at $w=0$; $(0.1, 0.6)$ at $w=1$; $(-0.2, 1.2)$ at $w=2$; $(-1.1, 3.0)$ at $w=5$. All lie on one line through the two calls, with $w$ the distance along it — $w > 1$ extrapolates past the conditional guess." },
      { q: "Derive the CFG formula. Starting from the sharpened target density proportional to $q(x_t)\\, q(y \\mid x_t)^w$, derive $\\tilde\\epsilon = \\epsilon_\\varnothing + w\\,(\\epsilon_y - \\epsilon_\\varnothing)$, citing the rule that justifies each move.",
        steps: [
          { do: "Take the log of the target: $\\log q(x_t) + w \\log q(y \\mid x_t) + \\text{const}$, where the constant is the normalizer's log.", why: "Log of a product is the sum of logs and log of a power is the power times the log (part 5); the normalizer does not depend on $x_t$." },
          { do: "Take the gradient in $x_t$: $\\nabla \\log q(x_t) + w\\, \\nabla \\log q(y \\mid x_t)$.", why: "Slopes add, a constant's slope is zero, and scaling a function by $w$ scales its slope by $w$ (part 11's slope facts)." },
          { do: "Rearrange the classifier-guidance identity — subtract $\\nabla \\log q(x_t)$ from both sides of $\\nabla \\log q(x_t \\mid y) = \\nabla \\log q(x_t) + \\nabla \\log q(y \\mid x_t)$ — to get $\\nabla \\log q(y \\mid x_t) = \\nabla \\log q(x_t \\mid y) - \\nabla \\log q(x_t)$.", why: "Subtracting the same quantity from both sides of a true equation keeps it true; this expresses the classifier's tilt using only scores of data densities." },
          { do: "Substitute: the guided score becomes $\\nabla \\log q(x_t) + w\\,\\big(\\nabla \\log q(x_t \\mid y) - \\nabla \\log q(x_t)\\big)$.", why: "Replacing a term by an expression proved equal to it changes nothing." },
          { do: "Multiply the whole equation by $-\\sqrt{1-\\bar\\alpha_t}$ and apply part 11's bridge to each score: $\\nabla \\log q(x_t)$ becomes $\\epsilon_\\varnothing$, and $\\nabla \\log q(x_t \\mid y)$ becomes $\\epsilon_y$.", why: "The bridge $\\nabla \\log q = -\\epsilon^{*}/\\sqrt{1-\\bar\\alpha_t}$ holds for the unconditional density and — rerunning part 11's Tweedie derivation on the label-$y$ sub-population — for the conditional one; multiplying both sides by the same factor preserves equality." },
          { do: "Read off the result: $\\tilde\\epsilon = \\epsilon_\\varnothing + w\\,(\\epsilon_y - \\epsilon_\\varnothing)$.", why: "The left side is the guided score converted to noise-guess units; the right side is the same conversion applied term by term." }
        ],
        answer: "$\\tilde\\epsilon = \\epsilon_\\varnothing + w\\,(\\epsilon_y - \\epsilon_\\varnothing)$: the guided guess is the unconditional guess plus $w$ times the steering direction, obtained by sharpening the label term to the power $w$, applying Bayes on scores, and converting scores to noise guesses with part 11's bridge." },
      { q: "Classifier versus classifier-free. Your team has a large, frozen, unconditional diffusion model and wants steered samples by next month. Colleague A proposes classifier guidance; colleague B proposes classifier-free guidance. Lay out what each plan requires, what each costs at sampling time, and which you would pick under the constraint that retraining the diffusion model is off the table.",
        steps: [
          { do: "Spell out plan A's requirement: train a new classifier $q(y \\mid x_t)$ that is accurate on noisy inputs at every level $t$ — a special model trained on noised data, not an off-the-shelf classifier — while the diffusion model itself stays frozen.", why: "The tilt term classifier guidance adds is that classifier's gradient; ordinary classifiers have never seen half-destroyed inputs and give unreliable slopes on them." },
          { do: "Spell out plan A's sampling cost: at every step, one diffusion-network call plus one classifier forward-and-backward pass (the tilt is a gradient, so backprop through the classifier runs at every step); quality is capped by the classifier wherever it is confidently wrong.", why: "The update needs $\\nabla_{x_t} \\log q(y \\mid x_t)$ numerically, and a slope through a network costs a backward pass; a wrong classifier tilts confidently the wrong way." },
          { do: "Spell out plan B's requirement: the network itself must know the condition — $\\epsilon_\\theta(x_t, t, y)$ trained with label dropout. A purely unconditional frozen model has no label input, so plan B requires retraining (or fine-tuning) the diffusion model.", why: "CFG's two calls are conditional and unconditional evaluations of one label-aware network; no such input exists in the frozen model." },
          { do: "Spell out plan B's sampling cost: two forward passes of the one network per step, no second model, no backprop at sampling time, no classifier ceiling on quality.", why: "$\\tilde\\epsilon$ is a weighted combination of two forward evaluations — arithmetic, not gradients." },
          { do: "Apply the constraint and decide: with retraining off the table, plan B is unavailable, so plan A — a noise-level classifier bolted onto the frozen model — is the workable route; if the constraint ever lifts, plan B is the simpler and stronger system, which is why modern systems use it.", why: "Classifier guidance's defining advantage is exactly that it steers a frozen unconditional model; CFG's advantages (one model, forward passes only, no classifier cap) all assume you can train the condition in." }
        ],
        answer: "Under the no-retraining constraint, choose classifier guidance: train a noise-robust classifier and pay a classifier backward pass per step, with quality capped by that classifier. Without the constraint, classifier-free guidance wins: one label-aware network trained with dropout, two forward calls and a knob at sampling, no extra model — the reason it is the modern default." },
      { q: "The cost argument at other sizes. (a) A 1024-by-1024 color image is diffused directly in pixel space; count its dimensions. (b) A latent diffusion system encodes it to a 128-by-128 latent with 4 channels; count the latent's dimensions and the shrink factor. (c) Do the same for a 256-by-256-by-3 image with a 32-by-32-by-4 latent. (d) What does the shrink factor buy, concretely?",
        steps: [
          { do: "(a) Multiply the pixel grid by the channels: $1024 \\times 1024 = 1{,}048{,}576$ pixels, times 3 color numbers: $3{,}145{,}728$ dimensions.", why: "Every pixel carries one number per color channel, and part 1 taught us an image is exactly its list of numbers — so count the list." },
          { do: "(b) The latent: $128 \\times 128 = 16{,}384$ positions, times 4 channels: $65{,}536$ dimensions.", why: "Same counting rule on the compressed grid." },
          { do: "Divide: $3{,}145{,}728 / 65{,}536 = 48$.", why: "The shrink factor is the ratio of the two dimension counts — and it lands on the same 48 as the 512 case, because both grid sides shrank 8-fold ($8 \\times 8 = 64$) while channels grew from 3 to 4 ($64 \\times 3/4 = 48$)." },
          { do: "(c) Small case: $256 \\times 256 \\times 3 = 196{,}608$ and $32 \\times 32 \\times 4 = 4{,}096$; ratio $196{,}608 / 4{,}096 = 48$ again.", why: "Identical geometry — sides divided by 8, channels 3 to 4 — gives the identical ratio at any resolution." },
          { do: "(d) Name the payoff: every tensor in the diffusion loop ($z_t$, $\\epsilon$, the network's input and output) is 48 times shorter, so memory and per-call compute drop roughly 48-fold, and the saving repeats at every one of the 20-200 sampler steps and every one of millions of training steps.", why: "The diffusion machinery's cost scales with the size of the lists it processes, and the sampler multiplies any per-call saving by its step count." }
        ],
        answer: "(a) 3,145,728 dimensions. (b) 65,536 — a 48-fold shrink. (c) 196,608 versus 4,096 — 48-fold again. (d) Roughly 48 times less memory and per-call compute, multiplied across every sampling step and every training step — the difference between a data center and a gaming laptop." },
      { q: "Map the machine. For each Stable Diffusion component, name the course part (or parts) where you learned it and the key object there: (a) the noise schedule; (b) the U-Net noise-guesser with its time embedding; (c) the training loss; (d) the 30-step deterministic sampler; (e) the two-calls-per-step guidance with the slider $w$; (f) the encoder and decoder around the diffusion loop; (g) the text encoder.",
        steps: [
          { do: "(a) The schedule is part 6 (with part 4's chain framing): $\\beta_t$, $\\alpha_t$, $\\bar\\alpha_t$, and the one-line sampler $x_t = \\sqrt{\\bar\\alpha_t}\\,x_0 + \\sqrt{1-\\bar\\alpha_t}\\,\\epsilon$.", why: "Part 6 owns the forward process; part 4 proved chains of small random steps forget their start." },
          { do: "(b) The U-Net is part 10's network contract at industrial scale: data-shaped in, data-shaped out, with the sinusoidal time embedding feeding $t$ in — our MLP was the honest small version.", why: "Part 10 built the noise-guesser and derived why it must be told $t$ and how the embedding encodes it." },
          { do: "(c) The loss is parts 8 and 9: the ELBO decomposed into per-step KL terms (part 8), reparameterized into $L_{\\mathrm{simple}} = \\mathbb{E}\\,\\|\\epsilon - \\epsilon_\\theta\\|^2$ (part 9, Algorithm 1).", why: "Part 8 proved which number to train on; part 9 turned it into the noise-prediction form the real systems minimize." },
          { do: "(d) The fast deterministic sampler is part 11: the score reading via Tweedie, the denoised estimate $\\hat{x}_0$, and the DDIM update on a sub-schedule of 20-50 levels.", why: "Part 11 proved endpoint-only jumps preserve the marginals, which is what makes few-step sampling legitimate." },
          { do: "(e) Guidance is this part: Bayes on scores gives the tilt, label dropout trains one network both ways, and $\\tilde\\epsilon = \\epsilon_\\varnothing + w(\\epsilon_y - \\epsilon_\\varnothing)$ is the slider.", why: "Part 12 derived CFG as the score of the sharpened density $q(x_t)\\,q(y \\mid x_t)^w$." },
          { do: "(f) The autoencoder is also this part (with the VAE lesson as background): encoder $E$ shrinks pixels to latents 48-fold, diffusion runs on latents, decoder $D$ inflates the result — PCA played $E$ and $D$ in our miniature.", why: "Part 12's cost argument showed the shrink is where affordability comes from; parts 6-11 apply unchanged in latent space." },
          { do: "(g) The text encoder is the one imported component: its interface — turn the condition into embedding vectors for the network — is part 12's label-embedding contract, but its internals (a transformer trained on image-caption pairs) belong to a different course.", why: "The course explains every part of the diffusion machine; the text encoder plugs into it through an interface we did build, which is the honest boundary to draw." }
        ],
        answer: "(a) part 6 (with 4); (b) part 10; (c) parts 8-9; (d) part 11; (e) part 12; (f) part 12 (background: the VAE lesson); (g) interface from part 12, internals imported — the only box the course does not open." },
      { q: "Design question: two conditions at once. You want a model of the digits that conditions on TWO labels — which digit (0-9) and stroke thickness (thin/thick) — and lets you steer each independently at sampling time. Design the training recipe and the sampling update, and name the assumption your sampling update leans on.",
        steps: [
          { do: "Give each condition its own embedding table — 10 + 1 rows for the digit (including its null token $\\varnothing_1$), 2 + 1 rows for thickness (including $\\varnothing_2$) — and hand the network both embeddings alongside the time embedding: $\\epsilon_\\theta(x_t, t, y_1, y_2)$.", why: "The label-embedding trick applies once per condition; separate tables keep the two conditions independently controllable, where one joint table of 20 combinations could not drop them separately." },
          { do: "Train with independent dropout: replace $y_1$ by $\\varnothing_1$ with probability 0.1 and, independently, $y_2$ by $\\varnothing_2$ with probability 0.1, so the network sees all four patterns — both labels, either alone, neither.", why: "Sampling will query the network with exactly these combinations, and a network only answers well on inputs it trained on (part 10's uniform-$t$ lesson, applied to labels)." },
          { do: "Build the sampling update by sharpening each condition separately — target the density proportional to $q(x_t)\\, q(y_1 \\mid x_t)^{w_1}\\, q(y_2 \\mid x_t)^{w_2}$ — and rerun this part's derivation: each factor contributes its own tilt with its own scale.", why: "Logs turn the product into a sum, gradients act term by term, and each tilt converts to a noise-guess difference by the same bridge — the derivation is linear in the conditions." },
          { do: "Write the result, with $\\epsilon_{\\varnothing} = \\epsilon_\\theta(x_t, t, \\varnothing_1, \\varnothing_2)$, $\\epsilon_{y_1} = \\epsilon_\\theta(x_t, t, y_1, \\varnothing_2)$, $\\epsilon_{y_2} = \\epsilon_\\theta(x_t, t, \\varnothing_1, y_2)$: $\\tilde\\epsilon = \\epsilon_{\\varnothing} + w_1\\,(\\epsilon_{y_1} - \\epsilon_{\\varnothing}) + w_2\\,(\\epsilon_{y_2} - \\epsilon_{\\varnothing})$ — three forward calls per step, two independent knobs.", why: "Each parenthesis is one condition's steering direction measured with the other condition switched off; the scales $w_1, w_2$ steer digit identity and thickness separately, which was the design goal." },
          { do: "Name the leaned-on assumption: treating the joint tilt as the sum of the two single-condition tilts assumes the conditions steer (approximately) independently — that $q(y_1, y_2 \\mid x_t)$ factorizes like $q(y_1 \\mid x_t)\\, q(y_2 \\mid x_t)$. For strongly interacting conditions (say, \"digit 1\" and \"very thick\", which rarely co-occur), the sum of tilts can point somewhere neither condition endorses, and the honest fix is a network also trained on the fully joint pattern $\\epsilon_\\theta(x_t, t, y_1, y_2)$ queried directly.", why: "Part 2 taught that independence is a property to check, not to assume — the factorized sampling update is exactly an independence bet, and stating the bet is what makes the design honest." }
        ],
        answer: "Two embedding tables each with a null token, independent 10 percent dropout per condition, and the update $\\tilde\\epsilon = \\epsilon_{\\varnothing} + w_1(\\epsilon_{y_1} - \\epsilon_{\\varnothing}) + w_2(\\epsilon_{y_2} - \\epsilon_{\\varnothing})$ with three calls per step and one knob per condition. It leans on the assumption that the two conditions tilt the score independently; when they interact strongly, query the jointly conditioned network instead." }
    ]
  });
})();
