/* Rigorous Courses · Diffusion Models — Part 11: Scores, shortcuts, and 20-step sampling.
   Owns the score, Tweedie's formula (network = scaled score estimator), the denoised
   estimate x-hat-0, the deterministic DDIM update, sub-schedule step-skipping, the eta
   knob, and determinism as a latent space. */
(function () {
  window.LESSONS.push({
    id: "diff-11-score-ddim",
    title: "Part 11 — Scores, shortcuts, and 20-step sampling",
    tagline: "Read the trained network as a compass pointing uphill toward realistic data, and 200 sampling steps collapse to 20",
    module: "Rigorous Courses · Diffusion Models",
    superGroup: "Rigorous Courses",
    template: "course",
    part: 11,
    partTotal: 12,
    prereqs: ["diff-10-train-ddpm"],
    goal: `<p>After this part you can:</p>
      <ul>
        <li>Compute the score of any 1-D Gaussian by hand — $(\\mu - x)/\\sigma^2$ — and read its sign and size correctly.</li>
        <li>Derive Tweedie's formula for a mixture of Gaussians and use it to prove that a perfectly trained noise-guesser is the score of the noised data, up to a known scale: $\\nabla_{x_t} \\log q(x_t) = -\\epsilon_\\theta^*(x_t, t)/\\sqrt{1-\\bar\\alpha_t}$.</li>
        <li>Convert any noise guess into the denoised estimate $\\hat{x}_0 = (x_t - \\sqrt{1-\\bar\\alpha_t}\\,\\epsilon_\\theta)/\\sqrt{\\bar\\alpha_t}$ and execute a DDIM update by hand.</li>
        <li>Explain, with the marginals, why DDIM can jump between distant noise levels while naively skipping steps of DDPM cannot.</li>
        <li>Say what determinism buys: reproducible samples, and a starting noise that behaves like a meaningful latent code.</li>
      </ul>`,
    sections: [
      { h: "Where we are",
        body: `<p>The theory is finished and the machine is built. Part 6 gave the forward process and its one-line sampler $x_t = \\sqrt{\\bar\\alpha_t}\\,x_0 + \\sqrt{1-\\bar\\alpha_t}\\,\\epsilon$, where $\\bar\\alpha_t$ is the fraction of the original signal surviving to step $t$. Part 7 derived the exact one-step denoiser $q(x_{t-1} \\mid x_t, x_0)$. Part 8 compressed "learn to denoise" into one trainable number, the ELBO. Part 9 collapsed the ELBO into $L_{\\mathrm{simple}} = \\mathbb{E}\\,\\|\\epsilon - \\epsilon_\\theta(x_t, t)\\|^2$ plus two short recipes, Algorithm 1 and Algorithm 2. And in part 10 you trained the real thing: a 21,250-parameter network that turns pure noise into an eight-cluster ring.</p>
        <p>Part 10 also ended on a sore point. Generating one batch took 200 network calls — one per step — and the naive shortcut of running only 20 of the 200 updates produced garbage. Yet real systems sample beautifully in 20 to 50 steps. This part explains how, and the answer needs <b>no new training at all</b>. We will re-read the network we already have: its noise guess is secretly an estimate of the <b>score</b> — the uphill direction of the data's log density. Tweedie's formula proves the reading. From it we get a reusable primitive (the denoised estimate $\\hat{x}_0$), and from that a deterministic sampler, <b>DDIM</b>, whose update only ever mentions the endpoints of a jump — so big jumps are as legitimate as small ones. Along the way we pick up an unexpected gift: determinism turns the starting noise into a meaningful latent code.</p>` },

      { h: "The score: which way is uphill?",
        body: `<p>Picture the data distribution the way part 1 taught you: a landscape over the space of data points, high where the data is crowded, near zero over the vast empty regions. Generation is a walk from a random spot toward the crowded highlands. To walk uphill you need one piece of local information: <b>which direction is up, and how steeply</b>. That is the score.</p>
        <p>Two words of vocabulary, then the definition. A <b>partial derivative</b> is the slope you feel when you wiggle one input alone, holding every other input fixed; the symbol $\\partial/\\partial x$ names it. The <b>gradient</b> $\\nabla_x$ is the list of all the partial derivatives, one per coordinate — and that list, read as a vector, points in the direction of steepest uphill. In one dimension the gradient is a single slope, which is all today's derivations need; "per coordinate" covers the rest, as usual.</p>
        <div class="formula-box">$$\\text{score}(x) \\;=\\; \\nabla_x \\log p(x)$$</div>
        <p><b>Read it out loud:</b> the score at a point $x$ is the slope of the log of the density there — the direction (and steepness) of uphill on the log-probability landscape.</p>
        <p>Why the log? Three reasons, in rising order of importance. First, logs turn products into sums (part 5), which keeps every calculation tame. Second, the log leaves the hilltops where they are: $\\log$ is an increasing function, so $p$ and $\\log p$ peak at exactly the same places, and uphill for one is uphill for the other. Third — the reason that matters most here — the score <b>ignores normalizing constants</b>: dividing a density by any constant subtracts a constant from its log, and a constant has slope zero. The hardest part of a density is usually its normalizer; the score never needs it.</p>
        <p>Let's compute the score of the one density we know best, $\\mathcal{N}(x;\\,\\mu,\\,\\sigma^2)$, one operation per step. The only slope facts we use: the slope of $x^2$ is $2x$, the slope of $a\\,x$ is $a$, the slope of a constant is $0$, and slopes add. (Check the first one with numbers: between $x = 2.999$ and $x = 3.001$, $x^2$ rises by $0.012$ over a run of $0.002$ — slope $6$, which is $2 \\times 3$.)</p>
        <ol>
          <li><b>Do:</b> write the density: $p(x) = \\frac{1}{\\sqrt{2\\pi\\sigma^2}}\\, e^{-(x-\\mu)^2 / (2\\sigma^2)}$. <b>Why:</b> part 3's anatomy of the Gaussian — a normalizer times a bell-shaped exponential.</li>
          <li><b>Do:</b> take the log: $\\log p(x) = -\\frac{(x-\\mu)^2}{2\\sigma^2} - \\frac{1}{2}\\log(2\\pi\\sigma^2)$. <b>Why:</b> the log of a product is the sum of the logs (part 5), and the log undoes the exponential.</li>
          <li><b>Do:</b> differentiate the second term: its slope is $0$. <b>Why:</b> it contains no $x$; a flat function has slope zero — this is the normalizer disappearing, as promised.</li>
          <li><b>Do:</b> expand the first term's square: $(x-\\mu)^2 = x^2 - 2\\mu x + \\mu^2$. <b>Why:</b> multiply out the product $(x-\\mu)(x-\\mu)$ term by term.</li>
          <li><b>Do:</b> take slopes term by term: slope of $x^2$ is $2x$; slope of $-2\\mu x$ is $-2\\mu$; slope of $\\mu^2$ is $0$; adding gives $2x - 2\\mu$. <b>Why:</b> the three slope facts above, plus "slopes add".</li>
          <li><b>Do:</b> factor: $2x - 2\\mu = 2(x - \\mu)$. <b>Why:</b> pull out the common factor 2 — the reverse of distributing.</li>
          <li><b>Do:</b> multiply by the constant $-\\frac{1}{2\\sigma^2}$ sitting in front of the square: the slope of the whole log density is $-\\frac{2(x-\\mu)}{2\\sigma^2} = \\frac{\\mu - x}{\\sigma^2}$. <b>Why:</b> scaling a function by a constant scales its slope by the same constant; then the 2s cancel.</li>
        </ol>
        <div class="formula-box">$$\\nabla_x \\log \\mathcal{N}(x;\\,\\mu,\\,\\sigma^2) \\;=\\; \\frac{\\mu - x}{\\sigma^2}$$</div>
        <p><b>Read it out loud:</b> the score of a Gaussian is the distance from where you are back to the center, divided by the variance — a pull toward the middle that grows with how far away you are and weakens when the bell is wide.</p>
        <p><b>Worked example.</b> Score of $\\mathcal{N}(1, 4)$ at $x = 3$: $\\frac{1 - 3}{4} = \\frac{-2}{4} = -0.5$. Negative means uphill is to the left — back toward the center at 1 — with steepness one half. At $x = 1$ the score is $0$ (the top of the hill is flat). At $x = -7$ it is $\\frac{1-(-7)}{4} = 2$: four times the pull at four times the distance, exactly the linear-in-distance behavior the formula promises.</p>` },

      { h: "Tweedie's formula: the network is secretly a compass",
        body: `<p>One Gaussian is a warm-up. The density we actually care about at sampling time is $q(x_t)$ — the distribution of the data <b>after</b> $t$ doses of noise. If we knew its score, we would know, at every point of space and every noise level, which way realistic data lies. Here is the astonishing fact this section proves: <b>the network we trained in part 10 already computes this score.</b> The proof is a short chain of algebra called <b>Tweedie's formula</b>, and for the two-point toy from part 9 we can carry it out completely.</p>
        <p>The setup, from part 9: clean data is $x_0 = +2$ or $x_0 = -2$, equal odds. Fix a noise level $t$ and write two shorthands to keep the lines readable: $a = \\sqrt{\\bar\\alpha_t}$ and $s = \\sqrt{1-\\bar\\alpha_t}$ (we drop the $t$ subscripts inside this derivation; $s^2 = 1-\\bar\\alpha_t$ is the noise variance). Part 6's closed form says: starting from $x_0 = +2$, the noisy point is distributed as $\\mathcal{N}(2a,\\ s^2)$; starting from $-2$, as $\\mathcal{N}(-2a,\\ s^2)$. And part 2's marginalization rule says the overall density is the weighted average over the possible starts:</p>
        <div class="formula-box">$$q(x) \\;=\\; \\tfrac{1}{2}\\, \\mathcal{N}(x;\\ 2a,\\ s^2) \\;+\\; \\tfrac{1}{2}\\, \\mathcal{N}(x;\\ -2a,\\ s^2)$$</div>
        <p><b>Read it out loud:</b> the density of the noisy data is half a bell centered at the shrunken $+2$, plus half a bell centered at the shrunken $-2$ — a mixture, one component per possible clean value.</p>
        <p>We need one new slope fact, the <b>log-slope rule</b>: the slope of $\\log f$ at $x$ equals the slope of $f$ at $x$, divided by $f(x)$. The plain-English reason: the log measures <b>relative</b> change — when $f$ grows by 1% the log grows by about $0.01$ whether $f$ is 5 or 5,000 — so the absolute slope of $f$ gets divided by the current value. (The notebook checks this rule numerically; you have already used its reverse in part 5, where the slope of a log-likelihood carried a $1/f$ inside every term.) Now the derivation, one operation per step.</p>
        <ol>
          <li><b>Do:</b> name the two bells $N_+(x) = \\mathcal{N}(x;\\, 2a,\\, s^2)$ and $N_-(x) = \\mathcal{N}(x;\\, -2a,\\, s^2)$, so $q(x) = \\tfrac{1}{2}N_+(x) + \\tfrac{1}{2}N_-(x)$. <b>Why:</b> naming the pieces lets us work on one at a time.</li>
          <li><b>Do:</b> write the score as $\\nabla_x \\log q(x) = \\frac{q'(x)}{q(x)}$, where $q'$ means the slope of $q$. <b>Why:</b> that is the log-slope rule applied to $q$.</li>
          <li><b>Do:</b> find the slope of $N_+$: the log-slope rule read backwards gives $N_+'(x) = N_+(x) \\cdot \\frac{2a - x}{s^2}$. <b>Why:</b> the previous section computed the slope of $\\log N_+$ to be $\\frac{2a - x}{s^2}$ (a Gaussian score with center $2a$), and multiplying both sides of the rule by $N_+(x)$ isolates $N_+'$.</li>
          <li><b>Do:</b> same for the other bell: $N_-'(x) = N_-(x) \\cdot \\frac{-2a - x}{s^2}$. <b>Why:</b> identical reasoning with center $-2a$.</li>
          <li><b>Do:</b> add: $q'(x) = \\tfrac{1}{2}N_+(x)\\frac{2a-x}{s^2} + \\tfrac{1}{2}N_-(x)\\frac{-2a-x}{s^2}$. <b>Why:</b> slopes add, and each mixture weight $\\tfrac{1}{2}$ is a constant scaling its term's slope.</li>
          <li><b>Do:</b> divide by $q(x)$: $\\nabla_x \\log q(x) = \\frac{\\tfrac{1}{2}N_+(x)}{q(x)}\\cdot\\frac{2a-x}{s^2} + \\frac{\\tfrac{1}{2}N_-(x)}{q(x)}\\cdot\\frac{-2a-x}{s^2}$. <b>Why:</b> step 2 says the score is $q'/q$; dividing each term separately is allowed because division distributes over a sum.</li>
          <li><b>Do:</b> recognize the two fractions as posterior probabilities: $w_+(x) = \\frac{\\tfrac{1}{2}N_+(x)}{q(x)} = P(x_0 = +2 \\mid x_t = x)$ and likewise $w_-(x)$ for $-2$. <b>Why:</b> Bayes' rule from part 2 — prior ($\\tfrac{1}{2}$) times likelihood (the bell) divided by the evidence ($q$) is exactly the probability of the start given the observation.</li>
          <li><b>Do:</b> rewrite: $\\nabla_x \\log q(x) = w_+(x)\\,\\frac{2a - x}{s^2} + w_-(x)\\,\\frac{-2a - x}{s^2}$. <b>Why:</b> substituting the names from step 7.</li>
          <li><b>Do:</b> split each term into its center part and its $-x$ part and regroup: $\\nabla_x \\log q(x) = \\frac{a\\,\\big(w_+(x)\\cdot 2 + w_-(x)\\cdot(-2)\\big) - \\big(w_+(x)+w_-(x)\\big)\\,x}{s^2}$. <b>Why:</b> distribute the weights over each numerator, then collect the two $x$ terms and the two center terms — reordering an addition is always allowed.</li>
          <li><b>Do:</b> use $w_+(x) + w_-(x) = 1$: the $x$ part becomes $-x$. <b>Why:</b> the posterior probabilities of the only two possibilities must sum to 1 (part 2).</li>
          <li><b>Do:</b> recognize the center part: $w_+(x)\\cdot 2 + w_-(x)\\cdot(-2) = \\mathbb{E}[x_0 \\mid x_t = x]$ — the posterior average of the clean value. <b>Why:</b> that is the definition of expectation (part 2), taken under the posterior weights from Bayes.</li>
        </ol>
        <div class="formula-box">$$\\nabla_{x_t} \\log q(x_t) \\;=\\; \\frac{\\sqrt{\\bar\\alpha_t}\\; \\mathbb{E}[x_0 \\mid x_t] \\;-\\; x_t}{1-\\bar\\alpha_t}$$</div>
        <p><b>Read it out loud:</b> the score of the noised data equals the gap between where you are and the shrunken posterior average of the clean point, divided by the noise variance — a pull toward your best guess of where you came from. This is <b>Tweedie's formula</b>. Compare it with the single-Gaussian score $(\\mu - x)/\\sigma^2$: same shape, with the fixed center $\\mu$ replaced by the data-dependent best guess $\\sqrt{\\bar\\alpha_t}\\,\\mathbb{E}[x_0 \\mid x_t]$.</p>
        <p>Nothing in the derivation used the number two: with any finite list of clean values $c_i$ and weights, steps 1-11 run word for word — the weights become $w_i(x)$, they still sum to 1, and the center part is still the posterior mean. (The formula in fact holds for any data distribution; it is named for Maurice Tweedie, and Herbert Robbins published the general argument in 1956. Our finite-mixture proof contains every idea.)</p>
        <p>Now connect it to the network. Two more steps close the loop:</p>
        <ol>
          <li><b>Do:</b> recall the exact identity from part 9: given $x_t$ and $x_0$, the noise was $\\epsilon = \\frac{x_t - a\\,x_0}{s}$; take the posterior average of both sides given $x_t$: $\\mathbb{E}[\\epsilon \\mid x_t] = \\frac{x_t - a\\,\\mathbb{E}[x_0 \\mid x_t]}{s}$. <b>Why:</b> linearity of expectation (part 2) — $x_t$ is a fixed number inside this conditional average, so it passes through untouched.</li>
          <li><b>Do:</b> solve Tweedie for the numerator piece — $a\\,\\mathbb{E}[x_0 \\mid x_t] = x_t + s^2\\,\\nabla_{x_t}\\log q(x_t)$ — substitute it in, and cancel: $\\mathbb{E}[\\epsilon \\mid x_t] = \\frac{x_t - x_t - s^2\\,\\nabla\\log q}{s} = -s\\,\\nabla_{x_t}\\log q(x_t)$. <b>Why:</b> multiplying Tweedie by $s^2$ and adding $x_t$ is reversible algebra, and $s^2/s = s$.</li>
        </ol>
        <p>Finally, recall part 9's Check 3: the best possible squared-error guesser of the noise is exactly the conditional mean, $\\epsilon_\\theta^*(x_t, t) = \\mathbb{E}[\\epsilon \\mid x_t]$ — and that is what training pushes our network toward. Flip the last equation:</p>
        <div class="formula-box">$$\\nabla_{x_t} \\log q(x_t) \\;=\\; -\\,\\frac{\\epsilon_\\theta^*(x_t, t)}{\\sqrt{1-\\bar\\alpha_t}}$$</div>
        <p><b>Read it out loud:</b> the score of the noised data is the ideal network's noise guess, flipped in sign and divided by the noise scale. The minus sign makes sense: the noise points from the data <b>out</b> to where you are, so minus the noise points back home. <b>The network is a compass.</b> Every arrow it outputs, negated and rescaled by the known constant $\\sqrt{1-\\bar\\alpha_t}$, points uphill toward realistic data — and the notebook draws this compass field for the part-10 network at three noise levels so you can watch the arrows converge on the ring and sharpen as $t$ drops. A compact second telling of this score view of diffusion lives in <a onclick="App.open('mod-diffusion')">the diffusion concept lesson</a>.</p>
        <p><b>Worked example (numbers you can redo).</b> Two-point toy, running schedule at $t = 3$: $\\bar\\alpha_3 = 0.504$, so $a = \\sqrt{0.504} = 0.7099$ and $s^2 = 0.496$, $s = 0.7043$. Observe $x_3 = 0.3$. Part 9's exact predictor gives the posterior mean in closed form: Bayes' odds of $+2$ versus $-2$ are the ratio of the two bells, which collapses to a single exponential, and the difference of the two weights becomes a tanh: $\\mathbb{E}[x_0 \\mid x_3] = 2\\tanh\\!\\big(\\frac{2 a x_3}{s^2}\\big)$. Plug in: $\\frac{2 \\times 0.7099 \\times 0.3}{0.496} = 0.8588$, and $2\\tanh(0.8588) = 2 \\times 0.6956 = 1.3913$. Tweedie: score $= \\frac{0.7099 \\times 1.3913 - 0.3}{0.496} = \\frac{0.6877}{0.496} = 1.3865$. Best noise guess: $\\mathbb{E}[\\epsilon \\mid x_3] = \\frac{0.3 - 0.7099 \\times 1.3913}{0.7043} = -0.9765$. Bridge check: $-\\frac{-0.9765}{0.7043} = 1.3865$ — the same number, to four decimals. The positive score says: from $x_3 = 0.3$, uphill is to the right, toward the $+2$ branch the posterior already favors ($w_+ \\approx 0.85$).</p>` },

      { h: "The denoised estimate: a clean guess at every moment",
        body: `<p>The bridge hands us a second gift. Part 9's inversion said that <b>knowing the noise means knowing the clean point</b>. Apply it to the network's guess instead of the true noise and you get the <b>denoised estimate</b>:</p>
        <div class="formula-box">$$\\hat{x}_0(x_t, t) \\;=\\; \\frac{x_t - \\sqrt{1-\\bar\\alpha_t}\\;\\epsilon_\\theta(x_t, t)}{\\sqrt{\\bar\\alpha_t}}$$</div>
        <p><b>Read it out loud:</b> the model's current guess of the finished sample: take the noisy point, subtract the guessed noise scaled the way noise was added, then undo the shrinking. Same algebra as part 9's inversion, with the guess standing in for the truth.</p>
        <p>What is this object, exactly, when the network is perfect? Three operations settle it:</p>
        <ol>
          <li><b>Do:</b> replace $\\epsilon_\\theta$ by the ideal guess $\\mathbb{E}[\\epsilon \\mid x_t] = \\frac{x_t - a\\,\\mathbb{E}[x_0 \\mid x_t]}{s}$. <b>Why:</b> part 9's Check 3 says the trained optimum is the conditional mean, and the previous section computed it.</li>
          <li><b>Do:</b> simplify the numerator: $x_t - s \\cdot \\frac{x_t - a\\,\\mathbb{E}[x_0 \\mid x_t]}{s} = x_t - x_t + a\\,\\mathbb{E}[x_0 \\mid x_t] = a\\,\\mathbb{E}[x_0 \\mid x_t]$. <b>Why:</b> the $s$ upstairs cancels the $s$ downstairs, then equal terms vanish under subtraction.</li>
          <li><b>Do:</b> divide by $a$: $\\hat{x}_0 = \\mathbb{E}[x_0 \\mid x_t]$. <b>Why:</b> $a = \\sqrt{\\bar\\alpha_t}$ is positive, so dividing by it is allowed and exact.</li>
        </ol>
        <p>So the denoised estimate is <b>the posterior mean of the clean point</b> — the best guess under squared error (part 2). In the worked example above: $\\hat{x}_0 = \\frac{0.3 - 0.7043 \\times (-0.9765)}{0.7099} = \\frac{0.9877}{0.7099} = 1.3913$, exactly the posterior mean we computed by Bayes. For a real, imperfect network it is an estimate of that mean — early in sampling (large $t$) it is a blurry committee average of everything the point might become; late (small $t$) it sharpens into a specific sample.</p>
        <p>Hold on to this primitive. It means that <b>at every moment mid-generation, the model has a full opinion about the final answer</b> — not just about the next small step. DDIM, next, is nothing more than the decision to trust that opinion and jump. (Part 12 will lean on the same primitive again: guidance works by editing the noise guess, which is the same thing as editing $\\hat{x}_0$.)</p>` },

      { h: "DDIM: jump to the guess, re-noise deterministically",
        body: `<p>Here is the whole DDIM sampler (Song, Meng and Ermon, 2020). One update, applied from $t = T$ down to $1$:</p>
        <div class="formula-box">$$x_{t-1} \\;=\\; \\sqrt{\\bar\\alpha_{t-1}}\\;\\hat{x}_0(x_t, t) \\;+\\; \\sqrt{1-\\bar\\alpha_{t-1}}\\;\\epsilon_\\theta(x_t, t)$$</div>
        <p><b>Read it out loud:</b> jump all the way to your current best guess of the clean point — then re-noise it back out to level $t-1$, deterministically, reusing the noise direction you already believe in instead of drawing fresh noise. No $z$, no randomness, anywhere.</p>
        <p>Look at its shape next to part 6's one-line sampler, $x_{t-1} = \\sqrt{\\bar\\alpha_{t-1}}\\,x_0 + \\sqrt{1-\\bar\\alpha_{t-1}}\\,\\epsilon$: the DDIM update is the <b>same formula with hats on</b> — the estimate $\\hat{x}_0$ where the clean point was, the guess $\\epsilon_\\theta$ where the true noise was. That resemblance is the entire correctness argument, and we can make it exact in the <b>oracle case</b>: suppose this particular $x_t$ really was built as $x_t = \\sqrt{\\bar\\alpha_t}\\,x_0 + \\sqrt{1-\\bar\\alpha_t}\\,\\epsilon$ from a known clean $x_0$, and the guesser is exact on it: $\\epsilon_\\theta = \\epsilon$. Three operations:</p>
        <ol>
          <li><b>Do:</b> evaluate the primitive: $\\hat{x}_0 = \\frac{x_t - \\sqrt{1-\\bar\\alpha_t}\\,\\epsilon}{\\sqrt{\\bar\\alpha_t}} = x_0$. <b>Why:</b> with the true noise, the inversion is exact — part 9 proved it recovers the clean point to every decimal.</li>
          <li><b>Do:</b> substitute into the update: $x_{t-1} = \\sqrt{\\bar\\alpha_{t-1}}\\,x_0 + \\sqrt{1-\\bar\\alpha_{t-1}}\\,\\epsilon$. <b>Why:</b> replacing $\\hat{x}_0$ and $\\epsilon_\\theta$ by the equal quantities $x_0$ and $\\epsilon$.</li>
          <li><b>Do:</b> read what that line is: the one-line sampler at level $t-1$, with the <b>same</b> $\\epsilon$ — a legitimate draw from $q(x_{t-1} \\mid x_0)$, since $\\epsilon$ was a standard Gaussian draw. <b>Why:</b> part 6's closed form says exactly this recipe produces the level-$(t-1)$ marginal.</li>
        </ol>
        <p>So each DDIM step lands <b>exactly on the right marginal</b> — the population of points it produces at level $t-1$ is distributed precisely as noised data at level $t-1$. Where DDPM re-rolls fresh randomness at every step ($\\sigma_t z$), DDIM carries the <b>original</b> randomness — the $\\epsilon$ hiding in $x_T$ — all the way down, unchanged. The sample's identity is decided once, at the start.</p>
        <p>One honest subtlety so you are not misled: the DDIM update is <b>not</b> a draw from part 7's posterior $q(x_{t-1} \\mid x_t, x_0)$ — it is a different reverse rule. The DDIM paper builds a whole family of alternative processes (non-Markovian ones) that share the same marginals $q(x_t \\mid x_0)$ and — this is the punchline — are trained by the <b>same</b> $L_{\\mathrm{simple}}$. That is why our part-10 network works here without any retraining: the loss only ever saw the marginals, and the marginals are what DDIM preserves.</p>
        <p><b>Worked example.</b> Continue the two-point toy from the Tweedie section: at $t = 3$ we had $x_3 = 0.3$, $\\epsilon_\\theta = -0.9765$ (the exact predictor), $\\hat{x}_0 = 1.3913$. Jump to level 2 ($\\bar\\alpha_2 = 0.72$): $x_2 = \\sqrt{0.72} \\times 1.3913 + \\sqrt{0.28} \\times (-0.9765) = 0.8485 \\times 1.3913 - 0.5292 \\times 0.9765 = 1.1805 - 0.5167 = 0.6638$. The point moved from $0.3$ to $0.6638$ — pulled toward the $+2$ branch its posterior favors, with no coin flipped.</p>` },

      { h: "One knob and twenty steps: eta and sub-schedules",
        body: `<p>DDPM and DDIM are not rivals but endpoints of a dial. The DDIM paper's general update carves the noise budget $1-\\bar\\alpha_{t-1}$ into a deterministic part and a fresh-noise part, controlled by a knob $\\eta$:</p>
        <div class="formula-box">$$x_{t-1} = \\sqrt{\\bar\\alpha_{t-1}}\\,\\hat{x}_0 + \\sqrt{1-\\bar\\alpha_{t-1}-\\sigma_t^2}\\;\\epsilon_\\theta(x_t, t) + \\sigma_t z, \\qquad \\sigma_t = \\eta\\,\\sqrt{\\tilde\\beta_t},\\quad z \\sim \\mathcal{N}(0, \\mathbf{I})$$</div>
        <p><b>Read it out loud:</b> jump to the clean guess, re-noise partly along the believed direction and partly with a fresh Gaussian kick of size $\\sigma_t$; the knob $\\eta$ sets the kick as a fraction of the true posterior spread $\\sqrt{\\tilde\\beta_t}$ from part 7. At $\\eta = 0$ the kick vanishes and this is exactly DDIM. At $\\eta = 1$ the kick equals the posterior spread and the update becomes DDPM's stochastic sampler — in its $\\sigma_t^2 = \\tilde\\beta_t$ variant; part 10's code used the other standard choice, $\\sigma_t^2 = \\beta_t$, and parts 8-9 showed both are legitimate. We state the $\\eta = 1$ equivalence without deriving it — the algebra is a patient exercise in part 7's formulas, and it lives in the DDIM paper (arXiv 2010.02502) — but the notebook needs only the $\\eta = 0$ end.</p>
        <p>Now the payoff. Stare at the DDIM update: it mentions $\\bar\\alpha_{t-1}$ and $\\bar\\alpha_t$ (inside $\\hat{x}_0$) — <b>the two endpoints of the jump — and nothing else</b>. No lone $\\beta_t$, no per-step bookkeeping. And the oracle argument never used the fact that $t-1$ is adjacent to $t$: replace $t-1$ by any earlier level and the same three operations land you exactly on that level's marginal. So pick a <b>sub-schedule</b> — say $S = 20$ levels $\\tau_1 < \\tau_2 < \\cdots < \\tau_S = T$ out of the 200 — and hop:</p>
        <div class="formula-box">$$x_{\\tau_{i-1}} \\;=\\; \\sqrt{\\bar\\alpha_{\\tau_{i-1}}}\\;\\hat{x}_0(x_{\\tau_i}, \\tau_i) \\;+\\; \\sqrt{1-\\bar\\alpha_{\\tau_{i-1}}}\\;\\epsilon_\\theta(x_{\\tau_i}, \\tau_i)$$</div>
        <p><b>Read it out loud:</b> the same jump-and-re-noise update, now between chosen levels $\\tau_i$ and $\\tau_{i-1}$ that may be ten steps apart — legitimate because the formula only ever asks for the survival fractions at the two endpoints. Twenty network calls instead of two hundred.</p>
        <p>Why does the same trick wreck DDPM, as part 10's teaser showed? DDPM's update is built from <b>single-step ingredients</b>: divide by $\\sqrt{\\alpha_t}$ (undo one step's shrink), subtract $\\frac{\\beta_t}{\\sqrt{1-\\bar\\alpha_t}}\\,\\epsilon_\\theta$ (remove one dose's worth of noise), add a $\\sqrt{\\beta_t}$-sized kick. Run it at only 20 of the 200 levels, keeping those per-step coefficients, and you undo 20 doses — the other 180 are simply never removed. The samples stay a noise blob. A back-of-envelope floor: the start blob's mean radius is about $1.25$ (the typical radius of a 2-D standard Gaussian draw), and the 20 kept steps stretch it by $\\prod 1/\\sqrt{\\alpha_{\\tau_i}} \\approx e^{0.1}$, giving $\\approx 1.4$ — before counting the 20 fresh kicks and the network's steering, both of which push outward. The notebook measures about 2: roughly half the data's radius of 4, still deep inside the ring.</p>
        <p>Could you instead <b>rebuild</b> a correct stochastic sampler on the sub-schedule, treating each 10-step gap as one big effective dose? Yes — but then each jump must inject a large fresh kick (the posterior spread $\\tilde\\beta$ grows with the gap), re-destroying structure with only a few steps left to rebuild it; and part 7's justification for the Gaussian shape of the reverse step assumed small doses, so big stochastic jumps also lean on an approximation that is no longer good. DDIM sidesteps both: its jump is exact in the oracle case at any gap, and it injects nothing that later steps must clean up. Its only error source is that $\\epsilon_\\theta$ is an imperfect guesser consulted 20 times instead of 200 — which is why $S = 20$ looks nearly perfect on our ring, and $S = 10$ starts to fray at the edges.</p>` },

      { h: "Determinism pays: a latent space for free",
        body: `<p>Delete the $z$ and something qualitative changes: the sampler becomes a fixed function. Write it as $x_0 = F(x_T)$ — twenty network calls and some arithmetic, composed. Feed the same starting noise twice and you get the <b>same sample, bit for bit</b> (the notebook asserts exact equality). Run DDPM twice from the same start and the per-step kicks send the two runs to different clusters.</p>
        <p>This makes $x_T$ more than disposable static — it becomes a <b>latent code</b>: a compact address that fully determines the sample. Three consequences, each of which practitioners use daily:</p>
        <ul>
          <li><b>Reproducibility.</b> A generated sample has a name — its code. Keep the code and you can regenerate the sample exactly, tomorrow, on another machine; or hold the code fixed while changing the step count or the checkpoint, and attribute every difference in the output to the thing you changed. Debugging a stochastic sampler offers no such controlled experiment.</li>
          <li><b>Meaningful neighborhoods.</b> $F$ is built from a smooth network and smooth arithmetic, so nearby codes map to similar samples. Slide a code gradually from one value to another and the outputs morph gradually — interpolation between two generated samples becomes a walk in code space. Under DDPM the fresh kicks re-randomize identity at every step, so "the same walk" produces unrelated endpoints.</li>
          <li><b>Encoding real data.</b> A deterministic update can be run in the reverse direction: starting from a data point, step the update backwards along the sub-schedule to recover (approximately) the code that would generate it. Edit near that code, decode, and you have image editing — the mechanism behind the img2img workflows built on Stable Diffusion.</li>
        </ul>
        <p>The notebook makes the first two visible: an exact-equality assert, and a plot of 30 codes flowing along smooth, non-crossing-ish trajectories from the Gaussian blob onto the ring — each path decided entirely by where it started.</p>` },

      { h: "Sanity checks",
        body: `<p>Every numeric claim here is verified in the notebook.</p>
        <ul>
          <li><b>Gaussian score behaves.</b> $(\\mu - x)/\\sigma^2$ is zero at the center (hilltops are flat), negative to the right of center and positive to the left (always pointing home), and scales like $1/\\sigma^2$ (wide bells pull gently). $\\mathcal{N}(1,4)$ at $x=3$: $-0.5$.</li>
          <li><b>Tweedie contains the warm-up.</b> With a single clean value $c$ (a "mixture" of one), the posterior weight is 1, $\\mathbb{E}[x_0 \\mid x_t] = c$, and Tweedie reads $\\frac{ac - x}{s^2}$ — exactly the single-Gaussian score with center $ac$. The general formula collapses to the special case it grew from.</li>
          <li><b>The bridge's two sides agree numerically.</b> On the two-point toy, the score from Tweedie's formula, the score from $-\\mathbb{E}[\\epsilon \\mid x_t]/s$, and a finite-difference slope of $\\log q$ computed from the raw mixture density agree to better than $10^{-3}$ across the whole grid.</li>
          <li><b>Scale across $t$.</b> As $t \\to 0$, $s = \\sqrt{1-\\bar\\alpha_t} \\to 0$ and the score magnitude $|\\epsilon_\\theta|/s$ blows up: near-clean densities are cliff-steep around the data. As $t \\to T$ the landscape flattens toward the standard Gaussian's gentle pull. The quiver plots at $t = 150, 75, 20$ show exactly this sharpening.</li>
          <li><b>The primitive is the posterior mean.</b> With a perfect guesser, $\\hat{x}_0 = \\mathbb{E}[x_0 \\mid x_t]$ (three-line proof in section 4), and in the oracle case $\\hat{x}_0 = x_0$ exactly.</li>
          <li><b>DDIM's last hop is part 9's $z = 0$ rule, sharpened.</b> Jumping to level 0 means $\\bar\\alpha_0 = 1$: the update reads $x_0 = 1 \\cdot \\hat{x}_0 + 0 \\cdot \\epsilon_\\theta = \\hat{x}_0$ — the sampler ends by reporting its clean guess, with no noise attached.</li>
          <li><b>The $\\eta$ endpoints.</b> $\\eta = 0$: kick size 0, deterministic DDIM. $\\eta = 1$: kick size $\\sqrt{\\tilde\\beta_t}$, the true posterior spread — DDPM's stochastic sampler in its $\\tilde\\beta_t$-variance variant.</li>
          <li><b>Naive skipping fails by a predictable amount.</b> Twenty kept DDPM steps stretch the start blob by only $\\approx e^{0.1} \\approx 1.1$, a no-steering back-of-envelope of radius $\\approx 1.4$; with the fresh kicks and the network's off-distribution steering the notebook measures about 2 — still only half of the data's 4 — while DDIM-20's mean radius lands within a few percent of 4.</li>
        </ul>` },

      { h: "What you can do now",
        body: `<p>You re-read a trained network and got a new machine for free. The score — the uphill direction of log density — turned out to be something you could compute exactly for Gaussians, then for mixtures, and Tweedie's formula revealed that the noise-guesser you trained in part 10 has been estimating it all along: $\\nabla_{x_t}\\log q(x_t) = -\\epsilon_\\theta^*(x_t,t)/\\sqrt{1-\\bar\\alpha_t}$. From the same identity you built the denoised estimate $\\hat{x}_0$, and from that the DDIM update — jump to the guess, re-noise deterministically — whose endpoint-only algebra makes 20-step sub-schedules legitimate where naive DDPM skipping collapses. And because DDIM flips no coins, the starting noise became a latent code: reproducible samples, smooth interpolation, invertible generation. All of it verified, none of it retrained.</p>
        <p>One capability is still missing, and it is the one everybody actually uses: <b>control</b>. Our model generates from the ring — but you cannot ask it for cluster 3, the way you ask Stable Diffusion for "a watercolor of a lighthouse". Part 12 adds the steering wheel, and the score view you just learned is exactly the tool: conditioning tilts the score by a Bayes term, classifier-free guidance turns that tilt into two network calls and a knob $w$, and latent diffusion shrinks the canvas so the whole machine fits real images. Then the finale: every component of Stable Diffusion, mapped to the part of this course where you built it.</p>` }
    ],
    symbols: [
      { sym: "$x_0$", desc: "a clean data point ($+2$ or $-2$ in the toy; a 2-D ring point in the notebook)" },
      { sym: "$x_t$", desc: "the data after $t$ small doses of noise" },
      { sym: "$T$", desc: "total number of noising steps; 200 in the notebook, 3 in the hand examples" },
      { sym: "$\\bar\\alpha_t$", desc: "the fraction of the original signal surviving to step $t$ (part 6)" },
      { sym: "$a, s$", desc: "derivation shorthands: $a = \\sqrt{\\bar\\alpha_t}$ (signal scale), $s = \\sqrt{1-\\bar\\alpha_t}$ (noise scale)" },
      { sym: "$\\partial/\\partial x$", desc: "partial derivative: the slope when you wiggle $x$ alone, all else held fixed" },
      { sym: "$\\nabla_x \\log p(x)$", desc: "the score: the uphill direction (and steepness) of the log density at $x$" },
      { sym: "$q(x_t)$", desc: "the density of the data after $t$ doses of noise — a mixture of shrunken-center Gaussians" },
      { sym: "$w_+(x), w_-(x)$", desc: "posterior probabilities of the clean values $+2$ / $-2$ given $x_t = x$, from Bayes" },
      { sym: "$\\mathbb{E}[x_0 \\mid x_t]$", desc: "the posterior mean of the clean point given the noisy one — what $\\hat{x}_0$ estimates" },
      { sym: "$\\mathbb{E}[\\epsilon \\mid x_t]$", desc: "the best possible noise guess; what a perfectly trained $\\epsilon_\\theta$ outputs" },
      { sym: "$\\epsilon_\\theta(x_t, t)$", desc: "the noise-guessing network from part 10; $\\epsilon_\\theta^*$ marks the ideal (perfectly trained) one" },
      { sym: "$\\hat{x}_0(x_t, t)$", desc: "the denoised estimate $(x_t - \\sqrt{1-\\bar\\alpha_t}\\,\\epsilon_\\theta)/\\sqrt{\\bar\\alpha_t}$ — the model's clean guess mid-flight" },
      { sym: "$\\tau_1 < \\cdots < \\tau_S$", desc: "a sub-schedule: the $S$ noise levels (out of $T$) that a fast sampler actually visits" },
      { sym: "$S$", desc: "the number of sampling steps (network calls); 200, 50, 20, 10 in the notebook" },
      { sym: "$\\eta$", desc: "the DDIM-to-DDPM dial: fresh-kick size as a fraction of the posterior spread; 0 = DDIM, 1 = DDPM" },
      { sym: "$\\tilde\\beta_t$", desc: "the true posterior variance from part 7: $(1-\\bar\\alpha_{t-1})\\beta_t/(1-\\bar\\alpha_t)$" },
      { sym: "$\\sigma_t$", desc: "the fresh-noise size in the generalized update: $\\sigma_t = \\eta\\sqrt{\\tilde\\beta_t}$" },
      { sym: "$F(x_T)$", desc: "the DDIM sampler as one deterministic function from starting noise (the latent code) to sample" },
      { sym: "$\\epsilon$", desc: "fresh standard Gaussian noise, from part 6's one-line sampler" },
      { sym: "$z$", desc: "the fresh Gaussian kick drawn at each stochastic sampling step" },
      { sym: "$\\beta_t$, $\\alpha_t = 1 - \\beta_t$", desc: "step $t$'s noise dose and survival fraction, from part 6" },
      { sym: "$\\mu$, $\\sigma^2$", desc: "a Gaussian's center and spread, from part 3 — the score section's running example" }
    ],
    recall: [
      "Define the score in plain English, and write the score of $\\mathcal{N}(\\mu, \\sigma^2)$ from memory.",
      "Why does the score never need the density's normalizing constant?",
      "State Tweedie's formula for the noised density $q(x_t)$. Which posterior quantity appears in it?",
      "State the bridge between the ideal noise guess and the score, including the sign and the scale factor.",
      "Write the denoised estimate $\\hat{x}_0$ in terms of $x_t$ and $\\epsilon_\\theta$. What is it equal to when the network is perfect?",
      "Write the DDIM update and say in words what its two terms do.",
      "Why can DDIM jump between distant noise levels while DDPM's update, run with its per-step coefficients, cannot?",
      "Name two practical abilities that DDIM's determinism enables and DDPM sampling does not."
    ],
    practice: [
      { q: "Score of a Gaussian. Compute the score of $\\mathcal{N}(-2,\\ 9)$ at $x = 1$ and at $x = -2$, and read each answer as a direction and a steepness.",
        steps: [
          { do: "Write the score formula for a Gaussian: $\\nabla_x \\log \\mathcal{N}(x;\\, \\mu, \\sigma^2) = \\frac{\\mu - x}{\\sigma^2}$, here with $\\mu = -2$ and $\\sigma^2 = 9$.", why: "Section 2 derived this in seven one-op steps; the normalizer contributed slope zero." },
          { do: "Evaluate at $x = 1$: $\\frac{-2 - 1}{9} = \\frac{-3}{9} = -\\frac{1}{3} \\approx -0.333$.", why: "Substitute the numbers and divide." },
          { do: "Read it: the score is negative, so uphill is to the left — from $x = 1$ back toward the center $-2$ — with steepness one third.", why: "A score's sign points toward the center; its size is the distance $3$ shrunk by the variance $9$." },
          { do: "Evaluate at $x = -2$: $\\frac{-2 - (-2)}{9} = \\frac{0}{9} = 0$.", why: "At the center the numerator is zero — the top of the hill is flat, so there is no uphill direction to report." }
        ],
        answer: "Score at $x = 1$: $-1/3$ (uphill points left, toward the center $-2$, with steepness $0.333$). Score at $x = -2$: $0$ (you are standing on the peak)." },
      { q: "Tweedie on fresh numbers. A two-point toy has clean values $x_0 = +1$ and $x_0 = -1$ with equal odds. At some noise level, $\\bar\\alpha_t = 0.5$, and you observe $x_t = 0.6$. Compute the posterior mean $\\mathbb{E}[x_0 \\mid x_t]$ (the tanh formula with clean values $\\pm c$ is $c\\tanh(c\\,a\\,x_t/s^2)$), the score via Tweedie, and the best noise guess — then verify the bridge $\\text{score} = -\\mathbb{E}[\\epsilon \\mid x_t]/s$.",
        steps: [
          { do: "Compute the shorthands: $a = \\sqrt{0.5} = 0.7071$ and $s^2 = 1 - 0.5 = 0.5$, so $s = 0.7071$.", why: "The signal scale is $\\sqrt{\\bar\\alpha_t}$ and the noise variance is $1-\\bar\\alpha_t$, from part 6's closed form." },
          { do: "Evaluate the tanh argument with $c = 1$: $\\frac{c\\,a\\,x_t}{s^2} = \\frac{1 \\times 0.7071 \\times 0.6}{0.5} = 0.8485$.", why: "Bayes' posterior odds of $+1$ versus $-1$ collapse to one exponential of exactly this quantity times 2; the tanh of it is the difference of the two posterior weights." },
          { do: "Posterior mean: $\\mathbb{E}[x_0 \\mid x_t] = 1 \\times \\tanh(0.8485) = 0.6903$.", why: "With clean values $\\pm 1$, the posterior mean is $w_+ - w_-$, which is the tanh." },
          { do: "Tweedie: score $= \\frac{a\\,\\mathbb{E}[x_0 \\mid x_t] - x_t}{s^2} = \\frac{0.7071 \\times 0.6903 - 0.6}{0.5} = \\frac{0.4881 - 0.6}{0.5} = \\frac{-0.1119}{0.5} = -0.2238$.", why: "Tweedie's formula: the gap between the shrunken posterior mean and where you are, divided by the noise variance." },
          { do: "Best noise guess: $\\mathbb{E}[\\epsilon \\mid x_t] = \\frac{x_t - a\\,\\mathbb{E}[x_0 \\mid x_t]}{s} = \\frac{0.6 - 0.4881}{0.7071} = \\frac{0.1119}{0.7071} = 0.1582$.", why: "Take the posterior average of the exact identity $\\epsilon = (x_t - a x_0)/s$; linearity of expectation lets the fixed $x_t$ pass through." },
          { do: "Bridge check: $-\\frac{0.1582}{0.7071} = -0.2238$ — the same number as Tweedie gave.", why: "The bridge says score $= -\\mathbb{E}[\\epsilon \\mid x_t]/s$; both routes must agree because one was derived from the other by exact algebra." }
        ],
        answer: "$\\mathbb{E}[x_0 \\mid x_t] = 0.6903$, score $= -0.2238$, best noise guess $= 0.1582$, and the bridge check reproduces $-0.2238$. Note the reading: the posterior leans toward $+1$, but $x_t = 0.6$ sits slightly to the right of the shrunken posterior mean $0.4881$, so uphill points slightly left." },
      { q: "The denoised estimate. In part 9's worked example the model saw $x_2 = 1.9616$ at $t = 2$ (running schedule, $\\bar\\alpha_2 = 0.72$) and guessed $\\epsilon_\\theta = 0.3$. Compute $\\hat{x}_0$.",
        steps: [
          { do: "Write the primitive: $\\hat{x}_0 = \\frac{x_2 - \\sqrt{1-\\bar\\alpha_2}\\,\\epsilon_\\theta}{\\sqrt{\\bar\\alpha_2}}$.", why: "Part 9's inversion of the one-line sampler, applied to the network's guess instead of the true noise." },
          { do: "Evaluate the two constants: $\\sqrt{1-0.72} = \\sqrt{0.28} = 0.5292$ and $\\sqrt{0.72} = 0.8485$.", why: "Fixed schedule numbers first — they appear in every later step." },
          { do: "Numerator: $1.9616 - 0.5292 \\times 0.3 = 1.9616 - 0.1587 = 1.8029$.", why: "Subtract the guessed noise scaled exactly the way noise was added." },
          { do: "Divide: $\\hat{x}_0 = 1.8029 / 0.8485 = 2.1247$.", why: "Undo the shrinking by dividing by the signal scale." }
        ],
        answer: "$\\hat{x}_0 = 2.1247$ — the same number part 9 called the implied clean point. The true $x_0$ was 2; a noise guess $0.2$ too low reads as a clean guess $0.125$ too high." },
      { q: "One DDIM update by hand. Continue problem 3: at level $t = 2$ you hold $x_2 = 1.9616$, $\\epsilon_\\theta = 0.3$, $\\hat{x}_0 = 2.1247$, and the running schedule gives $\\bar\\alpha_1 = 0.9$. Compute the DDIM jump to level 1.",
        steps: [
          { do: "Write the update with the target level's survival fraction: $x_1 = \\sqrt{\\bar\\alpha_1}\\,\\hat{x}_0 + \\sqrt{1-\\bar\\alpha_1}\\,\\epsilon_\\theta$.", why: "DDIM re-noises the clean guess out to the target level, reusing the guessed noise direction — no fresh draw." },
          { do: "Evaluate the constants: $\\sqrt{0.9} = 0.9487$ and $\\sqrt{0.1} = 0.3162$.", why: "The two endpoint quantities are all the update ever needs." },
          { do: "Signal part: $0.9487 \\times 2.1247 = 2.0157$.", why: "Scale the clean guess by the target level's signal scale." },
          { do: "Noise part: $0.3162 \\times 0.3 = 0.0949$; add: $x_1 = 2.0157 + 0.0949 = 2.1105$ (full precision $2.015653 + 0.094868 = 2.110521$).", why: "Scale the believed noise direction by the target level's noise scale, then add the two parts — the one-line sampler with hats on." }
        ],
        answer: "$x_1 = 2.1105$. Deterministic: rerunning this update gives 2.1105 every time — compare part 9's Algorithm 2 trace, where a drawn $z$ entered the same step." },
      { q: "Reasoning with marginals: why does a 20-step sub-schedule work for DDIM but wreck DDPM (when DDPM keeps its per-step coefficients)?",
        steps: [
          { do: "State what correctness means for a sampler: at every visited level $\\tau$, the population of points it produces should follow the level-$\\tau$ marginal — the distribution of genuinely noised data at that level.", why: "The network was trained on inputs drawn from those marginals (Algorithm 1 builds them with the one-line sampler); off-marginal inputs are jobs it never learned." },
          { do: "Check DDIM's jump against that standard: in the oracle case, $\\hat{x}_0 = x_0$ and the update outputs $\\sqrt{\\bar\\alpha_{\\tau'}}\\,x_0 + \\sqrt{1-\\bar\\alpha_{\\tau'}}\\,\\epsilon$ — a legitimate draw from $q(x_{\\tau'} \\mid x_0)$ for any earlier level $\\tau'$, adjacent or not.", why: "The update only consults $\\bar\\alpha$ at the two endpoints of the jump, and the oracle algebra (section 5) never used adjacency." },
          { do: "Check DDPM's update the same way: its three coefficients — $1/\\sqrt{\\alpha_t}$, $\\beta_t/\\sqrt{1-\\bar\\alpha_t}$, $\\sqrt{\\beta_t}$ — are single-step quantities that undo exactly one dose $\\beta_t$ of shrink-and-noise.", why: "Parts 7-9 derived that update as a Bayes flip between adjacent levels; one application removes one step's worth of noise, by construction." },
          { do: "Run the naive skip forward: applying the per-step update at only 20 of 200 levels undoes 20 doses and never touches the other 180, so the output remains mostly noise — a blob at about half the data's radius (the notebook measures $\\approx 2$; the no-steering back-of-envelope is 1.4) instead of a ring at radius 4.", why: "Noise that no update removes stays in the sample; the 20 kept steps stretch the blob by only $\\approx e^{0.1} \\approx 1.1$, and the fresh kicks plus the network's off-distribution steering account for the rest of the measured radius." },
          { do: "Close the loop: DDIM's samples stay on-marginal at every visited level, so the network always sees inputs of the kind it trained on; naive-skip DDPM's samples leave the marginals after the first jump, and every later network call runs on inputs from the wrong distribution.", why: "This is the standard from step 1 applied to both samplers — the marginal property is what the training distribution guarantees and what sampling must preserve." }
        ],
        answer: "DDIM's update depends only on the endpoint survival fractions and lands exactly on the target level's marginal for jumps of any size, so 20 well-chosen levels suffice. DDPM's per-step update removes exactly one dose per call; skipping leaves 180 of 200 doses in place, the samples exit the marginals immediately, and the network spends the rest of the walk answering questions it was never trained on." },
      { q: "The latent-space consequence. Your colleague generates a striking sample with DDIM ($S = 20$) and wants to (a) reproduce it exactly next week, (b) produce ten variations that morph smoothly from it to a second sample, and (c) claim the model has not memorized the training set by showing the sample changes smoothly as its origin changes. What single object must be saved, and why does each task work with DDIM but fail with DDPM sampling?",
        steps: [
          { do: "Identify the object: the starting noise $x_T$ (plus the fixed sampler settings — checkpoint, $S$, sub-schedule). With DDIM, $x_0 = F(x_T)$ is a deterministic function, so $x_T$ is a complete address for the sample.", why: "Section 7: with no fresh $z$, the sampler is a fixed composition of network calls and arithmetic — same input, same output, bit for bit." },
          { do: "(a) Reproduction: rerun $F$ on the saved $x_T$; the output is identical to every decimal.", why: "Determinism is exactly the property that reruns agree; the notebook asserts torch-exact equality of two runs." },
          { do: "(b) Variations: draw the second sample's code $x_T'$, then decode points along the path between $x_T$ and $x_T'$; because $F$ is built from smooth pieces, the outputs morph gradually.", why: "Nearby codes give nearby outputs when the map is smooth — a walk in code space becomes a morph in sample space." },
          { do: "(c) The smoothness demonstration is the same experiment read as evidence: outputs vary continuously with the code, behavior a lookup table of memorized samples would not show.", why: "A memorizing sampler would jump between stored examples; a smooth generative map fills the space between them." },
          { do: "Explain the DDPM failure for all three: Algorithm 2 draws a fresh $z$ at every step, so the output depends on $T$ unrecorded coin flips beyond $x_T$ — reruns from the same start diverge, and any 'path' in start-noise space is scrambled by new randomness at every step of every decode.", why: "The map from start to sample is not a function when extra randomness enters mid-flight; the notebook's contrast run shows two DDPM outputs from the same start disagreeing by whole clusters." }
        ],
        answer: "Save the latent code $x_T$ (with the sampler settings). DDIM makes generation a deterministic, smooth function of that code, so exact reproduction, smooth interpolation, and the smoothness argument all work; DDPM's per-step fresh noise breaks the function property, so none of the three are possible from the start noise alone." }
    ]
  });
})();
