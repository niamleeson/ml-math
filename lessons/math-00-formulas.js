/* AUTO-GENERATED by tools/gen-formulas-lessons.js from vector-math-formulas.md — do not edit by hand. */
(function () {
  var L = (window.LESSONS = window.LESSONS || []);
  var data = [{"id":"formula-00","title":"Overview & how to read","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"","nextId":"formula-01","html":"<p>Plain-text formulas (no hard-to-read symbols), each with a tiny worked example you can trace by hand. These are the vector operations behind embeddings, similarity search, clustering, and most of ML — plus the neighboring layers: matrix / linear algebra, vector calculus (the training math), distances and kernels, and probability-vector formulas.</p>\n<p>Throughout we reuse two example vectors so the numbers are easy to follow:</p>\n<pre><code class=\"language-text\">a = [3, 4]\nb = [4, 3]\n\nnorm(a) = sqrt(3*3 + 4*4) = sqrt(25) = 5\nnorm(b) = sqrt(4*4 + 3*3) = sqrt(25) = 5\ndot(a, b) = 3*4 + 4*3 = 12 + 12 = 24</code></pre>\n<p>Notation:</p>\n<ul><li><code>norm(v)</code>  = length of vector v (L2 norm unless said otherwise)</li><li><code>dot(a, b)</code> = dot product of a and b</li><li><code>sqrt(x)</code>  = square root, <code>^2</code> = squared, <code>*</code> = multiply</li><li><code>|x|</code>      = absolute value</li><li><code>v_hat</code>    = the unit-length version of v (length 1)</li><li><code>~</code>        = \"approximately equals\"</li></ul>\n<hr>"},{"id":"formula-01","title":"Part 1 — Building blocks","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-00","nextId":"formula-02","html":"<h2>1.1 Vector addition (component-wise)</h2>\n<pre><code class=\"language-text\">a + b = [a1+b1, a2+b2, ..., an+bn]</code></pre>\n<p>Example: <code>a + b = [3+4, 4+3] = [7, 7]</code>. Geometrically: place b's tail at a's head.</p>\n<h2>1.2 Vector subtraction</h2>\n<pre><code class=\"language-text\">a - b = [a1-b1, a2-b2, ..., an-bn]</code></pre>\n<p>Example: <code>a - b = [-1, 1]</code>. The arrow that points from b to a.</p>\n<h2>1.3 Scalar multiplication (stretch / shrink / flip)</h2>\n<pre><code class=\"language-text\">k * v = [k*v1, k*v2, ..., k*vn]</code></pre>\n<p>Example: <code>2 * a = [6, 8]</code> (twice as long, same direction). <code>-1 * a = [-3, -4]</code> (flipped).</p>\n<h2>1.4 Zero vector</h2>\n<pre><code class=\"language-text\">0 = [0, 0, ..., 0]</code></pre>\n<p>Has length 0 and no direction. You cannot normalize it (division by zero).</p>\n<h2>1.5 Linear combination</h2>\n<pre><code class=\"language-text\">c1*v1 + c2*v2 + ... + ck*vk</code></pre>\n<p>Example: <code>2*[1,0] + 3*[0,1] = [2, 3]</code>. Every vector is a linear combination of the standard basis vectors [1,0] and [0,1].</p>\n<h2>1.6 Weighted average (convex combination)</h2>\n<pre><code class=\"language-text\">w1*v1 + w2*v2 + ... + wk*vk       where the weights w add up to 1</code></pre>\n<p>Example: <code>0.25*[0,0] + 0.75*[4,4] = [3, 3]</code>. Stays \"between\" the inputs.</p>\n<hr>"},{"id":"formula-02","title":"Part 2 — Norm (length / size)","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-01","nextId":"formula-03","html":"<h2>2.1 L2 norm (Euclidean length) — the default</h2>\n<pre><code class=\"language-text\">norm(v) = sqrt( v1^2 + v2^2 + ... + vn^2 )</code></pre>\n<p>Example, v = [8, 6]: <code>sqrt(64 + 36) = sqrt(100) = 10</code>. The Pythagorean length of the arrow.</p>\n<h2>2.2 Norm is the dot product with itself</h2>\n<pre><code class=\"language-text\">norm(v) = sqrt( dot(v, v) )</code></pre>\n<p>Example: <code>dot(a, a) = 9 + 16 = 25</code>, <code>sqrt(25) = 5</code>.</p>\n<h2>2.3 Squared L2 norm (skip the square root)</h2>\n<pre><code class=\"language-text\">norm(v)^2 = dot(v, v) = v1^2 + v2^2 + ...</code></pre>\n<p>Example: <code>norm(a)^2 = 25</code>. Used in k-means and losses to avoid computing sqrt.</p>\n<h2>2.4 Scaling rule</h2>\n<pre><code class=\"language-text\">norm(k * v) = |k| * norm(v)</code></pre>\n<p>Example: <code>norm(10 * [3,4]) = norm([30,40]) = 50 = 10 * 5</code>. A longer vector = bigger norm.</p>\n<h2>2.5 L1 norm (taxicab / Manhattan length)</h2>\n<pre><code class=\"language-text\">norm_L1(v) = |v1| + |v2| + ... + |vn|</code></pre>\n<p>Example, v = [8, 6]: <code>8 + 6 = 14</code>. Distance walking along a grid.</p>\n<h2>2.6 L-infinity norm (Chebyshev / max)</h2>\n<pre><code class=\"language-text\">norm_Linf(v) = max( |v1|, |v2|, ..., |vn| )</code></pre>\n<p>Example, v = [8, 6]: <code>max(8, 6) = 8</code>. The largest single component.</p>\n<h2>2.7 Lp norm (the general family)</h2>\n<pre><code class=\"language-text\">norm_Lp(v) = ( |v1|^p + |v2|^p + ... )^(1/p)</code></pre>\n<p>p=1 gives L1, p=2 gives L2, p=infinity gives the max norm.</p>\n<hr>"},{"id":"formula-03","title":"Part 3 — Dot product","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-02","nextId":"formula-04","html":"<h2>3.1 Coordinate form (how you compute it)</h2>\n<pre><code class=\"language-text\">dot(a, b) = a1*b1 + a2*b2 + ... + an*bn</code></pre>\n<p>Example: <code>dot(a, b) = 3*4 + 4*3 = 24</code>. Multiply matching parts, add.</p>\n<h2>3.2 Geometric form (what it means)</h2>\n<pre><code class=\"language-text\">dot(a, b) = norm(a) * norm(b) * cos(angle between a and b)</code></pre>\n<p>Example: <code>5 * 5 * 0.96 = 24</code>. \"Length times length times how-aligned.\"</p>\n<h2>3.3 Dot with itself = length squared</h2>\n<pre><code class=\"language-text\">dot(v, v) = norm(v)^2</code></pre>\n<p>Example: <code>dot(a, a) = 25 = 5^2</code>.</p>\n<h2>3.4 Dot with a unit vector = projection length</h2>\n<pre><code class=\"language-text\">dot(a, b_hat) = how far a reaches along b's direction</code></pre>\n<p>Example: <code>b_hat = [0.8, 0.6]</code>, <code>dot(a, b_hat) = 3*0.8 + 4*0.6 = 4.8</code>.</p>\n<h2>3.5 Algebra rules (handy identities)</h2>\n<pre><code class=\"language-text\">dot(a, b)     = dot(b, a)                 order does not matter\ndot(a, b + c) = dot(a, b) + dot(a, c)     distributes over addition\ndot(k*a, b)   = k * dot(a, b)             scalars pull out</code></pre>\n<hr>"},{"id":"formula-04","title":"Part 4 — Similarity and angle","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-03","nextId":"formula-05","html":"<h2>4.1 Cosine similarity (pure angle, length removed)</h2>\n<pre><code class=\"language-text\">cosine(a, b) = dot(a, b) / ( norm(a) * norm(b) ) = cos(angle)</code></pre>\n<p>Example: <code>24 / (5*5) = 0.96</code>. Range: -1 (opposite) .. 0 (unrelated) .. +1 (same direction). The norms cancel (top and bottom), so only the angle survives.</p>\n<h2>4.2 Cosine distance (a \"smaller = closer\" version)</h2>\n<pre><code class=\"language-text\">cosine_distance(a, b) = 1 - cosine(a, b)</code></pre>\n<p>Example: <code>1 - 0.96 = 0.04</code>. 0 = identical direction, 2 = opposite.</p>\n<h2>4.3 Angle from cosine</h2>\n<pre><code class=\"language-text\">angle = arccos( cosine(a, b) )</code></pre>\n<p>Example: <code>arccos(0.96) ~ 16 degrees</code>.</p>\n<h2>4.4 Cauchy-Schwarz inequality (why cosine stays in -1..+1)</h2>\n<pre><code class=\"language-text\">| dot(a, b) |  &lt;=  norm(a) * norm(b)</code></pre>\n<p>Example: <code>|24| = 24 &lt;= 25</code>. Divide both sides by the norms and cosine is between -1 and 1.</p>\n<hr>"},{"id":"formula-05","title":"Part 5 — Normalization and standardization","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-04","nextId":"formula-06","html":"<h2>5.1 Normalize to unit length</h2>\n<pre><code class=\"language-text\">v_hat = v / norm(v)</code></pre>\n<p>Example, v = [8,6], norm 10: <code>[0.8, 0.6]</code>, and <code>norm([0.8,0.6]) = 1</code>. Keeps direction, sets length to 1. After this, dot product equals cosine.</p>\n<h2>5.2 Dot of two unit vectors = cosine</h2>\n<pre><code class=\"language-text\">dot(a_hat, b_hat) = cosine(a, b)</code></pre>\n<p>Because both lengths are 1: <code>1 * 1 * cos(angle) = cos(angle)</code>.</p>\n<h2>5.3 Mean-centering (subtract the average)</h2>\n<pre><code class=\"language-text\">v_centered = v - mean(v)      (subtract the average of v's own entries)</code></pre>\n<p>Example, v = [8, 6], mean = 7: <code>[1, -1]</code>. Moves the data so its center is at the origin.</p>\n<h2>5.4 Standardization (z-score: center, then scale to unit spread)</h2>\n<pre><code class=\"language-text\">z = (v - mean) / standard_deviation</code></pre>\n<p>Puts features on the same scale so no single feature dominates by units alone.</p>\n<hr>"},{"id":"formula-06","title":"Part 6 — Distance","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-05","nextId":"formula-07","html":"<h2>6.1 Euclidean (L2) distance</h2>\n<pre><code class=\"language-text\">distance(a, b) = norm(a - b) = sqrt( (a1-b1)^2 + (a2-b2)^2 + ... )</code></pre>\n<p>Example: <code>a - b = [-1, 1]</code>, <code>sqrt(1 + 1) = 1.41</code>. Straight-line distance.</p>\n<h2>6.2 Squared Euclidean distance (skip sqrt)</h2>\n<pre><code class=\"language-text\">distance(a, b)^2 = (a1-b1)^2 + (a2-b2)^2 + ...</code></pre>\n<p>Example: <code>1 + 1 = 2</code>. Same ranking as distance, cheaper to compute (k-means uses this).</p>\n<h2>6.3 Manhattan (L1) distance</h2>\n<pre><code class=\"language-text\">distance_L1(a, b) = |a1-b1| + |a2-b2| + ...</code></pre>\n<p>Example: <code>|-1| + |1| = 2</code>. Grid-walking distance.</p>\n<h2>6.4 Chebyshev (L-infinity) distance</h2>\n<pre><code class=\"language-text\">distance_Linf(a, b) = max( |a1-b1|, |a2-b2|, ... )</code></pre>\n<p>Example: <code>max(1, 1) = 1</code>. The biggest single-coordinate gap.</p>\n<h2>6.5 Minkowski distance (general Lp)</h2>\n<pre><code class=\"language-text\">distance_Lp(a, b) = ( |a1-b1|^p + |a2-b2|^p + ... )^(1/p)</code></pre>\n<p>p=1 gives Manhattan, p=2 gives Euclidean.</p>\n<h2>6.6 Metric properties (what makes a valid distance)</h2>\n<pre><code class=\"language-text\">distance(a, b) &gt;= 0                              never negative\ndistance(a, b) = 0  only if  a = b               zero only for the same point\ndistance(a, b) = distance(b, a)                  symmetric\ndistance(a, c) &lt;= distance(a, b) + distance(b, c) triangle inequality</code></pre>\n<hr>"},{"id":"formula-07","title":"Part 7 — The bridges (formulas that connect the others)","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-06","nextId":"formula-08","html":"<h2>7.1 Law of cosines (distance &lt;-&gt; dot product)</h2>\n<pre><code class=\"language-text\">distance(a, b)^2 = norm(a)^2 + norm(b)^2 - 2 * dot(a, b)</code></pre>\n<p>Example: <code>25 + 25 - 2*24 = 2</code> (matches distance^2 = 2). Most other formulas fall out of this.</p>\n<h2>7.2 On the unit sphere, distance and cosine agree</h2>\n<p>If a and b are normalized (length 1):</p>\n<pre><code class=\"language-text\">distance^2 = 2 - 2 * cosine</code></pre>\n<p>Smaller distance = bigger cosine. This is why vector databases normalize, then use fast distance search to do cosine search.</p>\n<h2>7.3 Polarization identity (dot product from lengths only)</h2>\n<pre><code class=\"language-text\">dot(a, b) = ( norm(a + b)^2 - norm(a)^2 - norm(b)^2 ) / 2</code></pre>\n<p>Lets you recover the dot product if you only know lengths.</p>\n<h2>7.4 Parallelogram law</h2>\n<pre><code class=\"language-text\">norm(a + b)^2 + norm(a - b)^2 = 2*norm(a)^2 + 2*norm(b)^2</code></pre>\n<p>Example: <code>norm([7,7])^2 + norm([-1,1])^2 = 98 + 2 = 100 = 2*25 + 2*25</code>.</p>\n<hr>"},{"id":"formula-08","title":"Part 8 — Projection and orthogonality","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-07","nextId":"formula-09","html":"<h2>8.1 Projection length (scalar: a's shadow on b)</h2>\n<pre><code class=\"language-text\">projection_length = dot(a, b) / norm(b)</code></pre>\n<p>Example: <code>24 / 5 = 4.8</code>.</p>\n<h2>8.2 Projection vector (the shadow itself)</h2>\n<pre><code class=\"language-text\">projection_vector = ( dot(a, b) / dot(b, b) ) * b</code></pre>\n<p>Example: <code>(24 / 25) * [4,3] = [3.84, 2.88]</code>. Used to remove a direction from an embedding.</p>\n<h2>8.3 Orthogonal decomposition (split a into \"along b\" + \"perpendicular to b\")</h2>\n<pre><code class=\"language-text\">a = projection_vector + perpendicular_part\nperpendicular_part = a - projection_vector</code></pre>\n<h2>8.4 Orthogonality (perpendicular = unrelated)</h2>\n<pre><code class=\"language-text\">a and b are orthogonal  &lt;=&gt;  dot(a, b) = 0</code></pre>\n<p>A dot of 0 means a 90-degree angle: independent directions, cosine 0.</p>\n<hr>"},{"id":"formula-09","title":"Part 9 — Other vector products","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-08","nextId":"formula-10","html":"<h2>9.1 Hadamard product (element-wise multiply)</h2>\n<pre><code class=\"language-text\">a (*) b = [a1*b1, a2*b2, ..., an*bn]</code></pre>\n<p>Example: <code>[3,4] (*) [4,3] = [12, 12]</code>. Common in neural-net gating/masks.</p>\n<h2>9.2 Outer product (makes a matrix)</h2>\n<pre><code class=\"language-text\">outer(a, b)[i][j] = ai * bj</code></pre>\n<p>Example, a=[3,4], b=[4,3]:</p>\n<pre><code class=\"language-text\">[ 3*4  3*3 ]   [ 12  9  ]\n[ 4*4  4*3 ] = [ 16  12 ]</code></pre>\n<h2>9.3 Cross product (3D only) — a vector perpendicular to both</h2>\n<pre><code class=\"language-text\">norm(a x b) = norm(a) * norm(b) * sin(angle) = area of the parallelogram</code></pre>\n<p>Points perpendicular to the plane of a and b (right-hand rule).</p>\n<hr>"},{"id":"formula-10","title":"Part 10 — Connection to statistics","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-09","nextId":"formula-11","html":"<h2>10.1 Mean / centroid of a set of vectors</h2>\n<pre><code class=\"language-text\">mean(v1, ..., vk) = (v1 + v2 + ... + vk) / k</code></pre>\n<p>Example, mean of [2,0], [0,2], [4,4]: <code>[6,6]/3 = [2,2]</code>. Cluster centers and averaged word vectors are just this.</p>\n<h2>10.2 Pearson correlation = cosine of mean-centered vectors</h2>\n<pre><code class=\"language-text\">correlation(a, b) = cosine( a - mean(a), b - mean(b) )</code></pre>\n<p>Correlation is literally the cosine similarity after centering. Very cool bridge from statistics to geometry.</p>\n<h2>10.3 Variance = squared norm of the centered vector / n</h2>\n<pre><code class=\"language-text\">variance(v) = norm(v - mean(v))^2 / n</code></pre>\n<h2>10.4 Covariance = dot product of centered vectors / n</h2>\n<pre><code class=\"language-text\">covariance(a, b) = dot( a - mean(a), b - mean(b) ) / n</code></pre>\n<hr>"},{"id":"formula-11","title":"Part 11 — Geometry operations","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-10","nextId":"formula-12","html":"<h2>11.1 Linear interpolation (blend two vectors)</h2>\n<pre><code class=\"language-text\">lerp(a, b, t) = (1 - t) * a + t * b        t goes 0 -&gt; 1</code></pre>\n<p>Example, t = 0.5: <code>0.5*[3,4] + 0.5*[4,3] = [3.5, 3.5]</code>. The midpoint at t=0.5.</p>\n<h2>11.2 Spherical interpolation (slerp, for unit vectors)</h2>\n<p>Blends two directions along the sphere (constant-angle steps) instead of a straight line. Used to interpolate between normalized embeddings smoothly.</p>\n<h2>11.3 2D rotation by angle t</h2>\n<pre><code class=\"language-text\">x_new = x*cos(t) - y*sin(t)\ny_new = x*sin(t) + y*cos(t)</code></pre>\n<p>Turns a vector by angle t without changing its length.</p>\n<h2>11.4 Reflection across a unit direction u</h2>\n<pre><code class=\"language-text\">reflect(v) = 2 * dot(v, u) * u - v</code></pre>\n<p>Mirror image of v about the line through u.</p>\n<hr>"},{"id":"formula-12","title":"Part 12 — Inequalities (bounds worth knowing)","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-11","nextId":"formula-13","html":"<h2>12.1 Triangle inequality (a detour is never shorter)</h2>\n<pre><code class=\"language-text\">norm(a + b)  &lt;=  norm(a) + norm(b)</code></pre>\n<p>Example: <code>norm([7,7]) = 9.9 &lt;= 5 + 5 = 10</code>.</p>\n<h2>12.2 Cauchy-Schwarz (dot is bounded by the norms)</h2>\n<pre><code class=\"language-text\">| dot(a, b) |  &lt;=  norm(a) * norm(b)</code></pre>\n<p>Example: <code>24 &lt;= 25</code>.</p>\n<hr>"},{"id":"formula-13","title":"Part 13 — Common ML uses (putting it together)","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-12","nextId":"formula-14","html":"<h2>13.1 Nearest neighbor search</h2>\n<pre><code class=\"language-text\">best match = the item with the largest cosine(query, item)\n           = the item with the smallest distance(query, item)   (on unit vectors these agree)</code></pre>\n<h2>13.2 Analogies (meaning as direction)</h2>\n<pre><code class=\"language-text\">king - man + woman  ~  queen\nParis - France + Italy  ~  Rome</code></pre>\n<p>Add and subtract concepts because embeddings store meaning as direction.</p>\n<h2>13.3 Softmax (turn a score vector into probabilities)</h2>\n<pre><code class=\"language-text\">softmax(v)[i] = exp(vi) / ( exp(v1) + exp(v2) + ... + exp(vn) )</code></pre>\n<p>Example, v = [2, 0]: <code>exp(2) / (exp(2)+exp(0)) = 7.39 / 8.39 ~ 0.88</code>, other ~ 0.12. Outputs are positive and add up to 1.</p>\n<h2>13.4 Sentence embedding by averaging word vectors</h2>\n<pre><code class=\"language-text\">sentence_vector = mean( word_vector(w) for each word w )</code></pre>\n<p>The simplest text encoder — average, then normalize, then compare by cosine.</p>\n<hr>"},{"id":"formula-14","title":"Part 14 — Matrix and linear algebra","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-13","nextId":"formula-15","html":"<p>Shared examples for this part:</p>\n<pre><code class=\"language-text\">M = [ 1  2 ]        N = [ 0  1 ]        v = [1, 1]\n    [ 3  4 ]            [ 1  0 ]</code></pre>\n<h2>14.1 Matrix times vector (transform a vector)</h2>\n<pre><code class=\"language-text\">(M v)[i] = dot( row i of M , v )</code></pre>\n<p>Example: <code>M v = [1*1 + 2*1, 3*1 + 4*1] = [3, 7]</code>. A matrix reshapes/rotates/scales a vector.</p>\n<h2>14.2 Matrix times matrix</h2>\n<pre><code class=\"language-text\">(M N)[i][j] = dot( row i of M , column j of N )</code></pre>\n<p>Example:</p>\n<pre><code class=\"language-text\">M N = [ 1*0+2*1  1*1+2*0 ] = [ 2  1 ]\n      [ 3*0+4*1  3*1+4*0 ]   [ 4  3 ]</code></pre>\n<h2>14.3 Transpose (flip rows and columns)</h2>\n<pre><code class=\"language-text\">transpose(M)[i][j] = M[j][i]</code></pre>\n<p>Example: <code>transpose(M) = [[1,3],[2,4]]</code>.</p>\n<h2>14.4 Identity matrix (does nothing)</h2>\n<pre><code class=\"language-text\">I = [ 1  0 ]      I v = v\n    [ 0  1 ]</code></pre>\n<h2>14.5 Determinant of a 2x2 (area scaling factor)</h2>\n<pre><code class=\"language-text\">det([[a,b],[c,d]]) = a*d - b*c</code></pre>\n<p>Example: <code>det(M) = 1*4 - 2*3 = -2</code>. Its size = how much the matrix scales area/volume; sign = whether it flips orientation. det = 0 means the matrix squashes space flat (not invertible).</p>\n<h2>14.6 Inverse of a 2x2 (undo the transform)</h2>\n<pre><code class=\"language-text\">inverse([[a,b],[c,d]]) = (1/det) * [[d, -b], [-c, a]]</code></pre>\n<p>Example: <code>inverse(M) = (1/-2)*[[4,-2],[-3,1]] = [[-2, 1], [1.5, -0.5]]</code>. Only exists when det is not 0. Undoes the matrix: <code>inverse(M) * M = I</code>.</p>\n<h2>14.7 Dot product written as matrix multiply</h2>\n<pre><code class=\"language-text\">dot(a, b) = transpose(a) * b        (a row vector times a column vector)</code></pre>\n<p>This is why you often see <code>a^T b</code> for the dot product.</p>\n<h2>14.8 Trace (sum of the diagonal)</h2>\n<pre><code class=\"language-text\">trace(M) = M[1][1] + M[2][2] + ...</code></pre>\n<p>Example: <code>trace(M) = 1 + 4 = 5</code>. Also equals the sum of the eigenvalues.</p>\n<h2>14.9 Rank (number of independent directions)</h2>\n<p>Rank = how many rows (or columns) are linearly independent. Full rank = the matrix does not collapse any dimension. Low rank = information is squeezed into fewer directions (the idea behind matrix factorization and compression).</p>\n<h2>14.10 Eigenvectors and eigenvalues (directions a matrix only stretches)</h2>\n<pre><code class=\"language-text\">M v = lambda * v</code></pre>\n<p>An eigenvector v keeps its direction under M; it is only scaled by the number lambda. Example, for D = [[2,0],[0,3]]: <code>D [1,0] = 2 [1,0]</code> (eigenvalue 2), <code>D [0,1] = 3 [0,1]</code> (eigenvalue 3). These are the \"natural axes\" of the transform.</p>\n<h2>14.11 Singular Value Decomposition (SVD)</h2>\n<pre><code class=\"language-text\">M = U * S * transpose(V)</code></pre>\n<p>Factors any matrix into a rotation (U), a stretch by the singular values on the diagonal of S, and another rotation (V). Keeping only the largest singular values gives the best low-rank approximation — the engine behind PCA, compression, and recommender factorization.</p>\n<h2>14.12 PCA (principal component analysis)</h2>\n<pre><code class=\"language-text\">1. center the data:   X_centered = X - mean\n2. covariance:        C = transpose(X_centered) * X_centered / n\n3. eigenvectors of C  = the principal directions (most variance first)</code></pre>\n<p>Projecting onto the top few eigenvectors reduces dimensions while keeping the most spread.</p>\n<h2>14.13 Orthogonal matrix (a pure rotation/reflection)</h2>\n<pre><code class=\"language-text\">transpose(Q) * Q = I        (its columns are unit-length and mutually perpendicular)</code></pre>\n<p>Preserves lengths and angles: <code>norm(Q v) = norm(v)</code>.</p>\n<h2>14.14 Gram-Schmidt (build perpendicular axes)</h2>\n<p>Take vectors one at a time; from each, subtract its projection onto the ones already kept (formula 8.2), then normalize. Produces an orthonormal basis.</p>\n<h2>14.15 Frobenius norm (the \"length\" of a whole matrix)</h2>\n<pre><code class=\"language-text\">frobenius(M) = sqrt( sum of every entry squared )</code></pre>\n<p>Example: <code>sqrt(1+4+9+16) = sqrt(30) = 5.48</code>. Just the L2 norm treating the matrix as one long vector.</p>\n<h2>14.16 Gram matrix (all pairwise dot products)</h2>\n<pre><code class=\"language-text\">G[i][j] = dot( vi , vj )</code></pre>\n<p>Collects every pair's similarity into one matrix; the basis of kernel methods.</p>\n<hr>"},{"id":"formula-15","title":"Part 15 — Vector calculus (the training math)","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-14","nextId":"formula-16","html":"<h2>15.1 Gradient (vector of slopes)</h2>\n<pre><code class=\"language-text\">gradient(f) = [ df/dx1 , df/dx2 , ... ]</code></pre>\n<p>The direction of steepest increase of f. Training moves the opposite way (downhill).</p>\n<h2>15.2 Gradient of a dot product (the workhorse of ML)</h2>\n<pre><code class=\"language-text\">f(w) = dot(w, x)      -&gt;      gradient with respect to w = x</code></pre>\n<p>Example, x = [3,4]: gradient = <code>[3, 4]</code>. This is why linear-model gradients are just the inputs.</p>\n<h2>15.3 Gradient of the squared norm</h2>\n<pre><code class=\"language-text\">f(v) = norm(v)^2 = dot(v, v)      -&gt;      gradient = 2 * v</code></pre>\n<p>Example, v = [3,4]: <code>[6, 8]</code>. The pull that L2 regularization applies toward zero.</p>\n<h2>15.4 Gradient of the norm itself</h2>\n<pre><code class=\"language-text\">f(v) = norm(v)      -&gt;      gradient = v / norm(v) = v_hat</code></pre>\n<p>Example, v = [3,4]: <code>[0.6, 0.8]</code>. The gradient of length is the unit direction.</p>\n<h2>15.5 Directional derivative (slope along a direction u)</h2>\n<pre><code class=\"language-text\">slope along u = dot( gradient(f) , u )</code></pre>\n<p>How fast f changes if you step in direction u. Largest when u points along the gradient.</p>\n<h2>15.6 Jacobian (gradient for vector-valued functions)</h2>\n<pre><code class=\"language-text\">Jacobian[i][j] = d(output_i) / d(input_j)</code></pre>\n<p>A matrix of all partial derivatives; the multi-output generalization of the gradient.</p>\n<h2>15.7 Chain rule (how backpropagation composes gradients)</h2>\n<pre><code class=\"language-text\">d/dx f(g(x)) = f'(g(x)) * g'(x)         (vector form: multiply the Jacobians)</code></pre>\n<h2>15.8 Gradient descent update (one training step)</h2>\n<pre><code class=\"language-text\">w_new = w - learning_rate * gradient</code></pre>\n<p>Step a little downhill. This is the loop that trained the embeddings in the M11 notebook.</p>\n<hr>"},{"id":"formula-16","title":"Part 16 — More distances and kernels","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-15","nextId":"formula-17","html":"<h2>16.1 Mahalanobis distance (distance that accounts for correlations)</h2>\n<pre><code class=\"language-text\">distance(a, b) = sqrt( transpose(a - b) * inverse(Covariance) * (a - b) )</code></pre>\n<p>Rescales each direction by how much the data varies there, so \"far\" means \"surprising,\" not just \"far in raw units.\"</p>\n<h2>16.2 Jaccard similarity (for sets / binary features)</h2>\n<pre><code class=\"language-text\">Jaccard(A, B) = size(A intersect B) / size(A union B)</code></pre>\n<p>Example, A={1,2,3}, B={2,3,4}: <code>2 / 4 = 0.5</code>. Jaccard distance = 1 - Jaccard.</p>\n<h2>16.3 Hamming distance (positions that differ)</h2>\n<pre><code class=\"language-text\">Hamming(a, b) = count of positions where a and b disagree</code></pre>\n<p>Example, [1,0,1,1] vs [1,1,0,1]: differ at positions 2 and 3 -&gt; <code>2</code>. Used for binary codes and strings.</p>\n<h2>16.4 Edit (Levenshtein) distance</h2>\n<p>The minimum number of insert/delete/substitute edits to turn one string into another. Example: \"cat\" -&gt; \"cut\" is <code>1</code> (substitute a-&gt;u).</p>\n<h2>16.5 Linear kernel (just the dot product)</h2>\n<pre><code class=\"language-text\">K(a, b) = dot(a, b)</code></pre>\n<h2>16.6 Polynomial kernel</h2>\n<pre><code class=\"language-text\">K(a, b) = ( dot(a, b) + c )^d</code></pre>\n<p>Lets a linear model act on polynomial feature combinations without building them.</p>\n<h2>16.7 RBF / Gaussian kernel (similarity that falls off with distance)</h2>\n<pre><code class=\"language-text\">K(a, b) = exp( - norm(a - b)^2 / (2 * sigma^2) )</code></pre>\n<p>Example, squared distance 2, sigma = 1: <code>exp(-1) = 0.37</code>. Equals 1 when identical, decays toward 0 as points get far apart.</p>\n<hr>"},{"id":"formula-17","title":"Part 17 — Probability-vector formulas","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-16","nextId":"formula-97","html":"<p>A probability vector has non-negative entries that add up to 1, e.g. p = [0.5, 0.5].</p>\n<h2>17.1 Softmax (turn any scores into a probability vector)</h2>\n<pre><code class=\"language-text\">softmax(v)[i] = exp(vi) / ( exp(v1) + exp(v2) + ... )</code></pre>\n<p>Example, v = [2, 0]: <code>exp(2)/(exp(2)+exp(0)) = 7.39/8.39 = 0.88</code>, other = 0.12.</p>\n<h2>17.2 Entropy (how spread out / uncertain a distribution is)</h2>\n<pre><code class=\"language-text\">H(p) = - ( p1*log(p1) + p2*log(p2) + ... )</code></pre>\n<p>Example, p = [0.5, 0.5]: <code>-2*(0.5*log(0.5)) = log(2) = 0.693</code>. Maximum uncertainty for two outcomes. A sure thing p = [1, 0] has entropy 0.</p>\n<h2>17.3 Cross-entropy (the usual classification loss)</h2>\n<pre><code class=\"language-text\">H(p, q) = - ( p1*log(q1) + p2*log(q2) + ... )</code></pre>\n<p>p = true labels, q = predicted probabilities. Smaller when q matches p. This is what \"log loss\" computes.</p>\n<h2>17.4 KL divergence (how far apart two distributions are)</h2>\n<pre><code class=\"language-text\">KL(p || q) = p1*log(p1/q1) + p2*log(p2/q2) + ...</code></pre>\n<p>Example, p = q = [0.5, 0.5]: <code>0</code> (identical). Always &gt;= 0; not symmetric, so it is not a true distance. Note: <code>KL(p||q) = cross_entropy(p,q) - entropy(p)</code>.</p>\n<h2>17.5 Dot product as an expected value</h2>\n<pre><code class=\"language-text\">dot(p, values) = p1*value1 + p2*value2 + ... = the mean outcome under p</code></pre>\n<p>Example, p = [0.5, 0.5], values = [10, 20]: <code>0.5*10 + 0.5*20 = 15</code>. The dot product of a probability vector with a value vector is just the expected value.</p>\n<hr>"},{"id":"formula-97","title":"The through-line","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-17","nextId":"formula-98","html":"<h3>The through-line</h3><p>Almost everything here comes from ONE formula:</p>\n<pre><code class=\"language-text\">dot(a, b) = norm(a) * norm(b) * cos(angle)</code></pre>\n<p>plus its self-version <code>norm(v) = sqrt(dot(v, v))</code>. From those two you can derive cosine, distance, the distance-cosine bridge, projection, orthogonality, angles, correlation, and more. It is really one idea — the dot product — wearing many hats.</p>"},{"id":"formula-98","title":"Practice drills (1,395 questions)","module":"Formulas","superGroup":"Math","template":"formulas","meta":"Math · Vector, matrix &amp; ML formula reference","prevId":"formula-97","nextId":"","html":"<p>Every formula on these pages has <strong>5 basic, 5 easy, and 5 advanced</strong> hand-solved questions — <strong>1,395 in total</strong> — each worked one step at a time.</p><ul><li><a href=\"https://colab.research.google.com/github/niamleeson/ml-math/blob/main/afp/notebooks/vector-math-drills.ipynb\">Open the self-checking notebook in Colab</a> (recomputes and asserts every answer).</li><li><a href=\"https://github.com/niamleeson/ml-math/blob/main/afp/notebooks/vector-math-drills.ipynb\">View the notebook on GitHub</a>.</li><li><a href=\"https://github.com/niamleeson/ml-math/blob/main/vector-math-drills.md\">Read all 1,395 questions and solutions (Markdown)</a>.</li></ul><p>The drills follow the same order as these formula pages: Part 1 building blocks through Part 17 probability vectors.</p>"}];
  data.forEach(function (o) { L.push(o); });
})();
