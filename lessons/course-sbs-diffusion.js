/* Step-by-Step Diffusion: An Elementary Tutorial — Nakkiran, Bradley, Zhou & Advani
   arXiv:2406.08929v2 · https://arxiv.org/abs/2406.08929
   Reproduced in full under CC BY-NC-SA 4.0 (http://creativecommons.org/licenses/by-nc-sa/4.0/).
   Text, equations and figures are the authors’ work; only the format was changed
   (LaTeX -> HTML) so it renders in this app. Figures live in figs/sbs-diffusion/.
   Generated from the arXiv LaTeX source, then patched in place to clean up
   conversion artifacts. Re-running the generator would undo those fixes. */
(function () {
  window.LESSONS.push({
    "id": "sbs-diffusion",
    "title": "Step-by-Step Diffusion: An Elementary Tutorial",
    "module": "Rigorous Courses · Reproduced Papers",
    "superGroup": "Rigorous Courses",
    "template": "course",
    "prereqs": [],
    "sections": [
      {
        "h": "Abstract",
        "body": "<h4 class=\"book-h\">Preface</h4><p>There are many existing resources for learning diffusion models. Why did we write another? Our goal was to teach diffusion as simply as possible, with minimal mathematical and machine learning prerequisites, but in enough detail to reason about its correctness. Unlike most tutorials on this subject, we take neither a Variational Auto Encoder (VAE) nor an Stochastic Differential Equations (SDE) approach. In fact, for the core ideas we will not need any SDEs, Evidence-Based-Lower-Bounds (ELBOs), Langevin dynamics, or even the notion of a score. The reader need only be familiar with basic probability, calculus, linear algebra, and multivariate Gaussians. The intended audience for this tutorial is technical readers at the level of at least advanced undergraduate or graduate students, who are learning diffusion for the first time and want a mathematical understanding of the subject.</p><p>This tutorial has five parts, each relatively self-contained, but covering closely related topics. Section 1 presents the fundamentals of diffusion: the problem we are trying to solve and an overview of the basic approach. Sections 2 and 3 show how to construct a stochastic and deterministic diffusion sampler, respectively, and give intuitive derivations for why these samplers correctly reverse the forward diffusion process. Section 4 covers the closely-related topic of Flow Matching, which can be thought of as a generalization of diffusion that offers additional flexibility (including what are called rectified flows or linear flows). Finally, in Section 5 we return to diffusion and connect this tutorial to the broader literature while highlighting some of the design choices that matter most in practice, including samplers, noise schedules, and parametrizations.</p>"
      },
      {
        "h": "Fundamentals of Diffusion",
        "body": "<p><b>The goal</b> of generative modeling is: given i.i.d. samples from some unknown distribution $p^*(x)$, construct a sampler for (approximately) the same distribution. For example, given a training set of dog images from some underlying distribution $p_{\\textrm{dog}}$, we want a method of producing new images of dogs from this distribution.</p><p>One way to solve this problem, at a high level, is to learn a transformation from some easy-to-sample distribution (such as Gaussian noise) to our target distribution $p^*$. Diffusion models offer a general framework for learning such transformations. The clever trick of diffusion is to reduce the problem of sampling from distribution $p^*(x)$ into to a sequence of <i>easier</i> sampling problems.</p><p>This idea is best explained via the following Gaussian diffusion example. We'll sketch the main ideas now, and in later sections we will use this setup to derive what are commonly known as the <i>DDPM</i> and <i>DDIM</i> samplers <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(These stand for Denoising Diffusion Probabilistic Models (DDPM) and Denoising Diffusion Implicit Models (DDIM), following [ho2020denoising] and [song2021denoising].)</span>, and reason about their correctness.</p>"
      },
      {
        "h": "&mdash; Gaussian Diffusion",
        "body": "<p>For Gaussian diffusion, let $x_0$ be a random variable in $\\R^d$ distributed according to the target distribution $p^*$ (e.g., images of dogs). Then construct a sequence of random variables $x_1, x_2, \\dots, x_T$, by successively adding independent Gaussian noise with some small scale $\\sigma$:</p><div class=\"formula-box\">$$x_{t+1} := x_{t} + \\eta_{t} ,\\quad \\eta_{t} \\sim  \\cN(0, \\sigma^2).$$</div><p>This is called the <i>forward process</i> <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(One benefit of using this particular forward process is computational: we can directly sample $x_t$ given $x_0$ in constant time.)</span>, which transforms the data distribution into a noise distribution. Equation&nbsp;(1) defines a joint distribution over all $(x_0, x_1, \\dots, x_T)$, and we let $\\{p_t\\}_{t \\in [T]}$ denote the marginal distributions of each $x_t$. Notice that at large step count $T$, the distribution $p_T$ is nearly Gaussian <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Formally, $p_T$ is close in KL divergence to $\\cN(0, T\\sigma^2)$, assuming $p_0$ has bounded moments.)</span>, so we can approximately sample from $p_T$ by just sampling a Gaussian.</p><figure style=\"margin:18px 0\"><img src=\"figs/sbs-diffusion/intro_diffusion.png\" alt=\"Probability distributions defined by diffusion forward process on one-dimensional target distribution $p_0$.\" loading=\"lazy\" style=\"display:block;margin:12px auto;max-width:100%;height:auto\"><figcaption class=\"vizcap\">Probability distributions defined by diffusion forward process on one-dimensional target distribution $p_0$.</figcaption></figure><p>Now, suppose we can solve the following subproblem:</p><p><i> &ldquo;Given a sample marginally distributed as $p_{t}$, produce a sample marginally distributed as $p_{t-1}$&rdquo;</i>.</p><p>We will call a method that does this a <i>reverse sampler</i> <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Reverse samplers will be formally defined in Section&nbsp;1.2 below.)</span>, since it tells us how to sample from $p_{t-1}$ assuming we can already sample from $p_{t}$. If we had a reverse sampler, we could sample from our target $p_0$ by simply starting with a Gaussian sample from $p_T$, and iteratively applying the reverse sampling procedure to get samples from $p_{T-1}, p_{T-2}, \\dots$ and finally $p_0=p^*$.</p><p>The key insight of diffusion is, <b>learning to reverse each intermediate step can be easier</b> than learning to sample from the target distribution in one step <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Intuitively this is because the distributions $(p_{t-1}, p_{t})$ are already quite close, so the reverse sampler does not need to do much.)</span>. There are many ways to construct reverse samplers, but for concreteness let us first see the standard diffusion sampler which we will call the <i>DDPM sampler</i> <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(This is the sampling strategy originally proposed in [OGdiffusion].)</span>.</p><p>The <i>Ideal DDPM sampler</i> uses the obvious strategy: At time $t$, given input $z$ (which is promised to be a sample from $p_t$), we output a sample from the conditional distribution</p><p>$$\n   \\label{eqn:ideal_ddpm_sampler}\n   p(x_{t-1} \\mid x_{t} = z).\n   \n   $$ This is clearly a correct reverse sampler. The problem is, it requires learning a generative model for the conditional distribution ${p(x_{t-1} \\mid x_{t})}$ for every $x_{t}$, which could be complicated. But if the per-step noise $\\sigma$ is sufficiently small, then it turns out this conditional distribution becomes simple:</p><figure style=\"margin:18px 0\"><img src=\"figs/sbs-diffusion/diffusion_sigmas.png\" alt=\"Illustration of Fact 1.\n    The prior distribution $p(x_{t-1})$, leftmost,\n    defines a joint distribution $(x_{t-1}, x_t)$\n    where \u0000M2\" loading=\"lazy\" style=\"display:block;margin:12px auto;max-width:100%;height:auto\"><figcaption class=\"vizcap\">Illustration of Fact 1.\n    The prior distribution $p(x_{t-1})$, leftmost,\n    defines a joint distribution $(x_{t-1}, x_t)$\n    where $p(x_t \\mid x_{t-1}) = \\cN(0, \\sigma^2)$.\n    We plot the reverse conditional distributions\n    $p(x_{t-1} \\mid x_t)$\n    for a fixed conditioning $x_t$,\n    and varying noise levels $\\sigma$.\n    Notice these distributions \n    become close to Gaussian for small $\\sigma$.\n    </figcaption></figure><div class=\"card\"><p><b>Fact.</b></p><p>[Diffusion Reverse Process]</p><p>For small $\\sigma$, and the Gaussian diffusion process defined in&nbsp;(1), the conditional distribution $p(x_{t-1} \\mid x_{t})$ is itself close to Gaussian. That is, for all times $t$ and conditionings $z \\in \\R^d$, there exists some mean parameter $\\mu \\in \\R^d$ such that</p><div class=\"formula-box\">$$p(x_{t-1} \\mid x_{t} = z)\n   \\approx \\cN(x_{t-1} ; ~~\\mu ~,~ \\sigma^2).$$</div></div><p>This is not an obvious fact; we will derive it in Section&nbsp;2.1. This fact enables a drastic simplification: instead of having to learn an arbitrary distribution $p(x_{t-1} \\mid x_{t})$ from scratch, we now know everything about this distribution except its mean, which we denote <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(We denote the mean as a function $\\mu_{t-1}: \\R^d \\to \\R^d$ because the mean of $p( x_{t-1} \\mid x_t )$ depends on the time $t$ as well as the conditioning $x_{t}$, as described in Fact&nbsp;2.)</span> $\\mu_{t-1}(x_{t})$. The fact that we can approximate the posterior distribution as Gaussian when $\\sigma$ is sufficiently small is illustrated in Fig 2. This is an important point, so to re-iterate: for a given time $t$ and conditioning value $x_{t}$, learning the mean of $p( x_{t-1} \\mid x_{t})$ is sufficient to learn the full conditional distribution $p( x_{t-1} \\mid x_{t})$.</p><p>Learning the mean of $p(x_{t-1} \\mid x_t)$ is a much simpler problem than learning the full conditional distribution, because we can solve it by regression. To elaborate, we have a joint distribution $(x_{t-1}, x_{t})$ from which we can easily sample, and we would like to estimate $\\E[ x_{t-1} \\mid x_{t}]$. This can be done by optimizing a standard regression loss <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Recall the generic fact that for any distribution over $(x, y)$, we have: $\\argmin_{f} \\E ||f(x) - y||^2 = \\E[y \\mid x]$)</span>:</p><div class=\"formula-box\">$$\\begin{aligned}\n{\\mu}_{t-1}(z) \n&:= \\E[ x_{t-1} \\mid x_{t} = z]\\\\\n\\implies \\mu_{t-1} &= \\argmin_{f: \\R^d \\to \\R^d}\n~~\\E_{x_t, x_{t-1}} || f(x_{t}) - x_{t-1} ||_2^2 \\\\\n&= \\argmin_{f: \\R^d \\to \\R^d}\n\\E_{x_{t-1},\\eta}\n|| f(x_{t-1} + \\eta_t) - x_{t-1}) ||_2^2,\n\n\\end{aligned}$$</div><p>where the expectation is taken over samples $x_0$ from our target distribution $p^*$. <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Notice that we simulate samples of $(x_{t-1}, x_{t})$ by adding noise to the samples of $x_0$, as defined in Equation&nbsp;1.)</span> This particular regression problem is well-studied in certain settings. For example, when the target $p^*$ is a distribution on images, then the corresponding regression problem (Equation&nbsp;3) is exactly an <i>image denoising objective</i>, which can be approached with familiar methods (e.g. convolutional neural networks).</p><p><b>Stepping back</b>, we have seen something remarkable: we have reduced the problem of learning to sample from an arbitrary distribution to the standard problem of regression.</p>"
      },
      {
        "h": "&mdash; Diffusions in the Abstract",
        "body": "<p>Let us now abstract away the Gaussian setting, to define diffusion-like models in a way that will capture their many instantiations (including deterministic samplers, discrete domains, and flow-matching).</p><p>Abstractly, here is how to construct a diffusion-like generative model: We start with our target distribution $p^*$, and we pick some base distribution $q(x)$ which is easy to sample from, e.g. a standard Gaussian or i.i.d bits. We then try to construct a sequence of distributions which interpolate between our target $p^*$ and the base distribution $q$. That is, we construct distributions $$\np_0 ~,~ p_1 ~,~ p_2 ~,~ \\dots,  p_T,\n$$ such that $p_0 = p^*$ is our target, $p_T = q$ the base distribution, and adjacent distributions $(p_{t-1}, p_{t})$ are marginally &ldquo;close&rdquo; in some appropriate sense. Then, we learn a <i>reverse sampler</i> which transforms distributions $p_{t}$ to $p_{t-1}$. This is the key learning step, which presumably is made easier by the fact that adjacent distributions are &ldquo;close.&rdquo; Formally, reverse samplers are defined below.</p><div class=\"card\"><p><b>Definition.</b></p><p>[Reverse Sampler] Given a sequence of marginal distributions $p_t$, a <i>reverse sampler</i> for step $t$ is a potentially stochastic function $F_t$ such that if $x_{t} \\sim p_{t}$, then the marginal distribution of $F_t(x_{t})$ is exactly $p_{t-1}$: $$\n\\{ F_t(z) : z \\sim p_t \\} \\equiv p_{t-1}.\n$$</p></div><p>There are many possible reverse samplers <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Notice that none of this abstraction is specific to the case of Gaussian noise&mdash; in fact, it does not even require the concept of &ldquo;adding noise&rdquo;. It is even possible to instantiate in discrete settings, where we consider distributions $p^*$ over a finite set, and define corresponding &ldquo;interpolating distributions&rdquo; and reverse samplers.)</span>, and it is even possible to construct reverse samplers which are <i>deterministic</i>. In the remainder of this tutorial we will see three popular reverse samplers more formally: the <i>DDPM</i> sampler discussed above (Section&nbsp;2.1), the <i>DDIM</i> sampler (Section&nbsp;3), which is deterministic, and the family of <i>flow-matching models</i> (Section&nbsp;4), which can be thought of as a generalization of DDIM. <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Given a set of marginal distributions $\\{p_t\\}$, there are many possible joint distributions consistent with these marginals (such joint distributions are called <i>couplings</i>). There is therefore no canonical reverse sampler for a given set of marginals $\\{p_t\\}$ &mdash; we are free to chose whichever coupling is most convenient.)</span></p>"
      },
      {
        "h": "&mdash; Discretization",
        "body": "<p>Before we proceed further, we need to be more precise about what we mean by adjacent distributions $p_{t}, p_{t-1}$ being ``close\". We want to think of the sequence $p_0, p_1, \\ldots, p_T$ as the discretization of some (well-behaved) time-evolving function $p(x, t)$, that starts from the target distribution $p_0$ at time $t=0$ and ends at the noisy distribution $p_T$ at time $t=1$: $$\n\\label{eqn:fwd_discretized}\np(x, k \\dt) = p_k(x), \\quad \\text{where } \\dt = \\frac{1}{T}.\n$$ The number of steps $T$ controls the fineness of the discretization (hence the closeness of adjacent distributions). <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(This naturally suggests taking the continuous-time limit, which we discuss in Section&nbsp;2.4, though it is not needed for most of our arguments.)</span></p><p>In order to ensure that the variance of the final distribution, $p_T$, is independent of the number of discretization steps, we also need to be more specific about the variance of each increment. Note that if $x_k = x_{k-1} + \\cN(0, \\sigma^2)$, then $x_T \\sim \\cN(x_0, T \\sigma^2)$. Therefore, we need to scale the variance of each increment by $\\dt = 1/T$, that is, choose</p><div class=\"formula-box\">$$\\begin{aligned}\n    \\sigma = \\sigma_q \\sqrt{\\dt},\n\\end{aligned}$$</div><p>where $\\sigma_q^2$ is the desired terminal variance. This choice ensures that the variance of $p_T$ is always $\\sigma_q^2$, regardless of $T$. (The $\\sqrt{\\dt}$ scaling will turn out to be important in our arguments for the correctness of our reverse solvers in the next chapter, and also connects to the SDE formulation in Section 2.4.)</p><p>At this point, it is convenient to adjust our notation. From here on, $t$ will represent a continuous-value in the interval $[0,1]$ (specifically, taking one of the values $0, \\dt, 2\\dt, \\ldots, T \\dt=1$). Subscripts will indicate <i>time</i> rather than <i>index</i>, so for example $x_t$ will now denote $x$ at a discretized time $t$. That is, Equation 1 becomes:</p><div class=\"formula-box\">$$x_{t+\\dt} := x_{t} + \\eta_{t}, \\quad \\eta_{t} \\sim  \\cN(0, \\sigma_q^2 \\dt),$$</div><p>which also implies that</p><div class=\"formula-box\">$$x_{t} \\sim  \\cN(x_0, \\sigma_t^2), \\quad\n    \\text{where } \\sigma_t := \\sigma_q \\sqrt{t},$$</div><p>since the total noise added up to time $t$ (i.e. $\\sum_{\\tau \\in \\{0, \\dt, 2\\dt, \\dots, t-\\dt\\}} \\eta_\\tau$) is also Gaussian with mean zero and variance $\\sum_\\tau \\sigma_q^2 \\dt = \\sigma_q^2 t$.</p>"
      },
      {
        "h": "Stochastic Sampling: DDPM",
        "body": "<p>In this section we review the DDPM-like reverse sampler discussed in Section&nbsp;1, and heuristically prove its correctness. This sampler is conceptually the same as the sampler popularized in <i>Denoising Diffusion Probabilistic Models</i> (DDPM) by [ho2020denoising] and originally introduced by [OGdiffusion], when adapted to our simplified setting. However, a word of warning for the reader familiar with [ho2020denoising]: Although the overall strategy of our sampler is identical to [ho2020denoising], certain technical details (like constants, etc) are slightly different <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">( For the experts, the main difference is we use the &ldquo;Variance Exploding&rdquo; diffusion forward process. We also use a constant noise schedule, and we do not discuss how to parameterize the predictor (&ldquo;predicting $x_0$ vs. $x_{t-1}$ vs. noise $\\eta$&rdquo;). We elaborate on the latter point in Section&nbsp;2.2.)</span>.</p><p>We consider the setup from Section&nbsp;1.3, with some target distribution $p^*$ and the joint distribution of noisy samples $(x_0, x_\\dt, \\dots, x_1)$ defined by Equation&nbsp;(5). The DDPM sampler will require estimates of the following conditional expectations:</p><div class=\"formula-box\">$$\\begin{aligned}\n    \\mu_{t}(z) := \\E[x_{t} \\mid x_{t+\\dt} = z].\n\\end{aligned}$$</div><p>This is a set of functions $\\{\\mu_t\\}$, one for every time step $t \\in \\{0, \\dt, \\dots, 1-\\dt\\}$. In the <i>training</i> phase, we estimate these functions from i.i.d. samples of $x_0$, by optimizing the denoising regression objective</p><div class=\"formula-box\">$$\\begin{aligned}\n\\mu_{t} &= \\argmin_{f: \\R^d \\to \\R^d}\n~~\\E_{x_t, x_{t+\\dt}} || f(x_{t+\\dt}) - x_{t} ||_2^2 ~,\n\n\\end{aligned}$$</div><p>typically with a neural-network <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(In practice, it is common to share parameters when learning the different regression functions $\\{\\mu_t\\}_t$, instead of learning a separate function for each timestep independently. This is usually implemented by training a model $f_\\theta$ that accepts the time $t$ as an additional argument, such that $f_\\theta(x_t, t) \\approx \\mu_t(x_t)$.)</span> parameterizing $f$. Then, in the <i>inference</i> phase, we use the estimated functions in the following reverse sampler.</p><p>[nobreak=true] <u>Algorithm 1: Stochastic Reverse Sampler (DDPM-like)</u>\\\\ For input sample $x_t$, and timestep $t$, output:</p><div class=\"formula-box\">$$\\begin{aligned}\n\n    \\hat{x}_{t-\\dt} \\gets {\\mu}_{t-\\dt}(x_t) + \\cN(0, \\sigma^2_q \\dt)\n\\end{aligned}$$</div><p>To actually generate a sample, we first sample $x_1$ as an isotropic Gaussian $x_1 \\sim \\cN(0, \\sigma_q^2)$, and then run the iteration of Algorithm 1 down to $t=0$, to produce a generated sample $\\hat{x}_0$. (Recall that in our discretized notation&nbsp;(6), $x_1$ is the fully-noised terminal distribution, and the iteration takes steps of size $\\dt$.) Explicit pseudocode for these algorithms are given in Section&nbsp;2.3.</p><p>We want to reason about correctness of this entire procedure: why does iterating Algorithm 1 produce a sample from [approximately] our target distribution $p^*$? The key missing piece is, we need to prove some version of Fact&nbsp;2: that the true conditional $p(x_{t-\\dt} \\mid x_t)$ can be well-approximated by a Gaussian, and this approximation gets better as we scale $\\dt \\to 0$.</p>"
      },
      {
        "h": "&mdash; Correctness of DDPM",
        "body": "<p>Here is a more precise version of Fact&nbsp;2, along with a heuristic derivation. This will complete the argument that Algorithm 1 is correct&mdash; i.e. that it approximates a valid reverse sampler in the sense of Definition&nbsp;1.2.</p><div class=\"card\"><p><b>Claim.</b></p><p>[Informal] Let $p_{t-\\dt}(x)$ be an arbitrary, sufficiently-smooth density over $\\R^d$. Consider the joint distribution of $(x_{t-\\dt}, x_{t})$, where $x_{t-\\dt} \\sim p_{t-\\dt}$ and $x_{t} \\sim x_{t-\\dt} + \\cN(0, \\sigma_q^2 \\dt )$. Then, for sufficiently small $\\dt$, the following holds. For all conditionings $z \\in \\R^d$, there exists $\\mu_z$ such that:</p><div class=\"formula-box\">$$\\begin{aligned}\n\np(x_{t-\\dt} \\mid x_{t} = z) \\approx \\cN(x_{t-\\dt} ; ~~\\mu_z ~,~ \\sigma_q^2 \\dt).\n\\end{aligned}$$</div><p>for some constant $\\mu_z$ depending only on $z$. Moreover, it suffices to take <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Experts will recognize this mean as related to the <i>score</i>. In fact, Tweedie's formula implies that this mean is exactly correct even for large $\\dt$, with no approximation required. That is, $\\E[ x_{t-\\dt} \\mid x_t=z ] = z + \\sigma_q^2 \\dt \\nabla \\log p_t(z)$. The distribution $p(x_{t-\\dt} \\mid x_t)$ may deviate from Gaussian, however, for larger $\\sigma$.)</span></p><div class=\"formula-box\">$$\\begin{aligned}\n\\mu_z &:= \\E_{(x_{t-\\dt}, x_t)}[x_{t-\\dt} \\mid x_{t} = z] \\\\\n&= z + (\\sigma_q^2 \\dt) \\nabla \\log p_{t}(z), \n\\end{aligned}$$</div><p>where $p_t$ is the marginal distribution of $x_t$.</p></div><p>Before we see the derivation, a few remarks: Claim&nbsp;2.1 implies that to sample from $x_{t-\\dt}$, it suffices to first sample from $x_t$, then sample from a <i>Gaussian</i> distribution centered around $\\E[x_{t-\\dt} \\mid x_t]$. This is exactly what DDPM does, in Equation&nbsp;(9). Finally, in these notes we will not actually need the expression for $\\mu_z$ in Equation&nbsp;(11); it is enough for us know that such a $\\mu_z$ exists, so we can learn it from samples.</p><div class=\"card\"><p><b>Proof.</b></p><p>[Proof of Claim &nbsp;2.1 (Informal)] Here is a heuristic argument for why the score appears in the reverse process. We will essentially just apply Bayes rule and then Taylor expand appropriately. We start with Bayes rule:</p><div class=\"formula-box\">$$\\begin{aligned}\np(x_{t-\\dt} | x_{t}) &= p( x_{t} | x_{t-\\dt} ) p_{t-\\dt}(x_{t-\\dt}) / p_{t}(x_{t})\n\\end{aligned}$$</div><p>Then take logs of both sizes. Throughout, we will drop any additive constants in the log (which translate to normalizing factors), and drop all terms of order $\\mathcal{O}(\\dt)$  <span class=\"fnote\" style=\"color:var(--ink-dim)\">(Note that $x_{t+1} - x_{t} \\sim \\mathcal{O}(\\sqrt{\\dt})$. Dropping $\\mathcal{O}(\\dt)$ terms means dropping $(x_{t+1} - x_{t})^2 \\sim \\mathcal{O}(\\dt)$ in the expansion of $p_t(x_t)$, but keeping $\\frac{1}{2 \\sigma_q^2 \\dt}(x_{t+1} - x_{t})^2 \\sim \\mathcal{O}(1)$ in $p(x_{t} | x_{t+1})$.)</span>. Note that we should think of $x_{t}$ as a constant in this derivation, since we want to understand the conditional probability as a function of $x_{t-\\dt}$. Now:</p><div class=\"formula-box\">$$\\begin{aligned}\n&\\log p(x_{t-\\dt} | x_{t}) = \\log p(x_{t} | x_{t-\\dt} ) + \\log p_{t-\\dt}(x_{t-\\dt})  \\cancel{-\\log p_t(x_t)}\n\\mathnote{Drop constants involving only $x_{t}$.}\\\\\n&= \\log p(x_{t} | x_{t-\\dt} ) + \\log p_{t}(x_{t-\\dt}) + \\mathcal{O}(\\dt) \\mathnote{Since $p_{t-\\dt}(\\cdot) = p_t(\\cdot) + \\dt \\frac{\\partial}{\\partial t} p_t(\\cdot)$.}\\\\\n&= -\\frac{1}{2 \\sigma_q^2 \\dt} ||x_{t-\\dt} -  x_{t}||_2^2 + \\log p_{t}(x_{t-\\dt}) \\mathnote{Definition of $\\log p(x_{t} | x_{t-\\dt} )$.}\\\\\n&= -\\frac{1}{2 \\sigma_q^2 \\dt} ||x_{t-\\dt} -  x_{t}||_2^2  \\\\\n&\\quad\\quad + \\cancel{\\log p_{t}(x_t)}\n+ \\langle \\nabla_x \\log p_{t}(x_{t}), (x_{t-\\dt} - x_{t})\\rangle +\\mathcal{O}(\\dt) \n\\mathnote{Taylor expand around $x_t$ and drop constants.} \\\\\n&= -\\frac{1}{2 \\sigma_q^2 \\dt} \\left(\n||x_{t-\\dt} -  x_{t}||_2^2 - 2\\sigma_q^2 \\dt \\langle \\nabla_x \\log p_{t}(x_{t}), (x_{t-\\dt} - x_{t})\\rangle \n\\right) \\\\\n&= -\\frac{1}{2 \\sigma_q^2 \\dt}\n||x_{t-\\dt} -  x_{t} - \\sigma_q^2 \\dt ~\\nabla_x \\log p_{t}(x_{t})||_2^2  + C\n\\mathnote{Complete the square in {(x_{t-\\dt}-x_{t})}, and drop constant $C$ involving only $x_{t}$.}\\\\\n&= -\\frac{1}{2 \\sigma_q^2 \\dt}\n||x_{t-\\dt} - \\mu||_2^2 \n\\mathnote{For $\\mu := x_t + (\\sigma_q^2 \\dt)\\nabla_x \\log p_t(x_{t})$.}\n\\end{aligned}$$</div><p>This is identical, up to additive factors, to the log-density of a Normal distribution with mean $\\mu$ and variance $\\sigma_q^2 \\dt$. Therefore, $$\np(x_{t-\\dt} \\mid x_{t}) \\approx \\cN(x_{t-\\dt} ;~ \\mu, \\sigma_q^2 \\dt).\n$$</p></div><p>Reflecting on this derivation, the main idea was that for small enough $\\dt$, the Bayes-rule expansion of the reverse process $p(x_{t-\\dt} \\mid x_t)$ is dominated by the term $p(x_t \\mid x_{t-\\dt})$, from the forward process. This is intuitively why the reverse process and the forward process have the same functional form (both are Gaussian here) <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(This general relationship between forward and reverse processes holds somewhat more generally than just Gaussian diffusion; see e.g. the discussion in [OGdiffusion].)</span>.</p><p></p><p><b>Technical Details [Optional].</b> The meticulous reader may notice that Claim&nbsp;2.1 is not obviously sufficient to imply correctness of the entire DDPM algorithm. The issue is: as we scale down $\\dt$, the error in our per-step approximation (Equation&nbsp;10) decreases, but the number of total steps required increases. So if the per-step error does not decrease fast enough (as a function of $\\dt$), then these errors could accumulate to a non-negligible error by the final step. Thus, we need to quantify how fast the per-step error decays. Lemma&nbsp;12 below is one way of quantifying this: it states that if the step-size (i.e. variance of the per-step noise) is $\\sigma^2$, then the KL error of the per-step Gaussian approximation is $\\cO(\\sigma^4)$. This decay rate is fast enough, because the number of steps only grows as <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(The chain rule for KL implies that we can add up these per-step errors: the approximation error for the final sample is bounded by the sum of all the per-step errors.)</span> $\\Omega(1/\\sigma^2)$.</p><div class=\"card\"><p><b>Lemma.</b></p><p>Let $p(x)$ be an arbitrary density over $\\R$, with bounded 1st to 4th order derivatives. Consider the joint distribution $(x_0, x_1)$, where $x_0 \\sim p$ and $x_1 \\sim x_0 + \\cN(0, \\sigma^2)$. Then, for any conditioning $z \\in \\R$, we have $$\n\\mathrm{KL}\\left(\n\\cN(\\mu_z, \\sigma^2 ) \n~~||~~\np_{x_0 \\mid x_1}(\\cdot \\mid x_1 = z)\n\\right)\n\\leq O(\\sigma^4),\n$$ where $$\n\\mu_z := z + \\sigma^2 \\nabla \\log p(z).\n$$</p></div><p>It is possible to prove Lemma&nbsp;12 by doing essentially a careful Taylor expansion; we include the full proof in Appendix&nbsp;6.1.</p>"
      },
      {
        "h": "&mdash; Variance Reduction: Predicting $x_0$",
        "body": "<p>Thus far, our diffusion models have been trained to predict $\\E[x_{t-\\dt} \\mid x_t]$: this is what Algorithm 1 requires, and what the training procedure of Pseudocode&nbsp;1 produces. However, many practical diffusion implementations actually train to predict $\\E[ x_0 \\mid x_t ]$, i.e. to predict the expectation of the initial point $x_0$ instead of the previous point $x_{t-\\dt}$. This difference turns out to be just a <i>variance reduction</i> trick, which estimates the same quantity in expectation. Formally, the two quantities can be related as follows:</p><div class=\"card\"><p><b>Claim.</b></p><p>For the Gaussian diffusion setting of Section&nbsp;1.3 , we have:</p><div class=\"formula-box\">$$\\begin{aligned}\n\\E[(x_{t-\\dt} - x_t) \\mid x_t]  \n= \n\\frac{\\dt}{t}\n\\E[(x_0 - x_t) \\mid x_t]. \n\\end{aligned}$$</div><p>Or equivalently: $$\n\\E[x_{t-\\dt} \\mid x_t]  \n= \n\\left(\\frac{\\dt}{t}\\right)\n\\E[x_0  \\mid x_t]\n+ \\left(1- \\frac{\\dt}{t}\\right)x_t.\n\\label{eqn:var_red}\n$$</p></div><p>This claim implies that if we want to estimate $\\E[x_{t-\\dt} \\mid x_t]$, we can instead estimate $\\E[x_0 \\mid x_t]$ and then then essentially divide by $(t/\\dt)$, which is the number of steps taken thus far. The variance-reduced versions of the DDPM training and sampling algorithms do exactly this; we include them in Appendix&nbsp;6.9.</p><figure style=\"margin:18px 0\"><img src=\"figs/sbs-diffusion/predict_eps_dt.png\" alt=\"The intuition behind Claim&amp;nbsp;2.2.\n      Given $x_t$, the final noise step $\\eta_{t-\\dt}$\n      is distributed identicall\" loading=\"lazy\" style=\"display:block;margin:12px auto;max-width:100%;height:auto\"><figcaption class=\"vizcap\">The intuition behind Claim&nbsp;2.2.\n      Given $x_t$, the final noise step $\\eta_{t-\\dt}$\n      is distributed identically as all other noise steps,\n      intuitively because we only know the sum $x_t = x_0 + \\sum_i \\eta_i$.\n      </figcaption></figure><p>The intuition behind Claim&nbsp;2.2 is illustrated in Figure&nbsp;3: first, observe that predicting $x_{t-\\dt}$ given $x_t$ is equivalent to predicting the last noise step, which is $\\eta_{t-\\dt} = (x_t - x_{t-\\dt})$ in the forward process of Equation&nbsp;(5). But, if we are only given the final $x_{t}$, then all of the previous noise steps $\\{\\eta_i\\}_{i < t}$ intuitively &ldquo;look the same&rdquo;&mdash; we cannot distinguish between noise that was added at the last step from noise that was added at the 5th step, for example. By this symmetry, we can conclude that all of the individual noise steps are distributed <i>identically</i> (though not independently) given $x_t$. Thus, instead of estimating a single noise step, we can equivalently estimate the <i>average of all prior noise steps</i>, which has much lower variance. There are $(t/\\dt)$ elapsed noise steps by time $t$, so we divide the total noise by this quantity in Equation&nbsp;13 to compute the average. See Appendix&nbsp;6.8 for a formal proof.</p><p><b>Word of warning:</b> Diffusion models should always be trained to estimate <i>expectations</i>. In particular, when we train a model to predict $\\E[x_0 \\mid x_t]$, we should <i>not</i> think of this as trying to learn &ldquo;how to sample from the distribution $p(x_0 \\mid x_t)$&rdquo;. For example, if we are training an image diffusion model, then the optimal model will output $\\E[x_0 \\mid x_t]$ which will look like a blurry mix of images (e.g. Figure 1b in [karras2022elucidating])&mdash; it will not look like an actual image sample. It is good to keep in mind that when diffusion papers colloquially discuss models &ldquo;predicting $x_0$&rdquo;, they do not mean producing something that looks like an actual sample of $x_0$.</p>"
      },
      {
        "h": "&mdash; Algorithms",
        "body": "<p>Pseudocode listings 1 and 2 give the explicit DDPM train loss and sampling code. To train <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Note that the training procedure optimizes $f_\\theta$ for all timesteps $t$ simultaneously, by sampling $t \\in [0, 1]$ uniformly in Line 2.)</span> the network $f_\\theta$, we must minimize the expected loss $L_\\theta$ output by Pseudocode&nbsp;1, typically by backpropagation.</p><p>Pseudocode&nbsp;3 describes the closely-related <i>DDIM</i> sampler, which will be discussed later in Section&nbsp;3.</p><p>[t]{0.47}</p><div class=\"card\" style=\"border-left:3px solid var(--accent);padding:10px 14px\"><p><b>Algorithm.</b> DDPM train loss</p><div style=\"font-family:var(--font-mono);font-size:14px;line-height:1.8\">[H]<br><br><br><b>Input:</b> Neural network $f_\\theta$<br>Sample-access to target distribution $p$.}<br><b>Data:</b> Terminal variance $\\sigma_q$; step-size $\\dt$.<br><b>Output:</b> Stochastic loss $L_\\theta$<br>$x_0 \\gets \\textrm{Sample}(p)$ \\;<br>$t \\gets \\textrm{Unif}[0, 1]$ \\;<br>$x_{t} \\gets x_0 + \\cN(0, \\sigma_q^2 t) $ \\;<br>$x_{t+\\dt} \\gets x_{t} + \\cN(0, \\sigma_q^2 \\dt) $ \\;<br>$L \\gets \\left\\Vert f_\\theta(x_{t+\\dt}, t+\\dt) -  x_{t}\\right\\Vert_2^2$ \\;<br>$L_\\theta$</div></div><p>[t]{0.47}</p><div class=\"card\" style=\"border-left:3px solid var(--accent);padding:10px 14px\"><p><b>Algorithm.</b> DDPM sampling (Code for Algorithm 1)</p><div style=\"font-family:var(--font-mono);font-size:14px;line-height:1.8\">[H]<br><br><br><b>Input:</b> Trained model $f_\\theta$.<br><b>Data:</b> Terminal variance $\\sigma_q$; step-size $\\dt$.<br><b>Output:</b> $x_0$<br>$x_1 \\gets \\cN(0, \\sigma_q^2)$ \\;<br>{$t=1,~(1-\\dt),(1-2\\dt), \\dots, \\dt$}{<br>$\\eta \\gets \\cN(0, \\sigma_q^2 \\dt)$  \\;<br>$x_{t-\\dt} \\gets f_\\theta(x_t, t) + \\eta$ \\;<br>}<br>$x_0$</div></div><div class=\"card\" style=\"border-left:3px solid var(--accent);padding:10px 14px\"><p><b>Algorithm.</b> DDIM sampling (Code for Algorithm 2)</p><div style=\"font-family:var(--font-mono);font-size:14px;line-height:1.8\"><br><br><b>Input:</b> Trained model $f_\\theta$<br><b>Data:</b> Terminal variance $\\sigma_q$; step-size $\\dt$.<br><b>Output:</b> $x_0$<br>$x_1 \\gets \\cN(0, \\sigma_q^2)$ \\;<br>{$t=1,~(1-\\dt),(1-2\\dt), \\dots, \\dt, 0$}{<br>$\\lambda \\gets \\frac{\\sqrt{t}}{\\sqrt{t-\\dt} + \\sqrt{t}}$ \\;<br>$x_{t-\\dt} \\gets x_t + \\lambda(f_\\theta(x_t, t) - x_t)$ \\;<br>}<br>$x_0$</div></div>"
      },
      {
        "h": "&mdash; Diffusions as SDEs [Optional]",
        "body": "<p>In this section <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Sections marked &ldquo;[Optional]&rdquo; are advanced material, and can be skipped on first read. None of the main sections depend on Optional material.)</span>, we connect the discrete-time processes we have discussed so far to stochastic differential equations (SDEs). In the continuous limit, as $\\dt \\to 0$, our discrete diffusion process turns into a stochastic differential equation. SDEs can also represent many other diffusion variants (corresponding to different drift and diffusion terms), offering flexibility in design choices, like scaling and noise-scheduling. The SDE perspective is powerful because existing theory provides a general closed-form solution for the time-reversed SDE. Discretization of the reverse-time SDE for our particular diffusion immediately yields the sampler we derived in this section, but reverse-time SDEs for other diffusion variants are also available <i>automatically</i> (and can then be solved with any off-the-shelf or custom SDE solver), enabling better training and sampling strategies as we will discuss further in Section 5. Though we mention these connections only briefly here, the SDE perspective has had significant impact on the field. For a more detailed discussion, we recommend Yang Song's blog post [songblog2021generative].</p><h4 class=\"book-h\">The Limiting SDE</h4><p>Recall our discrete update rule:</p><div class=\"formula-box\">$$\\begin{aligned}\n    x_{t+\\dt} &= x_{t} + \\sigma_q \\sqrt{\\dt} \\xi, \\quad \\xi \\sim \\mathcal{N}(0,1).\n\\end{aligned}$$</div><p>In this limit as $\\dt \\to 0$, this corresponds to a zero-drift SDE:</p><div class=\"formula-box\">$$\\begin{aligned}\n    dx &= \\sigma_q dw, \n\\end{aligned}$$</div><p>where $w$ is a Brownian motion. A Brownian motion is a stochastic process with i.i.d. Gaussian increments whose variance scales with $\\dt$. <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(See [eldan_blog_sde] for a high-level overview of Brownian motions and It\\^{o}’s formula. See also [evans2012introduction] for a gentle introductory textbook, and [kloeden2011numerical] for numerical methods.)</span> <i>Very heuristically</i>, we can think of $dw \\sim \\lim_{\\dt \\to 0} \\sqrt{\\dt} \\mathcal{N}(0,1),$ and thus &ldquo;derive&rdquo;&nbsp;(14) by</p><div class=\"formula-box\">$$\\begin{aligned}\n    dx &= \\lim_{\\dt \\to 0} (x_{t+\\dt} - x_{t}) = \\sigma_q \\lim_{\\dt \\to 0} \\sqrt{\\dt} \\xi  = \\sigma_q dw.\\\\\n\\end{aligned}$$</div><p>More generally, different variants of diffusion are equivalent to SDEs with different choices of drift and diffusion terms:</p><div class=\"formula-box\">$$\\begin{aligned}\n    dx &= f(x,t)dt + g(t)dw. \n\\end{aligned}$$</div><p>The SDE (14) simply has $f = 0$ and $g = \\sigma_q$. This formulation encompasses many other possibilities, though, corresponding to different choices of $f$, $g$ in the SDE. As we will revisit in Section 5, this flexibility is important for developing effective algorithms. Two important choices made in practice are tuning the noise schedule and scaling $x_t$; together these can help to control the variance of $x_t$, and control how much we focus on different noise levels. Adopting a flexible noise schedule $\\{ \\sigma_t \\}$ in place of the fixed schedule $\\sigma_t \\equiv \\sigma_q \\sqrt{t}$ corresponds to the SDE [song2020score]</p><div class=\"formula-box\">$$\\begin{aligned}\n    x_t \\sim \\cN(x_0, \\sigma_t^2) \\iff\n    x_{t} = x_{t-\\dt} + \\sqrt{\\sigma_t^2 - \\sigma_{t-\\dt}^2} z_{t-\\dt} \n    \\iff dx = \\sqrt{\\frac{d}{dt} \\sigma^2(t)} dw. \n\\end{aligned}$$</div><p>If we also wish to scale each $x_t$ by a factor $s(t)$, [karras2022elucidating] show that this corresponds to the SDE <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(As a sketch of how $f$ arises, let's ignore the noise and note that:</p><div class=\"formula-box\">$$\\begin{aligned}\n    x_t &= s(t) x_0 \\\\\n    \\iff x_{t + \\dt} &= \\frac{s(t+\\dt)}{s(t)} x_t \\\\\n    &= x_{t} + \\frac{s(t) - s(t + \\dt)}{s(t)} x_{t} \\\\\n    \\iff dx/dt &= \\frac{\\dot{s}}{s} x\n\\end{aligned}$$</div><p>)</span></p><div class=\"formula-box\">$$\\begin{aligned}\n    x_t \\sim \\cN(s(t) x_0, s(t)^2 \\sigma(t)^2) \\iff f(x) = \\frac{\\dot{s}(t)}{s(t)} x, \\quad g(t) = s(t) \\sqrt{2\\dot{\\sigma}(t)\\sigma(t)}.\n\\end{aligned}$$</div><p>These are only a few examples of the rich and useful design space enabled by the flexible SDE&nbsp;(15).</p><h4 class=\"book-h\">Reverse-Time SDE</h4><p>The <i>time-reversal</i> of an SDE runs the process <i>backward in time</i>. Reverse-time SDEs are the continuous-time analog of samplers like DDPM. A deep result due to [anderson1982reverse] (and nicely re-derived in [winkler_blog_reverse]) states that the time-reversal of SDE&nbsp;(15) is given by:</p><div class=\"formula-box\">$$\\begin{aligned}\n    dx &= \\big( f(x,t) - g(t)^2 \\nabla_x \\log p_t(x) \\big) dt + g(t) d\\bar w \n\\end{aligned}$$</div><p>That is, SDE&nbsp;(16) tells us how to run any SDE of the form&nbsp;(15) backward in time! This means that we don't have to re-derive the reversal in each case, and we can choose any SDE solver to yield a practical sampler. But nothing is free: we sill cannot use&nbsp;(16) directly to sample backward, since the term $\\nabla_x \\log p_t(x)$ &ndash; which is in fact the <i>score</i> that previously appeared in equation 11 &ndash; is unknown in general, since it depends on $p_t$. However, if we can <i>learn</i> the score, then we can solve the reverse SDE. This is analogous to discrete diffusion, where the <i>forward</i> process is easy to model (it just adds noise), while the <i>reverse</i> process must be learned.</p><p>Let us take a moment to discuss the score, $\\nabla_x \\log p_t(x)$, which plays a central role. Intuitively, since the score &ldquo;points toward higher probability&rdquo;, it helps to reverse the diffusion process, which &ldquo;flattens out&rdquo; the probability as it runs forward. The score is also related to the conditional expectation of $x_0$ given $x_t$. Recall that in the discrete case $$\\sigma_q^2 \\dt \\nabla \\log p_t(x_{t}) = \\E[ x_{t-\\dt} - x_t \\mid x_{t} ] = \\frac{\\dt}{t} \\E[ x_0 - x_t \\mid x_{t} ],$$ (by equations 11, 13).</p><p>Similarly, in the continuous case we have <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(We can see this directly by applying Tweedie's formula, which states: $$ \\E[\\mu_z | z] = z + \\sigma_z^2 \\nabla \\log p(z) \\text{ for } z \\sim \\cN(\\mu_z, \\sigma_z^2).$$ Since $x_t \\sim \\cN(x_0, t \\sigma_q^2),$ Tweedie with $z \\equiv x_t,$ $\\mu_z \\equiv x_0$ gives: $$\\E[x_0 | x_t] = x_t + t \\sigma_q^2 \\nabla \\log p(x_t).$$)</span></p><div class=\"formula-box\">$$\\begin{aligned}\n    \\sigma_q^2 \\nabla \\log p_t(x_{t}) \n    &= \\frac{1}{t} \\E[ x_0 - x_t \\mid x_{t} ]. \n\\end{aligned}$$</div><p>Returning to the reverse SDE, we can show that its discretization yields the DDPM sampler of Claim 2.1 as a special case. The reversal of the simple SDE (14) is:</p><div class=\"formula-box\">$$\\begin{aligned}\n    dx &= -\\sigma_q^2 \\nabla_x \\log p_t(x) dt + \\sigma_q d\\bar w \\\\\n    &= -\\frac{1}{t} \\E[ x_0 - x_t \\mid x_{t} ] dt + \\sigma_q d\\bar w\n\\end{aligned}$$</div><p>The discretization is</p><div class=\"formula-box\">$$\\begin{align}\n    x_t - x_{t-\\dt} \n    &= -\\frac{\\dt}{t} \\E[ x_0 - x_t \\mid x_{t} ] + \\cN(0, \\sigma_q^2 \\dt) \\\\\n    &= - \\E[ x_{t-\\dt} - x_t \\mid x_{t} ] + \\cN(0, \\sigma_q^2 \\dt) \\tag{by Eqn. 23} \\\\\n    \\implies\n    x_{t-\\dt}\n    &= \\E[ x_{t-\\dt} \\mid x_{t} ] + \\cN(0, \\sigma_q^2 \\dt)\n\\end{align}$$</div><p>which is exactly the stochastic (DDPM) sampler derived in Claim 2.1.</p>"
      },
      {
        "h": "Deterministic Sampling: DDIM",
        "body": "<p>   </p><p>We will now show a <i>deterministic</i> reverse sampler for Gaussian diffusion&mdash; which appears similar to the stochastic sampler of the previous section, but is conceptually quite different. This sampler is equivalent to the DDIM <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(DDIM stands for Denoising Diffusion Implicit Models, which reflects a perspective used in the original derivation of [song2021denoising]. Our derivation follows a different perspective, and the &ldquo;implicit&rdquo; aspect will not be important to us.)</span> update of [song2021denoising], adapted to in our simplified setting.</p><p>We consider the same Gaussian diffusion setup as the previous section, with the joint distribution $(x_0, x_\\dt, \\dots, x_1)$ and conditional expectation function $\\mu_{t}(z) := \\E[x_{t} \\mid x_{t+\\dt} = z].$ The reverse sampler is defined below, and listed explicitly in Pseudocode&nbsp;3.</p><p>[nobreak=true] <u>Algorithm 2: Deterministic Reverse Sampler (DDIM-like)</u>\\\\ For input sample $x_t$, and step index $t$, output:</p><div class=\"formula-box\">$$\\begin{aligned}\n\n    \\hat{x}_{t-\\dt} \\gets\n    x_t + \\lambda({\\mu}_{t-\\dt}(x_t) - x_t)\n\\end{aligned}$$</div><p>where $\\lambda := \\left( \\frac{\\sigma_{t}}{\\sigma_{t-\\dt}+ \\sigma_{t}} \\right )$ and $\\sigma_t \\equiv \\sigma_q \\sqrt{t}$ from Equation&nbsp;(6).</p><p>How do we show that this defines a valid reverse sampler? Since Algorithm 2 is <i>deterministic</i>, it does not make sense to argue that it <i>samples</i> from $p(x_{t-\\dt} \\mid x_t)$, as we argued for the DDPM-like stochastic sampler. Instead, we will directly show that Equation&nbsp;(20) implements a valid <i>transport map</i> between the marginal distributions $p_{t}$ and $p_{t-\\dt}$. That is, if we let $F_t$ be the update of Equation&nbsp;(20):</p><div class=\"formula-box\">$$\\begin{aligned}\n    F_t(z) :=& ~z + \\lambda(\\mu_{t-\\dt}(z) - z)\\\\\n    =& ~z + \\lambda(\\E[x_{t-\\dt} \\mid x_t = z] - z)\n\n\\end{aligned}$$</div><p>then we want to show that <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(The notation $F\\sharp p$ means the distribution of $\\{F(x)\\}_{x \\sim p}$. This is called the <i>pushforward</i> of $p$ by the function $F$.)</span></p><div class=\"formula-box\">$$\\begin{aligned}\nF_t \\sharp p_t \\approx p_{t-\\dt}.\n\\end{aligned}$$</div><p>{ Proof overview:} The usual way to prove this is to use tools from stochastic calculus, but we'll present an elementary derivation. Our strategy will be to first show that Algorithm 2 is correct in the simplest case of a point-mass distribution, and then lift this result to full distributions by marginalizing appropriately. For the experts, this is similar to &ldquo;flow-matching&rdquo; proofs.</p>"
      },
      {
        "h": "&mdash; Case 1: Single Point",
        "body": "<p>Let's first understand the simple case where the target distribution $p_0$ is a single point mass in $\\R^d$. Without loss of generality <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Because we can just &ldquo;shift&rdquo; our coordinates to make it so. Formally, our entire setup including Equation&nbsp;21 is translation-symmetric.)</span>, we can assume the point is at $x_0 = 0$. Is Algorithm 2 correct in this case? To reason about correctness, we want to consider the distributions of $x_t$ and $x_{t-\\dt}$ for arbitrary step $t$. According to the diffusion forward process (Equation&nbsp;5), at time $t$ the relevant random variables are <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(We omit the Identity matrix in these covariances for notational simplicity. The reader may assume dimension $d=1$ without loss of generality.)</span></p><div class=\"formula-box\">$$\\begin{aligned}\n    x_0 &= 0 \\quad\\textrm{(deterministically)} \\\\\n    x_{t-\\dt} &\\sim  \\cN(x_0, \\sigma_{t-\\dt}^2) \\\\\n    x_t &\\sim  \\cN(x_{t-\\dt}, \\sigma_{t}^2-\\sigma_{t-\\dt}^2).\n\\end{aligned}$$</div><p>The marginal distribution of $x_{t-\\dt}$ is $p_{t-\\dt} = \\cN(0, \\sigma_{t-1}^2)$, and the marginal distribution of $x_t$ is $p_t = \\cN(0, \\sigma_t^2)$.</p><p>Let us first find <i>some</i> deterministic function $G_t: \\R^d \\to \\R^d$, such that $G_t \\sharp p_t = p_{t-\\dt}$. There are many possible functions which will work <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(For example, we can always add a rotation around the origin to any valid map.)</span>, but this is the obvious one:</p><div class=\"formula-box\">$$\\begin{aligned}\n\n    G_t(z) &:= \\left(\\frac{\\sigma_{t-\\dt}}{\\sigma_t}\\right) z.\n\\end{aligned}$$</div><p>The function $G_t$ above simply re-scales the Gaussian distribution of $p_t$, to match variance of the Gaussian distribution $p_{t-\\dt}$. It turns out this $G_t$ is exactly equivalent to the step $F_t$ taken by Algorithm 2, which we will now show.</p><div class=\"card\"><p><b>Claim.</b></p><p>When the target distribution is a point mass $p_0 = \\delta_0$, then update $F_t$ (as defined in Equation&nbsp;21) is equivalent to the scaling $G_t$ (as defined in Equation&nbsp;23): $$\n    F_t \\equiv G_t.\n    $$ Thus Algorithm 2 defines a reverse sampler for target distribution $p_0 = \\delta_{0}.$</p></div><div class=\"card\"><p><b>Proof.</b></p><p>To apply $F_t$, we need to compute $\\E[x_{t-\\dt} \\mid x_t]$ for our simple distribution. Since $(x_{t-\\dt}, x_t)$ are jointly Gaussian, this is <span class=\"fnote\" style=\"color:var(--ink-dim)\">(Recall the conditional expectation of two jointly Gaussian random variables $(X, Y)$ is $\\E[X \\mid Y = y] = \\mu_X + \\Sigma_{XY} \\Sigma^{-1}_{YY}(y-\\mu_Y)$, where $\\mu_X, \\mu_Y$ are the respective means, and $\\Sigma_{XY}, \\Sigma_{YY}$ the cross-covariance of $(X, Y)$ and covariance of $Y$. Since $X = x_{t-\\dt}$ and $Y = x_t$ are centered at $0$, we have $\\mu_X=\\mu_Y=0$. For the covariance term, since $x_t = x_{t-\\dt} + \\eta$ we have $\\Sigma_{XY} = \\E[x_t x_{t-\\dt}^{T}] = \\E[x_{t-\\dt}x_{t-\\dt}^{T}]\n= \\sigma_{t-\\dt}^2 I_d$. Similarly, $\\Sigma_{YY} = \\E[x_tx_t^T] = \\sigma_t^2 I_d$.)</span></p><div class=\"formula-box\">$$\\begin{aligned}\n\n    \\E[x_{t-\\dt} \\mid x_t]\n    &= \\left(\\frac{\\sigma_{t-\\dt}^2}{\\sigma_t^2}\\right) x_t.\n\\end{aligned}$$</div><p>The rest is algebra:</p><div class=\"formula-box\">$$\\begin{aligned}\n    F_t(x_t) &:= x_t + \\lambda(\\E[x_{t-\\dt} \\mid x_t ] - x_t)  \\mathnote{by definition of $F_t$}\\\\\n    &= \n    x_t + \\left( \\frac{\\sigma_{t}}{\\sigma_{t-\\dt}+ \\sigma_{t}}  \\right)\n    (\\E[x_{t-\\dt} \\mid x_t ] - x_t) \\mathnote{by definition of $\\lambda$}\\\\\n    &= \n    x_t + \\left( \\frac{\\sigma_{t}}{\\sigma_{t-\\dt}+ \\sigma_{t}}  \\right)\n    \\left(\\frac{\\sigma_{t-\\dt}^2}{\\sigma_t^2} - 1\\right)x_t \\mathnote{by Equation~(39)}\\\\\n    &= \\left(\\frac{\\sigma_{t-\\dt}}{\\sigma_t}\\right) x_t  \\\\\n    &= G_t(x_t).\n\\end{aligned}$$</div><p>We therefore conclude that Algorithm 2 is a correct reverse sampler, since it is equivalent to $G_t$, and $G_t$ is valid.</p></div><p>The correctness of Algorithm 2 still holds <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(See Claim&nbsp;6.3 in Appendix&nbsp;6.3 for an explicit statement.)</span> if $x_0$ is an arbitrary point instead of $x_0 = 0$, since everything is transitionally symmetric.</p>"
      },
      {
        "h": "&mdash; Velocity Fields and Gases",
        "body": "<p>Before we move on, it will be helpful to think of the DDIM update as equivalent to a velocity field, which moves points at time $t$ to their positions at time $(t-\\dt)$. Specifically, define the vector field $$\n\\label{eqn:vt_ddim}\nv_t(x_t) := \\frac{\\lambda}{\\dt}(\\E[x_{t-\\dt} \\mid x_t] - x_t).\n$$ Then the DDIM update algorithm of Equation&nbsp;(20) can be written as:</p><div class=\"formula-box\">$$\\begin{aligned}\n    \\hat{x}_{t-\\dt} :=& ~x_t + \\lambda({\\mu}_{t-\\dt}(x_t) - x_t)\n    \\mathnote{from Equation~(40)}\\\\\n        =& ~x_t + v_t(x_t) \\dt.\n\\end{aligned}$$</div><p>The physical intuition for $v_t$ is: imagine a gas of non-interacting particles, with density field given by $p_t$. Then, suppose a particle at position $z$ moves in the direction $v_t(z)$. The resulting gas will have density field $p_{t-\\dt}$. We write this process as $$\np_t \\goto{v_t} p_{t-\\dt}.\n$$ In the limit of small stepsize $\\dt$, speaking informally, we can think of $v_t$ as a <i>velocity field</i> &mdash; which specifies the instantaneous velocity of particles moving according to the DDIM algorithm.</p><figure style=\"margin:18px 0\"><img src=\"figs/sbs-diffusion/flow.png\" alt=\"Velocity field $v_t$ when $p_0 = \\delta_{x_0}$, overlaid\n  on the Gaussian distribution $p_t$.\" loading=\"lazy\" style=\"display:block;margin:12px auto;max-width:100%;height:auto\"><figcaption class=\"vizcap\">Velocity field $v_t$ when $p_0 = \\delta_{x_0}$, overlaid\n  on the Gaussian distribution $p_t$.</figcaption></figure><p>As a concrete example, if the target distribution $p_0=\\delta_{x_0}$, as in Section&nbsp;3.1, then the velocity field of DDIM is $v_t(x_t) = \\left( \\frac{\\sigma_{t} - \\sigma_{t-\\dt}}{\\sigma_{t}} \\right) (x_0 - x_t) / \\dt$ which is a vector field that always points towards the initial point $x_0$ (see Figure&nbsp;4).</p>"
      },
      {
        "h": "&mdash; Case 2: Two Points",
        "body": "<p>Now let us show Algorithm 2 is correct when the target distribution is a mixture of two points:</p><div class=\"formula-box\">$$p_0 := \\frac{1}{2}\\delta_a + \\frac{1}{2}\\delta_b,$$</div><p>for some $a, b \\in \\R^d$. According to the diffusion forward process, the distribution at time $t$ will be a mixture of Gaussians <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Linearity of the forward process (with respect to $p_0$) was important here. That is, roughly speaking, diffusing a distribution is equivalent to diffusing each individual point in that distribution independently; the points don't interact.)</span>: $$\np_t := \\frac{1}{2}\\cN(a, \\sigma_t^2) + \\frac{1}{2}\\cN(b, \\sigma_t^2).\n$$ We want to show that with these distributions $p_t$, the DDIM velocity field $v_t$ (of Equation&nbsp;3.2) transports $p_t \\goto{v_t} p_{t-\\dt}$.</p><p>Let us first try to construct <i>some</i> velocity field $v^*_t$ such that $p_t \\goto{v^*_t} p_{t-\\dt}$. From our result in Section&nbsp;3.1 &mdash; the fact that DDIM update works for single points &mdash; we already know velocity fields which transport each mixture component $\\{a, b\\}$ individually. That is, we know the velocity field $\\va$ defined as $$\n\\label{eqn:def_va}\n\\va(x_t) := \\lambda\\E_{x_0 \\sim \\delta_a}[x_{t-\\dt} - x_t \\mid x_t]\n$$ transports <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Pay careful attention to which distributions we take expectations over! The expectation in Equation (26) is w.r.t. the single-point distribution $\\delta_a$, but our definition of the DDIM algorithm, and its vector field in Equation (3.2), are always w.r.t. the target distribution. In our case, the target distribution is $p_0$ of Equation&nbsp;(26).)</span> $$\n\\cN(a, \\sigma_t^2)\n\\goto{\\va}\n\\cN(a, \\sigma_{t-\\dt}^2),\n$$ and similarly for $\\vb$.</p><p>We now want some way of combining these two velocity fields into a single velocity $v^*_t$, which transports the mixture: $$\n\\label{eqn:guessT}\n\\underbrace{\\left(\\frac{1}{2}\\cN(a, \\sigma_t^2) + \\frac{1}{2}\\cN(b, \\sigma_t^2) \\right)}_{p_t}\n\\overset{v^*_t}{\\longrightarrow} \n\\underbrace{\n\\left(\n\\frac{1}{2}\\cN(a, \\sigma_{t-\\dt}^2) + \\frac{1}{2}\\cN(b, \\sigma_{t-\\dt}^2)\n\\right)}_{p_{t-\\dt}}\n$$ We may be tempted to just take the average velocity field $(v^*_t = 0.5\\va + 0.5\\vb)$, but this is incorrect. The correct combined velocity $v_t^*$ is a <i>weighted</i>-average of the individual velocity fields, weighted by their corresponding density fields <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Note that we can write the density $\\cN(x_t; a, \\sigma_t^2)$ as $p(x_t \\mid x_0=a)$.)</span>.</p><div class=\"formula-box\">$$\\begin{aligned}\nv_t^*(x_t)\n&= \\frac{\\va(x_t) \\cdot p(x_t \\mid x_0 = a) + \\vb(x_t) \\cdot p(x_t \\mid x_0 = b)}{ p(x_t \\mid x_0 = a)  +  p(x_t \\mid x_0 = b) } \\\\\n&= \\va(x_t) \\cdot p(x_0 = a \\mid x_t) + \\vb(x_t) \\cdot p(x_0 = b \\mid x_t).\n\n\\end{aligned}$$</div><p>Explicitly, the weight for $\\va$ at a point $x_t$ is the probability that $x_t$ was generated from initial point $x_0=a$, rather than $x_0=b$.</p><p>To be intuitively convinced of this <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(The time step must be small enough for this analogy to hold, so the DDIM updates are essentially infinitesimal steps. Otherwise, if the step size is large, it may not be possible to combine the two transport maps with &ldquo;local&rdquo; (i.e. pointwise) operations alone.)</span>, consider the corresponding question about gasses illustrated in Figure&nbsp;5. Suppose we have two overlapping gases, a red gas with density $\\cN(a, \\sigma^2)$ and velocity $\\va$, and a blue gas with density $\\cN(b, \\sigma^2)$ and velocity $\\vb$. We want to know, what is the effective velocity of the combined gas (as if we saw only in grayscale)? We should clearly take a <i>weighted</i>-average of the individual gas velocities, weighted by their respective densities &mdash; just as in Equation&nbsp;(27).</p><figure style=\"margin:18px 0\"><img src=\"figs/sbs-diffusion/flow_rb.svg\" alt=\"Illustration of combining the velocity fields of two gasses.\n    Left: The density and velocity fields of two independen\" loading=\"lazy\" style=\"display:block;margin:12px auto;max-width:100%;height:auto\"><img src=\"figs/sbs-diffusion/flow_combo.png\" alt=\"Illustration of combining the velocity fields of two gasses.\n    Left: The density and velocity fields of two independen\" loading=\"lazy\" style=\"display:block;margin:12px auto;max-width:100%;height:auto\"><figcaption class=\"vizcap\">Illustration of combining the velocity fields of two gasses.\n    Left: The density and velocity fields of two independent gases (in red and blue).\n    Right: The effective density and velocity field of the combined gas,\n    including streamlines.</figcaption></figure><p>We have now solved the main subproblem of this section: we have found <i>one particular</i> vector field $v_t^*$ which transports $p_t$ to $p_{t-\\dt}$, for our two-point distribution $p_0$. It remains to show that this $v_t^*$ is equivalent to the velocity field of Algorithm 2 ($v_t$ from Equation&nbsp;3.2).</p><p>To show this, first notice that the individual vector field $\\va$ can be written as a conditional expectation. Using the definition in Equation&nbsp;(26) <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(We add conditioning $x_0 = a$, because we want to take expectations w.r.t the two-point mixture distribution, not the single-point distribution.)</span>,</p><div class=\"formula-box\">$$\\begin{aligned}\n    \\va(x_t) &= \\lambda \\E_{x_0 \\sim \\delta_a}[ x_{t-\\dt} - x_t \\mid x_{t} ] \\\\\n    &= \\lambda \\E_{x_0 \\sim \\half\\delta_a + \\half\\delta_b} [ x_{t-\\dt} - x_t \\mid x_0 = a, x_{t} ].\n\\end{aligned}$$</div><p>Now the entire vector field $v_t^*$ can be written as a conditional expectation:</p><div class=\"formula-box\">$$\\begin{align}\nv_t^*(x_t)\n&=\n\\va(x_t) \\cdot p(x_0 = a \\mid x_t)\n+\n\\vb(x_t) \\cdot p(x_0 = b \\mid x_t) \\\\\n&=\n\\lambda \\E[ x_{t-\\dt} - x_t \\mid x_0 = a, x_{t}  ] \\cdot p(x_0 = a \\mid x_t ) \\\\\n&\\quad+ \\lambda \\E[ x_{t-\\dt} - x_t \\mid x_0 = b, x_{t}  ]  \\cdot p(x_0 = b \\mid x_t ) \\\\\n&=\n\\lambda \\E\\left[\nx_{t-\\dt} - x_t \\mid x_{t} \\right] \\\\\n&= v_t(x_t) \\tag{from Equation~40}\n\\end{align}$$</div><p>where all expectations are w.r.t. the distribution $x_0 \\sim \\half\\delta_a + \\half\\delta_b$. Thus, the combined velocity field $v^*_t$ is exactly the velocity field $v_t$ given by the updates of Algorithm 2 &mdash; so Algorithm 2 is a correct reverse sampler for our two-point mixture distribution.</p>"
      },
      {
        "h": "&mdash; Case 3: Arbitrary Distributions",
        "body": "<p>Now that we know how to handle two points, we can generalize this idea to arbitrary distributions of $x_0$. We will not go into details here, because the general proof will be subsumed by the subsequent section.</p><p>It turns out that our overall proof strategy for Algorithm 2 can be generalized significantly to other types of diffusions, without much work. This yields the idea of flow matching, which we will see in the following section. Once we develop the machinery of flows, it is actually straightforward to derive DDIM directly from the simple single-point scaling algorithm of Equation&nbsp;(23): see Appendix 6.5.</p><h4 class=\"book-h\">The Probability Flow ODE [Optional]</h4><p>Finally, we generalize our discrete-time deterministic sampler to an ordinary differential equation (ODE) called the <i>probability flow ODE</i> [song2020score]. The following section builds on our discussion of SDEs as the continuous limit of diffusion in section 2.4. Just as the reverse-time SDEs of section 2.4 offered a flexible continuous-time generalization of discrete stochastic samplers, so we will see that discrete deterministic samplers generalize to ODEs. The ODE formulation offers both a useful theoretical lens through which to view diffusion, as well as practical advantages, like the opportunity to choose from a variety of off-the-shelf and custom ODE solvers to improve sampling (like the popular DPM++ method, as discussed in chapter 5).</p><p>Recall the general SDE (15) from section 2.4: $$dx = f(x,t)dt + g(t)dw.$$ [song2020score] showed that is possible to convert this SDE into a <i>deterministic</i> equivalent called the probability flow ODE (PF-ODE): <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(A proof sketch is in appendix 6.2. It involves rewriting the SDE noise term as the deterministic score (recall the connection between noise and score in equation&nbsp;(11)). Although it is deterministic, the score is unknown since it depends on $p_t$.)</span></p><div class=\"formula-box\">$$\\begin{aligned}\n    \\frac{dx}{dt} &= \\tilde f(x, t), \\quad \\text{where } \\tilde f(x, t) = f(x,t) - \\frac{1}{2} g(t)^2 \\nabla_x \\log p_t(x) \n\\end{aligned}$$</div><p>SDE&nbsp;(15) and ODE&nbsp;(30) are equivalent in the sense that trajectories obtained by solving the PF-ODE have the same marginal distributions as the SDE trajectories at every point in time <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(To use a gas analogy: the SDE describes the (Brownian) motion of individual particles in a gas, while the PF-ODE describes the streamlines of the gas's velocity field. That is, the PF-ODE describes the motion of a &ldquo;test particle&rdquo; being transported by the gas&mdash; like a feather in the wind.)</span>. However, note that the score appears here again, as it did in the reverse SDE (16); just as for the reverse SDE, we must learn the score to make the ODE&nbsp;(30) practically useful.</p><p>Just as DDPM was a (discretized) special-case of the reverse-time SDE&nbsp;(16), so DDIM can be seen as a (discretized) special case of the PF-ODE&nbsp;(30). Recall from section 2.4 that the simple diffusion we have been studying corresponds to the SDE (14) with $f = 0$ and $g = \\sigma_q$. The corresponding ODE is</p><div class=\"formula-box\">$$\\begin{aligned}\n    \\frac{dx}{dt} &= -\\frac{1}{2} \\sigma_q^2 \\nabla_x \\log p_t(x) \\\\\n    &= -\\frac{1}{2t} \\E[ x_0 - x_t \\mid x_{t} ] \\quad \\text{(by eq. 28)}\n     \n\\end{aligned}$$</div><p>Reversing and discretizing yields</p><div class=\"formula-box\">$$\\begin{aligned}\n    x_{t-\\dt} \n    &= x_{t} + \\frac{\\dt}{2t} \\E[ x_0 - x_t \\mid x_{t} ] \\\\\n    &= x_{t} + \\frac{1}{2} (\\E[ x_{t-\\dt} \\mid x_{t} ] - x_{t}) \\quad \\text{(by eq. 23).}\n\\end{aligned}$$</div><p>Noting that $\\lim_{\\dt \\to 0} \\left( \\frac{\\sigma_{t}}{\\sigma_{t-\\dt}+ \\sigma_{t}} \\right ) = \\frac{1}{2}$, we recover the deterministic (DDIM) sampler (20).</p>"
      },
      {
        "h": "&mdash; Discussion: DDPM vs DDIM",
        "body": "<p>The two reverse samplers defined above (DDPM and DDIM) are conceptually significantly different: one is deterministic, and the other stochastic. To review, these samplers use the following strategies: <ol class=\"mbul\"><li>DDPM ideally implements a stochastic map $F_t$, such that the output $F_t(x_{t})$ is, pointwise, a sample from the conditional distribution $p(x_{t-\\dt} \\mid x_{t})$.</li><li>DDIM ideally implements a deterministic map $F_t$, such that the output $F_t(x_{t})$ is <i>marginally distributed</i> as $p_{t-\\dt}$. That is, $F_t \\sharp p_{t} = p_{t-\\dt}$.</li></ol></p><p>Although they both happen to take steps in the same direction <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Steps proportional to $({\\mu}_{t-\\dt}(x_t) - x_t)$.)</span> (given the same input $x_t$), the two algorithms end up evolving very differently. To see this, let's consider how each sampler ideally behaves, when started from the same initial point $x_1$ and iterated to completion.</p><p>DDPM will ideally produce a sample from $p(x_0 \\mid x_1)$. If the forward process mixes sufficiently (i.e. for large $\\sigma_q$ in our setup), then the final point $x_1$ will be nearly <i>independent</i> from the initial point. Thus $p(x_0 \\mid x_1) \\approx p(x_0)$, so the distribution output by the ideal DDPM will not depend at all <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Actual DDPMs may have a small dependency on the initial point $x_1$, because they do not mix perfectly (i.e. the final distribution $p_1$ is not perfectly Gaussian). Randomizing the initial point may thus help with sample diversity in practice.)</span> on the starting point $x_1$. In contrast, DDIM is deterministic, so it will always produce a fixed value for a given $x_1$, and thus will depend very strongly on $x_1$.</p><p>The picture to have in mind is, DDIM defines a deterministic map $\\R^d \\to \\R^d$, taking samples from a Gaussian distribution to our target distribution. At this level, the DDIM map may sound similar to other generative models &mdash; after all, GANs and Normalizing Flows also define maps from Gaussian noise to the true distribution. What is special about the DDIM map is, it is not allowed to be arbitrary: the target distribution $p^*$ exactly determines the ideal DDIM map (which we train models to emulate). This map is &ldquo;nice&rdquo;; for example we expect it to be smooth if our target distribution is smooth. GANs, in contrast, are free to learn any arbitrary mapping between noise and images. This feature of diffusion models may make the learning problem easier in some cases (since it is supervised), or harder in other cases (since there may be easier-to-learn maps which other methods could find).</p>"
      },
      {
        "h": "&mdash; Remarks on Generalization",
        "body": "<p>In this tutorial, we have not discussed the learning-theoretic aspects of diffusion models: How do we learn properties of the underlying distribution, given only finite samples and bounded compute? These are fundamental aspects of learning, but are not yet fully understood for diffusion models; it is an active area of research <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(We recommend the introductions of [chen2022sampling] and [chen2024learning] for an overview of recent learning-theoretic results. This line of work includes e.g. [de2021diffusion, de2022convergence, lee2023convergence, chen2023improved, chen2024probability].)</span>.</p><p>To appreciate the subtlety here, suppose we learn a diffusion model using the classic strategy of Empirical Risk Minimization (ERM): we sample a finite train set from the underlying distribution, and optimize all regression functions w.r.t. this empirical distribution. The problem is, we should not <i>perfectly</i> minimize the empirical risk, because this would yield a diffusion model which only reproduces the train samples <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(This is not specific to diffusion models: any perfect generative model of the empirical distribution will always output a uniformly random train point, which is far-from-optimal w.r.t. the true underlying distribution.)</span>.</p><p>In general learning the diffusion model must be <i>regularized</i>, implicitly or explicitly, to prevent overfitting and memorization of the training data. When we train deep neural networks for use in diffusion models, this regularization often occurs implicitly: factors such as finite model size and optimization randomness prevent the trained model from perfectly memorizing its train set. We will revisit these factors (as sources of error) in Section&nbsp;5.</p><p>This issue of memorizing training data has been seen &ldquo;in the wild&rdquo; in diffusion models trained on small image datasets, and it has been observed that memorization reduces as the training set size increases [somepalli2023diffusion, gu2023memorization]. Additionally, memorization as been noted as a potential security and copyright issue for neural networks as in [carlini2023extracting] where the authors found they can recover training data from stable diffusion with the right prompts.</p><p>Figure&nbsp;6 demonstrates the effect of training set size, and shows the DDIM trajectories for a diffusion model trained using a 3 layer ReLU network. We see that the diffusion model on $N=10$ samples &ldquo;memorizes&rdquo; its train set: its trajectories all collapse to one of the train points, instead of producing the underlying spiral distribution. As we add more samples, the model starts to generalize: the trajectories converge to the underlying spiral manifold. The trajectories also start to become more perpendicular the underlying manifold, suggesting that the low dimensional structure is being learned. We also note that in the $N=10$ case where the diffusion model fails, it is not at all obvious a human would be able to identify the &ldquo;correct&rdquo; pattern from these samples, so generalization may be too much to expect.</p><figure style=\"margin:18px 0\"><img src=\"figs/sbs-diffusion/diffusion_progression.png\" alt=\"The DDIM trajectories (shaded by timestep $t$) for a spiral dataset. We compare the trajectories with $10, 20$, and \u0000M4\" loading=\"lazy\" style=\"display:block;margin:12px auto;max-width:100%;height:auto\"><figcaption class=\"vizcap\">The DDIM trajectories (shaded by timestep $t$) for a spiral dataset. We compare the trajectories with $10, 20$, and $40$ training samples. Note that as we add more training points (moving left to right) the diffusion algorithm begins to <i>learn</i> the underlying spiral and the trajectories look more perpendicular to the underlying manifold. The network used here is a 3 layer ReLU network with 128 neurons per layer.</figcaption></figure>"
      },
      {
        "h": "Flow Matching",
        "body": "<figure style=\"margin:18px 0\"><img src=\"figs/sbs-diffusion/cfm_demo_vert.png\" alt=\"Running a flow which generates a spiral distribution (bottom) from\n  an annular distribution (top).\" loading=\"lazy\" style=\"display:block;margin:12px auto;max-width:100%;height:auto\"><figcaption class=\"vizcap\">Running a flow which generates a spiral distribution (bottom) from\n  an annular distribution (top).</figcaption></figure><p>We now introduce the framework of flow matching [peluchetti2022nondenoising, liu2022let, liu2022flow, lipman2023flow, albergo2023stochastic]. Flow matching can be thought of as a generalization of DDIM, which allows for more flexibility in designing generative models&mdash; including for example the <i>rectified flows</i> (sometimes called <i>linear flows</i>) used by Stable Diffusion 3 [liu2022flow, esser2024scaling].</p><p>We have actually already seen the main ideas behind flow matching, in our analysis of DDIM in Section&nbsp;3. At a high level, here is how we constructed a generative model in Section&nbsp;3: <ol class=\"mbul\"><li>First, we defined how to generate a single point. Specifically, we constructed vector fields $\\{\\va\\}_t$ which, when applied for all time steps, transported a standard Gaussian distribution to an arbitrary delta distribution $\\delta_a$.</li><li>Second, we determined how to combine two vector fields into a single effective vector field. This lets us construct a transport from the standard Gaussian to <i>two</i> points (or, more generally, to a <i>distribution</i> over points &mdash; our target distribution).</li></ol> Neither of these steps particularly require the Gaussian base distribution, or the Gaussian forward process (Equation&nbsp;1). The second step of combining vector fields remains identical for any two arbitrary vector fields, for example.</p><p>So let's drop all the Gaussian assumptions. Instead, we will begin by thinking at a basic level about how to map between any two points $x_0$ and $x_1$. Then, we see what happens when the two points are sampled from arbitrary distributions $p$ (data) and $q$ (base), respectively. We will see that this point of view encompasses DDIM as a special case, but that it is significantly more general.</p>"
      },
      {
        "h": "&mdash; Flows",
        "body": "<p>Let us first define the central notion of a <i>flow</i>. A <i>flow</i> is simply a collection of time-indexed vector fields $v = \\{v_t\\}_{t \\in [0, 1]}$. We should think of this as the velocity-field $v_t$ of a gas at each time $t$, as we did earlier in Section&nbsp;3.2. Any flow defines a trajectory taking initial points $x_1$ to final points $x_0$, by transporting the initial point along the velocity fields $\\{v_t\\}$.</p><p>Formally, for flow $v$ and initial point $x_1$, consider the ODE <span class=\"fnote\" style=\"color:var(--ink-dim)\">(The corresponding discrete-time analog is the iteration: $x_{t - \\dt} \\gets x_t + v_t(x_t)\\dt$, starting at $t=1$ with initial point $x_1$.)</span></p><div class=\"formula-box\">$$\\begin{aligned}\n    \\frac{dx_t}{dt} &= -v_t(x_t), \n\\end{aligned}$$</div><p>with initial condition $x_1$ at time $t=1$. We write</p><div class=\"formula-box\">$$\\begin{aligned}\nx_t := \\mathrm{RunFlow}(v, x_1, t)\n\\end{aligned}$$</div><p>to denote the solution to the flow ODE (Equation&nbsp;32) at time $t$, terminating at final point $x_0$. That is, RunFlow is the result of transporting point $x_1$ along the flow $v$ up to time $t$.</p><p>Just as flows define maps between initial and final points, they also define transports between entire <i>distributions</i>, by &ldquo;pushing forward&rdquo; points from the source distribution along their trajectories. If $p_1$ is a distribution on initial points <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Notational warning: Most of the flow matching literature uses a reversed time convention, so $t=1$ is the target distribution. We let $t=0$ be the target distribution to be consistent with the DDPM convention.)</span>, then applying the flow $v$ yields the distribution on final points <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(We could equivalently write this as the pushforward $\\rflow(v, \\cdot, 0) \\sharp p_1$.)</span> $$\np_0 = \\{\\rflow(v, x_1, t=0)\\}_{x_1 \\sim p_1}.\n$$ We denote this process as $\np_1 \\flowto{v} p_0\n$ meaning the flow $v$ transports initial distribution $p_1$ to final distribution <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(In our gas analogy, this means if we start with a gas of particles distributed according to $p_1$, and each particle follows the trajectory defined by $v$, then the final distribution of particles will be $p_0$.)</span> $p_0$.</p><p><b>The ultimate goal of flow matching</b> is to somehow learn a flow $v^*$ which transports $q \\flowto{v^*} p$, where $p$ is the target distribution and $q$ is some easy-to-sample base distribution (such as a Gaussian). If we had this $v^*$, we could generate samples from our target $p$ by first sampling $x_1 \\sim q$, then running our flow with initial point $x_1$ and outputting the resulting final point $x_0$. The DDIM algorithm of Section&nbsp;3 was actually a special case <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(To connect to diffusion: The continuous-time limit of DDIM (31) is a flow with $v_t(x_t) = \\frac{1}{2t} \\E[x_0 - x_t |x_t]$. The base distribution $p_1$ is Gaussian. DDIM Sampling (algorithm 3) is a discretized method for evaluating RunFlow. DDPM Training (algorithm 2) is a method for learning $v^\\star$ &ndash; but it relies on the Gaussian structure and differs somewhat from the flow-matching algorithm we will present in this chapter.)</span> of this, for a very particular choice of flow $v^*$. Now, how do we construct such flows in general?</p>"
      },
      {
        "h": "&mdash; Pointwise Flows",
        "body": "<p>Our basic building-block will be a <i>pointwise flow</i> which just transports a single point $x_1$ to a point $x_0$. Intuitively, given an arbitrary path $\\{x_t\\}_{t \\in [0, 1]}$ that connects $x_1$ to $x_0$, a pointwise flow describes this trajectory by giving its velocity $v_t(x_t)$ at each point $x_t$ along it (see Figure&nbsp;8). Formally, a <i>pointwise flow between $x_1$ and $x_0$</i> is any flow $\\{ v_t \\}_t$ that satisfies Equation&nbsp;32 with boundary conditions $x_1$ and $x_0$ at times $t=1, 0$ respectively. We denote such flows as $\\Tab{x_1}{x_0}$. Pointwise flows are not unique: there are many different choices of path between $x_0$ and $x_1$.</p><figure style=\"margin:18px 0\"><img src=\"figs/sbs-diffusion/pt_flow_fig.svg\" alt=\"A pointwise flow $\\Tab{x_1}{x_0}_t$ transporting $x_1$ to $x_0$.\" loading=\"lazy\" style=\"display:block;margin:12px auto;max-width:100%;height:auto\"><figcaption class=\"vizcap\">A pointwise flow $\\Tab{x_1}{x_0}_t$ transporting $x_1$ to $x_0$.</figcaption></figure>"
      },
      {
        "h": "&mdash; Marginal Flows",
        "body": "<p>Suppose that for all pairs of points $(x_1, x_0)$, we can construct an explicit pointwise flow $\\Tab{x_1}{x_0}$ that transports a source point $x_1$ to target point $x_0$. For example, we could let $x_t$ travel along a straight line from $x_1$ to $x_0$, or along any other explicit path. Recall in our gas analogy, this corresponds to an individual particle that moves between $x_1$ and $x_0$. Now, let us try to set up a collection of individual particles, such that at $t=1$ the particles are distributed according to $q$, and at $t=0$ they are distributed according to $p$. This is actually easy to do: We can pick any coupling <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(A <i>coupling</i> $\\Pi_{q, p}$ between $q$ and $p$, specifies how to jointly sample pairs $(x_1, x_0)$ of source and target points, such that $x_0$ is marginally distributed as $p$, and $x_1$ as $q$. The most basic coupling is the <i>independent coupling</i>, with corresponds to sampling $x_1, x_0$ independently.)</span> $\\Pi_{q,p}$ between $q$ and $p$, and consider particles corresponding to the pointwise flows $\\{ \\Tab{x_1}{x_0} \\}_{(x_1, x_0) \\sim \\Pi_{q,p}}$. This gives us a distribution over pointwise flows (i.e. a collection of particle trajectories) with the desired behavior in aggregate.</p><p>We would like to combine all of these pointwise flows somehow, to get a single flow $v^*$ that implements the same transport between distributions <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Why would we like this? As we will see later, it simplifies our learning problem: instead of having to learn the distribution of all the individual trajectories, we can instead just learn one velocity field representing their bulk evolution.)</span>. Our previous discussion <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Compare to Equation&nbsp;(27) in Section&nbsp;3. A formal statement of how to combine flows is given in Appendix&nbsp;6.4.)</span> in Section&nbsp;3 tells us how to do this: to determine the effective velocity $v_t^*(x_t)$, we should take a weighted-average of all individual particle velocities $\\Tab{x_1}{x_0}_t$, weighted by the probability that a particle at $x_t$ was generated by the pointwise flow $\\Tab{x_1}{x_0}$. The final result is <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(An alternate way of viewing this result at a high level is: we start with pointwise flows $\\Tab{x_1}{x_0}$ which transport delta distributions: $$\n\\delta_{x_1} \\flowto{\\Tab{x_1}{x_0}} \\delta_{x_0}.\n$$ And then Equation&nbsp;(34) gives us a fancy way of &ldquo;averaging these flows over $x_1$ and $x_0$&rdquo;, to get a flow $v^*$ transporting $$\nq = \\E_{x_1 \\sim q}[\\delta_{x_1}] \\flowto{v^*} \\E_{x_0 \\sim p}[\\delta_{x_0}] = p.\n$$)</span></p><div class=\"formula-box\">$$\\begin{aligned}\nv^*_t(x_t) &:= \\E_{x_0, x_1 \\mid x_t}[\\Tab{x_1}{x_0}_t(x_t) \\mid x_t] \n\n\\end{aligned}$$</div><p>where the expectation is w.r.t. the joint distribution of $(x_1, x_0, x_t)$ induced by sampling $(x_1, x_0) \\sim \\Pi_{q,p}$ and letting $x_t \\gets \\mathrm{RunFlow}(\\Tab{x_1}{x_0}, x_1, t)$.</p><p>At this point, we have a &ldquo;solution&rdquo; to our generative modeling problem in principle, but some important questions remain to make it useful in practice: <ul class=\"mbul\"><li>Which pointwise flow $\\Tab{x_1}{x_0}$ and coupling $\\Pi_{q,p}$ should we chose?</li><li>How do we compute the marginal flow $v^*$? We cannot compute it from Equation&nbsp;(34) directly, because this would require sampling from $p(x_0 \\mid x_t)$ for a given point $x_t$, which may be complicated in general.</li></ul> We answer these in the next sections.</p>"
      },
      {
        "h": "&mdash; A Simple Choice of Pointwise Flow",
        "body": "<figure style=\"margin:18px 0\"><img src=\"figs/sbs-diffusion/cfm_circle_linear.png\" alt=\"A marginal flow with linear pointwise flows, base distribution $q$ uniform over an annulus, and target distribution \u0000\" loading=\"lazy\" style=\"display:block;margin:12px auto;max-width:100%;height:auto\"><figcaption class=\"vizcap\">A marginal flow with linear pointwise flows, base distribution $q$ uniform over an annulus, and target distribution $p$ equal to a Dirac-delta at $x_0$. (This can also be thought of as the average over $x_1$ of the pointwise linear flows from $x_1 \\sim q$ to a fixed $x_0$).\n    Gray arrows depict the flow field at different times $t$.\n    The leftmost $(t=1)$ plot shows samples from the base distribution $q$.\n    Subsequent plots show these samples transported by the flow\n    at intermediate times $t$, \n    The final $(t=0)$ plot shows\n    all points collapsed to the target $x_0$.\n    This particular $x_0$ happens to be one point on the spiral distribution of Figure&nbsp;7.\n    </figcaption></figure><p>We need an explicit choices of: pointwise flow, base distribution $q$, and coupling $\\Pi_{q, p}$. There are many simple choices which would work <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Diffusion provides one possible construction, as we will see later in Section 4.6.)</span>.</p><p>The base distribution $q$ can be essentially any easy-to-sample distribution. Gaussians are a popular choice but certainly not the only one&mdash; Figure 7 uses an annular base distribution, for example. As for the coupling $\\Pi_{q,p}$ between the base and target distribution, the simplest choice is the independent coupling, i.e. sampling from $p$ and $q$ independently.</p><p>For a pointwise flow, arguably the simplest construction is a <i>linear pointwise flow</i>:</p><div class=\"formula-box\">$$\\begin{aligned}\n    \\Tab{x_1}{x_0}_t(x_t) &= x_0 - x_1, \\\\\n    \\implies \\mathrm{RunFlow}(\\Tab{x_1}{x_0}, x_1, t) &= tx_1 + (1-t)x_{0}\n    \n\\end{aligned}$$</div><p>which simply linearly interpolates between $x_1$ and $x_0$ (and corresponds to the choice made in [liu2022flow]). In Figure 9 we visualize a marginal flow composed of linear pointwise flows, the same annular base distribution $q$ of Figure 7, and target distribution equal to a point-mass ($p = \\delta_{x_0}$) <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(A marginal distribution with a point-mass target distribution &ndash; or equivalently the average of pointwise flows over the the base distribution only &ndash; is sometimes called a <i>(one-sided) conditional flow</i> [lipman2023flow].)</span>.</p>"
      },
      {
        "h": "&mdash; Flow Matching",
        "body": "<p>Now, the only remaining problem is that naively evaluating $v^*$ using Equation&nbsp;(34) requires sampling from $p(x_0 \\mid x_t)$ for a given $x_t$. If we knew how do this for $t=1$, we would have already solved the generative modeling problem!</p><p>Fortunately, we can take advantage of the same trick from DDPM: it is enough for us to be able to sample from the joint distribution $(x_0, x_t)$, and then solve a regression problem. Similar to DDPM, the conditional expectation function in Equation&nbsp;(34) can be written as a regressor <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(This result is analogous to Theorem 2 in [lipman2023flow], but ours is for a two-sided flow.)</span>:</p><div class=\"formula-box\">$$\\begin{aligned}\nv^*_t(x_t) &:= \\E_{x_0, x_1 \\mid x_t}[\\Tab{x_1}{x_0}_t(x_t) \\mid x_t] \\\\\n\\implies \nv_t^* \n&=\\argmin_{f: \\R^d \\to \\R^d} ~~\\E_{(x_0, x_1, x_t)}\n||f(x_t) -  \\Tab{x_1}{x_0}_t(x_t)||_2^2,\n\n\\end{aligned}$$</div><p>(by using the generic fact that $\\argmin_{f} \\E ||f(x) - y||^2 = \\E[y \\mid x]$).</p><p>In words, Equation&nbsp;(36) says that to compute the loss of a model $f_\\theta$ for a fixed time $t$, we should: <ol class=\"mbul\"><li>Sample source and target points $(x_1, x_0)$ from their joint distribution.</li><li>Compute the point $x_t$ deterministically, by running <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(If we chose linear pointwise flows, for example, this would mean $x_t \\gets tx_1 + (1-t)x_0$, via Equation&nbsp;(35).)</span> the pointwise flow $\\Tab{x_1}{x_0}$ starting from point $x_1$ up to time $t$.</li><li>Evaluate the model's prediction at $x_t$, as $f_\\theta(x_t)$. Evaluate the deterministic vector $\\Tab{x_1}{x_0}_t(x_t)$. Then compute L2 loss between these two quantities.</li></ol></p><p>To sample from the trained model (our estimate of $v_t^*$), we first sample a source point $x_1 \\sim q$, then transport it along the learnt flow to a target sample $x_0$. Pseudocode listings &nbsp;4 and &nbsp;5 give the explicit procedures for training and sampling from flow-based models (including the special case of linear flows for concreteness; matching Algorithm 1 in [liu2022flow].).</p>"
      },
      {
        "h": "&mdash; Summary",
        "body": "<p>To summarize, here is how to learn a flow-matching generative model for target distribution $p$.</p><p></p><p><b>The Ingredients.</b> We first choose: <ol class=\"mbul\"><li>A source distribution $q$, from which we can efficiently sample (e.g. a standard Gaussian).</li><li>A <i>coupling</i> $\\Pi_{q, p}$ between $q$ and $p$, which specifies a way to jointly sample a pair of source and target points $(x_1, x_0)$ with marginals $q$ and $p$ respectively. A standard choice is the independent coupling, i.e. sample $x_1 \\sim q$ and $x_0 \\sim p$ independently.</li><li>For all pairs of points $(x_1, x_0)$, an explicit <i>pointwise flow</i> $\\Tab{x_1}{x_0}$ which transports $x_1$ to $x_0$. We must be able to efficiently compute the vector field $\\Tab{x_1}{x_0}_t$ at all points.</li></ol> These ingredients determine, in theory, a marginal vector field $v^*$ which transports $q$ to $p$:</p><div class=\"formula-box\">$$\\begin{aligned}\n\nv^*_t(x_t) &:= \\E_{x_0, x_1 \\mid x_t}[\\Tab{x_1}{x_0}_t(x_t) \\mid x_t]\n\\end{aligned}$$</div><p>where the expectation is w.r.t. the joint distribution:</p><div class=\"formula-box\">$$\\begin{aligned}\n(x_1, x_0) &\\sim \\Pi_{q,p} \\\\\nx_t &:= \\mathrm{RunFlow}(\\Tab{x_1}{x_0}, x_1, t).\n\\end{aligned}$$</div><p></p><p><b>Training.</b> Train a neural network $f_\\theta$ by backpropogating the stochastic loss function computed by Pseudocode&nbsp;4. The optimal function for this expected loss is: $f_\\theta(x_t, t) = v^*_t(x_t)$.</p><p></p><p><b>Sampling.</b> Run Pseudocode&nbsp;5 to generate a sample $x_0$ from (approimately) the target distribution $p$.</p><p>[t]{0.47}</p><div class=\"card\" style=\"border-left:3px solid var(--accent);padding:10px 14px\"><p><b>Algorithm.</b> Flow-matching train loss, generic pointwise flow {[or linear flow]} </p><div style=\"font-family:var(--font-mono);font-size:14px;line-height:1.8\">[H]<br><br><br><b>Input:</b> Neural network $f_\\theta$<br><b>Data:</b> Sample-access to coupling $\\Pi_{q, p}$<br>Pointwise flows $\\{\\Tab{x_1}{x_0}_t\\}$ for all $x_1, x_0$.}<br><b>Output:</b> Stochastic loss $L$<br>$(x_1, x_0) \\gets \\mathrm{Sample}( \\Pi_{q,p} )$ \\;<br>$t \\gets \\textrm{Unif}[0, 1]$ \\;<br>$x_t \\gets \\mathcolor{red}{\\underbrace{\\mathcolor{black}{\\mathrm{RunFlow}(\\Tab{x_1}{x_0}, x_1, t)}}_{tx_1 + (1-t)x_{0}}}$  \\;<br>$L \\gets \\Big\\Vert f_\\theta(x_t, t) -  \\mathcolor{red}{\\underbrace{\\mathcolor{black}{\\Tab{x_1}{x_0}_t(x_t)}}_{(x_0 - x_1)}} \\Big\\Vert_2^2$ \\;<br>$L$</div></div><p>[t]{0.47}</p><div class=\"card\" style=\"border-left:3px solid var(--accent);padding:10px 14px\"><p><b>Algorithm.</b> Flow-matching sampling</p><div style=\"font-family:var(--font-mono);font-size:14px;line-height:1.8\">[H]<br><br><br><b>Input:</b> Trained network $f_\\theta$<br><b>Data:</b> Sample-access to base distribution $q$; step-size $\\dt$.<br><b>Output:</b> Sample from target distribution $p$.<br>$x_1 \\gets \\mathrm{Sample}(q)$ \\;<br>{$t=1,~(1-\\dt),(1-2\\dt), \\dots, \\dt$}{<br>$x_{t-\\dt} \\gets x_t + f_\\theta(x_t, t)\\dt$ \\;<br>}<br>$x_0$</div></div>"
      },
      {
        "h": "&mdash; DDIM as Flow Matching [Optional]",
        "body": "<p>The DDIM algorithm of Section&nbsp;3 can be seen as a special case of flow matching, for a particular choice of pointwise flows and coupling. We describe the exact correspondence here, which will allow us to notice an interesting relation between DDIM and linear flows.</p><p>We claim DDIM is equivalent to flow-matching with the following parameters: <ol class=\"mbul\"><li>{ Pointwise Flows:} Either of the two equivalent pointwise flows: $$\n    \\label{eqn:ptwise_ddim}\n    \\Tab{x_1}{x_0}_t(x_t) := \\frac{1}{2t} (x_t - x_0)\n    $$ or $$\n    \\label{eqn:ptwise_ddim2}\n    \\Tab{x_1}{x_0}_t(x_t)\n    := \\frac{1}{2\\sqrt{t}}(x_0 - x_1),\n    $$ which both generate the trajectory <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(See Appendix&nbsp;6.6 for details on why &nbsp;(4.6) and&nbsp;(4.6) are equivalent along their trajectories.)</span>: $$\n    \\label{eqn:ptwise_traj}\n    x_t = x_0 + (x_1 - x_0)\\sqrt{t}.\n    $$</li><li>{ Coupling:} The &ldquo;diffusion coupling&rdquo; &ndash; that is, the joint distribution on $(x_0, x_1)$ generated by $$\n    \\label{eqn:ddpm_coupling}\n    x_0 \\sim p; \\quad x_1 \\gets x_0 + \\cN(0, \\sigma_q^2).\n    $$</li></ol> This claim is straightforward to prove (see Appendix&nbsp;6.5), but the implication is somewhat surprising: we can recover the DDIM trajectories (which are not straight in general) as a combination of the <i>straight</i> pointwise trajectories in Equation&nbsp;(4.6). In fact, the DDIM trajectories are exactly equivalent to flow-matching trajectories for the above linear flows, with a different scaling of time ($\\sqrt{t}$ vs. $t$) <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(DDIM at time $t$ corresponds to the linear flow at time $\\sqrt{t}$; thus linear flows are &ldquo;slower&rdquo; than DDIM when $t$ is small. This may be beneficial for linear flows in practice (speculatively).)</span>. [nobreak]</p><div class=\"card\"><p><b>Claim.</b></p><p>[DDIM as Linear Flow; Informal] {The DDIM sampler (Algorithm 2)} is equivalent, up to time-reparameterization, to the marginal flow produced by linear pointwise flows (Equation&nbsp;35) with the diffusion coupling (Equation&nbsp;4.6).</p></div><p>A formal statement of this claim <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(In practice, linear flows are most often instantiated with the independent coupling, not the above &ldquo;diffusion coupling.&rdquo; However, for large enough terminal variance $\\sigma_q^2$, the diffusion coupling is close to independent. Therefore, Claim&nbsp;4.6 tells us that the common practice in flow matching (linear flows with a Gaussian terminal distribution and independent coupling) is nearly equivalent to standard DDIM, with a different time schedule. Finally, for the experts: this is a claim about the &ldquo;variance exploding&rdquo; version of DDIM, which is what we use throughout. Claim&nbsp;4.6 is false for variance-preserving DDIM.)</span> is provided in Appendix&nbsp;6.7.</p>"
      },
      {
        "h": "&mdash; Additional Remarks and References [Optional]",
        "body": "<figure style=\"margin:18px 0\"><img src=\"figs/sbs-diffusion/cfm_demo_traj.png\" alt=\"The trajectories of individual samples $x_1 \\sim q$ for the flow in Figure&amp;nbsp;7.\" loading=\"lazy\" style=\"display:block;margin:12px auto;max-width:100%;height:auto\"><figcaption class=\"vizcap\">The trajectories of individual samples $x_1 \\sim q$ for the flow in Figure&nbsp;7.</figcaption></figure><ul class=\"mbul\"><li>See Figure&nbsp;11 for a diagram of the different methods described in this tutorial, and their relations.</li><li>We highly recommend the flow-matching tutorial of [mathieu2024flow], which includes helpful visualizations of flows, and uses notation more consistent with the current literature.</li><li>As a curiosity, note that we never had to define an explicit &ldquo;forward process&rdquo; for flow-matching, as we did for Gaussian diffusion. Rather, it was enough to define the appropriate &ldquo;reverse processes&rdquo; (via flows).</li><li>What we called <i>pointwise flows</i> are also called <i>two-sided conditional flows</i> in the literature, and was developed in [albergo2022building, pooladian2023multisample, liu2022flow, tong2023simulation].</li><li>[albergo2023stochastic] define the framework of <i>stochastic interpolants</i>, which can be thought of as considering stochastic pointwise flows, instead of only deterministic ones. Their framework strictly generalizes both DDPM and DDIM.</li><li>See [stark2024dirichlet] for an interesting example of non-standard flows. They derive a generative model for discrete spaces by embedding into a continuous space (the probability simplex), then constructing a special flow on these simplices.</li></ul>"
      },
      {
        "h": "Diffusion in Practice",
        "body": "<p>To conclude, we mention some aspects of diffusion which are important in practice, but were not covered in this tutorial.</p><p></p><p><b>Samplers in Practice.</b> Our DDPM and DDIM samplers (algorithms 2 and 3) correspond to the samplers presented in [ho2020denoising] and [song2021denoising], respectively, but with different choice of schedule and parametrization (see footnote 2). DDPM and DDIM were some of the earliest samplers to be used in practice, but since then there has been significant progress in samplers for fewer-step generation (which is crucial since each step requires a typically-expensive model forward-pass). <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(Even the best samplers still require around $10$ sampling steps, which may be impractical. A variety of <i>time distillation</i> methods seek to train one-step-generator student models to match the output of diffusion teacher models, with the goal of high-quality sampling in one (or few) steps. Some examples include consistency models [song2023consistency] and adversarial distillation methods [lin2024sdxl, xu2023ufogen, sauer2024fast]. Note, however, that the distilled models are no longer diffusion models, nor are their samplers (even if multi-step) diffusion samplers.)</span> In sections 2.4 and 3.4, we showed that DDPM and DDIM can be seen as discretizations of the reverse SDE and Probability Flow ODE, respectively. The SDE and ODE perspectives automatically lead to many samplers corresponding to different black-box SDE and ODE numerical solvers (such as Euler, Heun, and Runge-Kutta). It is also possible to take advantage of the specific structure of the diffusion ODE, to improve upon black-box solvers [lu2022dpm, lu2022dpmplusplus, zhang2023fast].</p><p></p><p><b>Noise Schedules.</b> The <i>noise schedule</i> typically refers to $\\sigma_t$, which determines the amount of noise added at time $t$ of the diffusion process. The simple diffusion (1) has $p(x_t) \\sim \\cN(x_0, \\sigma_t^2)$ with $\\sigma_t \\propto \\sqrt{t}$. Notice that the variance of $x_t$ increases at every timestep. <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">([song2020score] made the distinction between &ldquo;variance-exploding&rdquo; (VE) and &ldquo;variance-preserving&rdquo; (VP) schedules while comparing SMLD [song2019generative] and DDPM [ho2020denoising]. The terms VE and VP often refer specifically to SMLD and DDPM, respectively. Our diffusion (1) could also be called a variance-exploding schedule, though our noise schedule differs from the one originally proposed in [song2019generative].)</span></p><p>In practice, schedules with controlled variance are often preferred. One of the most popular schedules, introduced in [ho2020denoising], uses a time-dependent variance and scaling such that the variance of $x_t$ remains bounded. Their discrete update is</p><div class=\"formula-box\">$$\\begin{aligned}\nx_t &= \\sqrt{1-\\beta(t)} x_{t-1} + \\sqrt{\\beta(t)} \\epsilon_t; \\quad \\epsilon_t \\sim \\cN(0, 1),\n\n\\end{aligned}$$</div><p>where $0 < \\beta(t)< 1$ is chosen so that $x_t$ is (very close to) clean data at $t=1$ and pure noise at $t=T$.</p><p>The general SDE (15) introduced in 2.4 offers additional flexibility. Our simple diffusion (1) has $f = 0$, $g = \\sigma_q$, while the diffusion (38) of [ho2020denoising] has $f = -\\frac{1}{2} \\beta(t)$, $g = \\sqrt{\\beta(t)}$. [karras2022elucidating] reparametrize the SDE in terms of an overall scaling $s(t)$ and variance $\\sigma(t)$ of $x_t$, as a more interpretable way to think about diffusion designs, and suggest a schedule with $s(t) = 1$, $\\sigma(t) = t$ (which corresponds to $f=0$, $g = \\sqrt{2t}$). Generally, the choice of $f, g$, or equivalently $s, \\sigma$, offers a convenient way to explore the design-space of possible schedules.</p><p></p><p><b>Likelihood Interpretations and VAEs.</b> One popular and useful interpretation of diffusion models is the Variational Auto Encoder (VAE) perspective <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(This was actually the original approach to derive the diffusion objective function, in [OGdiffusion] and also [ho2020denoising].)</span>. Briefly, diffusion models can be viewed as a special case of a deep hierarchical VAE, where each diffusion timestep corresponds to one &ldquo;layer&rdquo; of the VAE decoder. The corresponding VAE encoder is given by the forward diffusion process, which produces the sequence of noisy $\\{x_t\\}$ as the &ldquo;latents&rdquo; for input $x$. Notably, the VAE encoder here is not learnt, unlike usual VAEs. Because of the Markovian structure of the latents, each layer of the VAE decoder can be trained in isolation, without forward/backward passing through all previous layers; this helps with the notorious training instability of deep VAEs. We recommend the tutorials of [turner_blog] and [luo2022understanding] for more details on the VAE perspective.</p><p>One advantage of the VAE interpretation is, it gives us an estimate of the <i>data likelihood</i> under our generative model, by using the standard Evidence-Based-Lower-Bound (ELBO) for VAEs. This allows us to train diffusion models directly using a maximum-likelihood objective. It turns out that the ELBO for the diffusion VAE reduces to exactly the L2 regression loss that we presented, but with a particular <i>time-weighting</i> that weights the regression loss differently at different time-steps $t$. For example, regression errors at large times $t$ (i.e. at high noise levels) may need to be weighted differently from errors at small times, in order for the overall loss to properly reflect a likelihood. <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(See also Equation (5) in [kadkhodaie2024generalization] for a simple bound on KL divergence between the true distribution and generated distribution, in terms of regression excess risks.)</span> The best choice of time-weighting in practice, however, is still up for debate: the &ldquo;principled&rdquo; choice informed by the VAE interpretation does not always produce the best generated samples <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(For example, [ho2020denoising] drops the time-weighting terms, and just uniformly weights all timesteps.)</span>. See [kingma2023understanding] for a good discussion of different weightings and their effect.</p><p></p><p><b>Parametrization: $x_0$ / $\\epsilon$ / v -prediction.</b> Another important practical choice is which of several closely-related quantities &ndash; partially-denoised data, fully-denoised data, or the noise itself &ndash; we ask the network to predict. <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(More accurately, the network always predicts conditional expectations of these quantities.)</span> Recall that in DDPM Training (Algorithm 1), we asked the network $f_\\theta$ to learn to predict $\\E[x_{t-\\dt} | x_t]$ by minimizing $\\|f_\\theta(x_t, t) - x_{t-\\dt}\\|_2^2$. However, other parametrizations are possible. For example, recalling that $\\E[x_{t-\\dt} - x_t | x_t] \\overset{\\text{eq. } 23}{=} \\frac{\\dt}{t} \\E[x_0 - x_t | x_t]$, we see that that</p><div class=\"formula-box\">$$\\begin{aligned}\n    \\underset{\\theta}{\\min} &\\quad \\|f_\\theta(x_t, t) - x_0\\|_2^2 \\implies f^\\star_\\theta(x_t, t) = \\E[x_0 | x_t]\n\\end{aligned}$$</div><p>is a (nearly) equivalent problem, which is often called <i>$x_0$-prediction</i>. <span class=\"fnote\" style=\"color:var(&ndash;ink-dim)\">(This corresponds to the variance-reduced algorithm (6).)</span> The objectives differ only by a time-weighting factor of $\\frac{1}{t}$. Similarly, defining the <i>noise</i> $\\epsilon_t = \\frac{1}{\\sigma_t} \\E[x_0 - x_t | x_t]$, we see that we could alternatively ask the the network to predict $\\E[\\epsilon_t | x_t]$: this is usually called <i>$\\epsilon$-prediction</i>. Another parametrization, <i>v-prediction</i>, asks the model to predict $v = \\alpha_t \\epsilon - \\sigma_t x_0$ [salimans2022progressive] &ndash; mostly predicting data for high noise-levels and mostly noise for low noise-levels. All the parametrizations differ only by time-weightings (see Appendix 6.10 for more details).</p><p>Although the different time-weightings do not affect the optimal solution, they do impact training as discussed above. Furthermore, even if the time-weightings are adjusted to yield equivalent problems in principle, the different parametrizations may behave differently in practice, since learning is not perfect and certain objectives may be more robust to error. For example, $x_0$-prediction combined with a schedule that places a lot of weight on low noise levels may not work well in practice, since for low noise the identity function can achieve a relatively low objective value, but clearly is not what we want.</p><p></p><p><b>Sources of Error.</b> Finally, when using diffusion and flow models in practice, there are a number of sources of error which prevent the learnt generative model from exactly producing the target distribution. These can be roughly segregated into training-time and sampling-time errors. <ol class=\"mbul\"><li>Train-time error: Regression errors in learning the population-optimal regression function. The regression objective is the marginal flow $v^*_t$ in flow-matching, or the scores $\\E[x_0 \\ \\mid x_t]$ in diffusion models. For each fixed time $t$, this a standard kind of statistical error. It depends on the neural network architecture and size as well as the number of samples, and can be decomposed further into approximation and estimation errors in the usual way (e.g. see [advani2020high, Sec. 4] decomposing a 2-layer network into approximation error and over-fitting error).</li><li>Sampling-time error: Discretization errors from using finite step-sizes $\\dt$. This error is exactly the discretization error of the ODE or SDE solver used in sampling. These errors manifest in different ways: for DDPM, this reflects the error in using a Gaussian approximation of the reverse process (i.e. Fact 1 breaks for large $\\sigma$). For DDIM and flow matching, it reflects the error in simulating continuous-time flows in discrete time.</li></ol> These errors interact and compound in nontrivial ways, which are not yet fully understood. For example, it is not clear exactly how train-time error in the regression estimates translates into distributional error of the entire generative model. (And this question itself is complicated, since it is not always clear what type of distributional divergence we care about in practice). Interestingly, these &ldquo;errors&rdquo; can also have a beneficial effect on small train sets, because they act as a kind of <i>regularization</i> which prevents the diffusion model from just memorizing the train samples (as discussed in Section&nbsp;3.6).</p>"
      },
      {
        "h": "&mdash; Conclusion",
        "body": "<p>We have now covered the basics of diffusion models and flow matching. This is an active area of research, and there are many interesting aspects and open questions which we did not cover (see Page&nbsp;6 for recommended reading). We hope the foundations here equip the reader to understand more advanced topics in diffusion modeling, and perhaps contribute to the research themselves.</p><figure style=\"margin:18px 0\"><img src=\"figs/sbs-diffusion/concept_map.png\" alt=\"Commutative diagram of the different reverse samplers described in this tutorial, and their relations.\n    Each determin\" loading=\"lazy\" style=\"display:block;margin:12px auto;max-width:100%;height:auto\"><figcaption class=\"vizcap\">Commutative diagram of the different reverse samplers described in this tutorial, and their relations.\n    Each deterministic sampler produces identical marginal distributions as its stochastic counterpart.\n    There are also various ways to construct stochastic versions of flows,\n    which are not pictured here (e.g. [albergo2023stochastic]).\n    </figcaption></figure>"
      },
      {
        "h": "Additional Resources",
        "body": "<p>Several other helpful resources for learning diffusion (tutorials, blogs, papers), roughly in order of mathematical background required.</p><p></p><ol class=\"mbul\"></ol><p>\\end{fullwidth}</p>"
      },
      {
        "h": "&mdash; KL Error in Gaussian Approximation of Reverse Process",
        "body": "<p>Here we prove Lemma&nbsp;12, restated below.</p><div class=\"card\"><p><b>Lemma.</b></p><p>Let $p(x)$ be an arbitrary density over $\\R$, with bounded 1st to 4th order derivatives. Consider the joint distribution $(x_0, x_1)$, where $x_0 \\sim p$ and $x_1 \\sim x_0 + \\cN(0, \\sigma^2)$. Then, for any conditioning $z \\in \\R$ we have $$\n\\mathrm{KL}\\left(\n\\cN(\\mu_z, \\sigma^2 ) \n~~||~~\np_{x_0 \\mid x_1}(\\cdot \\mid x_1 = z)\n\\right)\n\\leq O(\\sigma^4)\n$$ where $$\n\\mu_z := z + \\sigma^2 \\nabla \\log p(z).\n$$</p></div><div class=\"card\"><p><b>Proof.</b></p><p>WLOG, we can take $z=0$. We want to estimate the KL:</p><div class=\"formula-box\">$$\\begin{aligned}\nKL(  \\cN(\\mu, \\sigma^2) || p(x_0 = \\cdot \\mid x_1 = 0) ) \n\\end{aligned}$$</div><p>where we will let $\\mu$ be arbitrary for now.</p><p>Let $q := \\cN(\\mu, \\sigma^2)$, and $p(x) =: \\exp(F(x))$. We have $x_1 \\sim p \\star \\cN(0, \\sigma^2)$. This implies: $$\np(x_1 = x) = \\E_{\\eta \\sim \\cN(0, \\sigma^2)}[ p(x + \\eta) ].\n$$</p><p>Let us first expand the logs of the two distributions we are comparing:</p><div class=\"formula-box\">$$\\begin{aligned}\n&\\log p(x_0 = x \\mid x_1 = 0)   \\\\\n&= \n\\log p(x_1 = 0 | x_0 = x) + \\log p(x_0 = x) - \\log p(x_1 = 0) \\\\\n&=\n-\\log(\\sigma \\sqrt{2\\pi})\n-0.5 x^2 \\sigma^{-2}\n+ \\log p(x_0 = x) - \\log p(x_1 = 0) \\\\\n&=\n-\\log(\\sigma \\sqrt{2\\pi})\n-0.5 x^2 \\sigma^{-2}\n+ F(x) - \\log p(x_1 = 0) \\\\\n\\end{aligned}$$</div><p>And also:</p><div class=\"formula-box\">$$\\begin{aligned}\n\\log q(x) &=\n-\\log(\\sigma \\sqrt{2\\pi})\n-0.5 (x-\\mu)^2 \\sigma^{-2}\n\\end{aligned}$$</div><p>Now we can expand the KL:</p><div class=\"formula-box\">$$\\begin{align}\n&KL( q || p(x_0 = \\cdot \\mid x_1 = 0))\\\\\n&= \n\\E_{x \\sim q}[\n\\log q(x) - \\log p(x_0 = x \\mid x_1 = 0)\n]\\\\\n&= \n\\E_{x \\sim q}[\n-\\log(\\sigma \\sqrt{2\\pi})\n-0.5 (x-\\mu)^2 \\sigma^{-2}\n-(-\\log(\\sigma \\sqrt{2\\pi})\n-0.5 x^2 \\sigma^{-2}\n+ F(x) - \\log p(x_1 = 0))\n]\\\\\n&= \n\\E_{x \\sim q}[\n-0.5 (x-\\mu)^2 \\sigma^{-2}\n+0.5 x^2 \\sigma^{-2}\n- F(x) + \\log p(x_1 = 0))\n]\\\\\n&= \n\\E_{\\eta \\sim \\cN(0, \\sigma^2); x = \\mu+\\eta}[\n-0.5 \\eta^2 \\sigma^{-2}\n+0.5 x^2 \\sigma^{-2}\n- F(x) + \\log p(x_1 = 0))\n]\n\\tag{work}\n\\\\\n&= \n-0.5 \\E[\\eta^2] \\sigma^{-2}\n+0.5 \\E[x^2] \\sigma^{-2}\n- \\E[F(x)] + \\log p(x_1 = 0))\n]\\\\\n&= \n-0.5 \\sigma^2 \\sigma^{-2}\n+0.5 (\\sigma^2 + \\mu^2) \\sigma^{-2}\n- \\E[F(x)]\n+ \\log p(x_1 = 0))\n]\\\\\n&=\n0.5 \\mu^2 \\sigma^{-2}\n+ \\log p(x_1 = 0) \n- \\E_{x\\sim q}[F(x)] \\\\\n&\\approx\n0.5 \\mu^2 \\sigma^{-2}\n+ \\log p(x_1 = 0)\n- \\E_{x \\sim q}[\nF(0) + F'(0)x + 0.5F''(0)x^2 + O(x^3) + O(x^4)] \\\\\n&= \n\\log p(x_1 = 0)\n+ 0.5 \\mu^2 \\sigma^{-2}\n- F(0)\n- F'(0)\\mu - 0.5F''(0)(\\mu^2 + \\sigma^2) + O(\\sigma^2\\mu + \\mu^2 + \\sigma^4) \n\\end{align}$$</div><p>We will now estimate the first term, $\\log p(x_1=0)$:</p><div class=\"formula-box\">$$\\begin{align}\n&\\log p(x_1 = 0) \\\\\n&= \\log \\E_{\\eta \\sim \\cN(0, \\sigma^2)}[p(\\eta)] \\\\\n&= \\log \\E_{\\eta \\sim \\cN(0, \\sigma^2)}[p(0) + p'(0)\\eta + 0.5p''(0)\\eta^2 + O(\\eta^3) + O(\\eta^4)] \\\\\n&= \\log \\left( p(0) + 0.5p''(0)\\sigma^2 + O(\\sigma^4) \\right)\\\\\n&= \\log p(0)  + \\frac{0.5p''(0)\\sigma^2 + O(\\sigma^4)}{p(0)} + O( \\sigma^4 )\n\\tag{Taylor expand $\\log(p(0) + \\eps)$ around $p(0)$} \\\\\n\n&= \\log p(0)  + 0.5\\sigma^2\\frac{p''(0)}{p(0)} + O( \\sigma^4 )\n\\end{align}$$</div><p>To compute the derivatives of $p$, observe that:</p><div class=\"formula-box\">$$\\begin{aligned}\nF(x) &= \\log p(x) \\\\\n\\implies \nF'(x) &= p'(x)/p(x) \\\\\n\\implies\nF''(x) &= p''(x)/p(x) - (p'(x)/p(x))^2 \\\\\n&= p''(x)/p(x) - (F'(x))^2 \\\\\n\\implies \np''(x)/p(x)\n&= F''(x) + (F'(x))^2\n\n\\end{aligned}$$</div><p>Thus, continuing from line&nbsp;(43):</p><div class=\"formula-box\">$$\\begin{align}    \n\\log p(x_1 = 0) &= \\log p(0)  + 0.5\\sigma^2\\frac{p''(0)}{p(0)} + O( \\sigma^4 )\\\\\n&= F(0)  + 0.5\\sigma^2(F''(0) - F'(0)^2)  + O( \\sigma^4 ) \\tag{by Line~103}\n\\end{align}$$</div><p>We can now plug this estimate of $\\log p(x_1=0)$ into Line&nbsp;(42). We omit the argument $(0)$ from $F$ for simplicity:</p><div class=\"formula-box\">$$\\begin{aligned}\n&KL( q || p(x_0 = \\cdot \\mid x_1 = 0))\\\\\n&=\\boxed{\\log p(x_1 = 0)}\n+ 0.5 \\mu^2 \\sigma^{-2}\n- F\n- F'\\mu - 0.5F''(\\mu^2 + \\sigma^2) + O(\\mu^4 + \\sigma^4)  \\\\\n&=\nF + 0.5\\sigma^2(F'' + F'^2)\n+ 0.5 \\mu^2 \\sigma^{-2}\n- F\n- F'\\mu - 0.5F''(\\mu^2 + \\sigma^2) + O(\\mu^4 + \\sigma^4)  \\\\\n&=\n+ 0.5\\sigma^2F''\n+ 0.5\\sigma^2F'^2\n+ 0.5 \\mu^2 \\sigma^{-2}\n- F'\\mu - 0.5F''\\mu^2 \n-0.5F''\\sigma^2\n+ O(\\mu^4 + \\sigma^4)   \\\\\n&=\n- F'\\mu \n+ 0.5 \\mu^2 \\sigma^{-2}\n+ 0.5F'^2\\sigma^2\n- 0.5F''\\mu^2 \n+ O(\\mu^4 + \\sigma^4) \n\\end{aligned}$$</div><p>Up to this point, $\\mu$ was arbitrary. We now set $$\n\\mu_* := F'(0)\\sigma^2.\n$$</p><p>And continue:</p><div class=\"formula-box\">$$\\begin{aligned}\n&KL( q || p(x_0 = \\cdot \\mid x_1 = 0))\\\\\n&=\n- F'\\mu_*\n+ 0.5 \\mu_*^2 \\sigma^{-2}\n+ 0.5F'^2\\sigma^2\n- 0.5F''\\mu_*^2 \n+ O(\\mu_*^4 + \\sigma^4)\\\\\n&=\n- F'^2\\sigma^2\n+ 0.5 F'^2 \\sigma^2\n+ 0.5F'^2\\sigma^2\n+ O(\\sigma^4)  \\\\\n&= O(\\sigma^4)\n\\end{aligned}$$</div><p>as desired.</p></div><p>Notice that our choice of $\\mu_*$ in the above proof was crucial; for example if we had set $\\mu_*=0$, the $\\Omega(\\sigma^2)$ terms in Line&nbsp;(47) would not have cancelled out.</p>"
      },
      {
        "h": "&mdash; SDE proof sketches",
        "body": "<p>Here is sketch of the proof of the equivalence of the SDE and Probability Flow ODE, which relies on the equivalence of the SDE to a Fokker-Planck equation. (See [song2020score] for full proof.)</p><div class=\"card\"><p><b>Proof.</b></p><div class=\"formula-box\">$$\\begin{aligned}\n    dx &= f(x,t)dt + g(t)dw \\\\\n    \\iff \\frac{\\partial p_t(x)}{\\partial t} &= -\\nabla_x (f p_t) + \\frac{1}{2} g^2 \\nabla_x^2 p_t \\quad \\text{(FP)} \\\\\n    &= -\\nabla_x (f p_t) + \\frac{1}{2} g^2 \\nabla_x (p_t \\nabla_x \\log p_t) \\\\\n    &= -\\nabla_x \\{ (f - \\frac{1}{2} g^2 \\nabla_x \\log p_t) p_t \\} \\\\\n    &= -\\nabla_x \\{ \\tilde f(x, t) p_t(x) \\}, \\quad \\tilde f(x, t) = f(x,t) - \\frac{1}{2} g(t)^2 \\nabla_x \\log p_t(x) \\\\\n    \\implies dx &= \\tilde f(x, t) dt\n\\end{aligned}$$</div></div><p>The equivalence of the SDE and Fokker-Planck equations follows from It\\^{o}'s formula and integration-by-parts. Here is an outline for a simplified case in 1d, where $g$ is constant (see [winkler_blog_fp] for full proof):</p><div class=\"card\"><p><b>Proof.</b></p><div class=\"formula-box\">$$\\begin{aligned}\n    dx &= f(x) dt + g dw, \\quad dw \\sim \\sqrt{dt} \\mathcal{N}(0,1) \\\\\n    \\text{For any $\\phi$:} \\quad d\\phi(x) \n    &= \\biggr( f(x) \\partial_x \\phi(x) + \\frac{1}{2} g^2 \\partial_x^2 \\phi(x) \\biggr) dt + g \\partial_x \\phi(x) dw \\mathnote{It\\^{o}'s formula} \\\\\n    \\implies \\frac{d}{dt} \\E[\\phi] &= \\E[f \\partial_x \\phi + \\frac{1}{2} g^2 \\partial_x^2 \\phi], \\quad (\\E[dw] = 0) \\\\ \n    \\int \\phi(x) \\partial_t p(x,t) dx &= \\int f(x) \\partial_x \\phi(x) p(x,t) dx + \\frac{1}{2} g^2 \\int \\partial_x^2 \\phi(x) p(x,t) dx \\\\\n    &= - \\int \\phi(x) \\partial_x (f(x) p(x,t)) dx + \\frac{1}{2} g^2 \\int \\phi(x) \\partial_x^2 p(x,t) dx, \\mathnote{integration-by-parts} \\\\\n    \\partial_t p(x) &= - \\partial_x (f(x) p(x,t)) + \\frac{1}{2} g^2 \\partial_{x}^2 p(x), \\mathnote{Fokker-Planck} \\\\\n    \\end{aligned}$$</div></div>"
      },
      {
        "h": "&mdash; DDIM Point-mass Claim",
        "body": "<p>Here is a version of Claim&nbsp;23 where $p_0$ is a delta at an arbitrary point $x_0$.</p><div class=\"card\"><p><b>Claim.</b></p><p>Suppose the target distribution is a point mass at $x_0 \\in \\R^d$, i.e. $p_0 = \\delta_{x_0}$. Define the function</p><div class=\"formula-box\">$$\\begin{aligned}\n   \\rG(x_t) = \\left(\\frac{\\sigma_{t-\\dt}}{\\sigma_t}\\right)(x_t - x_0) + x_0.\n\\end{aligned}$$</div><p>Then we clearly have $\\rG \\sharp p_t = p_{t-\\dt}$, and moreover $$\n    \\label{eqn:FtExp}\n    \\rG(x_t)\n    = \n    x_t + \\lambda(\\E[x_{t - \\dt} \\mid x_t] - x_t)\n    =: F_t(x_t).\n    $$ Thus Algorithm 2 defines a valid reverse sampler for target distribution $p_0 = \\delta_{x_0}.$</p></div>"
      },
      {
        "h": "&mdash; Flow Combining Lemma",
        "body": "<p>Here we provide a more formal statement of the marginal flow result stated in Equation (34).</p><p>Equation&nbsp;(34) follows from a more general lemma (Lemma&nbsp;6.4) which formalizes the &ldquo;gas combination&rdquo; analogy of Section&nbsp;3. The motivation for this lemma is, we need a way of combining flows: of taking several different flows and producing a single &ldquo;effective flow.&rdquo; As a warm-up for the lemma, suppose we have $n$ different flows, each with their own initial and final distributions $q_i, p_i$:</p><div class=\"formula-box\">$$\\begin{aligned}\nq_1 \\flowto{v^{(1)}} p_1, \\quad\nq_2 \\flowto{v^{(2)}} p_2, \\quad\n\\ldots, \\quad\nq_n \\flowto{v^{(n)}} p_n\n\\end{aligned}$$</div><p>We can imagine these as the flow of $n$ different gases, where gas $i$ has initial density $q_i$ and final density $p_i$. Now we want to construct an overall flow $v^*$ which takes the average initial-density to the average final-density: $$\n\\E_{i \\in [n]}[q_i]\n\\flowto{v^*}\n\\E_{i \\in [n]}[p_i].\n$$ To construct $v^*_t(x_t)$, we must take an average of the individual vector fields $v^{(i)}$, weighted by the probability mass the $i$-th flow places on $x_t$, at time $t$. (This is exactly analogous to Figure&nbsp;5).</p><p>This construction is formalized in Lemma&nbsp;6.4. There, instead of averaging over just a finite set of flows, we are allowed to average over any <i>distribution</i> over flows. To recover Equation&nbsp;(34), we can apply Lemma&nbsp;6.4 to a distribution $\\Gamma$ over $(v, q_v) = (\\Tab{x_1}{x_0}, \\delta_{x_1})$, that is, pointwise flows and their associated initial delta distributions.</p><p>[nobreak]</p><div class=\"card\"><p><b>Lemma.</b></p><p>[Flow Combining Lemma] Let $\\Gamma$ be an arbitrary joint distribution over pairs $(v, q_v)$ of flows $v$ and their associated initial distributions $q_v$. Let $v(q_v)$ denote the final distribution when initial distribution $q_v$ is transported by flow $v$, so $q_v \\flowto{v} v(q_v)$</p><p>For fixed $t \\in [0, 1]$, consider the joint distribution over $(x_1, x_t, w_t) \\in (\\R^d)^3$ generated by:</p><div class=\"formula-box\">$$\\begin{aligned}\n(v, q_v) &\\sim \\Gamma \\\\\nx_1 &\\sim q_v \\\\\nx_t &:= \\mathrm{RunFlow}(v, x_1, t) \\\\\nw_t &:= v_t(x_t).\n\\end{aligned}$$</div><p>Then, taking all expectations w.r.t. this joint distribution, the flow $v^*$ defined as</p><div class=\"formula-box\">$$\\begin{aligned}\nv^*_t(x_t) &:= \\E[w_t \\mid x_t] \\\\\n&= \\E[v_t(x_t) \\mid x_t] \n\\end{aligned}$$</div><p>is known as the <i>marginal flow for $\\Gamma$</i>, and transports:</p><div class=\"formula-box\">$$\\begin{aligned}\n\\E[q_v]\n\\flowto{v^*}\n\\E[ v(q_v) ].\n\\end{aligned}$$</div></div>"
      },
      {
        "h": "&mdash; Derivation of DDIM using Flows",
        "body": "<p>Now that we have the machinery of flows in hand, it is fairly easy to derive the DDIM algorithm &ldquo;from scratch&rdquo;, by extending our simple scaling algorithm from the single point-mass case.</p><p>First, we need to find the pointwise flow. Recall from Claim 6.3 that for the simple case where the target distribution $p_0$ is a Dirac-delta at $x_0$, the following scaling maps $p_t$ to $p_{t-\\dt}$:</p><div class=\"formula-box\">$$\\begin{aligned}\n    \\rG(x_t) = \\left(\\frac{\\sigma_{t-\\dt}}{\\sigma_t}\\right)(x_t - x_0) + x_0 \n    \\implies \n    G_t \\sharp p_t = p_{t-\\dt}.\n\\end{aligned}$$</div><p>$G_t$ implies the pointwise flow:</p><div class=\"formula-box\">$$\\begin{aligned}\n    \\lim_{t \\to 0} \\quad \\left(\\frac{\\sigma_{t-\\dt}}{\\sigma_t}\\right)  \n    &= \\sqrt{1 - \\frac{\\dt}{t}} = (1 - \\frac{\\dt}{2t}) \\\\\n    \\implies \\Tab{x_1}{x_0}_t(x_t) &= -\\underset{\\dt \\to 0}{\\lim} \\frac{G_t(x_t) - x_t}{\\dt} \n    = \\frac{1}{2t} (x_t - x_0),\n\\end{aligned}$$</div><p>which agrees with&nbsp;(4.6).</p><p>Now let us compute the marginal flow $v^*$ generated by the pointwise flow of Equation&nbsp;(4.6) and the coupling implied by the diffusion forward process. By Equation (37), the marginal flow is:</p><div class=\"formula-box\">$$\\begin{aligned}\nv^*_t(x_t)\n&= \\E_{x_1, x_0 \\mid x_t}[ \\Tab{x_1}{x_0}_t(x_t) \\mid x_t ] \\mathnote{By gas-lemma.}\\\\\n&= \\frac{1}{2t} \\E_{\n    \\substack{x_0 \\sim p;~ x_1 \\gets x_0 + \\cN(0, \\sigma_q^2) \\\\\n    x_t \\gets \\mathrm{RunFlow}(\\Tab{x_1}{x_0}_t, x_1, t)}}\n[ x_0 - x_t \\mid x_t ] \\mathnote{For our choices of coupling and flow.}\\\\\n&= \\frac{1}{2t} \\E_{\n    \\substack{x_0 \\sim p;~ x_1 \\gets x_0 + \\cN(0, \\sigma_q^2) \\\\\n    x_t \\gets x_1 \\sqrt{t} + (1-\\sqrt{t})x_0\n    }}[ x_0 - x_t \\mid x_t ] \\mathnote{Expanding the flow trajectory.} \\\\\n&= \\frac{1}{2t} \\E_{\n    \\substack{x_0 \\sim p\\\\\n    x_t \\gets \\sqrt{t} \\cN(0, \\sigma_q^2)\n    }}[ x_0 - x_t \\mid x_t ] \\mathnote{Plugging in $x_1 = x_0 + \\cN(0, \\sigma_q^2).$}\n\\end{aligned}$$</div><p>This is exactly the differential equation describing the trajectory of DDIM (see Equation&nbsp;31, which is the continuous-time limit of Equation&nbsp;20).</p>"
      },
      {
        "h": "&mdash; Two Pointwise Flows for DDIM give the same Trajectory",
        "body": "<p>We want to show that pointwise flow&nbsp;4.6: $$\n\\Tab{x_1}{x_0}_t(x_t) = \\frac{1}{2\\sqrt{t}}(x_0 - x_1)\n$$ is equivalent to the DDIM pointwise flow&nbsp;(4.6): $$\n\\Tab{x_1}{x_0}_t(x_t)= \\frac{1}{2t} (x_t - x_0)\n$$ because both these pointwise flows generate the same trajectory of $x_t$: $$\nx_t = x_0 + (x_1 - x_0)\\sqrt{t}.\n$$ To see this, we can solve the ODE determined by&nbsp;(4.6) via the Separable Equations method:</p><div class=\"formula-box\">$$\\begin{aligned}\n    \\frac{dx_t}{dt} &= -\\frac{1}{2t} (x_0 - x_t) \\\\\n    \\implies \\frac{\\frac{dx_t}{dt}}{x_t - x_0} &= \\frac{1}{2t} \\\\\n    \\implies \\int \\frac{1}{x_t - x_0} dx &= \\int \\frac{1}{2t} dt, \n    \\text{ since } \\frac{dx_t}{dt} dt = dx \\\\\n    \\implies \\log(x_t - x_0) &= \\log \\sqrt{t} + c \\\\\n    c &= \\log(x_1 - x_0) \\text{ (boundary cond.)}\\\\\n    \\implies \\log(x_t - x_0) &= \\log \\sqrt{t} (x_1 - x_0)  \\\\\n    \\implies x_t - x_0 &= \\sqrt{t} (x_1 - x_0).\n\\end{aligned}$$</div>"
      },
      {
        "h": "&mdash; DDIM vs Time-reparameterized linear flows",
        "body": "<div class=\"card\"><p><b>Lemma.</b></p><p>[DDIM vs Linear Flows] </p><p>Let $p_0$ be an arbitrary target distribution. Let $\\{x_t\\}_t$ be the joint distribution defined by the DDPM forward process applied to $p_0$, so the marginal distribution of $x_t$ is $p_t = p \\star \\cN(0, t\\sigma_q^2)$.</p><p>Let $x^* \\in \\R^d$ be an arbitrary initial point. Consider the following two deterministic trajectories: <ol class=\"mbul\"><li>The trajectory $\\{y_t\\}_t$ of the continuous-time DDIM flow, with respect to target distribution $p_0$, when started at initial point $y_1 = x^*$.</p><p>That is, $y_t$ is the solution to the following ODE (Equation&nbsp;31):</p><div class=\"formula-box\">$$\\begin{aligned}\n    \\frac{dy_t}{dt} &= -v^{\\mathrm{ddim}}(y_t) \\\\\n    &= -\\frac{1}{2t} \\E_{x_0 \\mid x_t}[x_0 - x_t \\mid x_t=y_t] \n    \\end{aligned}$$</div><p>with boundary condition $y_1$ at $t=1$.</li><li>The trajectory $\\{z_t\\}_t$ produced when initial point $z_1 = x^*$ is transported by the marginal flow constructed from: <ul class=\"mbul\"><li>Linear pointwise flows</li><li>The DDPM-coupling of Line&nbsp;(4.6).</li></ul> That is, the marginal flow</p><div class=\"formula-box\">$$\\begin{aligned}\n    v^\\star_t(x_t) &= \\E_{x_0, x_1|x_t} [\\Tab{x_1}{x_0}(x_t) | x_t] \\\\\n    &:= \\E_{x_0, x_1|x_t} [x_0 - x_1 | x_t] \\\\\n    &= \\E_{x_0|x_t} [x_0 - x_t | x_t] \\mathnote{since {\\E[x_1 | x_t] = x_t} under the DDPM coupling.} \n\\end{aligned}$$</div></li></ol>\nThen, we claim these two trajectories are identical with the following time-reparameterization:\n<div class=\"formula-box\">$$\\begin{aligned}\n\\forall t \\in [0,1]: \\quad\ny_t = z_{\\sqrt{t}}\n\\end{aligned}$$</div></div>"
      },
      {
        "h": "&mdash; Proof Sketch of Claim&nbsp;2.2",
        "body": "<p>We will show that, in the forward diffusion setup of Section&nbsp;1:</p><div class=\"formula-box\">$$\\begin{aligned}\n\\E[(x_{t} - x_{t-\\dt}) \\mid x_t]  \n= \n\\frac{\\dt}{t}\n\\E[(x_t - x_0) \\mid x_t].\n\\end{aligned}$$</div><div class=\"card\"><p><b>Proof.</b></p><p>[Proof sketch]</p><p>Recall $\\eta_t = x_{t+\\dt} - x_t$. So by linearity of expectation:</p><div class=\"formula-box\">$$\\begin{aligned}\n\\E[(x_t - x_0) \\mid x_t]\n&=\n\\E[\\sum_{i<t} \\eta_i \\mid x_t] \\\\\n&=\n\\sum_{i < t} \\E[\\eta_i \\mid x_t].\n\n\\end{aligned}$$</div><p>Now, we claim that for given $x_t$, the conditional distributions $p(\\eta_i \\mid x_t)$ are identical for all $i < t$. To see this, notice that the joint distribution function $p(x_0, x_t, \\eta_0, \\eta_\\dt, \\dots, \\eta_{t-\\dt})$ is symmetric in the $\\{\\eta_i\\}$s, by definition of the forward process, and therefore the conditional distribution function $p(\\eta_0, \\eta_\\dt, \\dots, \\eta_{t-\\dt} \\mid x_t)$ is also symmetric in the $\\{\\eta_i\\}$s. Therefore, all $\\eta_i$ have identical conditional expectations: $$\n\\E[\\eta_0 \\mid x_t] \n= \\E[\\eta_\\dt \\mid x_t] \n= \\dots\n= \\E[\\eta_{t-\\dt} \\mid x_t]\n$$ And since there are $(t/\\dt)$ of them, $$\n\\sum_{i < t} \\E[\\eta_i \\mid x_t]\n= \n\\frac{t}{\\dt} \\E[\\eta_{t-\\dt} \\mid x_t].\n$$ Now continuing from Line&nbsp;54,</p><div class=\"formula-box\">$$\\begin{aligned}\n\\E[(x_0 - x_t) \\mid x_t]\n&=\n\\sum_{i < t} \\E[\\eta_i \\mid x_t] \\\\\n&= \n(t/\\dt) \\E[\\eta_{t-\\dt} \\mid x_t] \\\\\n&= \n(t/\\dt) \\E[(x_{t} - x_{t-\\dt}) \\mid x_t]\n\\end{aligned}$$</div><p>as desired.</p></div>"
      },
      {
        "h": "&mdash; Variance-Reduced Algorithms",
        "body": "<p>Here we give the &ldquo;varianced-reduced&rdquo; versions of the DDPM training and sampling algorithms, where we train a network $g_\\theta$ to approximate $$\ng_\\theta(x, t) \\approx \\E[x_0 \\mid x_t]\n$$ instead of a network $f_\\theta$ to approximate $$\nf_\\theta(x, t) \\approx \\E[x_{t-\\dt} \\mid x_t].\n$$ Via Claim&nbsp;2.2, these two functions are equivalent via the transform: $$\nf_\\theta(x, t) = (\\dt/t) g_\\theta(x, t) + (1-\\dt/t)x.\n$$ Plugging this relation into Pseudocode&nbsp;2 yields the variance-reduced DDPM sampler of Pseudocode&nbsp;7.</p><p>[t]{0.47}</p><div class=\"card\" style=\"border-left:3px solid var(--accent);padding:10px 14px\"><p><b>Algorithm.</b> DDPM train loss ($x_0$-prediction)</p><div style=\"font-family:var(--font-mono);font-size:14px;line-height:1.8\">[H]<br><br><br><b>Input:</b> Neural network $g_\\theta$<br>Sample-access to train distribution $p$.}<br><b>Data:</b> Terminal variance $\\sigma_q$<br><b>Output:</b> Stochastic loss $L$<br>$x_0 \\gets \\textrm{Sample}(p)$ \\;<br>$t \\gets \\textrm{Unif}[0, 1]$ \\;<br>$x_{t} \\gets x_0 + \\cN(0, \\sigma_q^2 t) $ \\;<br>$L \\gets \\left\\Vert g_\\theta(x_{t}, t) -  x_{0}\\right\\Vert_2^2$ \\;<br>$L$</div></div><p>[t]{0.47}</p><div class=\"card\" style=\"border-left:3px solid var(--accent);padding:10px 14px\"><p><b>Algorithm.</b> DDPM sampling ($x_0$-prediction)</p><div style=\"font-family:var(--font-mono);font-size:14px;line-height:1.8\">[H]<br><br><br><b>Input:</b> Trained model $f_\\theta$.<br><b>Data:</b> Terminal variance $\\sigma_q$; step-size $\\dt$.<br><b>Output:</b> $x_0$<br>$x_1 \\gets \\cN(0, \\sigma_q^2)$ \\;<br>{$t=1,~(1-\\dt),(1-2\\dt), \\dots, \\dt$}{<br>$\\hat{\\eta_t} \\gets g_\\theta(x_t, t) - x_t$ \\;<br>$x_{t-\\dt} \\gets x_t + (1/t)\\hat{\\eta_t}\\dt + \\cN(0, \\sigma_q^2\\dt)$ \\;<br>}<br>$x_0$</div></div>"
      },
      {
        "h": "&mdash; Equivalence of and $x_0$- and $\\eps$-prediction",
        "body": "<p>We will discuss this in our usual simplified setup:</p><div class=\"formula-box\">$$\\begin{aligned}\n    x_t &= x_0 + \\sigma_t \\epsilon_t, \\quad \\sigma_t = \\sigma_q \\sqrt{t}, \\quad \\epsilon_t \\sim \\cN(0, 1);\n\\end{aligned}$$</div><p>the scaling factors are more complex in the general case (see [luo2022understanding] for VP diffusion, for example) but the idea is the same. The DDPM training algorithm 1 has objective and optimal value</p><div class=\"formula-box\">$$\\begin{aligned}\n    \\underset{\\theta}{\\min} &\\quad \\|f_\\theta(x_t, t) - x_{t-\\Delta t}\\|_2^2, \\quad f^\\star_\\theta(x_t, t) = \\E[x_{t-\\Delta t} | x_t]\n\\end{aligned}$$</div><p>That is, the network $f_\\theta$ to learn to predict $\\E[x_{t-\\Delta t} | x_t]$. However, we could instead require the network to predict other related quantities, as follows. Noting that</p><div class=\"formula-box\">$$\\begin{aligned}\n    &\\E[x_{t-\\Delta t} - x_t | x_t] \n    \\overset{\\text{eq. } 23}{=} \\frac{\\Delta t}{t} \\E[x_0 - x_t | x_t] \\equiv \\frac{\\Delta t}{t \\sigma_t} \\E[\\epsilon_t | x_t] \\\\\n    \\implies & \\|E[x_{t-\\Delta t} - x_t | x_t] - x_{t-\\Delta t}\\|_2^2 = \\|\\frac{\\Delta t}{t} (\\E[x_0 | x_t] - x_0) \\|_2^2 = \\|\\frac{\\Delta t}{t \\sigma_t} (\\E[\\epsilon_t | x_t] - \\epsilon_t) \\|_2^2\n\\end{aligned}$$</div><p>we get the following equivalent problems:</p><div class=\"formula-box\">$$\\begin{aligned}\n    \\underset{\\theta}{\\min} &\\quad \\|f_\\theta(x_t, t) - x_0 \\|_2^2 \\implies f^\\star_\\theta(x_t, t) = \\E[x_0 | x_t], \\quad \\text{time-weighting } = \\frac{1}{t} \\\\\n    \\underset{\\theta}{\\min} &\\quad \\|\\frac{\\Delta t}{t \\sigma_t} (f_\\theta(x_t, t) - \\epsilon_t) \\|_2^2 \\implies f^\\star_\\theta(x_t, t) = \\E[\\epsilon_t | x_t]  \\quad \\text{time-weighting } = \\frac{1}{t \\sigma_t}.\n\\end{aligned}$$</div>"
      }
    ]
  });
})();
