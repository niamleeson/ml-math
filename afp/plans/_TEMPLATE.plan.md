<!-- Copy to M<NN>-<slug>.plan.md and fill every section. See README.md for the 9 lesson
     structures (S1–S9), the example-type axis, and shared conventions. -->
# Module Plan — M<N> · <Title>

| Field | Value |
|---|---|
| Domain | Domain <D> · <name> |
| Skip if you can already… | <verbatim from the AFP-AI Learning Guide> |
| Maps to (projects) | <verbatim from the guide, e.g. "all" or "Creator Marketplace AI"> |
| Primary structure(s) | <S# names, e.g. S6 Applied Engineering> |
| Example type | <🧮 Numeric / 💻 Colab / ⚑ Both> |
| Sub-lessons | <count> |
| Notebooks | <count> |

## Module hub (the "complete list")
<1–2 warm sentences framing the module, then the sub-lesson checklist. This is the hub page —
it doubles as the mastery checklist and links to each sub-lesson.>

## Questions this module answers (→ which sub-lesson teaches the answer)
<List every question, each mapped to the sub-lesson(s) that make it answerable. This is the
coverage spine: the plan's whole job is to leave the learner able to answer ALL of these.
Every question MUST map to at least one sub-lesson.>
- <Q1> → M<N>.1
- <Q2> → M<N>.1
- <Q3> → M<N>.2
- …

## Concepts (ƒ = genuine, central formula)
<the concept list; mark ƒ only where a real formula belongs. Everything unmarked is prose —
do NOT manufacture math to fill a template.>

## Sub-lessons
<One block per sub-lesson. Group the questions/concepts into coherent sub-lessons so that,
together, the sub-lessons make every question above answerable.>

### M<N>.<k> · <sub-lesson title>  —  [S# <structure>, <🧮/💻/⚑>]
- **Makes answerable:** <the exact questions from the list above this sub-lesson teaches>
- **You'll be able to say:** <a 1–2 line sketch of the correct answer(s) the learner should be
  able to give after this sub-lesson — the concrete, checkable learning outcome. This is what the
  sub-lesson is FOR.>
- **Concepts:** <which concepts; note ƒ ones>
- **Key Idea focus:** <per the structure's Part-2 flavor>
- **Worked-example shape:** <per structure — 10+5+5 ramp (S1/S2), 5+5 derivations (S3/S4),
  naive→break→fix→scale (S6), knob-sweep→tradeoff-curve→operating-point (S7),
  measure→validate→debias (S8), instance→value/allocation/payment→control-loop (S9)>
- **Notebook:** <Yes/No> — <the demo: data source incl. a case where it breaks, signature viz,
  the genuine assert/metric>. Omit for 🧮.
- **Real numbers to cite:** <the concrete, re-derivable numbers this sub-lesson grounds itself in,
  AFP/ads-flavored where the module maps to a project>

## Coverage check
<Confirm every question in the list above is answered by at least one sub-lesson. Flag any gap.>

## Decision guide (only if the module has a when-to-pick-X-vs-Y)
<a compact comparison matrix, e.g. GBDT vs NN, HNSW vs IVF-PQ, PPO vs DPO. Omit if N/A.>

## Resources (from the guide)
<the module's resource list, verbatim from the guide's "Learning resources" section>

## SOTA papers (from the guide)
<the module's papers, verbatim from the guide's "SOTA paper reading" section; may be empty>

## Notes / caveats
<anything the author of the lesson must watch: overlaps with the concurrent topics/ curriculum,
where NOT to force math, GPU-only demos to avoid, etc.>
