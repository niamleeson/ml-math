node_modules/
.yarn/
screenshots/
.DS_Store
.playwright-mcp/
pt-tensors-new-template.png
*.png
# …but keep the committed favicon assets
!favicon-32.png
!apple-touch-icon.png

# Ask-Claude question queue written by tools/tutor-server.js (local, per-session)
.claude/ask-queue.jsonl
.claude/ask-done.jsonl

# Cached torchvision datasets (local verification only)
notebooks/data/
data/
