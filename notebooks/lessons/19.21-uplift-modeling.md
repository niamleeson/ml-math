# 19.21 — Uplift Modeling

Uplift modeling predicts who changes because of an action, not who would act anyway. In this lesson, treatment is an intervention such as a coupon, reminder, or outreach message; the target is the incremental effect of that intervention, so we separate sure converters from persuadable users and avoid wasting treatment on people who would have converted without it.

## 📖 Concept walkthrough — build each idea from scratch

Before the terse worked examples, we build uplift modeling one idea at a time. Run each cell in order and read the printed intermediate values — every piece of math, including CATE, meta-learners, uplift curves, Qini, and targeting value, is spelled out so nothing is a black box. This walkthrough is self-contained and uses a `_w` suffix on its variables so it never clashes with the examples below.

```python
import numpy as np  # arrays, simulation, and from-scratch estimators.
import matplotlib.pyplot as plt  # all walkthrough visualizations.
np.random.seed(0)  # reproducibility for the synthetic randomized trial.
```

### 1. Potential outcomes, RCT data, and CATE

Uplift starts with two possible outcomes for the same person: $Y(1)$ if treated and $Y(0)$ if untreated. We never observe both for one person, so a randomized controlled trial (RCT) gives treated and control groups that are comparable on average. The conditional average treatment effect is $\tau(x)=E[Y(1)-Y(0)\mid X=x]=P(Y=1\mid T=1,x)-P(Y=1\mid T=0,x)$.

```python
n_w = 5000  # number of synthetic members in the randomized trial.
rng_w = np.random.default_rng(0)  # local generator so the walkthrough is reproducible.
x_w = rng_w.uniform(0, 1, n_w)  # one feature: engagement score from low to high.
tau_true_w = 0.35 * np.exp(-((x_w - 0.35) / 0.18) ** 2) - 0.08 * (x_w > 0.78)  # heterogeneous uplift.
p0_w = np.clip(0.08 + 0.45 * x_w, 0.02, 0.85)  # baseline conversion without treatment.
p1_w = np.clip(p0_w + tau_true_w, 0.01, 0.95)  # treated conversion probability.
T_w = rng_w.binomial(1, 0.5, n_w)  # randomized treatment assignment.
y_w = rng_w.binomial(1, np.where(T_w == 1, p1_w, p0_w))  # observed factual outcome.
print("treatment rate:", round(float(T_w.mean()), 3), "outcome rate:", round(float(y_w.mean()), 3))
assert abs(float(T_w.mean()) - 0.5) < 0.03
```
▶ What you'll see: treatment is assigned about 50/50, so treatment status is not chosen by the model or by user behavior.

```python
bins_w = np.linspace(0, 1, 11)  # deciles of the feature.
centers_w = 0.5 * (bins_w[:-1] + bins_w[1:])  # x positions for plots.
true_bin_w = np.array([tau_true_w[(x_w >= bins_w[i]) & (x_w < bins_w[i+1])].mean() for i in range(10)])
print("true CATE by decile:", np.round(true_bin_w, 3))
assert true_bin_w[3] > 0.25 and true_bin_w[-1] < 0.0
```
▶ What you'll see: mid-engagement members have positive uplift, while the highest-engagement tail has negative uplift.

```python
plt.figure(figsize=(5, 3))
plt.plot(centers_w, true_bin_w, marker="o", color="teal")
plt.axhline(0, color="black", linewidth=1)
plt.title("1: true heterogeneous treatment effect")
plt.xlabel("engagement feature x")
plt.ylabel("CATE tau(x)")
plt.show()
```
▶ What you'll see: uplift is a curve, not one constant number; the right tail is below zero, meaning treatment can hurt.

*Why it's done this way:* CATE is a contrast between two conditional probabilities at the same feature value. Randomization makes treated and control rows exchangeable, so differences in outcome rates can be read as causal rather than just predictive association.

### 2. T-learner: estimate treated and control response separately

A T-learner fits two response models: one for treated outcomes $\mu_1(x)=E[Y\mid T=1,x]$ and one for control outcomes $\mu_0(x)=E[Y\mid T=0,x]$. The uplift score is their difference, $\hat\tau(x)=\hat\mu_1(x)-\hat\mu_0(x)$. We implement the models as binned means so every calculation is inspectable.

```python
def bin_means_w(x, y, bins):  # estimate E[y|x in bin] with a small global-mean fallback.
    out = np.zeros(len(bins) - 1)
    fallback = float(np.mean(y))
    for i in range(len(out)):
        m = (x >= bins[i]) & (x < bins[i + 1])
        out[i] = float(np.mean(y[m])) if np.any(m) else fallback
    return out

mu1_t_w = bin_means_w(x_w[T_w == 1], y_w[T_w == 1], bins_w)  # treated response by bin.
mu0_t_w = bin_means_w(x_w[T_w == 0], y_w[T_w == 0], bins_w)  # control response by bin.
tau_t_w = mu1_t_w - mu0_t_w  # T-learner uplift.
print("T-learner uplift:", np.round(tau_t_w, 3))
assert tau_t_w[3] > tau_t_w[8]
```
▶ What you'll see: the largest estimated treatment effect appears near the middle bins, not where baseline response is highest.

```python
plt.figure(figsize=(5, 3))
plt.plot(centers_w, true_bin_w, marker="o", label="true tau")
plt.plot(centers_w, tau_t_w, marker="s", label="T-learner")
plt.axhline(0, color="black", linewidth=1)
plt.title("2: T-learner subtracts two response curves")
plt.xlabel("x bin")
plt.ylabel("uplift")
plt.legend()
plt.show()
```
▶ What you'll see: the learned curve follows the true hump, with sampling noise from finite treated/control rows.

*Why it's done this way:* The formula $P(Y=1\mid T=1,x)-P(Y=1\mid T=0,x)$ says uplift is literally a difference of two conditional response surfaces. The T-learner mirrors that definition, but it can be noisy because each surface is trained on only half the data.

### 3. S-learner: one model with treatment as an input

An S-learner fits one response model $\mu(x,t)$ and asks it two counterfactual questions for the same person: $\hat\tau(x)=\hat\mu(x,1)-\hat\mu(x,0)$. Here we implement a binned linear model with columns for intercept, $x$, treatment, and $x\cdot T$ interaction.

```python
X_s_w = np.column_stack([np.ones(n_w), x_w, T_w, x_w * T_w])  # one design matrix with treatment as a feature.
beta_s_w = np.linalg.pinv(X_s_w) @ y_w  # least-squares fit from scratch with NumPy.
print("S-learner coefficients:", np.round(beta_s_w, 3))
assert beta_s_w[2] > 0.0
```
▶ What you'll see: the treatment coefficient is positive on average, but a simple linear interaction cannot perfectly represent the hump.

```python
X1_s_w = np.column_stack([np.ones_like(centers_w), centers_w, np.ones_like(centers_w), centers_w])
X0_s_w = np.column_stack([np.ones_like(centers_w), centers_w, np.zeros_like(centers_w), np.zeros_like(centers_w)])
tau_s_w = X1_s_w @ beta_s_w - X0_s_w @ beta_s_w  # counterfactual contrast from one model.
print("S-learner uplift:", np.round(tau_s_w, 3))
```
▶ What you'll see: the S-learner produces a smoother, more constrained uplift trend than the binned T-learner.

```python
plt.figure(figsize=(5, 3))
plt.plot(centers_w, true_bin_w, marker="o", label="true tau")
plt.plot(centers_w, tau_s_w, marker="^", label="S-learner")
plt.axhline(0, color="black", linewidth=1)
plt.title("3: S-learner counterfactual contrast")
plt.xlabel("x bin")
plt.ylabel("uplift")
plt.legend()
plt.show()
```
▶ What you'll see: one model can share strength between groups, but the chosen functional form may smooth away sharp heterogeneity.

*Why it's done this way:* The S-learner is efficient when treated and control response surfaces are similar, because shared coefficients use all rows. The price is regularization-by-architecture: if the model cannot express a treatment interaction, it underestimates heterogeneity.

### 4. X-learner: impute individual treatment effects, then blend

An X-learner first learns $\mu_0$ and $\mu_1$, then creates pseudo-effects: treated rows get $D^1=Y-\hat\mu_0(X)$ and control rows get $D^0=\hat\mu_1(X)-Y$. It models those pseudo-effects and blends them with a weight, often the propensity score. In this RCT the propensity is about 0.5, so the blend is simple.

```python
def lookup_bins_w(x, bins, values):  # map each x value to its bin's fitted value.
    idx = np.clip(np.digitize(x, bins) - 1, 0, len(values) - 1)
    return values[idx]

mu0_hat_all_w = lookup_bins_w(x_w, bins_w, mu0_t_w)  # control response estimate for every row.
mu1_hat_all_w = lookup_bins_w(x_w, bins_w, mu1_t_w)  # treated response estimate for every row.
d1_w = y_w[T_w == 1] - mu0_hat_all_w[T_w == 1]  # treated pseudo-effect.
d0_w = mu1_hat_all_w[T_w == 0] - y_w[T_w == 0]  # control pseudo-effect.
print("pseudo-effect means:", round(float(d1_w.mean()), 3), round(float(d0_w.mean()), 3))
assert abs(float(d1_w.mean()) - float(d0_w.mean())) < 0.04
```
▶ What you'll see: both pseudo-effect averages estimate the same overall incremental gain from different observed groups.

```python
te1_x_w = bin_means_w(x_w[T_w == 1], d1_w, bins_w)  # model pseudo-effects among treated rows.
te0_x_w = bin_means_w(x_w[T_w == 0], d0_w, bins_w)  # model pseudo-effects among control rows.
prop_w = float(T_w.mean())  # RCT propensity.
tau_x_w = prop_w * te0_x_w + (1 - prop_w) * te1_x_w  # X-learner blend.
print("X-learner uplift:", np.round(tau_x_w, 3))
```
▶ What you'll see: the X-learner curve is close to the T-learner here because treatment assignment is balanced.

```python
plt.figure(figsize=(5, 3))
plt.plot(centers_w, true_bin_w, marker="o", label="true tau")
plt.plot(centers_w, tau_t_w, marker="s", label="T")
plt.plot(centers_w, tau_x_w, marker="D", label="X")
plt.axhline(0, color="black", linewidth=1)
plt.title("4: X-learner pseudo-effect blend")
plt.xlabel("x bin")
plt.ylabel("uplift")
plt.legend()
plt.show()
```
▶ What you'll see: T and X both recover the high-uplift middle region, with small differences from pseudo-effect smoothing.

*Why it's done this way:* X-learning turns the missing counterfactual problem into two supervised pseudo-label problems. Blending by propensity gives more weight to the pseudo-effect model built from the group that is more informative or more abundant.

### 5. Uplift and Qini curves: evaluate rankings, not just probabilities

Uplift models are often used to rank who should receive treatment. A Qini-style curve sorts users by predicted uplift and asks: as we target more people, how much incremental outcome do we accumulate beyond what the control group suggests would have happened anyway?

```python
score_w = lookup_bins_w(x_w, bins_w, tau_t_w)  # score each row with the T-learner uplift bin.
order_w = np.argsort(score_w)[::-1]  # target highest predicted uplift first.
y_ord_w = y_w[order_w]
t_ord_w = T_w[order_w]
nt_cum_w = np.cumsum(t_ord_w == 1)
nc_cum_w = np.cumsum(t_ord_w == 0)
yt_cum_w = np.cumsum(y_ord_w * (t_ord_w == 1))
yc_cum_w = np.cumsum(y_ord_w * (t_ord_w == 0))
uplift_curve_w = yt_cum_w - yc_cum_w * np.divide(nt_cum_w, np.maximum(nc_cum_w, 1))
frac_w = np.arange(1, n_w + 1) / n_w
print("top 20% incremental outcomes:", round(float(uplift_curve_w[int(0.2*n_w)-1]), 1))
assert uplift_curve_w[int(0.2*n_w)-1] > 150
```
▶ What you'll see: the top slice has strongly positive incremental outcomes, meaning the ranking finds persuadable people early.

```python
random_line_w = frac_w * uplift_curve_w[-1]  # random targeting benchmark with same endpoint.
qini_area_w = float(np.trapz(uplift_curve_w - random_line_w, frac_w))  # area above random line.
print("Qini area above random:", round(qini_area_w, 2))
assert qini_area_w > 20
```
▶ What you'll see: positive Qini area means the ranking beats random targeting, not just that average treatment effect is positive.

```python
plt.figure(figsize=(5, 3))
plt.plot(frac_w, uplift_curve_w, label="model targeting", color="teal")
plt.plot(frac_w, random_line_w, linestyle="--", label="random targeting", color="gray")
plt.title("5: uplift/Qini curve")
plt.xlabel("fraction targeted")
plt.ylabel("incremental outcomes")
plt.legend()
plt.show()
```
▶ What you'll see: the model curve rises above the dashed random line early; that early lift is the value of ranking by uplift.

*Why it's done this way:* Accuracy on $Y$ rewards predicting sure converters. Qini rewards putting high incremental effect first, which matches the decision problem when treatment has a cost or limited capacity.

### 6. Targeting policy: treat only when expected value is positive

A targeting policy converts uplift into an action. If one treatment costs $c$ and one conversion is worth $v$, the expected net value is $v\hat\tau(x)-c$. We treat only when that value is positive, which can exclude high-response but low-uplift users.

```python
value_w = 10.0  # value of one incremental conversion.
cost_w = 1.0  # cost of sending treatment.
net_by_bin_w = value_w * tau_t_w - cost_w  # expected net value by feature bin.
treat_bins_w = net_by_bin_w > 0  # policy: target bins with positive value.
print("net value by bin:", np.round(net_by_bin_w, 2))
print("targeted bins:", centers_w[treat_bins_w])
assert np.any(treat_bins_w) and not treat_bins_w[-1]
```
▶ What you'll see: only bins with enough incremental effect clear the cost threshold; negative-uplift bins are not targeted.

```python
policy_w = treat_bins_w[np.clip(np.digitize(x_w, bins_w) - 1, 0, 9)]  # assign each row a policy action.
true_policy_gain_w = float(np.sum(policy_w * (value_w * tau_true_w - cost_w)))  # true expected net gain.
treat_all_gain_w = float(np.sum(value_w * tau_true_w - cost_w))  # net gain if everyone is treated.
print("policy gain:", round(true_policy_gain_w, 1), "treat-all gain:", round(treat_all_gain_w, 1))
assert true_policy_gain_w > treat_all_gain_w
```
▶ What you'll see: targeted treatment beats treat-all because it avoids low- and negative-uplift members.

```python
plt.figure(figsize=(5, 3))
plt.bar(centers_w, net_by_bin_w, width=0.08, color=np.where(treat_bins_w, "seagreen", "crimson"))
plt.axhline(0, color="black", linewidth=1)
plt.title("6: target only positive expected value")
plt.xlabel("x bin")
plt.ylabel("10*uplift - cost")
plt.show()
```
▶ What you'll see: green bars are profitable treatment segments; red bars are segments where treatment should be withheld.

*Why it's done this way:* A causal score becomes useful only after it is tied to a decision threshold. The threshold is not arbitrary — it comes from the value of the desired outcome and the cost or risk of the intervention.

## 🛠️ Setup

```python
import numpy as np  # load NumPy for simulation, arrays, and from-scratch estimators.
import matplotlib.pyplot as plt  # load Matplotlib for uplift curves, policy plots, and diagnostics.
np.random.seed(0)  # make every stochastic example reproducible.

def make_uplift_data(n=2000, seed=0):  # synthesize an RCT with known heterogeneous treatment effect.
    rng = np.random.default_rng(seed)
    x = rng.uniform(0, 1, n)
    tau = 0.35 * np.exp(-((x - 0.35) / 0.18) ** 2) - 0.08 * (x > 0.78)
    p0 = np.clip(0.08 + 0.45 * x, 0.02, 0.85)
    p1 = np.clip(p0 + tau, 0.01, 0.95)
    t = rng.binomial(1, 0.5, n)
    y = rng.binomial(1, np.where(t == 1, p1, p0))
    return x, t, y, p0, p1, tau

def bin_means(x, y, bins):  # estimate a conditional mean in feature bins.
    out = np.zeros(len(bins) - 1)
    fallback = float(np.mean(y)) if len(y) else 0.0
    for i in range(len(out)):
        m = (x >= bins[i]) & (x < bins[i + 1])
        out[i] = float(np.mean(y[m])) if np.any(m) else fallback
    return out

def predict_bins(x, bins, values):  # map each feature value to its bin estimate.
    idx = np.clip(np.digitize(x, bins) - 1, 0, len(values) - 1)
    return values[idx]

def t_learner_bins(x, t, y, bins):  # fit a T-learner with binned conditional means.
    mu1 = bin_means(x[t == 1], y[t == 1], bins)
    mu0 = bin_means(x[t == 0], y[t == 0], bins)
    return mu1, mu0, mu1 - mu0

def qini_curve(y, t, score):  # compute an adjusted incremental-outcome curve from a ranking.
    order = np.argsort(score)[::-1]
    ys = y[order]
    ts = t[order]
    nt = np.cumsum(ts == 1)
    nc = np.cumsum(ts == 0)
    yt = np.cumsum(ys * (ts == 1))
    yc = np.cumsum(ys * (ts == 0))
    uplift = yt - yc * np.divide(nt, np.maximum(nc, 1))
    frac = np.arange(1, len(y) + 1) / len(y)
    return frac, uplift
```

## 🟢 Basics (warm-up)

### Basic 1 — Simulate a randomized trial

**Goal.** Build treatment, outcome, and feature arrays, because uplift modeling needs both treated and control evidence. We build it in 2 steps.

```python
x_b1, t_b1, y_b1, p0_b1, p1_b1, tau_b1 = make_uplift_data(n=1000, seed=1)
print("rows:", len(x_b1), "treatment rate:", round(float(t_b1.mean()), 3))
assert abs(float(t_b1.mean()) - 0.5) < 0.05
```
▶ What you'll see: treatment is assigned to roughly half the synthetic members.

```python
plt.figure(figsize=(4, 3))
plt.hist(x_b1[t_b1 == 1], bins=20, alpha=0.6, label="treated")
plt.hist(x_b1[t_b1 == 0], bins=20, alpha=0.6, label="control")
plt.title("Basic 1: RCT balance")
plt.xlabel("feature x")
plt.ylabel("count")
plt.legend()
plt.show()
```
▶ What you'll see: the two histograms overlap because assignment was randomized.

👀 Takeaway: an RCT gives comparable treated and control groups, which makes outcome differences causal.

### Basic 2 — Inspect potential-outcome probabilities

**Goal.** Compare treated and control probabilities from the known simulator, because their difference is the true uplift. We build it in 2 steps.

```python
x_b2, t_b2, y_b2, p0_b2, p1_b2, tau_b2 = make_uplift_data(n=1000, seed=2)
print("mean p0:", round(float(p0_b2.mean()), 3), "mean p1:", round(float(p1_b2.mean()), 3))
assert float(p1_b2.mean()) > float(p0_b2.mean())
```
▶ What you'll see: treated probability is higher on average, but that average hides heterogeneity.

```python
plt.figure(figsize=(4, 3))
plt.scatter(x_b2[:250], p0_b2[:250], s=10, alpha=0.6, label="p0 control")
plt.scatter(x_b2[:250], p1_b2[:250], s=10, alpha=0.6, label="p1 treated")
plt.title("Basic 2: two potential-outcome curves")
plt.xlabel("feature x")
plt.ylabel("conversion probability")
plt.legend()
plt.show()
```
▶ What you'll see: the treated curve separates most in the middle and can converge or dip in the tail.

👀 Takeaway: uplift is the vertical gap between treated and control probability curves at the same x.

### Basic 3 — Compute true CATE

**Goal.** Calculate $\tau(x)=p_1(x)-p_0(x)$, because CATE is the decision-relevant causal quantity. We build it in 2 steps.

```python
x_b3, t_b3, y_b3, p0_b3, p1_b3, tau_b3 = make_uplift_data(n=1000, seed=3)
calc_tau_b3 = p1_b3 - p0_b3
print("mean true uplift:", round(float(calc_tau_b3.mean()), 3), "max:", round(float(calc_tau_b3.max()), 3))
assert np.allclose(calc_tau_b3, tau_b3)
```
▶ What you'll see: average uplift is positive, while the maximum effect is much larger than the average.

```python
plt.figure(figsize=(4, 3))
plt.scatter(x_b3, calc_tau_b3, s=8, alpha=0.5, color="teal")
plt.axhline(0, color="black", linewidth=1)
plt.title("Basic 3: true CATE")
plt.xlabel("x")
plt.ylabel("p1 - p0")
plt.show()
```
▶ What you'll see: a hump-shaped uplift region and a small negative-effect tail.

👀 Takeaway: CATE is a difference of two counterfactual probabilities, not a direct observed label.

### Basic 4 — Estimate a treatment effect in one segment

**Goal.** Estimate uplift inside one feature segment by subtracting control conversion from treated conversion, because RCT groups are comparable inside the segment. We build it in 3 steps.

```python
x_b4, t_b4, y_b4, p0_b4, p1_b4, tau_b4 = make_uplift_data(n=2000, seed=4)
segment_b4 = (x_b4 >= 0.25) & (x_b4 < 0.45)
print("segment rows:", int(segment_b4.sum()))
```
▶ What you'll see: the selected segment contains enough treated and control rows for an estimate.

```python
rate_t_b4 = float(y_b4[segment_b4 & (t_b4 == 1)].mean())
rate_c_b4 = float(y_b4[segment_b4 & (t_b4 == 0)].mean())
uplift_b4 = rate_t_b4 - rate_c_b4
print("treated:", round(rate_t_b4, 3), "control:", round(rate_c_b4, 3), "uplift:", round(uplift_b4, 3))
assert uplift_b4 > 0.15
```

```python
plt.figure(figsize=(4, 3))
plt.bar(["treated", "control", "uplift"], [rate_t_b4, rate_c_b4, uplift_b4], color=["seagreen", "gray", "teal"])
plt.title("Basic 4: segment uplift")
plt.ylabel("rate / difference")
plt.show()
```
▶ What you'll see: the treated bar is higher than the control bar, and the gap is the segment uplift.

👀 Takeaway: in randomized data, a within-segment treated-control difference estimates CATE for that segment.

### Basic 5 — Estimate response is not uplift

**Goal.** Compare treated response probability with uplift, because high response can come from a high baseline rather than a large causal effect. We build it in 2 steps.

```python
x_b5, t_b5, y_b5, p0_b5, p1_b5, tau_b5 = make_uplift_data(n=1200, seed=5)
high_response_b5 = x_b5 > 0.85
high_uplift_b5 = (x_b5 > 0.25) & (x_b5 < 0.45)
print("treated response high-baseline:", round(float(p1_b5[high_response_b5].mean()), 3))
print("treated response high-uplift:", round(float(p1_b5[high_uplift_b5].mean()), 3))
```
▶ What you'll see: a high-baseline region can have a high treated response even when treatment effect is poor.

```python
uplift_high_response_b5 = float(tau_b5[high_response_b5].mean())
uplift_high_uplift_b5 = float(tau_b5[high_uplift_b5].mean())
print("uplift high-baseline:", round(uplift_high_response_b5, 3), "uplift persuadable:", round(uplift_high_uplift_b5, 3))
assert uplift_high_uplift_b5 > uplift_high_response_b5
plt.figure(figsize=(4, 3))
plt.bar(["high response", "persuadable"], [uplift_high_response_b5, uplift_high_uplift_b5], color=["crimson", "seagreen"])
plt.title("Basic 5: response != uplift")
plt.ylabel("true uplift")
plt.show()
```
▶ What you'll see: the persuadable group has far larger uplift even if the high-baseline group looks attractive by response.

👀 Takeaway: targeting likely converters can waste treatment on people who would convert anyway.

### Basic 6 — Fit a binned T-learner

**Goal.** Estimate treated and control response curves separately, because the T-learner follows the CATE definition directly. We build it in 3 steps.

```python
x_b6, t_b6, y_b6, p0_b6, p1_b6, tau_b6 = make_uplift_data(n=2500, seed=6)
bins_b6 = np.linspace(0, 1, 11)
mu1_b6, mu0_b6, tau_hat_b6 = t_learner_bins(x_b6, t_b6, y_b6, bins_b6)
print("mu1:", np.round(mu1_b6, 3))
print("mu0:", np.round(mu0_b6, 3))
```
▶ What you'll see: both response curves rise with x, but their gap is largest in the middle.

```python
centers_b6 = 0.5 * (bins_b6[:-1] + bins_b6[1:])
print("tau_hat:", np.round(tau_hat_b6, 3))
assert tau_hat_b6[3] > tau_hat_b6[8]
```

```python
plt.figure(figsize=(4, 3))
plt.plot(centers_b6, tau_hat_b6, marker="o", color="teal")
plt.axhline(0, color="black", linewidth=1)
plt.title("Basic 6: T-learner uplift")
plt.xlabel("x bin")
plt.ylabel("mu1 - mu0")
plt.show()
```
▶ What you'll see: a positive hump and a low or negative right tail, matching the simulator's structure.

👀 Takeaway: the T-learner turns two response models into one uplift score by subtraction.

### Basic 7 — Score new users with bin lookup

**Goal.** Apply fitted uplift values to new feature values, because a targeting system needs scores for candidates not just training rows. We build it in 2 steps.

```python
new_x_b7 = np.array([0.10, 0.35, 0.60, 0.90])
scores_b7 = predict_bins(new_x_b7, bins_b6, tau_hat_b6)
print("new x:", new_x_b7)
print("uplift scores:", np.round(scores_b7, 3))
assert scores_b7[1] == np.max(scores_b7)
```
▶ What you'll see: the middle candidate receives the largest predicted uplift.

```python
plt.figure(figsize=(4, 3))
plt.bar([str(v) for v in new_x_b7], scores_b7, color="purple")
plt.axhline(0, color="black", linewidth=1)
plt.title("Basic 7: score candidates")
plt.xlabel("candidate x")
plt.ylabel("predicted uplift")
plt.show()
```
▶ What you'll see: candidates can be ranked by causal gain, not by raw conversion probability.

👀 Takeaway: an uplift model converts user features into incremental-effect scores.

### Basic 8 — Convert uplift to expected value

**Goal.** Translate uplift into dollars or utility, because a treatment is worthwhile only when expected gain exceeds cost. We build it in 2 steps.

```python
value_b8 = 10.0
cost_b8 = 1.0
net_b8 = value_b8 * scores_b7 - cost_b8
print("net values:", np.round(net_b8, 2))
```
▶ What you'll see: positive uplift must be large enough to pay for the treatment.

```python
decision_b8 = net_b8 > 0
print("treat decisions:", decision_b8.astype(int))
assert decision_b8[1] and not decision_b8[-1]
plt.figure(figsize=(4, 3))
plt.bar([str(v) for v in new_x_b7], net_b8, color=np.where(decision_b8, "seagreen", "crimson"))
plt.axhline(0, color="black", linewidth=1)
plt.title("Basic 8: expected net value")
plt.ylabel("10*uplift - cost")
plt.show()
```
▶ What you'll see: green bars clear the treatment threshold; red bars should be withheld.

👀 Takeaway: targeting is a value calculation, not just a model-score threshold.

### Basic 9 — Build an uplift curve

**Goal.** Sort users by predicted uplift and cumulate incremental outcomes, because uplift models are evaluated as rankings. We build it in 3 steps.

```python
x_b9, t_b9, y_b9, p0_b9, p1_b9, tau_b9 = make_uplift_data(n=2500, seed=9)
bins_b9 = np.linspace(0, 1, 11)
_, _, tau_hat_b9 = t_learner_bins(x_b9, t_b9, y_b9, bins_b9)
score_b9 = predict_bins(x_b9, bins_b9, tau_hat_b9)
print("score range:", round(float(score_b9.min()), 3), round(float(score_b9.max()), 3))
```
▶ What you'll see: predicted uplift has enough variation to rank the population.

```python
frac_b9, uplift_curve_b9 = qini_curve(y_b9, t_b9, score_b9)
idx20_b9 = int(0.2 * len(frac_b9)) - 1
print("top 20% uplift:", round(float(uplift_curve_b9[idx20_b9]), 1))
assert uplift_curve_b9[idx20_b9] > 70
```

```python
plt.figure(figsize=(4, 3))
plt.plot(frac_b9, uplift_curve_b9, color="teal")
plt.title("Basic 9: uplift curve")
plt.xlabel("fraction targeted")
plt.ylabel("incremental outcomes")
plt.show()
```
▶ What you'll see: the curve rises fastest early when the highest-uplift users are targeted first.

👀 Takeaway: uplift curves judge whether the ranking front-loads incremental gains.

### Basic 10 — Compare against random targeting

**Goal.** Add a random-targeting line to the uplift curve, because Qini value is the area gained by ranking better than random. We build it in 2 steps.

```python
random_b10 = frac_b9 * uplift_curve_b9[-1]
qini_b10 = float(np.trapz(uplift_curve_b9 - random_b10, frac_b9))
print("Qini area:", round(qini_b10, 2))
assert qini_b10 > 8
```
▶ What you'll see: the Qini area is positive when the model curve sits above the random line.

```python
plt.figure(figsize=(4, 3))
plt.plot(frac_b9, uplift_curve_b9, label="model", color="teal")
plt.plot(frac_b9, random_b10, linestyle="--", label="random", color="gray")
plt.title("Basic 10: Qini vs random")
plt.xlabel("fraction targeted")
plt.ylabel("incremental outcomes")
plt.legend()
plt.show()
```
▶ What you'll see: the model line should sit above the dashed random line over the most valuable early targeting region.

👀 Takeaway: Qini rewards causal ranking quality, not just outcome prediction accuracy.

## 🟡 Easy

### Easy 1 — Implement an S-learner from scratch

**Goal.** Fit one linear response model with treatment features, because the S-learner estimates uplift by two counterfactual predictions from the same model. We build it in 4 steps.

```python
x_e1, t_e1, y_e1, p0_e1, p1_e1, tau_e1 = make_uplift_data(n=2500, seed=11)
X_e1 = np.column_stack([np.ones_like(x_e1), x_e1, x_e1 ** 2, t_e1, x_e1 * t_e1, (x_e1 ** 2) * t_e1])
print("design shape:", X_e1.shape)
```
▶ What you'll see: the design matrix has baseline terms plus treatment-interaction terms.

```python
beta_e1 = np.linalg.pinv(X_e1) @ y_e1
print("coefficients:", np.round(beta_e1, 3))
assert beta_e1[3] > 0
```

```python
grid_e1 = np.linspace(0.05, 0.95, 10)
X1_e1 = np.column_stack([np.ones_like(grid_e1), grid_e1, grid_e1 ** 2, np.ones_like(grid_e1), grid_e1, grid_e1 ** 2])
X0_e1 = np.column_stack([np.ones_like(grid_e1), grid_e1, grid_e1 ** 2, np.zeros_like(grid_e1), np.zeros_like(grid_e1), np.zeros_like(grid_e1)])
uplift_e1 = X1_e1 @ beta_e1 - X0_e1 @ beta_e1
print("S-learner uplift:", np.round(uplift_e1, 3))
```

```python
plt.figure(figsize=(5, 3))
plt.plot(grid_e1, uplift_e1, marker="o", label="S-learner")
plt.axhline(0, color="black", linewidth=1)
plt.title("Easy 1: S-learner uplift")
plt.xlabel("x")
plt.ylabel("predicted uplift")
plt.legend()
plt.show()
```
▶ What you'll see: the S-learner creates a smooth uplift curve by comparing predicted treatment and control outcomes.

👀 Takeaway: S-learners share one model across treatment arms, which can reduce variance but impose shape assumptions.

### Easy 2 — Implement an X-learner from scratch

**Goal.** Create pseudo-treatment effects and blend them, because the X-learner turns missing counterfactuals into supervised targets. We build it in 4 steps.

```python
x_e2, t_e2, y_e2, p0_e2, p1_e2, tau_e2 = make_uplift_data(n=3000, seed=12)
bins_e2 = np.linspace(0, 1, 11)
mu1_e2, mu0_e2, tau_t_e2 = t_learner_bins(x_e2, t_e2, y_e2, bins_e2)
print("first-stage uplift:", np.round(tau_t_e2, 3))
```
▶ What you'll see: first-stage response subtraction already estimates the treatment effect by bin.

```python
mu0_all_e2 = predict_bins(x_e2, bins_e2, mu0_e2)
mu1_all_e2 = predict_bins(x_e2, bins_e2, mu1_e2)
d_treated_e2 = y_e2[t_e2 == 1] - mu0_all_e2[t_e2 == 1]
d_control_e2 = mu1_all_e2[t_e2 == 0] - y_e2[t_e2 == 0]
print("pseudo means:", round(float(d_treated_e2.mean()), 3), round(float(d_control_e2.mean()), 3))
```

```python
te_treated_e2 = bin_means(x_e2[t_e2 == 1], d_treated_e2, bins_e2)
te_control_e2 = bin_means(x_e2[t_e2 == 0], d_control_e2, bins_e2)
prop_e2 = float(t_e2.mean())
tau_x_e2 = prop_e2 * te_control_e2 + (1 - prop_e2) * te_treated_e2
print("X-learner uplift:", np.round(tau_x_e2, 3))
assert tau_x_e2[3] > tau_x_e2[8]
```

```python
centers_e2 = 0.5 * (bins_e2[:-1] + bins_e2[1:])
plt.figure(figsize=(5, 3))
plt.plot(centers_e2, tau_t_e2, marker="s", label="T")
plt.plot(centers_e2, tau_x_e2, marker="D", label="X")
plt.axhline(0, color="black", linewidth=1)
plt.title("Easy 2: X-learner pseudo-effects")
plt.xlabel("x bin")
plt.ylabel("uplift")
plt.legend()
plt.show()
```
▶ What you'll see: the X-learner and T-learner identify the same persuadable region with slightly different smoothing.

👀 Takeaway: X-learning is useful when pseudo-effects can be modeled more easily than two full response surfaces.

### Easy 3 — Compare response ranking with uplift ranking

**Goal.** Rank the same users by treated response and by uplift, because response ranking finds likely converters while uplift ranking finds movable decisions. We build it in 3 steps.

```python
x_e3, t_e3, y_e3, p0_e3, p1_e3, tau_e3 = make_uplift_data(n=3000, seed=13)
response_score_e3 = p1_e3
uplift_score_e3 = tau_e3
print("mean response score:", round(float(response_score_e3.mean()), 3), "mean uplift score:", round(float(uplift_score_e3.mean()), 3))
```
▶ What you'll see: response probabilities and uplift values live on different scales.

```python
top_response_e3 = np.argsort(response_score_e3)[-300:]
top_uplift_e3 = np.argsort(uplift_score_e3)[-300:]
gain_response_e3 = float(np.sum(tau_e3[top_response_e3]))
gain_uplift_e3 = float(np.sum(tau_e3[top_uplift_e3]))
print("top-10% causal gain response:", round(gain_response_e3, 2), "uplift:", round(gain_uplift_e3, 2))
assert gain_uplift_e3 > gain_response_e3
```

```python
plt.figure(figsize=(4, 3))
plt.bar(["rank by response", "rank by uplift"], [gain_response_e3, gain_uplift_e3], color=["gray", "seagreen"])
plt.title("Easy 3: response vs uplift targeting")
plt.ylabel("true incremental conversions")
plt.xticks(rotation=12)
plt.show()
```
▶ What you'll see: the uplift-ranked group delivers more incremental conversions even if response-ranked users look more likely to convert.

👀 Takeaway: uplift ranking optimizes incremental gain, while response ranking can over-target sure things.

### Easy 4 — Evaluate policy value with treatment cost

**Goal.** Choose a targeting threshold from value and cost, because treatment should be sent only when expected net benefit is positive. We build it in 3 steps.

```python
x_e4, t_e4, y_e4, p0_e4, p1_e4, tau_e4 = make_uplift_data(n=3000, seed=14)
value_e4 = 10.0
cost_e4 = 1.0
threshold_e4 = cost_e4 / value_e4
print("uplift threshold:", round(threshold_e4, 3))
```
▶ What you'll see: a cost of 1 and value of 10 means uplift must exceed 0.10.

```python
policy_e4 = tau_e4 > threshold_e4
policy_gain_e4 = float(np.sum(policy_e4 * (value_e4 * tau_e4 - cost_e4)))
treat_all_e4 = float(np.sum(value_e4 * tau_e4 - cost_e4))
print("targeted fraction:", round(float(policy_e4.mean()), 3))
print("policy gain:", round(policy_gain_e4, 1), "treat all:", round(treat_all_e4, 1))
assert policy_gain_e4 > treat_all_e4
```

```python
plt.figure(figsize=(4, 3))
plt.bar(["target if tau>c/v", "treat all"], [policy_gain_e4, treat_all_e4], color=["seagreen", "gray"])
plt.title("Easy 4: targeting policy value")
plt.ylabel("expected net value")
plt.xticks(rotation=10)
plt.show()
```
▶ What you'll see: selective targeting wins because it avoids negative or too-small treatment effects.

👀 Takeaway: the right threshold comes from economics: treat when value times uplift minus cost is positive.

### Easy 5 — Plot a Qini curve for two models

**Goal.** Compare a real uplift score with a noisy score, because Qini curves reveal whether ranking quality matters for cumulative gain. We build it in 3 steps.

```python
x_e5, t_e5, y_e5, p0_e5, p1_e5, tau_e5 = make_uplift_data(n=3500, seed=15)
bins_e5 = np.linspace(0, 1, 11)
_, _, tau_hat_e5 = t_learner_bins(x_e5, t_e5, y_e5, bins_e5)
model_score_e5 = predict_bins(x_e5, bins_e5, tau_hat_e5)
noisy_score_e5 = np.random.default_rng(15).normal(size=len(x_e5))
print("model score sd:", round(float(model_score_e5.std()), 3), "noisy score sd:", round(float(noisy_score_e5.std()), 3))
```
▶ What you'll see: both scores vary, but only one is tied to treatment effect.

```python
frac_model_e5, curve_model_e5 = qini_curve(y_e5, t_e5, model_score_e5)
frac_noise_e5, curve_noise_e5 = qini_curve(y_e5, t_e5, noisy_score_e5)
area_model_e5 = float(np.trapz(curve_model_e5 - frac_model_e5 * curve_model_e5[-1], frac_model_e5))
area_noise_e5 = float(np.trapz(curve_noise_e5 - frac_noise_e5 * curve_noise_e5[-1], frac_noise_e5))
print("areas:", round(area_model_e5, 2), round(area_noise_e5, 2))
assert area_model_e5 > area_noise_e5
```

```python
plt.figure(figsize=(5, 3))
plt.plot(frac_model_e5, curve_model_e5, label="uplift model", color="teal")
plt.plot(frac_noise_e5, curve_noise_e5, label="noisy score", color="crimson", alpha=0.8)
plt.title("Easy 5: Qini curve comparison")
plt.xlabel("fraction targeted")
plt.ylabel("incremental outcomes")
plt.legend()
plt.show()
```
▶ What you'll see: the uplift model should climb earlier and produce a larger area than the noisy score.

👀 Takeaway: Qini curves compare targeting rankings by cumulative incremental gain.

## 🔴 Advanced

### Advanced 1 — Show why observational targeting is biased

**Goal.** Break randomization on purpose, because uplift estimates are biased when treatment assignment depends on user features and we ignore that propensity. We build it in 4 steps.

```python
rng_a1 = np.random.default_rng(21)
x_a1 = rng_a1.uniform(0, 1, 4000)
tau_a1 = 0.35 * np.exp(-((x_a1 - 0.35) / 0.18) ** 2) - 0.08 * (x_a1 > 0.78)
p0_a1 = np.clip(0.08 + 0.45 * x_a1, 0.02, 0.85)
p1_a1 = np.clip(p0_a1 + tau_a1, 0.01, 0.95)
prop_a1 = 0.15 + 0.7 * x_a1
t_a1 = rng_a1.binomial(1, prop_a1)
y_a1 = rng_a1.binomial(1, np.where(t_a1 == 1, p1_a1, p0_a1))
print("treatment rate low x:", round(float(t_a1[x_a1 < 0.3].mean()), 3), "high x:", round(float(t_a1[x_a1 > 0.7].mean()), 3))
```
▶ What you'll see: high-feature users are treated much more often, so treated/control groups are not comparable.

```python
bins_a1 = np.linspace(0, 1, 11)
_, _, naive_tau_a1 = t_learner_bins(x_a1, t_a1, y_a1, bins_a1)
true_bin_a1 = bin_means(x_a1, tau_a1, bins_a1)
bias_a1 = naive_tau_a1 - true_bin_a1
print("mean abs bias:", round(float(np.mean(np.abs(bias_a1))), 3))
assert np.mean(np.abs(bias_a1)) < 0.12
```

```python
raw_naive_a1 = float(y_a1[t_a1 == 1].mean() - y_a1[t_a1 == 0].mean())
true_ate_a1 = float(tau_a1.mean())
print("raw difference:", round(raw_naive_a1, 3), "true ATE:", round(true_ate_a1, 3))
assert raw_naive_a1 > true_ate_a1
```

```python
centers_a1 = 0.5 * (bins_a1[:-1] + bins_a1[1:])
plt.figure(figsize=(5, 3))
plt.plot(centers_a1, true_bin_a1, marker="o", label="true tau")
plt.plot(centers_a1, naive_tau_a1, marker="s", label="naive binned")
plt.axhline(0, color="black", linewidth=1)
plt.title("Advanced 1: observational bias")
plt.xlabel("x bin")
plt.ylabel("uplift")
plt.legend()
plt.show()
```
▶ What you'll see: the raw treated-control difference is inflated because treated users had higher baseline response.

👀 Takeaway: uplift estimation needs randomized treatment or credible propensity/confounding adjustment.

### Advanced 2 — Bootstrap uncertainty for segment uplift

**Goal.** Add uncertainty bands to a segment effect, because a noisy positive uplift estimate should not automatically trigger targeting. We build it in 4 steps.

```python
x_a2, t_a2, y_a2, p0_a2, p1_a2, tau_a2 = make_uplift_data(n=1800, seed=22)
segment_a2 = (x_a2 >= 0.25) & (x_a2 < 0.45)
idx_a2 = np.where(segment_a2)[0]
print("segment size:", len(idx_a2))
```
▶ What you'll see: the segment has a finite sample, so its estimated uplift has sampling uncertainty.

```python
point_a2 = float(y_a2[idx_a2][t_a2[idx_a2] == 1].mean() - y_a2[idx_a2][t_a2[idx_a2] == 0].mean())
print("point uplift:", round(point_a2, 3))
assert point_a2 > 0.15
```

```python
rng_a2 = np.random.default_rng(22)
boots_a2 = []
for _ in range(300):
    sample_a2 = rng_a2.choice(idx_a2, size=len(idx_a2), replace=True)
    yt_a2 = y_a2[sample_a2][t_a2[sample_a2] == 1]
    yc_a2 = y_a2[sample_a2][t_a2[sample_a2] == 0]
    boots_a2.append(float(yt_a2.mean() - yc_a2.mean()))
lo_a2, hi_a2 = np.percentile(boots_a2, [2.5, 97.5])
print("95% interval:", round(float(lo_a2), 3), round(float(hi_a2), 3))
assert hi_a2 > lo_a2
```

```python
plt.figure(figsize=(4, 3))
plt.hist(boots_a2, bins=25, color="teal", alpha=0.75)
plt.axvline(point_a2, color="black", label="point")
plt.axvline(lo_a2, color="crimson", linestyle="--", label="95% interval")
plt.axvline(hi_a2, color="crimson", linestyle="--")
plt.title("Advanced 2: segment uplift uncertainty")
plt.xlabel("bootstrap uplift")
plt.legend()
plt.show()
```
▶ What you'll see: a distribution of plausible segment effects, not just one optimistic number.

👀 Takeaway: uncertainty matters because targeting decisions are costly and noisy effects can flip sign.

### Advanced 3 — Detect negative uplift and suppress treatment

**Goal.** Find segments where treatment hurts, because ignoring negative uplift can damage users and waste budget. We build it in 3 steps.

```python
x_a3, t_a3, y_a3, p0_a3, p1_a3, tau_a3 = make_uplift_data(n=3000, seed=23)
bins_a3 = np.linspace(0, 1, 11)
_, _, tau_hat_a3 = t_learner_bins(x_a3, t_a3, y_a3, bins_a3)
centers_a3 = 0.5 * (bins_a3[:-1] + bins_a3[1:])
print("estimated uplift:", np.round(tau_hat_a3, 3))
```
▶ What you'll see: some right-tail estimates are near zero or below zero.

```python
harm_bins_a3 = tau_hat_a3 < 0
suppress_a3 = centers_a3[harm_bins_a3]
print("suppress bins:", suppress_a3)
assert np.any(harm_bins_a3)
```

```python
plt.figure(figsize=(5, 3))
plt.bar(centers_a3, tau_hat_a3, width=0.08, color=np.where(harm_bins_a3, "crimson", "seagreen"))
plt.axhline(0, color="black", linewidth=1)
plt.title("Advanced 3: negative uplift")
plt.xlabel("x bin")
plt.ylabel("estimated uplift")
plt.show()
```
▶ What you'll see: red bars mark segments where treatment should be suppressed rather than targeted.

👀 Takeaway: uplift models must preserve the sign of treatment effect, not just rank positive responders.

### Advanced 4 — Optimize a limited treatment budget

**Goal.** Choose the best users under a fixed budget, because many campaigns can treat only a fraction of the population. We build it in 4 steps.

```python
x_a4, t_a4, y_a4, p0_a4, p1_a4, tau_a4 = make_uplift_data(n=4000, seed=24)
bins_a4 = np.linspace(0, 1, 11)
_, _, tau_hat_a4 = t_learner_bins(x_a4, t_a4, y_a4, bins_a4)
score_a4 = predict_bins(x_a4, bins_a4, tau_hat_a4)
budget_a4 = int(0.20 * len(x_a4))
print("budget:", budget_a4)
```
▶ What you'll see: the policy must select only a limited number of users.

```python
top_model_a4 = np.argsort(score_a4)[-budget_a4:]
rng_a4 = np.random.default_rng(24)
top_random_a4 = rng_a4.choice(np.arange(len(x_a4)), size=budget_a4, replace=False)
gain_model_a4 = float(np.sum(tau_a4[top_model_a4]))
gain_random_a4 = float(np.sum(tau_a4[top_random_a4]))
print("model gain:", round(gain_model_a4, 2), "random gain:", round(gain_random_a4, 2))
assert gain_model_a4 > gain_random_a4
```

```python
oracle_a4 = np.argsort(tau_a4)[-budget_a4:]
gain_oracle_a4 = float(np.sum(tau_a4[oracle_a4]))
print("oracle gain:", round(gain_oracle_a4, 2))
assert gain_oracle_a4 >= gain_model_a4
```

```python
plt.figure(figsize=(4, 3))
plt.bar(["random", "model", "oracle"], [gain_random_a4, gain_model_a4, gain_oracle_a4], color=["gray", "teal", "seagreen"])
plt.title("Advanced 4: limited-budget targeting")
plt.ylabel("true incremental conversions")
plt.show()
```
▶ What you'll see: model targeting beats random but remains below the oracle because estimates are noisy.

👀 Takeaway: with a fixed budget, uplift scores are used for ranking, and the top scores receive treatment.

### Advanced 5 — Audit group-level targeting parity

**Goal.** Check whether a policy reaches groups differently, because even causal targeting can create fairness concerns when interventions are beneficial and unevenly allocated. We build it in 4 steps.

```python
x_a5, t_a5, y_a5, p0_a5, p1_a5, tau_a5 = make_uplift_data(n=4000, seed=25)
group_a5 = (x_a5 > 0.5).astype(int)
bins_a5 = np.linspace(0, 1, 11)
_, _, tau_hat_a5 = t_learner_bins(x_a5, t_a5, y_a5, bins_a5)
score_a5 = predict_bins(x_a5, bins_a5, tau_hat_a5)
print("group shares:", round(float(np.mean(group_a5 == 0)), 3), round(float(np.mean(group_a5 == 1)), 3))
```
▶ What you'll see: the two groups are roughly feature-defined halves of the candidate population.

```python
threshold_a5 = 0.10
policy_a5 = score_a5 > threshold_a5
rate0_a5 = float(policy_a5[group_a5 == 0].mean())
rate1_a5 = float(policy_a5[group_a5 == 1].mean())
print("treatment rates by group:", round(rate0_a5, 3), round(rate1_a5, 3))
assert abs(rate0_a5 - rate1_a5) > 0.1
```

```python
gain0_a5 = float(np.mean(tau_a5[(group_a5 == 0) & policy_a5])) if np.any((group_a5 == 0) & policy_a5) else 0.0
gain1_a5 = float(np.mean(tau_a5[(group_a5 == 1) & policy_a5])) if np.any((group_a5 == 1) & policy_a5) else 0.0
print("treated-user mean uplift:", round(gain0_a5, 3), round(gain1_a5, 3))
```

```python
plt.figure(figsize=(5, 3))
plt.bar(["group 0 rate", "group 1 rate"], [rate0_a5, rate1_a5], color=["teal", "orange"])
plt.title("Advanced 5: targeting parity audit")
plt.ylabel("fraction treated")
plt.ylim(0, 1)
plt.show()
```
▶ What you'll see: the policy can allocate treatment unevenly because uplift is concentrated in one feature region.

👀 Takeaway: uplift policies should be audited for both incremental value and group-level intervention access.
